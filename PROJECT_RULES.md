# 项目代码修改规则

> 本文件是 Codex 和 Claude Code 共用的项目规则正文。项目负责人可以直接在下方补充、修改或删除规则。
这是为您后端项目定制的 Codex & Claude Code 编程规则集，涵盖了接口管理、安全校验、敏感信息处理和对话总结等关键环节。

---
# 后端项目 Codex & Claude Code 协作编程规则 v1.4


## 一、通用原则

1. **安全第一**：任何功能实现必须优先考虑安全性，禁止为了快速实现功能而省略校验、过滤或加密步骤。

2. **文档即代码**：接口文档与代码具有同等重要性，任何接口变更必须同步更新文档。

3. **可追溯性**：发生代码、接口、配置或项目文档变更时，输出一次清晰的改动与验证总结；只读审查、问答和无改动任务使用对应的审查结论，不套用改动模板。

4. **最小权限原则**：敏感信息（密码、密钥、Token）不得硬编码，必须通过环境变量或配置中心注入。

5. **执行与批准边界**：普通 bug 修复、局部重构、测试、文档和可逆配置调整，在目标明确且不改变公开契约/生产风险时直接执行；只在缺失信息会改变目标，或动作不可逆、涉及生产/外部发布、受保护资源、公开契约或重大安全风险时暂停请求选择。验证强度与改动风险匹配，不因可选证据缺失而提前停止。


## 二、接口管理规则（前端可见）

> **目的**：确保前端团队能实时、准确地获取接口定义，减少联调障碍。

### 2.1 新增接口

以下完整契约要求适用于对外 HTTP 接口、前端联调接口和跨系统消费的接口。内部调试路由、一次性实验路由或仅供同一模块调用的接口使用精简契约，但仍必须说明权限、输入输出结构、错误行为和适用范围。

**必须做**：
1. 在项目 `docs/api/` 目录下创建或更新对应的接口文档（推荐 OpenAPI/Swagger 格式）。
2. 文档必须包含：**URL**、**Method**、**Headers**、**请求体（Request Body/Params）**、**请求参数表（含每个参数的意义）**、**返回参数表（含每个返回字段的意义）**、**请求/响应示例**、**错误码说明**、**接口使用方法与业务规则**。没有请求体或返回体时明确写"无请求体"/"无返回体"，禁止只写 URL 和一句话用途。
3. 在代码中添加对应的路由和 Controller 层，并确保入参校验逻辑完整。
4. 在统一的【改动总结】中记录该接口；仅在本次确实新增接口时填写“新增接口”小节。

**禁止做**：
- 接口参数定义模糊（如 `data` 字段不说明内部结构）。
- 只写 URL 和一段用途描述，不写完整的请求参数、返回参数和字段含义。

### 2.1.1 接口契约详细要求

对外 HTTP 接口、前端联调接口和跨系统消费接口的文档和 OpenAPI 定义必须达到“前端不看源码也能完成联调”的程度，至少包含以下内容。内部调试、一次性实验或同模块私有路由沿用 2.1 的精简契约，不强制套用下面的完整模板。

**统一文档模板（适用于完整契约接口）**：`docs/api/` 下的对外、前端联调和跨系统接口文档必须按以下结构完整输出；内部私有路由至少记录权限、输入输出、错误行为和适用范围：

````markdown
#### <接口名>

**基本信息**：用途、完整 URL、HTTP Method、是否需要登录、所需角色/权限、Content-Type、成功状态码。

**请求参数**：表格逐行列出每个参数的位置、类型、必填、默认值、校验规则（长度/范围/格式/枚举/互斥依赖）和业务含义。

**请求体示例**：完整的 URL/query + Headers + Body JSON 示例；没有请求体时写”无请求体”，并附至少一个非法示例。

**返回参数**：表格逐行列出每个返回字段的类型、必返、空值含义、枚举含义、业务含义和示例值；嵌套对象/数组必须继续展开。

**返回示例**：成功响应 JSON；分页接口说明 `items/page/page_size/total/has_more`；无数据时给出空数组/空对象示例；无返回体时写”无返回体”。

**使用方法与业务规则**：前置条件、调用顺序、幂等、频率/额度限制、状态流转、边界场景、前端处理建议。

**错误**：HTTP 状态码、业务错误码、触发条件、错误响应 JSON、前端处理建议。
````

以下每一项都是硬性要求，不得省略：

1. **基本信息**：接口名称、用途、完整 URL、HTTP Method、是否需要登录、所需角色/权限、Content-Type 和响应 Content-Type。
2. **请求参数表**：每个参数单独列出以下字段：
   - 参数名。
   - 所在位置：`path`、`query`、`header`、`body` 或 `cookie`。
   - 数据类型和 JSON 类型。
   - 是否必填。
   - 默认值。
   - 长度、范围、格式、枚举值和互斥/依赖关系。
   - 参数业务含义。
   - 合法示例和至少一个非法示例。
3. **请求示例**：必须分别提供完整的 URL/query 示例、Headers 示例和 Body 示例；没有 Body 时明确写“无请求体”。
4. **返回参数表**：每个返回字段单独列出字段名、类型、是否必返、空值含义、枚举含义、业务含义和示例值。嵌套对象和数组必须继续展开，禁止只写 `data: object` 或 `items: list` 而不说明内部结构。
5. **返回示例**：至少提供一个成功响应 JSON；分页接口必须说明 `items`、`page`、`page_size`、`total`、`has_more` 等字段含义；无数据时提供空数组或空对象示例。
6. **错误契约**：列出可能的 HTTP 状态码、业务错误码（如项目已使用）、触发条件、前端处理建议和错误响应 JSON。不能只写“返回错误”。
7. **状态和权限**：说明资源归属校验、状态流转、幂等规则、额度扣减/退还、重复提交、过期、删除和并发场景。
8. **兼容性**：修改或新增字段必须说明是否向后兼容、旧客户端如何处理、字段废弃时间和迁移方式。
9. **使用方法与业务规则**：必须单独说明该接口"怎么用、在什么条件下能用"，至少覆盖：
   - 前置条件：登录、绑定手机号、实名、会员、必须有服务单/订单/目标用户存在等。
   - 调用顺序：必须先调用哪个接口再调用哪个（如先发验证码再登录、先创建订单再支付）。
   - 幂等与防重：`Idempotency-Key` 要求、重复请求返回同一结果、不能重复扣费/扣次数。
   - 频率/额度/次数限制：限流窗口、每日额度、验证码 60 秒间隔、24 小时上限等。
   - 状态流转：枚举状态之间的合法迁移，非法流转返回什么错误。
   - 边界场景：空列表、无权限、资源过期、并发提交、对方注销/拉黑后的表现。

**文档完成自检清单**（仅对适用完整契约的接口逐项确认，缺任一项视为文档未完成）：
- [ ] 有请求参数表，且每个参数都写了业务含义
- [ ] 有完整请求体示例（无请求体时已明确标注）
- [ ] 有返回参数表，每个字段都写了业务含义，嵌套结构已展开
- [ ] 有成功返回示例（分页接口含分页字段说明；无返回体时已明确标注）
- [ ] 有"使用方法与业务规则"小节，覆盖前置条件、调用顺序、幂等、限流、状态流转和边界场景
- [ ] 有错误码表：HTTP 状态码、触发条件、前端处理建议
- [ ] 至少一个非法参数示例

**存量文档补全**：`docs/api/` 下已存在但未达到上述标准的对外、前端联调或跨系统接口文档，在修改对应接口或接到前端联调请求时按本模板补全；内部私有路由只需补齐其实际消费方需要的契约，不为凑完整模板扩大范围。

接口代码也必须与契约一致：请求使用明确的 Pydantic Schema，响应优先使用明确的 `response_model`，不要使用无结构的 `dict`、`list` 或 `Any` 替代已经确定的接口模型。OpenAPI 中的参数描述、枚举和示例应与 `docs/api/` 保持一致。

### 2.2 修改接口（包括入参、出参、路径、方法）

**必须做**：
1. 同步更新接口文档，并高亮标记 **变更内容**（如 `// changed` 或使用 OpenAPI 的 `deprecated` + 新版本）。
2. 若为破坏性变更（Breaking Change），必须在文档中注明 **迁移指南** 或提供过渡方案。
3. 在统一的【改动总结】中记录变更点及实际影响范围；没有实质影响时不强行扩展影响清单。
4. 接口请求参数，返回内容格式都要写

**禁止做**：
- 只改代码不改文档。
- 删除或重命名旧字段而不提供兼容处理或通知前端。

### 2.2.1 接口变更记录要求

接口发生以下任一变化，都必须在文档中增加“变更记录”小节，并明确变更前、变更后和影响范围：

- 请求参数新增、删除、改名、改类型、改为必填或修改校验范围。
- 响应字段新增、删除、改名、改类型、修改空值或枚举含义。
- URL、HTTP Method、认证方式、权限要求或错误码变化。
- 分页、排序、过滤、额度、状态流转或幂等行为变化。

Breaking Change 必须提供版本迁移方案，不能只在代码中直接替换旧契约。

### 2.3 删除接口

**必须做**：
1. 先确认该接口无任何调用方（可通过日志或调用链分析）。
2. 对外、前端联调或跨系统接口，在文档中标记为 `deprecated` 至少一个版本周期后再移除；内部私有或一次性实验路由可记录替代方案后直接移除。
3. 删除代码路由和相关 Handler。
4. 在统一的【改动总结】中记录删除原因和替代方案（如有）；仅在本次确实删除接口时填写该小节。

**禁止做**：
- 直接删除代码而不通知团队。
- 删除后不更新文档导致前端引用失效。

### 2.4 接口实现优先复用现有能力

正常开发中，优先使用项目已经存在的库、框架、公共函数、Schema、依赖注入、数据库表和服务层代码，禁止在没有必要的情况下重复造轮子：

1. **先搜索再实现**：编码前必须检查 `app/core`、`app/api`、`app/schemas`、`app/services`、`tests` 和 `docs/api` 中是否已有可复用实现。已有的鉴权、参数校验、分页、错误处理、文件处理、JSON 处理、加密、限流、通知和数据库访问逻辑，优先复用原实现。
2. **优先使用现有依赖**：优先使用项目已安装并在 `pyproject.toml`/`uv.lock` 中声明的库，例如 FastAPI、Pydantic、SQLAlchemy、Redis、Pillow 和 aiofiles。不得为了一个简单功能重复实现成熟库已经提供的解析、校验、加密、图片处理或缓存逻辑。
3. **新增依赖要有理由**：只有现有依赖确实不能满足需求时才引入新库。新增前必须说明候选库、选择理由、许可证/维护状态、兼容性、安装影响和替代方案，并同步更新 `pyproject.toml`、锁文件、安装文档和测试。
4. **避免重复工具函数**：新增工具函数前必须确认项目中没有同等能力。若已有函数不完全满足需求，应优先扩展或抽象已有函数，并评估对调用方的影响，不能复制一份近似实现造成行为分叉。
5. **优先复用现有数据模型**：已有表、状态枚举和关系模型可以满足需求时，不得另建重复表或字段。若确实需要改表，必须先说明数据迁移、兼容旧数据和回滚方案。
6. **自研必须说明原因**：若最终仍需自行实现，代码或文档中必须简要说明现有能力为何不适用、自研边界、异常处理、并发安全和测试覆盖，避免形成无法维护的内部框架。
7. **复用不等于盲目复用**：复用现有代码前必须检查其权限、事务、异常、性能和敏感信息处理是否适用于新场景；不能因为“已有代码”而跳过安全校验。
例如：
### 请求体
```json
{
  "phone":"13800138000",
  "purpose":"login",
  "code":"123456",
  "device_id":"device-1",
  "platform":"wechat-mini-program",
  "app_version":"1.0.0"
}
```

### 返回参数

| 字段 | 类型 | 必返 | 含义 |
| --- | --- | --- | --- |
| `access_token` | string | 是 | 调用受保护接口的访问 Token |
| `refresh_token` | string | 是 | 刷新访问 Token 的长期 Token |
| `token_type` | string | 是 | 固定为 `bearer` |
| `expires_in` | integer | 是 | Access Token 有效秒数 |
| `need_bind_phone` | boolean | 是 | 是否需要绑定手机号，手机号登录成功后固定为 `false` |
| `user_id` | integer | 是 | 平台内部用户 ID |

成功响应：

```json
{
  "access_token":"<access-token>",
  "refresh_token":"<refresh-token>",
  "token_type":"bearer",
  "expires_in":3600,
  "need_bind_phone":false,
  "user_id":1
}
```


## 三、安全与校验规则

> **核心**：接口不能只实现业务功能，必须内置安全防御。

### 3.1 输入校验（防注入、防篡改）

**所有用户输入**（包括 Path Params、Query Params、Body、Headers）必须进行：
- **类型校验**（如 `int`、`string`、`email`）。
- **长度/范围校验**（防止溢出或 DoS）。
- **格式校验**（如 UUID、日期格式、枚举值）。
- **业务校验**（如订单金额不能为负、状态流转必须合法）。

**实现方式**：使用 Validation 框架（如 `@Valid`、`Joi`、`pydantic`）并定义清晰错误信息。

### 3.2 敏感信息处理

**绝对禁止**：在代码、日志、注释、返回值中泄露真实密码、密钥、Token。

**必须**：
- 所有密钥/密码通过环境变量 `process.env` 或配置中心读取。
- 日志中对于密码、身份证号等字段进行脱敏（如 `******`）。
- 返回给前端的响应中，移除密码哈希、密钥等字段（可在全局序列化层过滤）。

#### 3.2.1 语音 Provider（STT/TTS）专项约束

- 阿里云 NLS `api_key`/`app_key` 仅存于被忽略的 `.env`，不进 `.env.example`、不进代码、不进日志。
- **音频不进审计**：`GatewayCallRecord` 与 `GenerationAuditEvent` 只记录元数据（provider、model、duration、error_code），绝不包含音频原文、转写文本、音频 URL 或密钥。
- 转写文本（transcript）通过 `payload_summary` 存储、通过 `result_payload` 透传，不写入 `result_ref`（varchar(128) 长度限制）。
- 临时音频文件须有过期清理（`ai_voice_audio_retention_hours`），转写完成后短期保留即删除，合规要求不长期留存原始音频。
- 生产启用须满足三道审批门禁且 `ai_voice_provider != "mock"`（`_validate_ai_feature_gates` 已强制 fail-closed）。

### 3.3 接口权限与校验

**每个接口必须明确权限要求**（如 `@Permission("user:read")`）。

**关键操作**（删除、修改、资金操作）必须进行：
- **身份校验**（Token/Session 有效性）。
- **操作权限校验**（当前用户是否拥有该资源操作权限）。
- **幂等性控制**（如使用 Idempotent-Key 防止重复提交）。


## 四、敏感信息填写指引

> 当新功能或修改涉及以下信息时，**必须** 告知用户自行填写，绝不可擅自填充示例值或默认生产值。

**涉及内容**：
- 数据库连接串（`DB_URL`）
- 第三方 API Key / Secret（如支付、短信、OSS）
- JWT Secret / 加密 Salt
- 管理员初始密码/邮箱
- Redis / RabbitMQ 连接密码

**告知方式**：在对话总结中明确写出：
> **【敏感信息填写位置】**：
> 请在 `./config/.env.example` 中找到 `YOUR_XXX_KEY` 变量，将其替换为您实际的值。该文件不会提交至 Git，请妥善保管。


## 五、每次对话结束的【改动总结】模板

> 发生代码、接口、配置或项目文档变更时，在每次对话最后输出一次总结，方便前端和其他成员查阅。
> 只填写实际发生的类别、验证结果和遗留事项，不为凑模板虚构内容。只读审查、问答和无改动任务输出对应结论即可。

````markdown
### 📋 本次对话改动总结（YYYY-MM-DD）

#### 1. 新增接口
- `POST /api/v1/orders` - 创建订单
  - 用途：用户下单
  - 文档更新：`docs/api/orders.yaml#L12-L45`

#### 2. 修改接口
- `GET /api/v1/users/{id}` 
  - 变更：响应中新增 `userLevel` 字段，移除 `score` 字段（已废弃）
  - 文档标记：`deprecated` 并指向新版本 `v2`

#### 3. 删除接口
- `DELETE /api/v1/products/{id}/force` （硬删除）
  - 原因：改用软删除 `PUT /api/v1/products/{id}/status`
  - 替代方案：请使用软删除接口

#### 4. 安全与校验增强
- `POST /api/v1/payments` 增加幂等性校验（`Idempotent-Key` Header）
- 所有 `String` 类型入参增加 XSS 过滤（使用 `xss-filter` 中间件）

#### 5. 敏感信息填写位置
- 支付宝支付回调密钥：请在 `config/production.env` 中配置 `ALIPAY_APP_SECRET`
- 数据库读写分离密码：请在 `config/.env` 中配置 `DB_REPLICA_PASSWORD`

#### 6. 其他变动
- 升级 `express` 从 `4.18` 至 `4.19`（安全补丁）
- 统一错误码 `E10001` 改为 `E_INVALID_PARAM`
````


## 六、技术方案可行性评判与批准边界

> 本节用于识别会改变架构、外部契约、数据安全或不可逆状态的决策。普通 bug 修复、局部重构、测试、文档、复用现有能力和小范围配置调整，直接按既有项目模式执行。

### 6.1 评判范围

只有下列变更会触发完整的技术方案评判，并在需要用户选择时暂停：

- 整体技术选型（如"用 MongoDB 替换 MySQL"、"改用微服务"）
- 架构设计方案（如"用消息队列解耦"、"引入缓存层"）
- 第三方服务/中间件引入（如"接入某某支付"、"用 Redis 做分布式锁"）
- 数据库设计变更（如"改表结构"、"分库分表"）
- 性能优化策略（如"用多线程并发处理"）
- 安全方案（如"用 JWT 替代 Session"）
- 业务逻辑实现路径只有在改变公开状态流转、数据一致性、权限边界或不可逆结果时才触发评判；普通实现路径由 Agent 自行选择。

### 6.2 评判标准（五维度）

对每个技术方案，从以下五个维度逐一评估：

| 维度 | 评估要点 |
|------|----------|
| **技术可行性** | 当前技术栈是否支持？是否有现成库/框架？是否需要大量造轮子？ |
| **安全性** | 是否引入新的攻击面（如注入、越权、数据泄露）？是否涉及敏感信息处理？ |
| **性能影响** | 是否会导致响应延迟增加？是否会造成资源瓶颈（CPU/内存/IO）？是否符合 SLA？ |
| **维护成本** | 是否增加系统复杂度？团队成员是否熟悉？未来扩展是否困难？ |
| **兼容性** | 是否影响现有功能？是否需要数据迁移？前端/下游是否需要同步改动？ |

### 6.3 评判结果与处理方式

评判后必须给出明确的结论和后续行动：

#### ✅ 可行（可直接执行）
- 给出明确的"可行"结论，并简述依据（如"该方案在当前技术栈下已有成熟实现，无安全风险"）。
- 然后按原规则执行编码。

#### ⚠️ 有条件可行（需调整后执行）
- 明确指出**需要修改的部分**，并给出具体建议。
- 示例："该方案基本可行，但建议将 `Redis` 作为缓存而非消息队列使用，因为您的场景是读多写少。调整后即可执行。"
- 只有调整会改变用户目标、外部契约、生产风险或不可逆结果时，才等待用户确认；同范围内的安全和兼容性优化直接执行并在总结中说明。

#### ❌ 不可行（建议否决）
- 明确说明不可行的原因（引用具体技术依据）。
- 提供 **替代方案**（至少一个），并解释为什么替代方案更优。
- 示例："使用 `悲观锁` 处理并发订单不推荐，因为在高并发下会导致大量连接阻塞。建议改用 `乐观锁` 或 `分布式锁`。如果您同意，我将按新方案实现。"

### 6.4 评判输出格式（必须）

在每次技术方案评判后，必须按以下格式输出：

````markdown
### 🔍 技术方案可行性评判

**用户提出的方案**：[简述，如"用 RabbitMQ 处理订单超时关闭"]

**评判结果**：⚠️ 有条件可行 / ✅ 可行 / ❌ 不可行

**详细分析**：
- **技术可行性**：[评估结果]
- **安全性**：[评估结果]
- **性能影响**：[评估结果]
- **维护成本**：[评估结果]
- **兼容性**：[评估结果]

**结论与建议**：
[具体说明建议，如需调整则写明调整步骤，如需否决则写明替代方案]

**下一步操作**：
- 若可行 → 继续执行，并记录必要的风险判断。
- 若需调整但不改变目标或范围 → 直接采用调整方案并说明原因。
- 若涉及重大范围、不可逆操作、外部发布或产品/安全语义 → 明确列出选项并等待用户选择。
````

### 6.5 特殊场景处理

| 场景 | 处理方式 |
|------|----------|
| 用户方案明显存在安全漏洞 | 直接给出 **❌ 不可行** 结论，并强调安全风险，提供安全替代方案 |
| 用户方案成本过高（如引入全新中间件） | 给出 **⚠️ 有条件可行**，建议先用现有工具实现，并说明引入新中间件的代价 |
| 用户方案与现有架构严重冲突 | 给出 **❌ 不可行**，详细解释冲突点，并推荐兼容性更好的方案 |
| 用户明确要求采用不推荐方案 | 记录风险和后果；只要不违反安全边界且用户明确要求继续，就按授权实现，并在总结中高亮风险 |


## 七、功能新增/修改完整工作流

> 涉及后端代码或公开接口的实现/修改，按相关项执行；纯文档、纯审查和不涉及接口的局部修复不强制套用接口设计模板。

0. 按第六章判断当前任务是否触发完整方案评估；未触发时直接执行，触发时只在需要用户选择的高风险决策处暂停。

1. **分析需求**，明确本次改动的输入、输出和异常场景；不涉及接口时只覆盖实际受影响的边界。

2. **如涉及接口**，设计路径、方法、权限、请求参数、返回参数、错误码、状态流转和兼容性；不涉及接口时跳过。

3. **如涉及完整契约接口**，编写或同步更新接口文档；内部私有路由采用精简契约。

4. **实现业务逻辑**。

5. **加入校验**（输入、权限、幂等、敏感数据脱敏）。

6. **编写/更新单元测试**（覆盖正常、边界、异常情况）。

7. **检查敏感信息**：若有新增外部依赖，指引用户配置。

8. **代码审查自检**：
   - [ ] 是否有 SQL 注入风险？（使用 ORM/参数化查询）
   - [ ] 是否有敏感信息硬编码？
   - [ ] 日志是否安全？
   - [ ] 错误信息是否会暴露系统内部细节？

9. **生成最终改动总结**（按第五部分模板）。


## 八、特别约束（Codex / Claude Code 执行层）

- **禁止跳过安全检查**：如果用户要求省略输入校验、权限校验、敏感信息处理或其他明确安全门禁，拒绝省略该部分并提供安全实现路径；“快速实现”或“最小版本”本身不构成跳过安全检查的理由。

- **环境隔离**：在操作数据库或缓存时先识别当前环境；生产环境的破坏性或批量操作必须暂停确认，开发/测试环境优先使用隔离数据，不能隔离时明确警告。

- **变更影响分析**：仅在确有接口、数据、权限、兼容性、性能或运行流程影响时提示，并说明受影响对象；没有实质影响时不强行生成影响清单。

- **技术方案评判优先级**：需要评估的高风险决策必须先完成对应评估；普通实现、测试、文档和小范围修复不因本节等待二次确认。


## 九、未完成事项记录规则

只有确实产生未完成事项、外部依赖、人工确认项或阻塞项时，才在 `docs/待完成事项.md` 中新增或更新记录，并标注状态、阻塞原因和下一步。纯审查、纯格式修正、已闭环修改和没有遗留事项的任务不创建或更新该文件；历史事项完成后保留记录并标记为“已完成”。

## 十、规则集版本信息

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2026-07-18 | 初始版本 |
| v1.1 | 2026-07-18 | 新增「技术方案可行性评判」规则（第六章），并整合入工作流 |
| v1.2 | 2026-07-20 | 细化接口请求/返回契约文档要求，新增现有库和代码优先复用规则 |
| v1.3 | 2026-08-01 | 强化接口文档统一模板与完成自检清单，强制要求请求体、返回参数、每个参数意义和使用方法/业务规则 |
| v1.4 | 2026-09-05 | 收窄完整契约和技术评估的触发范围，统一自主执行、批准、验证和完成边界 |


---

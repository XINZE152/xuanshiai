# AI 编码规则入口

本文件供 Codex 使用。开始任何代码修改前，必须先阅读项目根目录的 `PROJECT_RULES.md`，并将其中规则视为本项目的强制约束。

同时阅读与当前任务相关的 `README.md`、`docs/DEVELOPMENT.md` 以及模块内文档。

只读审查、规则/文档整理和不涉及接口的局部修复，按实际范围读取证据并执行匹配验证，不强制套用完整编码工作流；只有任务触发 `PROJECT_RULES.md` 的高风险评估或批准边界时才暂停请求用户选择。

规则正文统一维护在：`PROJECT_RULES.md`

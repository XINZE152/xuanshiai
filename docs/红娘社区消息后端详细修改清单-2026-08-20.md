# 红娘、社区、消息后端及后台管理后端详细修改清单

编制日期：2026-08-20  
代码基线：`main@e858189`  
对应评估：`docs/红娘社区消息后端评估结果-2026-08-20.md`

## 一、执行规则

1. 本清单只描述修改范围和验收要求，不代表相关功能已经完成。
2. 新增或修改接口必须同步更新 `docs/api/*`，并满足 `PROJECT_RULES.md` 中关于 URL、权限、请求参数、返回字段、错误码、状态流转、幂等和示例的要求。
3. 所有资金、积分、次数、联系方式、实名、人脸、消息和组织数据都必须在服务端校验，不能依赖前端限制。
4. 所有后台查询必须明确管理员身份、权限码、数据范围和资源归属；不能只做登录校验。
5. 所有跨模块事件必须明确事务边界、幂等键、重试策略、失败补偿和审计记录。
6. 每项完成后必须补充单元测试、越权测试、真实数据库测试或明确记录阻塞原因。

## 二、优先级和状态定义

| 优先级 | 含义 |
| --- | --- |
| P0 | 安全、权限、数据隔离、资金和一期主流程阻塞项，必须优先完成 |
| P1 | 产品主流程和后台运营能力，影响完整验收 |
| P2 | 实时通信、AI 扩展、统计、迁移和体验增强 |

状态使用：`未开始`、`进行中`、`部分完成`、`已完成`、`阻塞待确认`。

## 三、公共基础与验收治理

### G-01 产品口径冻结

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 冻结红娘联系方式展示、授权、收费和实名/人脸门禁。
  - 冻结活动报名、支付、审核、取消、退款、签到和奖励规则。
  - 冻结“喜欢”与“关注”Tab 的唯一映射。
  - 冻结申请认识的拒绝、撤回、过期、返次和额度规则。
  - 冻结纸飞机匿名身份、资料解锁和媒体权限规则。
  - 冻结消息公告、个人通知、系统通知和社区通知的职责边界。
  - 冻结 AI 红娘、AI 分身、AI 军师和 AI 匹配一期是否纳入范围。
- 涉及文档：`docs/方案B产品口径冻结与待确认项-2026-08-12.md`、各模块 API 文档。
- 验收标准：产品、前端、后端和测试对上述口径逐项确认，未确认项不进入开发验收。

### G-02 统一状态机和错误码

- 优先级：P0
- 当前状态：未完成
- 修改内容：
  - 为服务申请、订单、约见、活动报名、纸飞机会话、聊天结构化请求、消息、通知和举报建立状态转换表。
  - 统一 HTTP 状态码、业务错误码、错误响应结构和前端处理建议。
  - 统一 UTC 数据库存储与 `Asia/Shanghai` 自然日计算规则。
  - 对非法状态跃迁、重复操作和并发冲突返回明确错误码。
- 验收标准：所有 P0/P1 接口均能在文档中找到状态流转、错误码和边界场景；非法状态测试全部通过。

### G-03 幂等、事务和事件可靠性

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 为订单、活动报名、服务申请、联系方式授权、消息发送、通知写入和积分奖励统一设计幂等键。
  - 对“同意认识后创建会话并发送欢迎消息”等跨表操作明确事务边界。
  - 引入或复用 Outbox/事件表，保证消息通知、积分奖励和异步动作可重试。
  - 对支付、退款、提现和返次操作增加守恒校验和重复请求保护。
- 验收标准：重复请求不会重复扣款、重复返次、重复建会话、重复发奖或重复通知；事务失败可回滚或补偿。

### G-04 测试和 CI 基础

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 修复 `tests/test_business_features.py` 对 OpenAPI 标签顺序的脆弱断言，改为按名称校验或同步预期。
  - 为红娘后台新增账号、RBAC、数据范围和敏感操作测试。
  - 为社区活动、纸飞机和社区后台新增真实数据库测试。
  - 为消息自动欢迎、会话管理、通知、结构化请求和消息审计新增测试。
  - 将真实 MySQL、Redis、HTTP、并发测试纳入可控 CI 环境，禁止无条件跳过关键验收。
  - 增加 OpenAPI 快照和文档路径覆盖检查。
- 验收标准：全量测试无未说明失败；关键测试不因环境缺失静默跳过；接口、文档和 Schema 变化可被 CI 发现。

### G-05 数据库迁移和回滚

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 将当前初始化脚本中的新增表和字段整理为正式迁移文件。
  - 增加重复执行、失败恢复、备份、回滚和历史数据回填脚本。
  - 对消息、活动、纸飞机、红娘服务和后台账号表增加必要索引。
  - 明确生产环境禁止启动时自动建表和结构变更。
- 验收标准：测试库和预发布库完成迁移演练；迁移后数量、约束、索引和回滚结果核对通过。

## 四、红娘 C 端修改清单

### M-01 红娘公开档案和申请审核闭环

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 拆分“红娘申请材料”和“公开红娘档案”。
  - 增加申请列表、申请详情、审核历史、补充材料、审核通过、拒绝、发布、下线和版本审计接口。
  - 审核通过时仅发布允许公开的字段，联系方式和证件材料进入受控字段。
  - 增加公开档案的头像、简介、擅长领域、服务价格、成功案例、服务范围和可接单状态。
- 重点接口：`/api/v1/matchmaker/applications`、`/api/v1/matchmakers` 及后台对应接口。
- 验收标准：后台无需预先知道申请 ID 即可完成审核；未发布或下线资料不会出现在 C 端；敏感材料不通过公开接口返回。

### M-02 成功案例和评价治理

- 优先级：P1
- 当前状态：部分完成
- 修改内容：
  - 增加红娘成功案例提交、编辑、审核、发布、下线和版本记录。
  - 增加服务评价表和评价接口，只允许真实完成且未全额退款的服务参与人评价。
  - 支持评分、标签、文字、匿名展示、举报、隐藏、恢复和审计。
  - 更新红娘卡片和详情中的 `success_count`、`rating_score`、`rating_count` 计算规则。
- 验收标准：重复评价、非服务用户评价、退款后评价和越权查看全部被拒绝；统计可重算。

### M-03 咨询预约和时段管理

- 优先级：P1
- 当前状态：未开始
- 修改内容：
  - 增加红娘可预约日期、时间段、服务方式和时区字段。
  - 增加时段查询、锁定、释放、超时回收和冲突检测。
  - 订单支付与时段占位绑定，取消、退款和改期必须释放或重新占位。
  - 增加预约提醒、红娘确认、用户取消和异常补偿。
- 验收标准：并发预约同一时段只能成功一个；支付失败、超时和退款不会遗留不可用占位。

### M-04 私人定制服务

- 优先级：P2，需产品确认后实施
- 当前状态：范围已识别，代码未实现
- 修改内容：
  - 建立基础、高级、尊享三档套餐和价格配置。
  - 建立七阶段履约状态：需求沟通、会员了解、资源开放、推荐、形象指导、约见安排、结果反馈。
  - 增加阶段完成、退回、暂停、取消、退款和服务证据记录。
- 验收标准：每个阶段有明确责任人、状态、时间、输入、输出和审计记录；退款和取消不会破坏已完成阶段数据。

### M-05 服务单闭环

- 优先级：P0
- 当前状态：未完成
- 修改内容：
  - 增加分配、改派、接受、处理中、完成、取消、超时、退款和申诉状态。
  - 增加自动分配规则、人工改派、超时提醒、回收和重新分配。
  - 将订单、服务单、约见、联系方式授权和通知建立关联。
  - 增加服务完成后的评价入口和退款后的权限收回。
- 验收标准：订单、服务单、约见和联系方式状态一致；服务单不能跳过前置状态直接完成或交付联系方式。

### M-06 红娘排行榜和运营指标

- 优先级：P1
- 当前状态：未完成
- 修改内容：
  - 明确定义热心红娘指标、统计周期、权重、并列规则和缓存策略。
  - 增加反作弊、异常数据剔除、申诉和人工修正。
  - 后台支持周期切换、权重配置、榜单下线和审计。
- 验收标准：榜单可解释、可重算、可追溯；异常服务和刷量不会直接提升排名。

## 五、红娘后台管理修改清单

### MA-01 全量 RBAC 权限落地

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 为账号管理、会员、红娘、商品、服务单、门店、资源、约见、财务、提现和报表定义细粒度权限码。
  - 所有后台路由显式调用权限校验，不允许仅依赖 `get_current_matchmaker_admin` 登录检查。
  - 增加权限矩阵接口和后台菜单权限映射。
  - 增加超级管理员、自身账号保护、禁止自锁、禁止越权授权等规则。
- 验收标准：无权限返回 `403`；权限修改即时生效；管理员不能授予自身超出授权范围的权限。

### MA-02 数据范围 SQL 隔离

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 统一实现 `SELF`、`STORE`、`ORGANIZATION`、`ALL` 数据范围解析。
  - 所有会员、服务单、资源、约见、财务、提现、统计和门店查询必须注入范围条件。
  - 对 URL 中的门店、组织、会员和红娘 ID 做资源归属校验。
  - 增加跨门店、跨组织、禁用账号和无效绑定测试。
- 验收标准：低范围账号无法通过改变 query、path 或 body ID 读取或修改其他组织数据；所有查询均有范围审计信息。

### MA-03 统一后台认证和审计

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 评估统一管理员 Token 与红娘后台 Token 的兼容方案，避免同一后台需要两次登录。
  - 统一登录、刷新、退出、会话撤销、密码重置和设备管理。
  - 对登录、授权失败、敏感读取、退款、提现、资源调整和资料发布写入审计日志。
  - 日志不得写入密码、Token、证件原图和联系方式明文。
- 验收标准：同一后台工作流可使用统一身份完成；敏感操作可按账号、时间、资源和结果追溯。

### MA-04 红娘运营工作台

- 优先级：P1
- 当前状态：未开始
- 修改内容：
  - 增加公开档案发布、成功案例审核、评价治理、预约时段、服务套餐和排行榜配置。
  - 增加申请待办、服务单待办、超时服务、待约见、退款和投诉工作台。
  - 增加按组织、门店、红娘、服务类型和状态的统计。
- 验收标准：运营人员可以不依赖数据库手工操作完成日常红娘业务；所有操作有权限和审计。

### MA-05 财务和资源安全

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 订单结算、退款、提现审核、佣金释放增加幂等键、状态锁和二次确认。
  - 统一订单金额、佣金、余额和提现流水的守恒校验。
  - 资源分配增加冲突检测、改派、结束和历史快照。
  - 财务和资源接口全部接入数据范围和组织过滤。
- 验收标准：重复退款、重复结算、重复释放、并发分配和跨组织操作均不能造成资金或资源异常。

## 六、社区 C 端修改清单

### C-01 社区动态和媒体闭环

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 统一动态发布的媒体 ID、媒体归属、绑定、删除和内容审核状态。
  - 补齐图片、视频、语音的大小、格式、时长、敏感内容和存储生命周期校验。
  - 统一实名、人脸和封禁门禁矩阵，并在 API 文档中逐项说明。
  - 明确动态流中的喜欢、关注、同城、最新和我的内容映射。
- 验收标准：不能绑定他人媒体；审核中内容不会错误公开；删除动态不会留下可访问媒体；门禁和文档一致。

### C-02 活动商业闭环

- 优先级：P1
- 当前状态：未完成
- 修改内容：
  - 增加活动支付订单、报名幂等、待审、通过、拒绝、取消、退款、签到和结束状态。
  - 报名审核拒绝时自动退款；未支付报名允许取消；活动结束后禁止报名。
  - 活动地点、联系方式和敏感资料按报名状态解锁。
  - 增加积分、奖励、优惠和异常补偿记录。
  - 增加客服答疑、分享和推荐活动关联。
- 验收标准：支付、审核、退款、签到和奖励状态一致；重复报名和重复回调不会重复扣款或发奖。

### C-03 话题和 Banner 内容消费

- 优先级：P1
- 当前状态：基础读取已完成，运营配置未完成
- 修改内容：
  - 明确话题参与、动态自动绑定话题、话题排序和话题状态。
  - 增加 Banner 类型、跳转目标、城市范围、开始结束时间、排序、上下线和素材审核。
  - 增加无效跳转、过期 Banner 和城市切换后的缓存刷新。
- 验收标准：运营配置无需改数据库；用户只看到当前城市、当前时间和当前状态可用的内容。

### C-04 纸飞机完整玩法

- 优先级：P1
- 当前状态：基础收发和匿名会话已完成，完整玩法未完成
- 修改内容：
  - 增加首次使用昵称、随机昵称、匿名头像和身份设置。
  - 增加三次免费捡拾额度、次数扣减、失败返还和付费/积分解锁规则。
  - 增加纸飞机撤回、收回、举报、隐藏、结束和内容审计。
  - 增加资料积分解锁、媒体权限和匿名状态转换。
- 验收标准：匿名身份不会泄露真实用户信息；次数和积分守恒；结束或收回后前端不能继续发送消息。

### C-05 社区通知和统计

- 优先级：P1
- 当前状态：部分完成
- 修改内容：
  - 建立社区通知类型字典，覆盖点赞、评论、关注、话题、活动、纸飞机、审核和举报结果。
  - 增加分类筛选、分类未读、全部已读、跳转目标和过期策略。
  - 增加社区运营统计：发帖、互动、审核、举报、活动转化、纸飞机转化和留存。
  - 明确系统公告、用户通知、社区通知的数据职责。
- 验收标准：分类未读与列表一致；通知跳转资源受权限控制；统计可按时间、城市和活动查询。

## 七、社区后台管理修改清单

### CA-01 话题和 Banner 运营后台

- 优先级：P1
- 当前状态：部分完成
- 修改内容：
  - 话题创建、编辑、审核、推荐、排序、城市投放、上下线和操作审计。
  - Banner 创建、编辑、素材审核、跳转配置、城市投放、时间窗口、排序和上下线。
  - 增加预览、冲突检测和过期自动下线。
- 验收标准：运营人员可以通过后台完成话题和 Banner 全生命周期管理；前端无需猜字段或拼接规则。

### CA-02 活动运营后台

- 优先级：P1
- 当前状态：基础 CRUD 和报名审核已完成，商业闭环未完成
- 修改内容：
  - 增加活动支付订单查询、退款、报名批量审核、签到、取消和奖励补发。
  - 增加活动地点、报名资料和敏感字段权限控制。
  - 增加活动转化、收入、退款、签到和奖励统计。
- 验收标准：后台可完成活动从创建到结束的全流程；财务、报名、签到和奖励数据一致。

### CA-03 社区治理工作台

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 统一审核任务、举报、申诉、封禁、内容下架、恢复和证据查看。
  - 增加审核员权限、队列分配、超时提醒、复审和审计抽样。
  - 支持动态、评论、纸飞机、媒体和活动资料的统一治理。
  - 对查看敏感内容和导出操作增加二次确认与水印。
- 验收标准：每次治理动作都有任务、对象、原因、操作者、时间、前后状态和证据记录。

### CA-04 社区统计和配置

- 优先级：P1
- 当前状态：未开始
- 修改内容：
  - 增加城市、话题、活动、内容类型和时间维度统计。
  - 增加敏感词、额度、举报原因、活动规则和通知类型配置。
  - 增加配置版本、发布、回滚和审计。
- 验收标准：配置变更可回滚，统计口径固定且有字段说明。

## 八、消息 C 端修改清单

### MSG-01 申请认识状态和额度闭环

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 统一 PENDING、ACCEPTED、REJECTED、WITHDRAWN、EXPIRED 状态。
  - 明确拒绝返次、撤回返次、过期返次、每日额度和异常补偿规则。
  - 增加重复申请、拉黑、封禁、双重认证和关系变化校验。
- 验收标准：申请次数、拒绝次数和返还次数守恒；所有状态跃迁具备并发锁和审计。

### MSG-02 自动欢迎消息和通知事件

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 将“同意认识、创建/复用会话、写欢迎消息、通知对方”统一放入可重试事务流程。
  - 普通消息发送、结构化请求处理、撤回和举报接入统一通知服务。
  - 增加通知失败记录、重试、死信和人工补偿。
- 验收标准：同意操作只产生一条欢迎消息；通知失败可追踪并重试；重复回调不重复通知。

### MSG-03 会话和消息生命周期

- 优先级：P1
- 当前状态：部分完成
- 修改内容：
  - 统一消息状态：发送中、已发送、已送达、已读、已撤回、审核中、已屏蔽、发送失败。
  - 增加媒体消息的上传、绑定、审核、过期和删除。
  - 明确单侧隐藏、双方删除、撤回、保留期和恢复规则。
  - 增加消息内容敏感审核和违规消息处置。
- 验收标准：消息状态可解释、可审计；撤回和隐藏不绕过取证要求；媒体不会产生孤儿文件。

### MSG-04 实时通信

- 优先级：P2
- 当前状态：未开始
- 修改内容：
  - 设计 WebSocket 或 SSE 通道、鉴权、心跳、重连和多端连接管理。
  - 增加发送序号、幂等键、ACK、送达回执和离线补偿。
  - 与现有 HTTP 历史消息接口兼容，支持灰度切换和故障回退。
- 验收标准：断线重连不丢消息、不重复展示；实时服务异常时 HTTP 查询和补偿机制可用。

### MSG-05 结构化请求实际业务动作

- 优先级：P1
- 当前状态：部分完成
- 修改内容：
  - ANSWER_EXCHANGE 接受后进入答案交换页面和权限控制。
  - LOVE_MODE 接受后写入双方模式状态并支持撤回或失效。
  - WECHAT/PHONE 接受后按实名、付费、隐私和双方同意规则交付联系方式。
  - 增加拒绝、撤回、过期、隐藏和审计。
- 验收标准：结构化请求状态与实际权限一致；未授权用户不能看到联系方式或答案内容。

### MSG-06 公告、通知和推送

- 优先级：P1
- 当前状态：部分完成
- 修改内容：
  - 统一系统公告、个人通知、社区通知和活动通知模型。
  - 增加公告列表、详情、发布、撤回、已读、有效期和定向人群。
  - 接入微信订阅消息模板、发送记录、失败重试和退订处理。
  - 增加通知类型配置、跳转资源和过期策略。
- 验收标准：公告和通知职责不重叠；发送记录可追踪；失败消息可重试和人工补偿。

### MSG-07 AI 消息能力边界

- 优先级：P2
- 当前状态：AI 一期已完成基础能力，产品扩展未完成
- 修改内容：
  - 明确 AI 助手读取聊天上下文的范围、脱敏、保留期和用户授权。
  - 增加 AI 会话删除、上下文清理、敏感内容拦截和人工投诉。
  - 若实施 AI 分身或 AI 军师，单独设计身份标识、授权对象、提示词隔离和消息审计。
- 验收标准：AI 不读取无关用户或已删除消息；用户可以查看、删除和撤回 AI 上下文；AI 输出不冒充真实用户。

## 九、消息后台管理修改清单

### MSG-A01 消息管理工作台

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 增加按用户、会话、时间、消息类型、状态和举报编号查询会话和消息。
  - 增加会话详情、消息详情、上下文查看、违规标记、屏蔽、撤回和恢复接口。
  - 只返回脱敏数据，联系方式、证件和敏感内容需要更高权限。
- 验收标准：管理员只能查看授权范围内的数据；每次查看、导出和处置都有审计。

### MSG-A02 证据快照和合规取证

- 优先级：P0
- 当前状态：未开始
- 修改内容：
  - 增加消息证据快照、举报关联、哈希、时间、操作者和前后状态。
  - 增加带水印导出、导出审批、有效期和下载日志。
  - 明确消息保留期、删除、匿名化、法务冻结和恢复规则。
- 验收标准：导出内容不可被无痕修改；无权限账号不能导出；法务冻结期间不得清理数据。

### MSG-A03 公告和推送运营后台

- 优先级：P1
- 当前状态：部分完成
- 修改内容：
  - 公告创建、编辑、预览、定向发布、撤回、定时发布和已读统计。
  - 微信订阅模板配置、发送批次、失败记录、重试和退订统计。
  - 按用户、城市、会员、活动或组织定向推送，并校验数据范围。
- 验收标准：运营人员可完成公告和推送全生命周期管理，发送结果可追踪。

### MSG-A04 消息运营统计

- 优先级：P1
- 当前状态：未开始
- 修改内容：
  - 增加会话数、消息数、送达率、已读率、未读量、撤回量、举报量、违规量和处理时长统计。
  - 支持按日期、城市、会员、用户状态和消息类型筛选。
  - 明确统计口径、时区和数据刷新时间。
- 验收标准：统计结果可复算，并与消息、通知和举报明细一致。

## 十、接口文档修改清单

### DOC-01 补齐现有接口契约

- 优先级：P0
- 当前状态：部分完成
- 修改内容：
  - 补齐红娘、社区、消息及后台接口的完整 URL、方法、Header、权限、请求参数和返回字段。
  - 展开嵌套对象和数组字段，不能只写 `data: object` 或 `items: list`。
  - 增加成功、空数据、非法参数、未登录、无权限、状态冲突和幂等重试示例。
  - 标注业务时区、状态枚举、前置条件、调用顺序、频率限制和前端处理建议。
- 验收标准：前端不查看源代码即可完成接口联调。

### DOC-02 建立需求到验收追踪矩阵

- 优先级：P0
- 当前状态：未开始
- 修改内容：
  - 建立“产品需求 -> 修改项 -> API -> 数据表 -> 测试 -> 验收记录”映射。
  - 标记旧 `/loveadmin/*`、`/commonadmin/*` 路径的迁移关系。
  - 对转 P2 或阻塞待确认项保留原记录和原因。
- 验收标准：任何产品需求都可以追踪到代码、文档、数据和测试；未实现能力不会被误标为完成。

## 十一、推荐实施顺序

### 第一阶段：安全和主流程阻塞项

1. G-01、G-02、G-03、G-04、G-05。
2. MA-01、MA-02、MA-03、MA-05。
3. M-01、M-05、MSG-01、MSG-02。
4. C-01、CA-03。
5. DOC-01、DOC-02。

### 第二阶段：产品和运营闭环

1. M-02、M-03、M-06、MA-04。
2. C-02、C-03、C-04、C-05、CA-01、CA-02、CA-04。
3. MSG-03、MSG-05、MSG-06、MSG-A01、MSG-A02、MSG-A03、MSG-A04。

### 第三阶段：实时和 AI 扩展

1. M-04 私人定制服务。
2. MSG-04 实时通信。
3. MSG-07 AI 消息能力边界和扩展。
4. 运营统计、灰度发布、监控告警和旧平台迁移。

## 十二、最终完成定义

只有同时满足以下条件，模块才可标记为“完成”：

- 产品口径中不存在影响一期的未确认冲突。
- 所有接口文档、OpenAPI、Schema 和前端联调样例一致。
- 红娘后台所有路由完成细粒度 RBAC 和组织数据隔离。
- 红娘订单、服务单、约见、联系方式和评价形成闭环。
- 社区活动、纸飞机、话题、Banner、通知和治理形成闭环。
- 消息申请、会话、消息状态、结构化请求、通知、公告和后台取证形成闭环。
- 资金、积分、次数、额度、奖励和通知满足幂等、守恒和可补偿要求。
- 真实 MySQL、Redis、HTTP、并发和关键回调测试通过，关键测试不无条件跳过。
- 生产迁移、备份、回滚、监控、灰度和告警方案完成演练。
- P0/P1 缺陷清零，P2 项有明确延期记录和负责人。


## Phase 1 Implementation Record (2026-08-20)

### Modified items

- MA-01/MA-03: `app/api/dependencies.py` now derives back-office permission codes from path and HTTP method; `CurrentMatchmakerAdmin.scope_condition` supports SELF, STORE, ORGANIZATION and ALL; account self-protection prevents changing own permissions or locking own account.
- MA-02/M-05: `admin_list_service_requests` and `admin_update_service_request` apply matchmaker organization-scope filtering, so service requests are not protected by login-only checks.
- MSG-01/MSG-02: normal messages, structured chat requests, request responses and recalls write unified notifications; notification writes commit in the same transaction as the main message/request write; structured requests use duplicate-pending checks and row locks.
- G-04: the OpenAPI tag test now validates names/descriptions instead of order, so insertion of the AI tag does not create a false failure.
- DOC-01/DOC-02: module API documentation was supplemented and `docs/phase1_traceability_2026-08-20.md` was added.

### Main interfaces

- `GET /api/v1/admin/matchmaker/service-requests`: scope-filtered service request listing.
- `PATCH /api/v1/admin/matchmaker/service-requests/{service_id}`: permission- and scope-filtered service request update.
- `POST /api/v1/chat/sessions/{session_id}/messages`: sends a message and emits a message notification.
- `POST /api/v1/chat/sessions/{session_id}/requests`: creates a structured request and notifies the recipient.
- `PATCH /api/v1/chat/session-requests/{request_id}`: accepts, rejects or withdraws a structured request and notifies the other party.
- Message recall endpoint: hides body/media after recall and notifies the recipient.
- Public matchmaker list/detail: returns only approved, active users with an active service-matchmaker role.

### Verification

- Compilation check passed.
- Full test suite: `220 passed, 13 skipped, 1 warning`. Skips are caused by missing `LIVE_API_BASE` or the real community test database.

### Not marked production-complete

Formal migration/rollback drills, remaining back-office scope integrations, service timeout/reassignment and refund permission reclaim, Outbox/dead-letter retries, real community governance database tests, and complete per-endpoint examples remain partial and are retained as such in the traceability matrix.
## 当前代码复核修正（2026-08-20）

本节以当前工作区代码、顶层路由注册结果和全量测试为准，覆盖此前实施记录中可能提前标记的第二阶段事项。

### 当前已确认可用的能力

- 现有红娘公开列表、详情、商品、服务订单、服务单、约见、联系方式授权和申请审核接口继续可用。
- 现有社区动态、评论、话题消费、活动报名、Banner 消费、纸飞机、举报和治理接口继续可用。
- 现有消息会话、普通消息、游标消息、已读、撤回、结构化请求、通知和隐私接口继续可用。
- 红娘后台权限推导、账号自保护、服务单数据范围过滤和既有社区治理审计仍保留。
- 当前全量测试结果为 `220 passed, 13 skipped, 1 warning`。

### 第二阶段代码文件状态

| 事项 | 当前代码情况 | 实际接口状态 | 文档状态 |
|---|---|---|---|
| M-02 红娘评价 | `app/schemas/matchmaker.py` 和 `app/services/matchmaker.py` 已有评价 Schema 和服务函数 | 未发现评价路由已挂载到 OpenAPI | 进行中，不得标记已完成 |
| CA-01 话题/Banner 运营 | `app/api/routes/community_admin.py` 已有草稿路由 | 未被 `app/api/router.py` 顶层 `include_router` 挂载 | 进行中，不得标记接口可联调 |
| MSG-A01 消息后台 | `app/api/routes/message_admin.py`、`app/services/message_admin.py` 已有草稿实现 | 未被顶层路由挂载 | 进行中，不得标记后台已上线 |
| MSG-A03 公告后台 | 草稿服务和路由存在 | 未被顶层路由挂载，且仅覆盖创建 | 进行中 |
| C-03 话题/Banner 消费 | 现有 `community.py` 已提供消费侧接口 | 可通过现有社区路由访问 | 部分完成 |

### 不能作为已交付接口的路径

以下路径在草稿路由文件或此前实施记录中出现，但当前 OpenAPI 未发现，不能作为已交付接口：

- `POST /api/v1/matchmaker/service-requests/{service_id}/rating`
- `GET /api/v1/matchmakers/{matchmaker_id}/ratings`
- `/api/v1/admin/community/topics`
- `/api/v1/admin/community/banners`
- `/api/v1/admin/messages`
- `/api/v1/admin/messages/{message_id}/moderation`
- `/api/v1/admin/messages/announcements`

### 当前第二阶段结论

第二阶段目前应评估为“基础代码已有、可访问运营闭环未完成”。代码文件存在不等于路由已经挂载，也不等于已经完成权限、数据库、审计和前端联调验收。

## 二十二、现有代码复核后的状态更新（2026-08-24）

### 22.1 已完成或已具备代码基础

| 编号 | 模块 | 当前状态 | 代码依据 | 说明 |
|---|---|---|---|---|
| G-02 | 统一状态和范围基础 | 部分完成 | `app/services/social.py`、`app/services/matchmaker.py`、`app/api/dependencies.py` | 关键状态和部分范围校验已存在，仍需全量组织 SQL 审计。 |
| G-03 | 幂等、事务和通知基础 | 部分完成 | `app/core/idempotency.py`、`app/services/notifications.py`、社区/消息服务 | 部分流程已有事务内通知和幂等，Outbox、重试、死信未完成。 |
| G-04 | 测试和契约基础 | 已完成本地基础 | `tests/` | 当前全量 `225 passed, 13 skipped, 1 warning`；真实环境测试仍未完成。 |
| G-05 | 数据库初始化基础 | 部分完成 | `database_setup_marriage.py` | 现有表和索引初始化较完整，但正式迁移/回滚管线仍缺失。 |
| M-01/M-05 | 红娘资料、服务申请和联系授权 | 部分完成 | `app/api/routes/matchmaker.py`、`app/services/matchmaker.py` | 主流程已挂载；超时、退款回收、重新分派和完整服务闭环未完成。 |
| MA-01/MA-03 | 红娘后台账号、认证和部分权限 | 部分完成 | `app/api/routes/matchmaker_admin_account.py`、`dependencies.py` | 路由已存在，但完整权限矩阵和生产审计需补。 |
| MA-04 | 红娘记录、CRM和仪表盘基础 | 部分完成 | `member_records_admin.py`、`matchmaker_crm_admin.py`、`matchmaker_dashboard_admin.py` | 当前已进入顶层路由，仍需真实数据范围和统计口径验收。 |
| C-01 | 社区动态、媒体、评论和互动 | 部分完成 | `community.py`、`community_media.py`、`social.py` | C 端基础闭环可用，商业化和全量环境验收未完成。 |
| C-02/C-03 | 活动、话题和 Banner 消费 | 部分完成 | `community.py`、`activity_admin.py` | C 端消费和活动后台基础已挂载，完整运营闭环未完成。 |
| C-04 | 纸飞机 | 部分完成 | `community.py`、`community.py service` | 基础发送、捡取、查看和回复能力存在，完整额度/商业化/统计未完成。 |
| CA-03 | 社区治理 | 部分完成 | `admin.py`、`community.py`、`moderation.py` | 举报、审核、下架/恢复基础可用，运营工作台和水印敏感导出未完成。 |
| MSG-01/MSG-03/MSG-05 | 消息会话、消息生命周期和结构化请求 | 部分完成 | `social.py`、`app/services/social.py` | HTTP 主流程和状态机基础存在，实时通信和完整业务动作未完成。 |
| MSG-02 | 消息通知基础 | 部分完成 | `app/services/notifications.py`、`social.py` | 部分通知在同一事务中写入，Outbox/推送可靠性未完成。 |
| MSG-06 | 公告查询基础 | 部分完成 | `app/api/routes/admin_home.py` | 管理端公告查询已挂载；公告创建、发布、下线和运营审计未形成闭环。 |

### 22.2 当前必须继续处理的任务

| 编号 | 任务 | 当前问题 | 下一步 |
|---|---|---|---|
| CA-01 | 挂载社区话题/Banner 后台 | `app/api/routes/community_admin.py` 存在但未加入 `app/api/router.py` | 增加顶层注册、权限矩阵、组织范围、接口文档和路由测试。 |
| MSG-A01 | 挂载消息后台 | `app/api/routes/message_admin.py` 存在但未加入 `app/api/router.py` | 增加顶层注册、管理员依赖、消息查询/处置/公告接口契约和审计测试。 |
| MSG-A02 | 消息证据和处置审计 | 当前服务基础处置字段不足 | 增加证据快照、处置前后状态、操作者、原因和敏感字段脱敏。 |
| MSG-A03 | 公告管理闭环 | 当前只有公告查询和未挂载创建服务 | 补创建、发布、下线、版本、定时生效和审计。 |
| MA-02 | 全量数据范围 SQL | 仍不能证明所有后台查询遵守组织范围 | 对 SELF/STORE/ORGANIZATION/ALL 逐接口做真实数据库隔离测试。 |
| C-05/CA-04 | 社区统计和配置 | 现有消费接口不能证明统计和配置完成 | 明确指标、聚合口径、权限和导出审批。 |
| MSG-04/MSG-06 | 实时通信与推送 | 当前以 HTTP 和数据库通知为主 | 增加 WebSocket/推送适配、Outbox、重试、死信和批次监控。 |
| G-05 | 正式迁移回滚 | 当前初始化脚本不是生产迁移流程 | 增加版本化迁移、回滚和发布窗口校验。 |

### 22.3 开发顺序更新

1. 先挂载 `community_admin` 和 `message_admin`，但在挂载前先完成权限和路径冲突检查。
2. 为挂载接口补齐完整 `docs/api/` 文档和 OpenAPI 契约测试。
3. 对后台接口补齐数据范围、审计、敏感字段和错误码。
4. 完成公告、消息处置和社区运营最小闭环。
5. 增加真实数据库隔离、事务、通知和额度测试。
6. 建设 Outbox、推送重试、死信和统计监控。
7. 最后进行实时通信、导出审批和生产灰度。

### 22.4 本次代码复核验收记录

- [x] 已复核顶层 API 路由注册。
- [x] 已复核社区 C 端和消息 C 端路由存在性。
- [x] 已复核社区后台和消息后台文件存在但未挂载的事实。
- [x] 已运行全量测试：`225 passed, 13 skipped, 1 warning`。
- [ ] 未完成真实 MySQL、Redis、HTTP、支付、推送和并发环境验收。
- [ ] 未将未挂载后台路由标记为完成。

### 22.5 文档状态说明

本清单保留原有任务编号和历史记录，并在本节追加当前代码复核结果。后续每次路由挂载、权限矩阵、数据库迁移或真实环境验收完成后，应继续在本文件追加日期、代码文件、测试结果和剩余阻塞，不直接删除历史结论。


## 二十三、本轮后端实施记录（2026-08-24）

本节承接第 22 节的复核结论，记录已经完成的代码修改、验证结果和后续开发顺序。第 22 节中“未挂载”的描述保留为修改前历史状态，当前状态以本节为准。

### 23.1 已修改文件

| 文件 | 修改内容 |
|---|---|
| `app/api/router.py` | 挂载社区运营后台和消息后台路由。 |
| `app/api/routes/community_admin.py` | 完善话题/Banner 增改查、全局写限制、资源冲突处理、行锁和前后审计快照。 |
| `app/api/routes/message_admin.py` | 挂载消息查询、消息处置和公告创建入口，并传递完整管理员上下文。 |
| `app/services/message_admin.py` | 增加四类数据范围过滤、越权隐藏、手机号脱敏、撤回/恢复行锁、前后证据快照和公告全局写限制。 |
| `app/schemas/community.py` | 修正 Banner 时间和跳转字段的可读校验提示。 |
| `app/schemas/message_admin.py` | 整理后台消息和公告 Schema 契约。 |
| `tests/test_community_message_admin.py` | 新增路由、认证、全局写、范围 SQL 和脱敏测试。 |
| `docs/api/社区运营与消息后台-2026-08-24.md` | 新增 9 个后台接口的完整联调契约。 |

### 23.2 任务状态更新

| 编号 | 当前状态 | 本轮完成内容 | 仍需完成 |
|---|---|---|---|
| CA-01 | 部分完成（最小闭环已交付） | 已挂载话题/Banner 后台；完成查询、创建、修改、重复名称校验、404/409、全局写限制、更新行锁和审计快照。 | Banner 同位置时间冲突检测、独立上下线、版本回滚、多租户/组织模型、真实数据库并发测试。 |
| MSG-A01 | 部分完成（最小闭环已交付） | 已挂载消息列表、撤回/恢复处置和公告创建；查询与处置应用 `SELF/STORE/ORGANIZATION/ALL` 范围；越权资源按 404 处理。 | 多维筛选、批量处置、运营工作台、真实数据库范围矩阵、消息统计。 |
| MSG-A02 | 部分完成 | 消息处置已记录脱敏后的前后快照、操作者、动作和原因；列表与快照中的大陆手机号已脱敏。 | 敏感字段分类分级、原始证据加密留存、导出审批、下载水印、下载审计、保留期和销毁策略。 |
| MSG-A03 | 部分完成 | 已提供公告创建接口，仅允许全局范围管理员操作，并记录创建后快照。 | 编辑、下线、版本、审批、定时发布、发布失败补偿和推送批次。 |
| MSG-A04 | 未开始 | 本轮未新增消息运营统计。 | 冻结指标口径后建设发送量、送达率、已读率、失败率、处置量和公告效果统计。 |
| MA-02 | 部分完成 | 消息后台已按管理员范围拼接参数化 SQL。 | 在真实 MySQL 中覆盖四类范围、子组织、无分配、历史分配和跨组织越权用例。 |
| G-05 | 未开始 | 本轮未修改表结构，没有新增生产迁移。 | 发布前确认依赖表、索引、审计容量，补版本化迁移和回滚脚本。 |

### 23.3 本轮开发步骤完成情况

- [x] 检查草稿路由与顶层路径冲突。
- [x] 在顶层 API 注册社区运营与消息后台路由。
- [x] 为平台级社区配置和公告增加全局写门禁。
- [x] 为消息查询和处置增加管理员数据范围过滤。
- [x] 为越权消息采用 404 隐藏资源存在性。
- [x] 为消息处置和社区配置修改增加行锁与审计快照。
- [x] 为消息列表和审计快照增加手机号脱敏。
- [x] 补齐接口契约文档和自动化契约测试。
- [x] 完成编译、Ruff、相关模块测试和全量回归。
- [ ] 完成真实 MySQL 的权限范围、事务和并发验收。
- [ ] 完成公告完整生命周期、消息导出取证和推送可靠性建设。

### 23.4 验证记录

| 验证项 | 结果 |
|---|---|
| Python 编译检查 | 通过 |
| Ruff 改动文件检查 | 通过 |
| 相关模块测试 | `60 passed, 4 skipped, 1 warning` |
| 全量测试 | `230 passed, 13 skipped, 1 warning` |
| OpenAPI 路径 | 社区 6 个、消息 3 个后台路径已注册 |

13 个跳过项仍依赖真实 HTTP 和测试数据库环境。当前自动化结果不能替代生产 MySQL、Redis、推送、并发和组织隔离验收。

### 23.5 后续开发顺序

1. **真实数据范围验收**：准备测试组织树、红娘账号、用户分配和跨组织数据，逐一验证 `SELF/STORE/ORGANIZATION/ALL` 查询与处置边界。
2. **社区运营完整化**：增加 Banner 位置与时间窗口冲突检测、上下线、版本和回滚，再决定是否引入租户/组织字段。
3. **公告生命周期**：实现查询详情、编辑、审批、定时发布、下线、版本记录和失败补偿。
4. **消息合规取证**：建立敏感字段分级、原始证据加密、导出审批、水印、下载审计、保留期和销毁流程。
5. **可靠通知链路**：建设 Outbox、推送批次、幂等、重试、死信和告警，再接入实时通信或第三方推送。
6. **运营统计**：冻结统计口径后实现消息、公告和社区运营指标及受控导出。
7. **生产发布准备**：评审索引和审计容量，补迁移/回滚脚本，完成灰度、监控和故障演练。

### 23.6 现有代码仍需关注的问题

- 社区配置表缺少组织或租户维度，当前只能通过全局写门禁保证安全，无法支持分区域独立运营。
- 消息范围依赖用户资源分配数据；分配缺失或历史数据不完整时，管理员可能看不到应管理的消息，需要数据治理和监控。
- 数据范围查询包含组织子树子查询，真实数据量下需要执行计划和索引验证。
- 当前脱敏仅覆盖大陆手机号，不能代替完整 DLP 和敏感信息分类体系。
- 审计快照存储在业务审计表中，需要评估内容长度、增长速度、访问权限、加密和归档。
- 公告固定写入默认租户是当前单租户框架约束，不是最终多租户方案。
- 后台接口当前是最小可联调框架，不应将其描述为完整的社区运营平台或消息运营平台。

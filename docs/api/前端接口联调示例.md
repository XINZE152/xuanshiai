# 前端接口联调示例

> 本文档由 `scripts/generate_frontend_api_examples.py` 根据当前 FastAPI OpenAPI 定义生成。每个 HTTP 操作独立成节，示例值用于开发联调，不是生产密钥。字段含义优先参考对应模块中文文档；本文件重点保证请求和响应 JSON 结构完整。

## 通用调用约定

- 服务地址：`http://127.0.0.1:8000`；所有 `/api/v1` 路径均以此为基础拼接。
- 登录接口之后，将返回的 `access_token` 放入 `Authorization: Bearer <access-token>`。
- JSON 请求使用 `Content-Type: application/json`；上传接口按文档标注的 `multipart/form-data` 提交。
- `204 No Content` 响应没有 JSON，前端不要调用 `response.json()`。
- 示例中的 `<access-token>`、`<signed-payload>` 和文件路径必须替换为实际值。

## 1. `GET /api/v1/users/me/location` 查询我的位置共享状态

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/location`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "enabled": true,
  "latitude": 1.2,
  "longitude": 1.2,
  "accuracy_m": 1.2,
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `enabled` | boolean | 是 | 是否开启该功能 | `true` |
| `latitude` | number | 是 | 纬度，范围 -90 到 90 | `1.2` |
| `longitude` | number | 是 | 经度，范围 -180 到 180 | `1.2` |
| `accuracy_m` | number | 是 | 接口中的 `accuracy_m` 字段 | `1.2` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 2. `POST /api/v1/users/me/location` 上报当前位置

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/location`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "latitude": 1.2,
  "longitude": 1.2,
  "accuracy_m": 1.2,
  "source": "device"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `latitude` | number | 是 | minimum=-90.0; maximum=90.0 | 纬度，范围 -90 到 90 | `1.2` |
| `longitude` | number | 是 | minimum=-180.0; maximum=180.0 | 经度，范围 -180 到 180 | `1.2` |
| `accuracy_m` | number | 否 | minimum=0.0; maximum=10000.0 | 接口中的 `accuracy_m` 字段 | `1.2` |
| `source` | string | 否 | minLength=1; maxLength=32 | 接口中的 `source` 字段 | `"device"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "enabled": true,
  "latitude": 1.2,
  "longitude": 1.2,
  "accuracy_m": 1.2,
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `enabled` | boolean | 是 | 是否开启该功能 | `true` |
| `latitude` | number | 是 | 纬度，范围 -90 到 90 | `1.2` |
| `longitude` | number | 是 | 经度，范围 -180 到 180 | `1.2` |
| `accuracy_m` | number | 是 | 接口中的 `accuracy_m` 字段 | `1.2` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 3. `DELETE /api/v1/users/me/location` 关闭位置共享

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/location`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "enabled": true,
  "latitude": 1.2,
  "longitude": 1.2,
  "accuracy_m": 1.2,
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `enabled` | boolean | 是 | 是否开启该功能 | `true` |
| `latitude` | number | 是 | 纬度，范围 -90 到 90 | `1.2` |
| `longitude` | number | 是 | 经度，范围 -180 到 180 | `1.2` |
| `accuracy_m` | number | 是 | 接口中的 `accuracy_m` 字段 | `1.2` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 4. `PUT /api/v1/users/me/location/sharing` 开启或关闭位置共享

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/location/sharing`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "enabled": true
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `enabled` | boolean | 是 | 按 Schema 类型校验 | 是否开启该功能 | `true` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "enabled": true,
  "latitude": 1.2,
  "longitude": 1.2,
  "accuracy_m": 1.2,
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `enabled` | boolean | 是 | 是否开启该功能 | `true` |
| `latitude` | number | 是 | 纬度，范围 -90 到 90 | `1.2` |
| `longitude` | number | 是 | 经度，范围 -180 到 180 | `1.2` |
| `accuracy_m` | number | 是 | 接口中的 `accuracy_m` 字段 | `1.2` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 5. `GET /api/v1/users/nearby` 查询附近在线用户

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/nearby`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `latitude` | query | number | 是 | minimum=-90；maximum=90 | 接口参数 | `1.2` |
| `longitude` | query | number | 是 | minimum=-180；maximum=180 | 接口参数 | `1.2` |
| `radius_km` | query | number | 否 | maximum=100 | 接口参数 | `20` |
| `limit` | query | integer | 否 | minimum=1；maximum=200 | 接口参数 | `100` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "latitude": 1.2,
  "longitude": 1.2,
  "radius_km": 20,
  "limit": 100
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "latitude": 1.2,
      "longitude": 1.2,
      "distance_km": 1.2,
      "online": true,
      "location_updated_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "total": 1,
  "nearest_distance_km": 1.2,
  "radius_km": 1.2
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "latitude": 1.2, "longitude": 1.2, "distance_km": 1.2, "online": true, "location_updated_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].latitude` | number | 是 | 纬度，范围 -90 到 90 | `1.2` |
| `items[].longitude` | number | 是 | 经度，范围 -180 到 180 | `1.2` |
| `items[].distance_km` | number | 是 | 与目标的距离，单位公里 | `1.2` |
| `items[].online` | boolean | 是 | 是否在线 | `true` |
| `items[].location_updated_at` | string | 是 | 接口中的 `items[].location_updated_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `nearest_distance_km` | number | 是 | 接口中的 `nearest_distance_km` 字段 | `1.2` |
| `radius_km` | number | 是 | 查询半径，单位公里 | `1.2` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 6. `GET /api/v1/health` 服务健康检查

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/health`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 7. `POST /api/v1/auth/sms/send` 发送短信验证码

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/auth/sms/send`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "phone": "11",
  "purpose": "login"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `phone` | string | 是 | pattern=^1[3-9]\d{9}$ | 11位大陆手机号 | `"11"` |
| `purpose` | login/bind_phone | 否 | 枚举=login, bind_phone | 接口中的 `purpose` 字段 | `"login"` |

### 成功返回示例

成功状态码：`202`。

```json
{
  "message": "example",
  "expires_in": 1,
  "retry_after": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `message` | string | 是 | 接口中的 `message` 字段 | `"example"` |
| `expires_in` | integer | 是 | 接口中的 `expires_in` 字段 | `1` |
| `retry_after` | integer | 是 | 接口中的 `retry_after` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 8. `POST /api/v1/auth/phone/login` 手机号验证码登录

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/auth/phone/login`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "phone": "11",
  "purpose": "login",
  "code": "11",
  "device_id": "example",
  "platform": "example",
  "app_version": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `phone` | string | 是 | pattern=^1[3-9]\d{9}$ | 11位大陆手机号 | `"11"` |
| `purpose` | login/bind_phone | 否 | 枚举=login, bind_phone | 接口中的 `purpose` 字段 | `"login"` |
| `code` | string | 是 | minLength=6; maxLength=6; pattern=^\d{6}$ | 短信验证码，必须为6位数字 | `"11"` |
| `device_id` | string | 否 | maxLength=128 | 接口中的 `device_id` 字段 | `"example"` |
| `platform` | string | 否 | maxLength=32 | 接口中的 `platform` 字段 | `"example"` |
| `app_version` | string | 否 | maxLength=32 | 接口中的 `app_version` 字段 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "access_token": "<access-token>",
  "refresh_token": "<access-token>",
  "token_type": "bearer",
  "expires_in": 1,
  "need_bind_phone": true,
  "user_id": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `access_token` | string | 是 | 访问受保护接口的短期 Token | `"<access-token>"` |
| `refresh_token` | string | 是 | 用于刷新访问 Token 的长期 Token | `"<access-token>"` |
| `token_type` | string | 否 | 接口中的 `token_type` 字段 | `"bearer"` |
| `expires_in` | integer | 是 | 接口中的 `expires_in` 字段 | `1` |
| `need_bind_phone` | boolean | 是 | 接口中的 `need_bind_phone` 字段 | `true` |
| `user_id` | integer | 是 | 用户 ID | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 9. `POST /api/v1/auth/wechat/login` 微信登录

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/auth/wechat/login`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "code": "example",
  "nickname": "example",
  "avatar": "/storage/uploads/example.jpg",
  "device_id": "example",
  "platform": "example",
  "app_version": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `code` | string | 是 | minLength=1; maxLength=512 | 业务编码或地区编码 | `"example"` |
| `nickname` | string | 否 | maxLength=64 | 用户昵称 | `"example"` |
| `avatar` | string | 否 | maxLength=255 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `device_id` | string | 否 | maxLength=128 | 接口中的 `device_id` 字段 | `"example"` |
| `platform` | string | 否 | maxLength=32 | 接口中的 `platform` 字段 | `"example"` |
| `app_version` | string | 否 | maxLength=32 | 接口中的 `app_version` 字段 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "access_token": "<access-token>",
  "refresh_token": "<access-token>",
  "token_type": "bearer",
  "expires_in": 1,
  "need_bind_phone": true,
  "user_id": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `access_token` | string | 是 | 访问受保护接口的短期 Token | `"<access-token>"` |
| `refresh_token` | string | 是 | 用于刷新访问 Token 的长期 Token | `"<access-token>"` |
| `token_type` | string | 否 | 接口中的 `token_type` 字段 | `"bearer"` |
| `expires_in` | integer | 是 | 接口中的 `expires_in` 字段 | `1` |
| `need_bind_phone` | boolean | 是 | 接口中的 `need_bind_phone` 字段 | `true` |
| `user_id` | integer | 是 | 用户 ID | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 10. `POST /api/v1/auth/test-login` 使用已有账号测试登录

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/auth/test-login`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "phone": "11",
  "password": "Password123",
  "device_id": "example",
  "platform": "example",
  "app_version": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `phone` | string | 是 | pattern=^1[3-9]\d{9}$ | 手机号；返回时通常脱敏 | `"11"` |
| `password` | string | 是 | minLength=8; maxLength=128 | 接口中的 `password` 字段 | `"Password123"` |
| `device_id` | string | 否 | maxLength=128 | 接口中的 `device_id` 字段 | `"example"` |
| `platform` | string | 否 | maxLength=32 | 接口中的 `platform` 字段 | `"example"` |
| `app_version` | string | 否 | maxLength=32 | 接口中的 `app_version` 字段 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "access_token": "<access-token>",
  "refresh_token": "<access-token>",
  "token_type": "bearer",
  "expires_in": 1,
  "need_bind_phone": true,
  "user_id": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `access_token` | string | 是 | 访问受保护接口的短期 Token | `"<access-token>"` |
| `refresh_token` | string | 是 | 用于刷新访问 Token 的长期 Token | `"<access-token>"` |
| `token_type` | string | 否 | 接口中的 `token_type` 字段 | `"bearer"` |
| `expires_in` | integer | 是 | 接口中的 `expires_in` 字段 | `1` |
| `need_bind_phone` | boolean | 是 | 接口中的 `need_bind_phone` 字段 | `true` |
| `user_id` | integer | 是 | 用户 ID | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 11. `POST /api/v1/auth/bind-phone` 绑定手机号

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/auth/bind-phone`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "phone": "11",
  "purpose": "login",
  "code": "11",
  "device_id": "example",
  "platform": "example",
  "app_version": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `phone` | string | 是 | pattern=^1[3-9]\d{9}$ | 11位大陆手机号 | `"11"` |
| `purpose` | login/bind_phone | 否 | 枚举=login, bind_phone | 接口中的 `purpose` 字段 | `"login"` |
| `code` | string | 是 | minLength=6; maxLength=6; pattern=^\d{6}$ | 短信验证码，必须为6位数字 | `"11"` |
| `device_id` | string | 否 | maxLength=128 | 接口中的 `device_id` 字段 | `"example"` |
| `platform` | string | 否 | maxLength=32 | 接口中的 `platform` 字段 | `"example"` |
| `app_version` | string | 否 | maxLength=32 | 接口中的 `app_version` 字段 | `"example"` |

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 12. `POST /api/v1/auth/refresh` 刷新登录令牌

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/auth/refresh`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "refresh_token": "<access-token>xxxxxx"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `refresh_token` | string | 是 | minLength=20; maxLength=512 | 用于刷新访问 Token 的长期 Token | `"<access-token>xxxxxx"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "access_token": "<access-token>",
  "refresh_token": "<access-token>",
  "token_type": "bearer",
  "expires_in": 1,
  "need_bind_phone": true,
  "user_id": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `access_token` | string | 是 | 访问受保护接口的短期 Token | `"<access-token>"` |
| `refresh_token` | string | 是 | 用于刷新访问 Token 的长期 Token | `"<access-token>"` |
| `token_type` | string | 否 | 接口中的 `token_type` 字段 | `"bearer"` |
| `expires_in` | integer | 是 | 接口中的 `expires_in` 字段 | `1` |
| `need_bind_phone` | boolean | 是 | 接口中的 `need_bind_phone` 字段 | `true` |
| `user_id` | integer | 是 | 用户 ID | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 13. `POST /api/v1/auth/logout` 退出当前设备

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/auth/logout`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 14. `POST /api/v1/auth/logout-all` 退出所有设备

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/auth/logout-all`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 15. `GET /api/v1/auth/me` 获取当前用户信息

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/auth/me`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "phone_masked": "13800138000",
  "nickname": "example",
  "avatar": "/storage/uploads/example.jpg",
  "status": 1,
  "phone_verified": true,
  "realname_status": 1,
  "face_verified": 0,
  "need_bind_phone": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `phone_masked` | string | 是 | 接口中的 `phone_masked` 字段 | `"13800138000"` |
| `nickname` | string | 是 | 用户昵称 | `"example"` |
| `avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `phone_verified` | boolean | 是 | 接口中的 `phone_verified` 字段 | `true` |
| `realname_status` | integer | 是 | 接口中的 `realname_status` 字段 | `1` |
| `face_verified` | integer | 是 | 人脸认证状态：`0`未认证、`1`通过、`2`失败 | `0` |
| `need_bind_phone` | boolean | 是 | 接口中的 `need_bind_phone` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 16. `POST /api/v1/auth/agreements/accept` 签署用户协议

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/auth/agreements/accept`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "agreement_type": "user_service",
  "version": "example",
  "content_hash": "examplexxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
  "scene": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `agreement_type` | user_service/privacy_policy/safety_pledge/community_rules | 是 | 枚举=user_service, privacy_policy, safety_pledge, community_rules | 接口中的 `agreement_type` 字段 | `"user_service"` |
| `version` | string | 是 | minLength=1; maxLength=32 | 接口中的 `version` 字段 | `"example"` |
| `content_hash` | string | 否 | minLength=64; maxLength=64 | 接口中的 `content_hash` 字段 | `"examplexxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"` |
| `scene` | string | 否 | maxLength=32 | 接口中的 `scene` 字段 | `"example"` |

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 17. `GET /api/v1/auth/agreements` 查询当前协议版本

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/auth/agreements`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 18. `POST /api/v1/auth/realname` 提交实名认证

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/auth/realname`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "real_name": "example",
  "id_card": "110101199001011234"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `real_name` | string | 是 | minLength=2; maxLength=64 | 接口中的 `real_name` 字段 | `"example"` |
| `id_card` | string | 是 | pattern=^[1-9]\d{5}(18|19|20)\d{2}(0[1-9]|1[0-2])(0[1-9]|[12]\d|3[01])\d{3}[0-9Xx]$ | 接口中的 `id_card` 字段 | `"110101199001011234"` |

### 成功返回示例

成功状态码：`200`。

```json
{}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 19. `GET /api/v1/users/me/overview` 获取我的页面聚合信息

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/overview`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "user_id": 1,
  "nickname": "example",
  "avatar": "/storage/uploads/example.jpg",
  "account_status": 1,
  "completion_score": 1.2,
  "certification": {
    "status": 0,
    "label": "example"
  },
  "membership": {
    "is_vip": true,
    "package_type": "example",
    "expires_at": "2026-08-05T10:00:00+08:00"
  },
  "unread_notification_count": 1,
  "incoming_application_count": 1,
  "outgoing_application_count": 1,
  "match_count": 1,
  "visitor_count": 1,
  "favorite_count": 1,
  "favorite_received_count": 1,
  "superlike_sent_count": 1,
  "superlike_received_count": 1,
  "shortcuts": {
    "can_browse": true,
    "can_apply": true,
    "can_chat": true,
    "can_edit_profile": true,
    "can_manage_media": true
  }
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `nickname` | string | 是 | 用户昵称 | `"example"` |
| `avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `account_status` | 1/2/3 | 是 | 枚举=1, 2, 3 | 接口中的 `account_status` 字段 | `1` |
| `completion_score` | number | 是 | 接口中的 `completion_score` 字段 | `1.2` |
| `certification` | object | 是 | 接口中的 `certification` 字段 | `{"status": 0, "label": "example"}` |
| `certification.status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `certification.label` | string | 是 | 接口中的 `certification.label` 字段 | `"example"` |
| `membership` | object | 是 | 接口中的 `membership` 字段 | `{"is_vip": true, "package_type": "example", "expires_at": "2026-08-05T10:00:00+08:00"}` |
| `membership.is_vip` | boolean | 是 | 当前是否为有效会员 | `true` |
| `membership.package_type` | string | 是 | 接口中的 `membership.package_type` 字段 | `"example"` |
| `membership.expires_at` | string | 是 | 到期时间 | `"2026-08-05T10:00:00+08:00"` |
| `unread_notification_count` | integer | 是 | 接口中的 `unread_notification_count` 字段 | `1` |
| `incoming_application_count` | integer | 是 | 接口中的 `incoming_application_count` 字段 | `1` |
| `outgoing_application_count` | integer | 是 | 接口中的 `outgoing_application_count` 字段 | `1` |
| `match_count` | integer | 是 | 接口中的 `match_count` 字段 | `1` |
| `visitor_count` | integer | 是 | 接口中的 `visitor_count` 字段 | `1` |
| `favorite_count` | integer | 是 | 接口中的 `favorite_count` 字段 | `1` |
| `favorite_received_count` | integer | 是 | 接口中的 `favorite_received_count` 字段 | `1` |
| `superlike_sent_count` | integer | 是 | 接口中的 `superlike_sent_count` 字段 | `1` |
| `superlike_received_count` | integer | 是 | 接口中的 `superlike_received_count` 字段 | `1` |
| `shortcuts` | object | 是 | 接口中的 `shortcuts` 字段 | `{"can_browse": true, "can_apply": true, "can_chat": true, "can_edit_profile": true, "can_manage_media": true}` |
| `shortcuts.can_browse` | boolean | 是 | 接口中的 `shortcuts.can_browse` 字段 | `true` |
| `shortcuts.can_apply` | boolean | 是 | 接口中的 `shortcuts.can_apply` 字段 | `true` |
| `shortcuts.can_chat` | boolean | 是 | 接口中的 `shortcuts.can_chat` 字段 | `true` |
| `shortcuts.can_edit_profile` | boolean | 否 | 接口中的 `shortcuts.can_edit_profile` 字段 | `true` |
| `shortcuts.can_manage_media` | boolean | 否 | 接口中的 `shortcuts.can_manage_media` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 20. `GET /api/v1/users/me/profile` 获取个人资料

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/profile`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "user_id": 1,
  "nickname": "example",
  "gender": 1,
  "birthday": "2026-08-05",
  "age": 1,
  "is_married": 1,
  "height": 1,
  "weight": 1,
  "occupation": "example",
  "industry": "example",
  "education_level": 1,
  "income": 1.2,
  "hometown_province_code": "example",
  "hometown_city_code": "example",
  "hometown_district_code": "example",
  "residence_province_code": "example",
  "residence_city_code": "example",
  "residence_district_code": "example",
  "self_intro": "example",
  "interest_tags": [
    "example"
  ],
  "personality_tags": [
    "example"
  ],
  "mbti": "INTJ",
  "avatar": "/storage/uploads/example.jpg",
  "background_wall": "example",
  "tag_selections": {},
  "photos": [
    {
      "id": 1,
      "media_type": "avatar",
      "file_url": "/storage/uploads/example.jpg",
      "thumbnail_url": "/storage/uploads/example.jpg",
      "sort_order": 1,
      "is_primary": true,
      "duration_seconds": 1
    }
  ],
  "video": {
    "id": 1,
    "media_type": "avatar",
    "file_url": "/storage/uploads/example.jpg",
    "thumbnail_url": "/storage/uploads/example.jpg",
    "sort_order": 1,
    "is_primary": true,
    "duration_seconds": 1
  },
  "completion_score": 1.2
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `nickname` | string | 是 | 用户昵称 | `"example"` |
| `gender` | integer | 是 | 接口中的 `gender` 字段 | `1` |
| `birthday` | string | 是 | 接口中的 `birthday` 字段 | `"2026-08-05"` |
| `age` | integer | 是 | 接口中的 `age` 字段 | `1` |
| `is_married` | integer | 是 | 接口中的 `is_married` 字段 | `1` |
| `height` | integer | 是 | 接口中的 `height` 字段 | `1` |
| `weight` | integer | 是 | 接口中的 `weight` 字段 | `1` |
| `occupation` | string | 是 | 接口中的 `occupation` 字段 | `"example"` |
| `industry` | string | 是 | 接口中的 `industry` 字段 | `"example"` |
| `education_level` | integer | 是 | 接口中的 `education_level` 字段 | `1` |
| `income` | number | 是 | 接口中的 `income` 字段 | `1.2` |
| `hometown_province_code` | string | 是 | 接口中的 `hometown_province_code` 字段 | `"example"` |
| `hometown_city_code` | string | 是 | 接口中的 `hometown_city_code` 字段 | `"example"` |
| `hometown_district_code` | string | 是 | 接口中的 `hometown_district_code` 字段 | `"example"` |
| `residence_province_code` | string | 是 | 接口中的 `residence_province_code` 字段 | `"example"` |
| `residence_city_code` | string | 是 | 接口中的 `residence_city_code` 字段 | `"example"` |
| `residence_district_code` | string | 是 | 接口中的 `residence_district_code` 字段 | `"example"` |
| `self_intro` | string | 是 | 接口中的 `self_intro` 字段 | `"example"` |
| `interest_tags` | array<string> | 是 | 接口中的 `interest_tags` 字段 | `["example"]` |
| `personality_tags` | array<string> | 是 | 接口中的 `personality_tags` 字段 | `["example"]` |
| `mbti` | INTJ/INTP/ENTJ/ENTP/INFJ/INFP/ENFJ/ENFP/ISTJ/ISFJ/ESTJ/ESFJ/ISTP/ISFP/ESTP/ESFP | 是 | 枚举=INTJ, INTP, ENTJ, ENTP, INFJ, INFP, ENFJ, ENFP, ISTJ, ISFJ, ESTJ, ESFJ, ISTP, ISFP, ESTP, ESFP | 接口中的 `mbti` 字段 | `"INTJ"` |
| `avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `background_wall` | string | 是 | 接口中的 `background_wall` 字段 | `"example"` |
| `tag_selections` | object | 是 | 接口中的 `tag_selections` 字段 | `{}` |
| `photos` | array<object> | 是 | 接口中的 `photos` 字段 | `[{"id": 1, "media_type": "avatar", "file_url": "/storage/uploads/example.jpg", "thumbnail_url": "/storage/uploads/example.jpg", "sort_order": 1, "is_primary": true, "duration_seconds": 1}]` |
| `photos[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `photos[].media_type` | avatar/background/photo/video | 是 | 枚举=avatar, background, photo, video | 接口中的 `photos[].media_type` 字段 | `"avatar"` |
| `photos[].file_url` | string | 是 | 接口中的 `photos[].file_url` 字段 | `"/storage/uploads/example.jpg"` |
| `photos[].thumbnail_url` | string | 是 | 接口中的 `photos[].thumbnail_url` 字段 | `"/storage/uploads/example.jpg"` |
| `photos[].sort_order` | integer | 是 | 接口中的 `photos[].sort_order` 字段 | `1` |
| `photos[].is_primary` | boolean | 是 | 接口中的 `photos[].is_primary` 字段 | `true` |
| `photos[].duration_seconds` | integer | 是 | 接口中的 `photos[].duration_seconds` 字段 | `1` |
| `video` | object | 是 | 接口中的 `video` 字段 | `{"id": 1, "media_type": "avatar", "file_url": "/storage/uploads/example.jpg", "thumbnail_url": "/storage/uploads/example.jpg", "sort_order": 1, "is_primary": true, "duration_seconds": 1}` |
| `video.id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `video.media_type` | avatar/background/photo/video | 是 | 枚举=avatar, background, photo, video | 接口中的 `video.media_type` 字段 | `"avatar"` |
| `video.file_url` | string | 是 | 接口中的 `video.file_url` 字段 | `"/storage/uploads/example.jpg"` |
| `video.thumbnail_url` | string | 是 | 接口中的 `video.thumbnail_url` 字段 | `"/storage/uploads/example.jpg"` |
| `video.sort_order` | integer | 是 | 接口中的 `video.sort_order` 字段 | `1` |
| `video.is_primary` | boolean | 是 | 接口中的 `video.is_primary` 字段 | `true` |
| `video.duration_seconds` | integer | 是 | 接口中的 `video.duration_seconds` 字段 | `1` |
| `completion_score` | number | 是 | 接口中的 `completion_score` 字段 | `1.2` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 21. `PATCH /api/v1/users/me/profile` 修改个人资料

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/profile`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "gender": 1,
  "birthday": "2026-08-05",
  "is_married": 1,
  "height": 1,
  "weight": 1,
  "occupation": "example",
  "industry": "example",
  "education_level": 1,
  "income": 1.2,
  "hometown_province_code": "example",
  "hometown_city_code": "example",
  "hometown_district_code": "example",
  "residence_province_code": "example",
  "residence_city_code": "example",
  "residence_district_code": "example",
  "self_intro": "example",
  "interest_tags": [
    "example"
  ],
  "personality_tags": [
    "example"
  ],
  "mbti": "INTJ",
  "tag_selections": {}
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `gender` | 1/2 | 否 | 枚举=1, 2 | 接口中的 `gender` 字段 | `1` |
| `birthday` | string | 否 | 按 Schema 类型校验 | 接口中的 `birthday` 字段 | `"2026-08-05"` |
| `is_married` | 1/2/3 | 否 | 枚举=1, 2, 3 | 接口中的 `is_married` 字段 | `1` |
| `height` | integer | 否 | minimum=140.0; maximum=220.0 | 接口中的 `height` 字段 | `1` |
| `weight` | integer | 否 | minimum=40.0; maximum=120.0 | 接口中的 `weight` 字段 | `1` |
| `occupation` | string | 否 | maxLength=128 | 接口中的 `occupation` 字段 | `"example"` |
| `industry` | string | 否 | maxLength=128 | 接口中的 `industry` 字段 | `"example"` |
| `education_level` | integer | 否 | minimum=1.0; maximum=8.0 | 接口中的 `education_level` 字段 | `1` |
| `income` | number | 否 | minimum=0.0; maximum=1000000.0 | 接口中的 `income` 字段 | `1.2` |
| `hometown_province_code` | string | 否 | maxLength=32 | 接口中的 `hometown_province_code` 字段 | `"example"` |
| `hometown_city_code` | string | 否 | maxLength=32 | 接口中的 `hometown_city_code` 字段 | `"example"` |
| `hometown_district_code` | string | 否 | maxLength=32 | 接口中的 `hometown_district_code` 字段 | `"example"` |
| `residence_province_code` | string | 否 | maxLength=32 | 接口中的 `residence_province_code` 字段 | `"example"` |
| `residence_city_code` | string | 否 | maxLength=32 | 接口中的 `residence_city_code` 字段 | `"example"` |
| `residence_district_code` | string | 否 | maxLength=32 | 接口中的 `residence_district_code` 字段 | `"example"` |
| `self_intro` | string | 否 | maxLength=500 | 接口中的 `self_intro` 字段 | `"example"` |
| `interest_tags` | array<string> | 否 | 按 Schema 类型校验 | 接口中的 `interest_tags` 字段 | `["example"]` |
| `personality_tags` | array<string> | 否 | 按 Schema 类型校验 | 接口中的 `personality_tags` 字段 | `["example"]` |
| `mbti` | INTJ/INTP/ENTJ/ENTP/INFJ/INFP/ENFJ/ENFP/ISTJ/ISFJ/ESTJ/ESFJ/ISTP/ISFP/ESTP/ESFP | 否 | 枚举=INTJ, INTP, ENTJ, ENTP, INFJ, INFP, ENFJ, ENFP, ISTJ, ISFJ, ESTJ, ESFJ, ISTP, ISFP, ESTP, ESFP | 接口中的 `mbti` 字段 | `"INTJ"` |
| `tag_selections` | object | 否 | 按 Schema 类型校验 | 接口中的 `tag_selections` 字段 | `{}` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "user_id": 1,
  "nickname": "example",
  "gender": 1,
  "birthday": "2026-08-05",
  "age": 1,
  "is_married": 1,
  "height": 1,
  "weight": 1,
  "occupation": "example",
  "industry": "example",
  "education_level": 1,
  "income": 1.2,
  "hometown_province_code": "example",
  "hometown_city_code": "example",
  "hometown_district_code": "example",
  "residence_province_code": "example",
  "residence_city_code": "example",
  "residence_district_code": "example",
  "self_intro": "example",
  "interest_tags": [
    "example"
  ],
  "personality_tags": [
    "example"
  ],
  "mbti": "INTJ",
  "avatar": "/storage/uploads/example.jpg",
  "background_wall": "example",
  "tag_selections": {},
  "photos": [
    {
      "id": 1,
      "media_type": "avatar",
      "file_url": "/storage/uploads/example.jpg",
      "thumbnail_url": "/storage/uploads/example.jpg",
      "sort_order": 1,
      "is_primary": true,
      "duration_seconds": 1
    }
  ],
  "video": {
    "id": 1,
    "media_type": "avatar",
    "file_url": "/storage/uploads/example.jpg",
    "thumbnail_url": "/storage/uploads/example.jpg",
    "sort_order": 1,
    "is_primary": true,
    "duration_seconds": 1
  },
  "completion_score": 1.2
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `nickname` | string | 是 | 用户昵称 | `"example"` |
| `gender` | integer | 是 | 接口中的 `gender` 字段 | `1` |
| `birthday` | string | 是 | 接口中的 `birthday` 字段 | `"2026-08-05"` |
| `age` | integer | 是 | 接口中的 `age` 字段 | `1` |
| `is_married` | integer | 是 | 接口中的 `is_married` 字段 | `1` |
| `height` | integer | 是 | 接口中的 `height` 字段 | `1` |
| `weight` | integer | 是 | 接口中的 `weight` 字段 | `1` |
| `occupation` | string | 是 | 接口中的 `occupation` 字段 | `"example"` |
| `industry` | string | 是 | 接口中的 `industry` 字段 | `"example"` |
| `education_level` | integer | 是 | 接口中的 `education_level` 字段 | `1` |
| `income` | number | 是 | 接口中的 `income` 字段 | `1.2` |
| `hometown_province_code` | string | 是 | 接口中的 `hometown_province_code` 字段 | `"example"` |
| `hometown_city_code` | string | 是 | 接口中的 `hometown_city_code` 字段 | `"example"` |
| `hometown_district_code` | string | 是 | 接口中的 `hometown_district_code` 字段 | `"example"` |
| `residence_province_code` | string | 是 | 接口中的 `residence_province_code` 字段 | `"example"` |
| `residence_city_code` | string | 是 | 接口中的 `residence_city_code` 字段 | `"example"` |
| `residence_district_code` | string | 是 | 接口中的 `residence_district_code` 字段 | `"example"` |
| `self_intro` | string | 是 | 接口中的 `self_intro` 字段 | `"example"` |
| `interest_tags` | array<string> | 是 | 接口中的 `interest_tags` 字段 | `["example"]` |
| `personality_tags` | array<string> | 是 | 接口中的 `personality_tags` 字段 | `["example"]` |
| `mbti` | INTJ/INTP/ENTJ/ENTP/INFJ/INFP/ENFJ/ENFP/ISTJ/ISFJ/ESTJ/ESFJ/ISTP/ISFP/ESTP/ESFP | 是 | 枚举=INTJ, INTP, ENTJ, ENTP, INFJ, INFP, ENFJ, ENFP, ISTJ, ISFJ, ESTJ, ESFJ, ISTP, ISFP, ESTP, ESFP | 接口中的 `mbti` 字段 | `"INTJ"` |
| `avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `background_wall` | string | 是 | 接口中的 `background_wall` 字段 | `"example"` |
| `tag_selections` | object | 是 | 接口中的 `tag_selections` 字段 | `{}` |
| `photos` | array<object> | 是 | 接口中的 `photos` 字段 | `[{"id": 1, "media_type": "avatar", "file_url": "/storage/uploads/example.jpg", "thumbnail_url": "/storage/uploads/example.jpg", "sort_order": 1, "is_primary": true, "duration_seconds": 1}]` |
| `photos[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `photos[].media_type` | avatar/background/photo/video | 是 | 枚举=avatar, background, photo, video | 接口中的 `photos[].media_type` 字段 | `"avatar"` |
| `photos[].file_url` | string | 是 | 接口中的 `photos[].file_url` 字段 | `"/storage/uploads/example.jpg"` |
| `photos[].thumbnail_url` | string | 是 | 接口中的 `photos[].thumbnail_url` 字段 | `"/storage/uploads/example.jpg"` |
| `photos[].sort_order` | integer | 是 | 接口中的 `photos[].sort_order` 字段 | `1` |
| `photos[].is_primary` | boolean | 是 | 接口中的 `photos[].is_primary` 字段 | `true` |
| `photos[].duration_seconds` | integer | 是 | 接口中的 `photos[].duration_seconds` 字段 | `1` |
| `video` | object | 是 | 接口中的 `video` 字段 | `{"id": 1, "media_type": "avatar", "file_url": "/storage/uploads/example.jpg", "thumbnail_url": "/storage/uploads/example.jpg", "sort_order": 1, "is_primary": true, "duration_seconds": 1}` |
| `video.id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `video.media_type` | avatar/background/photo/video | 是 | 枚举=avatar, background, photo, video | 接口中的 `video.media_type` 字段 | `"avatar"` |
| `video.file_url` | string | 是 | 接口中的 `video.file_url` 字段 | `"/storage/uploads/example.jpg"` |
| `video.thumbnail_url` | string | 是 | 接口中的 `video.thumbnail_url` 字段 | `"/storage/uploads/example.jpg"` |
| `video.sort_order` | integer | 是 | 接口中的 `video.sort_order` 字段 | `1` |
| `video.is_primary` | boolean | 是 | 接口中的 `video.is_primary` 字段 | `true` |
| `video.duration_seconds` | integer | 是 | 接口中的 `video.duration_seconds` 字段 | `1` |
| `completion_score` | number | 是 | 接口中的 `completion_score` 字段 | `1.2` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 22. `PATCH /api/v1/users/me/nickname` 修改个人昵称

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/nickname`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "nickname": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `nickname` | string | 是 | minLength=1; maxLength=64 | 用户昵称，去除首尾空格后不能为空 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "user_id": 1,
  "nickname": "example",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `nickname` | string | 是 | 用户昵称 | `"example"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 23. `GET /api/v1/users/me/completion` 查询资料完整度

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/completion`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "score": 1.2,
  "missing_items": [
    "example"
  ],
  "items": [
    {
      "key": "example",
      "label": "example",
      "weight": 1,
      "completed": true
    }
  ],
  "can_browse": true,
  "can_apply": true,
  "can_chat": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `score` | number | 是 | 接口中的 `score` 字段 | `1.2` |
| `missing_items` | array<string> | 是 | 接口中的 `missing_items` 字段 | `["example"]` |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"key": "example", "label": "example", "weight": 1, "completed": true}]` |
| `items[].key` | string | 是 | 接口中的 `items[].key` 字段 | `"example"` |
| `items[].label` | string | 是 | 接口中的 `items[].label` 字段 | `"example"` |
| `items[].weight` | integer | 是 | 接口中的 `items[].weight` 字段 | `1` |
| `items[].completed` | boolean | 是 | 接口中的 `items[].completed` 字段 | `true` |
| `can_browse` | boolean | 是 | 接口中的 `can_browse` 字段 | `true` |
| `can_apply` | boolean | 是 | 接口中的 `can_apply` 字段 | `true` |
| `can_chat` | boolean | 是 | 接口中的 `can_chat` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 24. `GET /api/v1/users/me/profile/preview` 预览个人主页

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/profile/preview`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "preview_notice": "example",
  "profile": {
    "user_id": 1,
    "nickname": "example",
    "gender": 1,
    "birthday": "2026-08-05",
    "age": 1,
    "is_married": 1,
    "height": 1,
    "weight": 1,
    "occupation": "example",
    "industry": "example",
    "education_level": 1,
    "income": 1.2,
    "hometown_province_code": "example",
    "hometown_city_code": "example",
    "hometown_district_code": "example",
    "residence_province_code": "example",
    "residence_city_code": "example",
    "residence_district_code": "example",
    "self_intro": "example",
    "interest_tags": [
      "example"
    ],
    "personality_tags": [
      "example"
    ],
    "mbti": "INTJ",
    "avatar": "/storage/uploads/example.jpg",
    "background_wall": "example",
    "tag_selections": {},
    "photos": [
      {
        "id": 1,
        "media_type": "avatar",
        "file_url": "/storage/uploads/example.jpg",
        "thumbnail_url": "/storage/uploads/example.jpg",
        "sort_order": 1,
        "is_primary": true,
        "duration_seconds": 1
      }
    ],
    "video": {
      "id": 1,
      "media_type": "avatar",
      "file_url": "/storage/uploads/example.jpg",
      "thumbnail_url": "/storage/uploads/example.jpg",
      "sort_order": 1,
      "is_primary": true,
      "duration_seconds": 1
    },
    "completion_score": 1.2
  }
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `preview_notice` | string | 是 | 接口中的 `preview_notice` 字段 | `"example"` |
| `profile` | object | 是 | 接口中的 `profile` 字段 | `{"user_id": 1, "nickname": "example", "gender": 1, "birthday": "2026-08-05", "age": 1, "is_married": 1, "height": 1, "weight": 1, "occupation": "example", "industry": "example", "education_level": 1, "income": 1.2, "hometown_province_code": "example", "hometown_city_code": "example", "hometown_district_code": "example", "residence_province_code": "example", "residence_city_code": "example", "residence_district_code": "example", "self_intro": "example", "interest_tags": ["example"], "personality_tags": ["example"], "mbti": "INTJ", "avatar": "/storage/uploads/example.jpg", "background_wall": "example", "tag_selections": {}, "photos": [{"id": 1, "media_type": "avatar", "file_url": "/storage/uploads/example.jpg", "thumbnail_url": "/storage/uploads/example.jpg", "sort_order": 1, "is_primary": true, "duration_seconds": 1}], "video": {"id": 1, "media_type": "avatar", "file_url": "/storage/uploads/example.jpg", "thumbnail_url": "/storage/uploads/example.jpg", "sort_order": 1, "is_primary": true, "duration_seconds": 1}, "completion_score": 1.2}` |
| `profile.user_id` | integer | 是 | 用户 ID | `1` |
| `profile.nickname` | string | 是 | 用户昵称 | `"example"` |
| `profile.gender` | integer | 是 | 接口中的 `profile.gender` 字段 | `1` |
| `profile.birthday` | string | 是 | 接口中的 `profile.birthday` 字段 | `"2026-08-05"` |
| `profile.age` | integer | 是 | 接口中的 `profile.age` 字段 | `1` |
| `profile.is_married` | integer | 是 | 接口中的 `profile.is_married` 字段 | `1` |
| `profile.height` | integer | 是 | 接口中的 `profile.height` 字段 | `1` |
| `profile.weight` | integer | 是 | 接口中的 `profile.weight` 字段 | `1` |
| `profile.occupation` | string | 是 | 接口中的 `profile.occupation` 字段 | `"example"` |
| `profile.industry` | string | 是 | 接口中的 `profile.industry` 字段 | `"example"` |
| `profile.education_level` | integer | 是 | 接口中的 `profile.education_level` 字段 | `1` |
| `profile.income` | number | 是 | 接口中的 `profile.income` 字段 | `1.2` |
| `profile.hometown_province_code` | string | 是 | 接口中的 `profile.hometown_province_code` 字段 | `"example"` |
| `profile.hometown_city_code` | string | 是 | 接口中的 `profile.hometown_city_code` 字段 | `"example"` |
| `profile.hometown_district_code` | string | 是 | 接口中的 `profile.hometown_district_code` 字段 | `"example"` |
| `profile.residence_province_code` | string | 是 | 接口中的 `profile.residence_province_code` 字段 | `"example"` |
| `profile.residence_city_code` | string | 是 | 接口中的 `profile.residence_city_code` 字段 | `"example"` |
| `profile.residence_district_code` | string | 是 | 接口中的 `profile.residence_district_code` 字段 | `"example"` |
| `profile.self_intro` | string | 是 | 接口中的 `profile.self_intro` 字段 | `"example"` |
| `profile.interest_tags` | array<string> | 是 | 接口中的 `profile.interest_tags` 字段 | `["example"]` |
| `profile.personality_tags` | array<string> | 是 | 接口中的 `profile.personality_tags` 字段 | `["example"]` |
| `profile.mbti` | INTJ/INTP/ENTJ/ENTP/INFJ/INFP/ENFJ/ENFP/ISTJ/ISFJ/ESTJ/ESFJ/ISTP/ISFP/ESTP/ESFP | 是 | 枚举=INTJ, INTP, ENTJ, ENTP, INFJ, INFP, ENFJ, ENFP, ISTJ, ISFJ, ESTJ, ESFJ, ISTP, ISFP, ESTP, ESFP | 接口中的 `profile.mbti` 字段 | `"INTJ"` |
| `profile.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `profile.background_wall` | string | 是 | 接口中的 `profile.background_wall` 字段 | `"example"` |
| `profile.tag_selections` | object | 是 | 接口中的 `profile.tag_selections` 字段 | `{}` |
| `profile.photos` | array<object> | 是 | 接口中的 `profile.photos` 字段 | `[{"id": 1, "media_type": "avatar", "file_url": "/storage/uploads/example.jpg", "thumbnail_url": "/storage/uploads/example.jpg", "sort_order": 1, "is_primary": true, "duration_seconds": 1}]` |
| `profile.photos[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `profile.photos[].media_type` | avatar/background/photo/video | 是 | 枚举=avatar, background, photo, video | 接口中的 `profile.photos[].media_type` 字段 | `"avatar"` |
| `profile.photos[].file_url` | string | 是 | 接口中的 `profile.photos[].file_url` 字段 | `"/storage/uploads/example.jpg"` |
| `profile.photos[].thumbnail_url` | string | 是 | 接口中的 `profile.photos[].thumbnail_url` 字段 | `"/storage/uploads/example.jpg"` |
| `profile.photos[].sort_order` | integer | 是 | 接口中的 `profile.photos[].sort_order` 字段 | `1` |
| `profile.photos[].is_primary` | boolean | 是 | 接口中的 `profile.photos[].is_primary` 字段 | `true` |
| `profile.photos[].duration_seconds` | integer | 是 | 接口中的 `profile.photos[].duration_seconds` 字段 | `1` |
| `profile.video` | object | 是 | 接口中的 `profile.video` 字段 | `{"id": 1, "media_type": "avatar", "file_url": "/storage/uploads/example.jpg", "thumbnail_url": "/storage/uploads/example.jpg", "sort_order": 1, "is_primary": true, "duration_seconds": 1}` |
| `profile.video.id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `profile.video.media_type` | avatar/background/photo/video | 是 | 枚举=avatar, background, photo, video | 接口中的 `profile.video.media_type` 字段 | `"avatar"` |
| `profile.video.file_url` | string | 是 | 接口中的 `profile.video.file_url` 字段 | `"/storage/uploads/example.jpg"` |
| `profile.video.thumbnail_url` | string | 是 | 接口中的 `profile.video.thumbnail_url` 字段 | `"/storage/uploads/example.jpg"` |
| `profile.video.sort_order` | integer | 是 | 接口中的 `profile.video.sort_order` 字段 | `1` |
| `profile.video.is_primary` | boolean | 是 | 接口中的 `profile.video.is_primary` 字段 | `true` |
| `profile.video.duration_seconds` | integer | 是 | 接口中的 `profile.video.duration_seconds` 字段 | `1` |
| `profile.completion_score` | number | 是 | 接口中的 `profile.completion_score` 字段 | `1.2` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 25. `GET /api/v1/users/me/intro-templates` 查询自我介绍模板

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/intro-templates`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "key": "example",
    "title": "example",
    "content": "example"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 26. `GET /api/v1/users/me/preferences` 获取择偶要求

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/preferences`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "user_id": 1,
  "age_min": 1,
  "age_max": 1,
  "height_min": 1,
  "height_max": 1,
  "education_min": 1,
  "income_min": 1.2,
  "marriage_status": 1,
  "preferred_province_code": "example",
  "preferred_city_codes": [
    "example"
  ],
  "accept_long_distance": true,
  "accept_cross_province": true,
  "housing_requirement": 1,
  "smoking_requirement": 1,
  "drinking_requirement": 1,
  "extra_requirement": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `age_min` | integer | 是 | 接口中的 `age_min` 字段 | `1` |
| `age_max` | integer | 是 | 接口中的 `age_max` 字段 | `1` |
| `height_min` | integer | 是 | 接口中的 `height_min` 字段 | `1` |
| `height_max` | integer | 是 | 接口中的 `height_max` 字段 | `1` |
| `education_min` | integer | 是 | 接口中的 `education_min` 字段 | `1` |
| `income_min` | number | 是 | 接口中的 `income_min` 字段 | `1.2` |
| `marriage_status` | integer | 是 | 接口中的 `marriage_status` 字段 | `1` |
| `preferred_province_code` | string | 是 | 接口中的 `preferred_province_code` 字段 | `"example"` |
| `preferred_city_codes` | array<string> | 是 | 接口中的 `preferred_city_codes` 字段 | `["example"]` |
| `accept_long_distance` | boolean | 是 | 接口中的 `accept_long_distance` 字段 | `true` |
| `accept_cross_province` | boolean | 是 | 接口中的 `accept_cross_province` 字段 | `true` |
| `housing_requirement` | integer | 是 | 接口中的 `housing_requirement` 字段 | `1` |
| `smoking_requirement` | integer | 是 | 接口中的 `smoking_requirement` 字段 | `1` |
| `drinking_requirement` | integer | 是 | 接口中的 `drinking_requirement` 字段 | `1` |
| `extra_requirement` | string | 是 | 接口中的 `extra_requirement` 字段 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 27. `PUT /api/v1/users/me/preferences` 更新择偶要求

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/preferences`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "age_min": 1,
  "age_max": 1,
  "height_min": 1,
  "height_max": 1,
  "education_min": 1,
  "income_min": 1.2,
  "marriage_status": 0,
  "preferred_province_code": "example",
  "preferred_city_codes": [
    "example"
  ],
  "accept_long_distance": true,
  "accept_cross_province": true,
  "housing_requirement": 0,
  "smoking_requirement": 0,
  "drinking_requirement": 0,
  "extra_requirement": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `age_min` | integer | 否 | minimum=18.0; maximum=100.0 | 接口中的 `age_min` 字段 | `1` |
| `age_max` | integer | 否 | minimum=18.0; maximum=100.0 | 接口中的 `age_max` 字段 | `1` |
| `height_min` | integer | 否 | minimum=100.0; maximum=250.0 | 接口中的 `height_min` 字段 | `1` |
| `height_max` | integer | 否 | minimum=100.0; maximum=250.0 | 接口中的 `height_max` 字段 | `1` |
| `education_min` | integer | 否 | minimum=1.0; maximum=8.0 | 接口中的 `education_min` 字段 | `1` |
| `income_min` | number | 否 | minimum=0.0; maximum=1000000.0 | 接口中的 `income_min` 字段 | `1.2` |
| `marriage_status` | 0/1/2/3 | 否 | 枚举=0, 1, 2, 3 | 接口中的 `marriage_status` 字段 | `0` |
| `preferred_province_code` | string | 否 | maxLength=32 | 接口中的 `preferred_province_code` 字段 | `"example"` |
| `preferred_city_codes` | array<string> | 否 | 按 Schema 类型校验 | 接口中的 `preferred_city_codes` 字段 | `["example"]` |
| `accept_long_distance` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `accept_long_distance` 字段 | `true` |
| `accept_cross_province` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `accept_cross_province` 字段 | `true` |
| `housing_requirement` | 0/1/2 | 否 | 枚举=0, 1, 2 | 接口中的 `housing_requirement` 字段 | `0` |
| `smoking_requirement` | 0/1/2 | 否 | 枚举=0, 1, 2 | 接口中的 `smoking_requirement` 字段 | `0` |
| `drinking_requirement` | 0/1/2 | 否 | 枚举=0, 1, 2 | 接口中的 `drinking_requirement` 字段 | `0` |
| `extra_requirement` | string | 否 | maxLength=255 | 接口中的 `extra_requirement` 字段 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "user_id": 1,
  "age_min": 1,
  "age_max": 1,
  "height_min": 1,
  "height_max": 1,
  "education_min": 1,
  "income_min": 1.2,
  "marriage_status": 1,
  "preferred_province_code": "example",
  "preferred_city_codes": [
    "example"
  ],
  "accept_long_distance": true,
  "accept_cross_province": true,
  "housing_requirement": 1,
  "smoking_requirement": 1,
  "drinking_requirement": 1,
  "extra_requirement": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `age_min` | integer | 是 | 接口中的 `age_min` 字段 | `1` |
| `age_max` | integer | 是 | 接口中的 `age_max` 字段 | `1` |
| `height_min` | integer | 是 | 接口中的 `height_min` 字段 | `1` |
| `height_max` | integer | 是 | 接口中的 `height_max` 字段 | `1` |
| `education_min` | integer | 是 | 接口中的 `education_min` 字段 | `1` |
| `income_min` | number | 是 | 接口中的 `income_min` 字段 | `1.2` |
| `marriage_status` | integer | 是 | 接口中的 `marriage_status` 字段 | `1` |
| `preferred_province_code` | string | 是 | 接口中的 `preferred_province_code` 字段 | `"example"` |
| `preferred_city_codes` | array<string> | 是 | 接口中的 `preferred_city_codes` 字段 | `["example"]` |
| `accept_long_distance` | boolean | 是 | 接口中的 `accept_long_distance` 字段 | `true` |
| `accept_cross_province` | boolean | 是 | 接口中的 `accept_cross_province` 字段 | `true` |
| `housing_requirement` | integer | 是 | 接口中的 `housing_requirement` 字段 | `1` |
| `smoking_requirement` | integer | 是 | 接口中的 `smoking_requirement` 字段 | `1` |
| `drinking_requirement` | integer | 是 | 接口中的 `drinking_requirement` 字段 | `1` |
| `extra_requirement` | string | 是 | 接口中的 `extra_requirement` 字段 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 28. `POST /api/v1/users/me/avatar` 上传头像

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/avatar`

```json
{
  "Content-Type": "multipart/form-data",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体示例（multipart/form-data）

字段 `file` 使用真实文件上传，其余字段按表单字段提交。

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `file` | string | 是 | 按 Schema 类型校验 | 接口中的 `file` 字段 | `"example"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "media_type": "avatar",
  "file_url": "/storage/uploads/example.jpg",
  "thumbnail_url": "/storage/uploads/example.jpg",
  "sort_order": 1,
  "is_primary": true,
  "duration_seconds": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `media_type` | avatar/background/photo/video | 是 | 枚举=avatar, background, photo, video | 接口中的 `media_type` 字段 | `"avatar"` |
| `file_url` | string | 是 | 接口中的 `file_url` 字段 | `"/storage/uploads/example.jpg"` |
| `thumbnail_url` | string | 是 | 接口中的 `thumbnail_url` 字段 | `"/storage/uploads/example.jpg"` |
| `sort_order` | integer | 是 | 接口中的 `sort_order` 字段 | `1` |
| `is_primary` | boolean | 是 | 接口中的 `is_primary` 字段 | `true` |
| `duration_seconds` | integer | 是 | 接口中的 `duration_seconds` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 29. `POST /api/v1/users/me/background` 上传主页背景墙

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/background`

```json
{
  "Content-Type": "multipart/form-data",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体示例（multipart/form-data）

字段 `file` 使用真实文件上传，其余字段按表单字段提交。

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `file` | string | 是 | 按 Schema 类型校验 | 接口中的 `file` 字段 | `"example"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "media_type": "avatar",
  "file_url": "/storage/uploads/example.jpg",
  "thumbnail_url": "/storage/uploads/example.jpg",
  "sort_order": 1,
  "is_primary": true,
  "duration_seconds": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `media_type` | avatar/background/photo/video | 是 | 枚举=avatar, background, photo, video | 接口中的 `media_type` 字段 | `"avatar"` |
| `file_url` | string | 是 | 接口中的 `file_url` 字段 | `"/storage/uploads/example.jpg"` |
| `thumbnail_url` | string | 是 | 接口中的 `thumbnail_url` 字段 | `"/storage/uploads/example.jpg"` |
| `sort_order` | integer | 是 | 接口中的 `sort_order` 字段 | `1` |
| `is_primary` | boolean | 是 | 接口中的 `is_primary` 字段 | `true` |
| `duration_seconds` | integer | 是 | 接口中的 `duration_seconds` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 30. `POST /api/v1/users/me/photos` 上传相册图片

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/photos`

```json
{
  "Content-Type": "multipart/form-data",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体示例（multipart/form-data）

字段 `file` 使用真实文件上传，其余字段按表单字段提交。

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `file` | string | 是 | 按 Schema 类型校验 | 接口中的 `file` 字段 | `"example"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "media_type": "avatar",
  "file_url": "/storage/uploads/example.jpg",
  "thumbnail_url": "/storage/uploads/example.jpg",
  "sort_order": 1,
  "is_primary": true,
  "duration_seconds": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `media_type` | avatar/background/photo/video | 是 | 枚举=avatar, background, photo, video | 接口中的 `media_type` 字段 | `"avatar"` |
| `file_url` | string | 是 | 接口中的 `file_url` 字段 | `"/storage/uploads/example.jpg"` |
| `thumbnail_url` | string | 是 | 接口中的 `thumbnail_url` 字段 | `"/storage/uploads/example.jpg"` |
| `sort_order` | integer | 是 | 接口中的 `sort_order` 字段 | `1` |
| `is_primary` | boolean | 是 | 接口中的 `is_primary` 字段 | `true` |
| `duration_seconds` | integer | 是 | 接口中的 `duration_seconds` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 31. `POST /api/v1/users/me/video` 上传个人视频

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/video`

```json
{
  "Content-Type": "multipart/form-data",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体示例（multipart/form-data）

字段 `file` 使用真实文件上传，其余字段按表单字段提交。

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `file` | string | 是 | 按 Schema 类型校验 | 接口中的 `file` 字段 | `"example"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "media_type": "avatar",
  "file_url": "/storage/uploads/example.jpg",
  "thumbnail_url": "/storage/uploads/example.jpg",
  "sort_order": 1,
  "is_primary": true,
  "duration_seconds": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `media_type` | avatar/background/photo/video | 是 | 枚举=avatar, background, photo, video | 接口中的 `media_type` 字段 | `"avatar"` |
| `file_url` | string | 是 | 接口中的 `file_url` 字段 | `"/storage/uploads/example.jpg"` |
| `thumbnail_url` | string | 是 | 接口中的 `thumbnail_url` 字段 | `"/storage/uploads/example.jpg"` |
| `sort_order` | integer | 是 | 接口中的 `sort_order` 字段 | `1` |
| `is_primary` | boolean | 是 | 接口中的 `is_primary` 字段 | `true` |
| `duration_seconds` | integer | 是 | 接口中的 `duration_seconds` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 32. `PUT /api/v1/users/me/photos/order` 调整相册顺序

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/photos/order`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "media_ids": [
    1
  ]
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `media_ids` | array<integer> | 是 | 按 Schema 类型校验 | 接口中的 `media_ids` 字段 | `[1]` |

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 33. `PUT /api/v1/users/me/photos/{media_id}/primary` 设置相册首图

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/photos/{media_id}/primary`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `media_id` | path | integer | 是 | 无额外约束 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "media_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "media_type": "avatar",
  "file_url": "/storage/uploads/example.jpg",
  "thumbnail_url": "/storage/uploads/example.jpg",
  "sort_order": 1,
  "is_primary": true,
  "duration_seconds": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `media_type` | avatar/background/photo/video | 是 | 枚举=avatar, background, photo, video | 接口中的 `media_type` 字段 | `"avatar"` |
| `file_url` | string | 是 | 接口中的 `file_url` 字段 | `"/storage/uploads/example.jpg"` |
| `thumbnail_url` | string | 是 | 接口中的 `thumbnail_url` 字段 | `"/storage/uploads/example.jpg"` |
| `sort_order` | integer | 是 | 接口中的 `sort_order` 字段 | `1` |
| `is_primary` | boolean | 是 | 接口中的 `is_primary` 字段 | `true` |
| `duration_seconds` | integer | 是 | 接口中的 `duration_seconds` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 34. `DELETE /api/v1/users/me/photos/{media_id}` 删除相册图片

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/photos/{media_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `media_id` | path | integer | 是 | 无额外约束 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "media_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 35. `GET /api/v1/users/me/certifications` 查询三项资质认证

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/certifications`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "education": {
    "kind": "example",
    "status": 1,
    "material_submitted": true,
    "submitted_at": "2026-08-05T10:00:00+08:00",
    "reviewed_at": "2026-08-05T10:00:00+08:00",
    "fail_reason": "example",
    "next_action": "example"
  },
  "house": {
    "kind": "example",
    "status": 1,
    "material_submitted": true,
    "submitted_at": "2026-08-05T10:00:00+08:00",
    "reviewed_at": "2026-08-05T10:00:00+08:00",
    "fail_reason": "example",
    "next_action": "example"
  },
  "marriage": {
    "kind": "example",
    "status": 1,
    "material_submitted": true,
    "submitted_at": "2026-08-05T10:00:00+08:00",
    "reviewed_at": "2026-08-05T10:00:00+08:00",
    "fail_reason": "example",
    "next_action": "example"
  }
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `education` | object | 是 | 接口中的 `education` 字段 | `{"kind": "example", "status": 1, "material_submitted": true, "submitted_at": "2026-08-05T10:00:00+08:00", "reviewed_at": "2026-08-05T10:00:00+08:00", "fail_reason": "example", "next_action": "example"}` |
| `education.kind` | string | 是 | 接口中的 `education.kind` 字段 | `"example"` |
| `education.status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `education.material_submitted` | boolean | 是 | 接口中的 `education.material_submitted` 字段 | `true` |
| `education.submitted_at` | string | 是 | 接口中的 `education.submitted_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `education.reviewed_at` | string | 是 | 接口中的 `education.reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `education.fail_reason` | string | 是 | 接口中的 `education.fail_reason` 字段 | `"example"` |
| `education.next_action` | string | 是 | 接口中的 `education.next_action` 字段 | `"example"` |
| `house` | object | 是 | 接口中的 `house` 字段 | `{"kind": "example", "status": 1, "material_submitted": true, "submitted_at": "2026-08-05T10:00:00+08:00", "reviewed_at": "2026-08-05T10:00:00+08:00", "fail_reason": "example", "next_action": "example"}` |
| `house.kind` | string | 是 | 接口中的 `house.kind` 字段 | `"example"` |
| `house.status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `house.material_submitted` | boolean | 是 | 接口中的 `house.material_submitted` 字段 | `true` |
| `house.submitted_at` | string | 是 | 接口中的 `house.submitted_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `house.reviewed_at` | string | 是 | 接口中的 `house.reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `house.fail_reason` | string | 是 | 接口中的 `house.fail_reason` 字段 | `"example"` |
| `house.next_action` | string | 是 | 接口中的 `house.next_action` 字段 | `"example"` |
| `marriage` | object | 是 | 接口中的 `marriage` 字段 | `{"kind": "example", "status": 1, "material_submitted": true, "submitted_at": "2026-08-05T10:00:00+08:00", "reviewed_at": "2026-08-05T10:00:00+08:00", "fail_reason": "example", "next_action": "example"}` |
| `marriage.kind` | string | 是 | 接口中的 `marriage.kind` 字段 | `"example"` |
| `marriage.status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `marriage.material_submitted` | boolean | 是 | 接口中的 `marriage.material_submitted` 字段 | `true` |
| `marriage.submitted_at` | string | 是 | 接口中的 `marriage.submitted_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `marriage.reviewed_at` | string | 是 | 接口中的 `marriage.reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `marriage.fail_reason` | string | 是 | 接口中的 `marriage.fail_reason` 字段 | `"example"` |
| `marriage.next_action` | string | 是 | 接口中的 `marriage.next_action` 字段 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 36. `PUT /api/v1/users/me/certifications/education` 提交学历认证材料

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/certifications/education`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "education": "小学"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `education` | 小学/初中/高中/中专/大专/本科/硕士/博士 | 是 | 枚举=小学, 初中, 高中, 中专, 大专, 本科, 硕士, 博士 | 接口中的 `education` 字段 | `"小学"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "education": {
    "kind": "example",
    "status": 1,
    "material_submitted": true,
    "submitted_at": "2026-08-05T10:00:00+08:00",
    "reviewed_at": "2026-08-05T10:00:00+08:00",
    "fail_reason": "example",
    "next_action": "example"
  },
  "house": {
    "kind": "example",
    "status": 1,
    "material_submitted": true,
    "submitted_at": "2026-08-05T10:00:00+08:00",
    "reviewed_at": "2026-08-05T10:00:00+08:00",
    "fail_reason": "example",
    "next_action": "example"
  },
  "marriage": {
    "kind": "example",
    "status": 1,
    "material_submitted": true,
    "submitted_at": "2026-08-05T10:00:00+08:00",
    "reviewed_at": "2026-08-05T10:00:00+08:00",
    "fail_reason": "example",
    "next_action": "example"
  }
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `education` | object | 是 | 接口中的 `education` 字段 | `{"kind": "example", "status": 1, "material_submitted": true, "submitted_at": "2026-08-05T10:00:00+08:00", "reviewed_at": "2026-08-05T10:00:00+08:00", "fail_reason": "example", "next_action": "example"}` |
| `education.kind` | string | 是 | 接口中的 `education.kind` 字段 | `"example"` |
| `education.status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `education.material_submitted` | boolean | 是 | 接口中的 `education.material_submitted` 字段 | `true` |
| `education.submitted_at` | string | 是 | 接口中的 `education.submitted_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `education.reviewed_at` | string | 是 | 接口中的 `education.reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `education.fail_reason` | string | 是 | 接口中的 `education.fail_reason` 字段 | `"example"` |
| `education.next_action` | string | 是 | 接口中的 `education.next_action` 字段 | `"example"` |
| `house` | object | 是 | 接口中的 `house` 字段 | `{"kind": "example", "status": 1, "material_submitted": true, "submitted_at": "2026-08-05T10:00:00+08:00", "reviewed_at": "2026-08-05T10:00:00+08:00", "fail_reason": "example", "next_action": "example"}` |
| `house.kind` | string | 是 | 接口中的 `house.kind` 字段 | `"example"` |
| `house.status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `house.material_submitted` | boolean | 是 | 接口中的 `house.material_submitted` 字段 | `true` |
| `house.submitted_at` | string | 是 | 接口中的 `house.submitted_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `house.reviewed_at` | string | 是 | 接口中的 `house.reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `house.fail_reason` | string | 是 | 接口中的 `house.fail_reason` 字段 | `"example"` |
| `house.next_action` | string | 是 | 接口中的 `house.next_action` 字段 | `"example"` |
| `marriage` | object | 是 | 接口中的 `marriage` 字段 | `{"kind": "example", "status": 1, "material_submitted": true, "submitted_at": "2026-08-05T10:00:00+08:00", "reviewed_at": "2026-08-05T10:00:00+08:00", "fail_reason": "example", "next_action": "example"}` |
| `marriage.kind` | string | 是 | 接口中的 `marriage.kind` 字段 | `"example"` |
| `marriage.status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `marriage.material_submitted` | boolean | 是 | 接口中的 `marriage.material_submitted` 字段 | `true` |
| `marriage.submitted_at` | string | 是 | 接口中的 `marriage.submitted_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `marriage.reviewed_at` | string | 是 | 接口中的 `marriage.reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `marriage.fail_reason` | string | 是 | 接口中的 `marriage.fail_reason` 字段 | `"example"` |
| `marriage.next_action` | string | 是 | 接口中的 `marriage.next_action` 字段 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 37. `PUT /api/v1/users/me/certifications/house` 提交房产认证材料

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/certifications/house`

```json
{
  "Content-Type": "multipart/form-data",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体示例（multipart/form-data）

字段 `file` 使用真实文件上传，其余字段按表单字段提交。

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `file` | string | 是 | 按 Schema 类型校验 | 接口中的 `file` 字段 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "education": {
    "kind": "example",
    "status": 1,
    "material_submitted": true,
    "submitted_at": "2026-08-05T10:00:00+08:00",
    "reviewed_at": "2026-08-05T10:00:00+08:00",
    "fail_reason": "example",
    "next_action": "example"
  },
  "house": {
    "kind": "example",
    "status": 1,
    "material_submitted": true,
    "submitted_at": "2026-08-05T10:00:00+08:00",
    "reviewed_at": "2026-08-05T10:00:00+08:00",
    "fail_reason": "example",
    "next_action": "example"
  },
  "marriage": {
    "kind": "example",
    "status": 1,
    "material_submitted": true,
    "submitted_at": "2026-08-05T10:00:00+08:00",
    "reviewed_at": "2026-08-05T10:00:00+08:00",
    "fail_reason": "example",
    "next_action": "example"
  }
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `education` | object | 是 | 接口中的 `education` 字段 | `{"kind": "example", "status": 1, "material_submitted": true, "submitted_at": "2026-08-05T10:00:00+08:00", "reviewed_at": "2026-08-05T10:00:00+08:00", "fail_reason": "example", "next_action": "example"}` |
| `education.kind` | string | 是 | 接口中的 `education.kind` 字段 | `"example"` |
| `education.status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `education.material_submitted` | boolean | 是 | 接口中的 `education.material_submitted` 字段 | `true` |
| `education.submitted_at` | string | 是 | 接口中的 `education.submitted_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `education.reviewed_at` | string | 是 | 接口中的 `education.reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `education.fail_reason` | string | 是 | 接口中的 `education.fail_reason` 字段 | `"example"` |
| `education.next_action` | string | 是 | 接口中的 `education.next_action` 字段 | `"example"` |
| `house` | object | 是 | 接口中的 `house` 字段 | `{"kind": "example", "status": 1, "material_submitted": true, "submitted_at": "2026-08-05T10:00:00+08:00", "reviewed_at": "2026-08-05T10:00:00+08:00", "fail_reason": "example", "next_action": "example"}` |
| `house.kind` | string | 是 | 接口中的 `house.kind` 字段 | `"example"` |
| `house.status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `house.material_submitted` | boolean | 是 | 接口中的 `house.material_submitted` 字段 | `true` |
| `house.submitted_at` | string | 是 | 接口中的 `house.submitted_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `house.reviewed_at` | string | 是 | 接口中的 `house.reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `house.fail_reason` | string | 是 | 接口中的 `house.fail_reason` 字段 | `"example"` |
| `house.next_action` | string | 是 | 接口中的 `house.next_action` 字段 | `"example"` |
| `marriage` | object | 是 | 接口中的 `marriage` 字段 | `{"kind": "example", "status": 1, "material_submitted": true, "submitted_at": "2026-08-05T10:00:00+08:00", "reviewed_at": "2026-08-05T10:00:00+08:00", "fail_reason": "example", "next_action": "example"}` |
| `marriage.kind` | string | 是 | 接口中的 `marriage.kind` 字段 | `"example"` |
| `marriage.status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `marriage.material_submitted` | boolean | 是 | 接口中的 `marriage.material_submitted` 字段 | `true` |
| `marriage.submitted_at` | string | 是 | 接口中的 `marriage.submitted_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `marriage.reviewed_at` | string | 是 | 接口中的 `marriage.reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `marriage.fail_reason` | string | 是 | 接口中的 `marriage.fail_reason` 字段 | `"example"` |
| `marriage.next_action` | string | 是 | 接口中的 `marriage.next_action` 字段 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 38. `PUT /api/v1/users/me/certifications/marriage` 提交婚姻认证材料

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/certifications/marriage`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "is_unmarried": true
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `is_unmarried` | boolean | 是 | 按 Schema 类型校验 | 接口中的 `is_unmarried` 字段 | `true` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "education": {
    "kind": "example",
    "status": 1,
    "material_submitted": true,
    "submitted_at": "2026-08-05T10:00:00+08:00",
    "reviewed_at": "2026-08-05T10:00:00+08:00",
    "fail_reason": "example",
    "next_action": "example"
  },
  "house": {
    "kind": "example",
    "status": 1,
    "material_submitted": true,
    "submitted_at": "2026-08-05T10:00:00+08:00",
    "reviewed_at": "2026-08-05T10:00:00+08:00",
    "fail_reason": "example",
    "next_action": "example"
  },
  "marriage": {
    "kind": "example",
    "status": 1,
    "material_submitted": true,
    "submitted_at": "2026-08-05T10:00:00+08:00",
    "reviewed_at": "2026-08-05T10:00:00+08:00",
    "fail_reason": "example",
    "next_action": "example"
  }
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `education` | object | 是 | 接口中的 `education` 字段 | `{"kind": "example", "status": 1, "material_submitted": true, "submitted_at": "2026-08-05T10:00:00+08:00", "reviewed_at": "2026-08-05T10:00:00+08:00", "fail_reason": "example", "next_action": "example"}` |
| `education.kind` | string | 是 | 接口中的 `education.kind` 字段 | `"example"` |
| `education.status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `education.material_submitted` | boolean | 是 | 接口中的 `education.material_submitted` 字段 | `true` |
| `education.submitted_at` | string | 是 | 接口中的 `education.submitted_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `education.reviewed_at` | string | 是 | 接口中的 `education.reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `education.fail_reason` | string | 是 | 接口中的 `education.fail_reason` 字段 | `"example"` |
| `education.next_action` | string | 是 | 接口中的 `education.next_action` 字段 | `"example"` |
| `house` | object | 是 | 接口中的 `house` 字段 | `{"kind": "example", "status": 1, "material_submitted": true, "submitted_at": "2026-08-05T10:00:00+08:00", "reviewed_at": "2026-08-05T10:00:00+08:00", "fail_reason": "example", "next_action": "example"}` |
| `house.kind` | string | 是 | 接口中的 `house.kind` 字段 | `"example"` |
| `house.status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `house.material_submitted` | boolean | 是 | 接口中的 `house.material_submitted` 字段 | `true` |
| `house.submitted_at` | string | 是 | 接口中的 `house.submitted_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `house.reviewed_at` | string | 是 | 接口中的 `house.reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `house.fail_reason` | string | 是 | 接口中的 `house.fail_reason` 字段 | `"example"` |
| `house.next_action` | string | 是 | 接口中的 `house.next_action` 字段 | `"example"` |
| `marriage` | object | 是 | 接口中的 `marriage` 字段 | `{"kind": "example", "status": 1, "material_submitted": true, "submitted_at": "2026-08-05T10:00:00+08:00", "reviewed_at": "2026-08-05T10:00:00+08:00", "fail_reason": "example", "next_action": "example"}` |
| `marriage.kind` | string | 是 | 接口中的 `marriage.kind` 字段 | `"example"` |
| `marriage.status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `marriage.material_submitted` | boolean | 是 | 接口中的 `marriage.material_submitted` 字段 | `true` |
| `marriage.submitted_at` | string | 是 | 接口中的 `marriage.submitted_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `marriage.reviewed_at` | string | 是 | 接口中的 `marriage.reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `marriage.fail_reason` | string | 是 | 接口中的 `marriage.fail_reason` 字段 | `"example"` |
| `marriage.next_action` | string | 是 | 接口中的 `marriage.next_action` 字段 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 39. `GET /api/v1/membership/packages` 查询在售会员套餐

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/membership/packages`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "code": "example",
    "name": "example",
    "duration_days": 1,
    "price": 1.2,
    "original_price": 1.2,
    "daily_price": 1.2,
    "badge": "example",
    "rights": {}
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 40. `GET /api/v1/users/me/membership` 查询当前会员状态

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/membership`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "is_vip": true,
  "package_type": "example",
  "start_at": "2026-08-05T10:00:00+08:00",
  "end_at": "2026-08-05T10:00:00+08:00",
  "rights": {}
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `is_vip` | boolean | 是 | 当前是否为有效会员 | `true` |
| `package_type` | string | 是 | 接口中的 `package_type` 字段 | `"example"` |
| `start_at` | string | 是 | 开始时间 | `"2026-08-05T10:00:00+08:00"` |
| `end_at` | string | 是 | 结束时间 | `"2026-08-05T10:00:00+08:00"` |
| `rights` | object | 是 | 接口中的 `rights` 字段 | `{}` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 41. `GET /api/v1/users/me/membership/history` 查询会员历史

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/membership/history`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "is_vip": true,
      "package_type": "example",
      "start_at": "2026-08-05T10:00:00+08:00",
      "end_at": "2026-08-05T10:00:00+08:00",
      "rights": {},
      "id": 1,
      "amount": 1.2,
      "order_no": "example",
      "status": 1
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"is_vip": true, "package_type": "example", "start_at": "2026-08-05T10:00:00+08:00", "end_at": "2026-08-05T10:00:00+08:00", "rights": {}, "id": 1, "amount": 1.2, "order_no": "example", "status": 1}]` |
| `items[].is_vip` | boolean | 是 | 当前是否为有效会员 | `true` |
| `items[].package_type` | string | 是 | 接口中的 `items[].package_type` 字段 | `"example"` |
| `items[].start_at` | string | 是 | 开始时间 | `"2026-08-05T10:00:00+08:00"` |
| `items[].end_at` | string | 是 | 结束时间 | `"2026-08-05T10:00:00+08:00"` |
| `items[].rights` | object | 是 | 接口中的 `items[].rights` 字段 | `{}` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].amount` | number | 是 | 金额，服务端按货币最小单位/定点值处理 | `1.2` |
| `items[].order_no` | string | 是 | 业务订单号，用于后续查询或支付 | `"example"` |
| `items[].status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 42. `POST /api/v1/membership/orders` 创建会员测试支付订单

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/membership/orders`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>",
  "Idempotency-Key": "example"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `Idempotency-Key` | header | string | 否 | 无额外约束 | 接口参数 | `"example"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "Idempotency-Key": "example"
}
```

### 请求体 JSON 示例

```json
{
  "package_code": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `package_code` | string | 是 | minLength=1; maxLength=32 | 套餐业务编码 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "order_no": "example",
  "package_code": "example",
  "product_name": "example",
  "amount": 1.2,
  "pay_type": 1,
  "status": 1,
  "expire_at": "2026-08-05T10:00:00+08:00",
  "payment_required": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `order_no` | string | 是 | 业务订单号，用于后续查询或支付 | `"example"` |
| `package_code` | string | 是 | 套餐业务编码 | `"example"` |
| `product_name` | string | 是 | 接口中的 `product_name` 字段 | `"example"` |
| `amount` | number | 是 | 金额，服务端按货币最小单位/定点值处理 | `1.2` |
| `pay_type` | integer | 是 | 接口中的 `pay_type` 字段 | `1` |
| `status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `expire_at` | string | 是 | 订单或资源到期时间 | `"2026-08-05T10:00:00+08:00"` |
| `payment_required` | boolean | 是 | 接口中的 `payment_required` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 43. `GET /api/v1/membership/orders/{order_no}` 查询会员订单

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/membership/orders/{order_no}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `order_no` | path | string | 是 | 无额外约束 | 接口参数 | `"example"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "order_no": "example"
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "order_no": "example",
  "package_code": "example",
  "product_name": "example",
  "amount": 1.2,
  "pay_type": 1,
  "status": 1,
  "expire_at": "2026-08-05T10:00:00+08:00",
  "payment_required": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `order_no` | string | 是 | 业务订单号，用于后续查询或支付 | `"example"` |
| `package_code` | string | 是 | 套餐业务编码 | `"example"` |
| `product_name` | string | 是 | 接口中的 `product_name` 字段 | `"example"` |
| `amount` | number | 是 | 金额，服务端按货币最小单位/定点值处理 | `1.2` |
| `pay_type` | integer | 是 | 接口中的 `pay_type` 字段 | `1` |
| `status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `expire_at` | string | 是 | 订单或资源到期时间 | `"2026-08-05T10:00:00+08:00"` |
| `payment_required` | boolean | 是 | 接口中的 `payment_required` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 44. `POST /api/v1/payments/wechat/callback` 微信支付回调

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/payments/wechat/callback`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "order_no": "example",
  "transaction_id": "example",
  "signature": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `order_no` | string | 是 | minLength=1; maxLength=64 | 业务订单号，用于后续查询或支付 | `"example"` |
| `transaction_id` | string | 是 | minLength=1; maxLength=64 | 支付渠道交易号 | `"example"` |
| `signature` | string | 是 | minLength=1; maxLength=512 | 第三方回调签名，服务端用于验签 | `"example"` |

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 45. `POST /api/v1/payments/test/pay` 执行测试支付并履约

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/payments/test/pay`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "order_no": "examplex",
  "success": true,
  "transaction_id": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `order_no` | string | 是 | minLength=8; maxLength=64 | 业务订单号，用于后续查询或支付 | `"examplex"` |
| `success` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `success` 字段 | `true` |
| `transaction_id` | string | 否 | minLength=1; maxLength=64 | 支付渠道交易号 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "order_no": "example",
  "status": 1,
  "transaction_id": "example",
  "payment_required": true,
  "fulfilled": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `order_no` | string | 是 | 业务订单号，用于后续查询或支付 | `"example"` |
| `status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `transaction_id` | string | 是 | 支付渠道交易号 | `"example"` |
| `payment_required` | boolean | 是 | 接口中的 `payment_required` 字段 | `true` |
| `fulfilled` | boolean | 是 | 接口中的 `fulfilled` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 46. `GET /api/v1/boost/packages` 查询置顶套餐

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/boost/packages`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "code": "example",
    "name": "example",
    "days": 1,
    "price": "11",
    "recommended": true
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 47. `GET /api/v1/users/me/boost/status` 查询我的置顶状态

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/boost/status`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "active": true,
  "remaining_days": 1,
  "expires_at": "2026-08-05T10:00:00+08:00",
  "order_no": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `active` | boolean | 是 | 是否启用 | `true` |
| `remaining_days` | integer | 是 | 接口中的 `remaining_days` 字段 | `1` |
| `expires_at` | string | 是 | 到期时间 | `"2026-08-05T10:00:00+08:00"` |
| `order_no` | string | 是 | 业务订单号，用于后续查询或支付 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 48. `POST /api/v1/boost/orders` 创建置顶测试支付订单

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/boost/orders`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>",
  "Idempotency-Key": "examplex"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `Idempotency-Key` | header | string | 是 | minLength=8；maxLength=128 | 接口参数 | `"examplex"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "Idempotency-Key": "examplex"
}
```

### 请求体 JSON 示例

```json
{
  "package_code": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `package_code` | string | 是 | minLength=1; maxLength=32 | 套餐业务编码 | `"example"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "order_no": "example",
  "product_code": "example",
  "product_name": "example",
  "amount": "11",
  "status": 1,
  "expire_at": "2026-08-05T10:00:00+08:00",
  "payment_required": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `order_no` | string | 是 | 业务订单号，用于后续查询或支付 | `"example"` |
| `product_code` | string | 是 | 商品业务编码 | `"example"` |
| `product_name` | string | 是 | 接口中的 `product_name` 字段 | `"example"` |
| `amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 金额，服务端按货币最小单位/定点值处理 | `"11"` |
| `status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `expire_at` | string | 是 | 订单或资源到期时间 | `"2026-08-05T10:00:00+08:00"` |
| `payment_required` | boolean | 是 | 接口中的 `payment_required` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 49. `GET /api/v1/boost/orders/{order_no}` 查询置顶订单

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/boost/orders/{order_no}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `order_no` | path | string | 是 | minLength=8；maxLength=64 | 接口参数 | `"examplex"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "order_no": "examplex"
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "order_no": "example",
  "product_code": "example",
  "product_name": "example",
  "amount": "11",
  "status": 1,
  "expire_at": "2026-08-05T10:00:00+08:00",
  "payment_required": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `order_no` | string | 是 | 业务订单号，用于后续查询或支付 | `"example"` |
| `product_code` | string | 是 | 商品业务编码 | `"example"` |
| `product_name` | string | 是 | 接口中的 `product_name` 字段 | `"example"` |
| `amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 金额，服务端按货币最小单位/定点值处理 | `"11"` |
| `status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `expire_at` | string | 是 | 订单或资源到期时间 | `"2026-08-05T10:00:00+08:00"` |
| `payment_required` | boolean | 是 | 接口中的 `payment_required` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 50. `POST /api/v1/spotlights/payments` 创建爆灯测试支付订单

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/spotlights/payments`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>",
  "Idempotency-Key": "examplex"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `Idempotency-Key` | header | string | 是 | minLength=8；maxLength=128 | 接口参数 | `"examplex"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "Idempotency-Key": "examplex"
}
```

### 请求体 JSON 示例

```json
{
  "target_user_id": 1
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_user_id` | integer | 是 | minimum=1.0 | 目标用户 ID | `1` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "order_no": "example",
  "product_code": "example",
  "product_name": "example",
  "amount": "11",
  "status": 1,
  "expire_at": "2026-08-05T10:00:00+08:00",
  "payment_required": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `order_no` | string | 是 | 业务订单号，用于后续查询或支付 | `"example"` |
| `product_code` | string | 是 | 商品业务编码 | `"example"` |
| `product_name` | string | 是 | 接口中的 `product_name` 字段 | `"example"` |
| `amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 金额，服务端按货币最小单位/定点值处理 | `"11"` |
| `status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `expire_at` | string | 是 | 订单或资源到期时间 | `"2026-08-05T10:00:00+08:00"` |
| `payment_required` | boolean | 是 | 接口中的 `payment_required` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 51. `GET /api/v1/spotlights/payments/{order_no}` 查询爆灯支付订单

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/spotlights/payments/{order_no}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `order_no` | path | string | 是 | minLength=8；maxLength=64 | 接口参数 | `"examplex"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "order_no": "examplex"
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "order_no": "example",
  "product_code": "example",
  "product_name": "example",
  "amount": "11",
  "status": 1,
  "expire_at": "2026-08-05T10:00:00+08:00",
  "payment_required": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `order_no` | string | 是 | 业务订单号，用于后续查询或支付 | `"example"` |
| `product_code` | string | 是 | 商品业务编码 | `"example"` |
| `product_name` | string | 是 | 接口中的 `product_name` 字段 | `"example"` |
| `amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 金额，服务端按货币最小单位/定点值处理 | `"11"` |
| `status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `expire_at` | string | 是 | 订单或资源到期时间 | `"2026-08-05T10:00:00+08:00"` |
| `payment_required` | boolean | 是 | 接口中的 `payment_required` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 52. `GET /api/v1/users/me/quotas` 查询当前用户各项剩余次数

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/quotas`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "quota_code": "example",
      "daily_limit": 1,
      "daily_used": 1,
      "daily_remaining": 1,
      "extra_remaining": 1,
      "total_remaining": 1
    }
  ]
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"quota_code": "example", "daily_limit": 1, "daily_used": 1, "daily_remaining": 1, "extra_remaining": 1, "total_remaining": 1}]` |
| `items[].quota_code` | string | 是 | 接口中的 `items[].quota_code` 字段 | `"example"` |
| `items[].daily_limit` | integer | 是 | 接口中的 `items[].daily_limit` 字段 | `1` |
| `items[].daily_used` | integer | 是 | 接口中的 `items[].daily_used` 字段 | `1` |
| `items[].daily_remaining` | integer | 是 | 接口中的 `items[].daily_remaining` 字段 | `1` |
| `items[].extra_remaining` | integer | 是 | 接口中的 `items[].extra_remaining` 字段 | `1` |
| `items[].total_remaining` | integer | 是 | 接口中的 `items[].total_remaining` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 53. `GET /api/v1/users/me/points` 查询积分余额

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/points`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "balance": 1,
  "total_earned": 1,
  "total_spent": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `balance` | integer | 是 | 操作后的余额 | `1` |
| `total_earned` | integer | 是 | 接口中的 `total_earned` 字段 | `1` |
| `total_spent` | integer | 是 | 接口中的 `total_spent` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 54. `GET /api/v1/users/me/points/ledger` 查询积分流水

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/points/ledger`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "type": 1,
      "amount": 1,
      "balance": 1,
      "description": "example",
      "created_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "type": 1, "amount": 1, "balance": 1, "description": "example", "created_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].type` | integer | 是 | 接口中的 `items[].type` 字段 | `1` |
| `items[].amount` | integer | 是 | 金额，服务端按货币最小单位/定点值处理 | `1` |
| `items[].balance` | integer | 是 | 操作后的余额 | `1` |
| `items[].description` | string | 是 | 详细描述 | `"example"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 55. `POST /api/v1/users/me/checkin` 每日签到

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/checkin`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "checked_in": true,
  "points": 1,
  "balance": 1,
  "checkin_date": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `checked_in` | boolean | 是 | 接口中的 `checked_in` 字段 | `true` |
| `points` | integer | 是 | 本次积分变动值 | `1` |
| `balance` | integer | 是 | 操作后的余额 | `1` |
| `checkin_date` | string | 是 | 接口中的 `checkin_date` 字段 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 56. `GET /api/v1/users/me/tasks` 查询积分任务

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/tasks`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "task_code": "example",
    "title": "example",
    "reward": 1,
    "status": 1,
    "completed_at": "2026-08-05T10:00:00+08:00"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 57. `POST /api/v1/users/me/tasks/{task_code}/claim` 领取任务积分

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/tasks/{task_code}/claim`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `task_code` | path | string | 是 | 无额外约束 | 接口参数 | `"example"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "task_code": "example"
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "task_code": "example",
  "claimed": true,
  "points": 1,
  "balance": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `task_code` | string | 是 | 任务业务编码 | `"example"` |
| `claimed` | boolean | 是 | 接口中的 `claimed` 字段 | `true` |
| `points` | integer | 是 | 本次积分变动值 | `1` |
| `balance` | integer | 是 | 操作后的余额 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 58. `GET /api/v1/users/me/invites` 查询邀请记录

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/invites`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "invitee_id": 1,
      "status": 1,
      "register_rewarded": true,
      "realname_rewarded": true,
      "created_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "invitee_id": 1, "status": 1, "register_rewarded": true, "realname_rewarded": true, "created_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].invitee_id` | integer | 是 | 接口中的 `items[].invitee_id` 字段 | `1` |
| `items[].status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `items[].register_rewarded` | boolean | 是 | 接口中的 `items[].register_rewarded` 字段 | `true` |
| `items[].realname_rewarded` | boolean | 是 | 接口中的 `items[].realname_rewarded` 字段 | `true` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 59. `GET /api/v1/points/products` 查询积分商品和权益

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/points/products`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "code": "example",
    "name": "example",
    "product_type": "example",
    "points_cost": 1,
    "value": "example",
    "stock": 1
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 60. `POST /api/v1/users/me/points/redeem` 兑换积分商品或权益

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/points/redeem`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>",
  "Idempotency-Key": "example"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `Idempotency-Key` | header | string | 否 | 无额外约束 | 接口参数 | `"example"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "Idempotency-Key": "example"
}
```

### 请求体 JSON 示例

```json
{
  "product_code": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `product_code` | string | 是 | 按 Schema 类型校验 | 商品业务编码 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "order_no": "example",
  "product_code": "example",
  "product_name": "example",
  "points_cost": 1,
  "status": 1,
  "balance": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `order_no` | string | 是 | 业务订单号，用于后续查询或支付 | `"example"` |
| `product_code` | string | 是 | 商品业务编码 | `"example"` |
| `product_name` | string | 是 | 接口中的 `product_name` 字段 | `"example"` |
| `points_cost` | integer | 是 | 接口中的 `points_cost` 字段 | `1` |
| `status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `balance` | integer | 是 | 操作后的余额 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 61. `GET /api/v1/regions/provinces` 查询省份

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/regions/provinces`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "code": "example",
      "name": "example"
    }
  ],
  "total": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"code": "example", "name": "example"}]` |
| `items[].code` | string | 是 | 业务编码或地区编码 | `"example"` |
| `items[].name` | string | 是 | 展示名称 | `"example"` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 62. `GET /api/v1/regions/cities` 按省份查询城市

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/regions/cities`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `province_code` | query | string | 是 | minLength=2；maxLength=2；pattern=^\d{2}$ | 接口参数 | `"11"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "province_code": "11"
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "code": "example",
      "name": "example"
    }
  ],
  "total": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"code": "example", "name": "example"}]` |
| `items[].code` | string | 是 | 业务编码或地区编码 | `"example"` |
| `items[].name` | string | 是 | 展示名称 | `"example"` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 63. `GET /api/v1/regions/districts` 按城市查询区县

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/regions/districts`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `city_code` | query | string | 是 | minLength=4；maxLength=4；pattern=^\d{4}$ | 接口参数 | `"1101"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "city_code": "1101"
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "code": "example",
      "name": "example"
    }
  ],
  "total": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"code": "example", "name": "example"}]` |
| `items[].code` | string | 是 | 业务编码或地区编码 | `"example"` |
| `items[].name` | string | 是 | 展示名称 | `"example"` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 64. `POST /api/v1/users/me/presence/heartbeat` 刷新在线心跳

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/presence/heartbeat`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "status": 0,
  "last_active_at": "2026-08-05T10:00:00+08:00",
  "heartbeat_interval_seconds": 1,
  "expires_after_seconds": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | 0/1/2 | 是 | 枚举=0, 1, 2 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `last_active_at` | string | 是 | 接口中的 `last_active_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `heartbeat_interval_seconds` | integer | 是 | 接口中的 `heartbeat_interval_seconds` 字段 | `1` |
| `expires_after_seconds` | integer | 是 | 接口中的 `expires_after_seconds` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 65. `POST /api/v1/users/me/presence/status` 设置在线或隐身状态

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/presence/status`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "status": 1
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "status": 0,
  "last_active_at": "2026-08-05T10:00:00+08:00",
  "heartbeat_interval_seconds": 1,
  "expires_after_seconds": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | 0/1/2 | 是 | 枚举=0, 1, 2 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `last_active_at` | string | 是 | 接口中的 `last_active_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `heartbeat_interval_seconds` | integer | 是 | 接口中的 `heartbeat_interval_seconds` 字段 | `1` |
| `expires_after_seconds` | integer | 是 | 接口中的 `expires_after_seconds` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 66. `GET /api/v1/users/me/presence` 查询我的在线状态

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/presence`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "status": 0,
  "last_active_at": "2026-08-05T10:00:00+08:00",
  "heartbeat_interval_seconds": 1,
  "expires_after_seconds": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | 0/1/2 | 是 | 枚举=0, 1, 2 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `last_active_at` | string | 是 | 接口中的 `last_active_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `heartbeat_interval_seconds` | integer | 是 | 接口中的 `heartbeat_interval_seconds` 字段 | `1` |
| `expires_after_seconds` | integer | 是 | 接口中的 `expires_after_seconds` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 67. `GET /api/v1/registration/intents` 查询注册身份选项

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/registration/intents`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "intent_type": "example",
    "label": "example",
    "description": "example"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 68. `GET /api/v1/auth/registration-intent` 查询当前注册身份

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/auth/registration-intent`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "intent_type": "example",
  "label": "example",
  "description": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `intent_type` | string | 是 | 接口中的 `intent_type` 字段 | `"example"` |
| `label` | string | 是 | 接口中的 `label` 字段 | `"example"` |
| `description` | string | 是 | 详细描述 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 69. `PUT /api/v1/auth/registration-intent` 提交注册身份

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/auth/registration-intent`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "intent_type": "self_match",
  "source": "register"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `intent_type` | self_match/parent_match/companion | 是 | 枚举=self_match, parent_match, companion | 接口中的 `intent_type` 字段 | `"self_match"` |
| `source` | register/profile | 否 | 枚举=register, profile | 接口中的 `source` 字段 | `"register"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "intent_type": "example",
  "label": "example",
  "description": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `intent_type` | string | 是 | 接口中的 `intent_type` 字段 | `"example"` |
| `label` | string | 是 | 接口中的 `label` 字段 | `"example"` |
| `description` | string | 是 | 详细描述 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 70. `GET /api/v1/matchmaker/application-types` 查询红娘申请类型

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/application-types`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {}
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 71. `POST /api/v1/matchmaker/applications` 提交红娘申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/applications`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "application_type": "promoter",
  "real_name": "example",
  "phone": "11",
  "intro": "examplexxx",
  "cert_images": [
    "/storage/uploads/example.jpg"
  ],
  "application_details": {
    "wechat": "example",
    "avatar": "/storage/uploads/example.jpg",
    "specialties": [
      "example"
    ],
    "expected_price": 1.2,
    "success_cases": [
      {
        "description": "example",
        "images": [
          "/storage/uploads/example.jpg"
        ]
      }
    ]
  }
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `application_type` | promoter/partner/service_matchmaker | 是 | 枚举=promoter, partner, service_matchmaker | 接口中的 `application_type` 字段 | `"promoter"` |
| `real_name` | string | 是 | minLength=2; maxLength=64 | 接口中的 `real_name` 字段 | `"example"` |
| `phone` | string | 是 | pattern=^1[3-9]\d{9}$ | 手机号；返回时通常脱敏 | `"11"` |
| `intro` | string | 是 | minLength=10; maxLength=2000 | 接口中的 `intro` 字段 | `"examplexxx"` |
| `cert_images` | array<string> | 否 | 按 Schema 类型校验 | 接口中的 `cert_images` 字段 | `["/storage/uploads/example.jpg"]` |
| `application_details` | object | 否 | 按 Schema 类型校验 | 审核资料；仅返回给申请人本人或管理员，不用于公开红娘资料。 | `{"wechat": "example", "avatar": "/storage/uploads/example.jpg", "specialties": ["example"], "expected_price": 1.2, "success_cases": [{"description": "example", "images": ["/storage/uploads/example.jpg"]}]}` |
| `application_details.wechat` | string | 否 | maxLength=128 | 接口中的 `application_details.wechat` 字段 | `"example"` |
| `application_details.avatar` | string | 否 | maxLength=512 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `application_details.specialties` | array<string> | 否 | 按 Schema 类型校验 | 接口中的 `application_details.specialties` 字段 | `["example"]` |
| `application_details.expected_price` | number | 否 | minimum=0.0; maximum=1000000.0 | 接口中的 `application_details.expected_price` 字段 | `1.2` |
| `application_details.success_cases` | array<object> | 否 | 按 Schema 类型校验 | 接口中的 `application_details.success_cases` 字段 | `[{"description": "example", "images": ["/storage/uploads/example.jpg"]}]` |
| `application_details.success_cases[].description` | string | 是 | minLength=1; maxLength=1000 | 详细描述 | `"example"` |
| `application_details.success_cases[].images` | array<string> | 否 | 按 Schema 类型校验 | 接口中的 `application_details.success_cases[].images` 字段 | `["/storage/uploads/example.jpg"]` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "application_type": "example",
  "status": 1,
  "real_name": "example",
  "phone_masked": "13800138000",
  "intro": "example",
  "cert_images": [
    "/storage/uploads/example.jpg"
  ],
  "application_details": {
    "wechat": "example",
    "avatar": "/storage/uploads/example.jpg",
    "specialties": [
      "example"
    ],
    "expected_price": 1.2,
    "success_cases": [
      {
        "description": "example",
        "images": [
          "/storage/uploads/example.jpg"
        ]
      }
    ]
  },
  "fail_reason": "example",
  "created_at": "example",
  "reviewed_at": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `application_type` | string | 是 | 接口中的 `application_type` 字段 | `"example"` |
| `status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `real_name` | string | 是 | 接口中的 `real_name` 字段 | `"example"` |
| `phone_masked` | string | 是 | 接口中的 `phone_masked` 字段 | `"13800138000"` |
| `intro` | string | 是 | 接口中的 `intro` 字段 | `"example"` |
| `cert_images` | array<string> | 是 | 接口中的 `cert_images` 字段 | `["/storage/uploads/example.jpg"]` |
| `application_details` | object | 是 | 审核资料；仅返回给申请人本人或管理员，不用于公开红娘资料。 | `{"wechat": "example", "avatar": "/storage/uploads/example.jpg", "specialties": ["example"], "expected_price": 1.2, "success_cases": [{"description": "example", "images": ["/storage/uploads/example.jpg"]}]}` |
| `application_details.wechat` | string | 否 | maxLength=128 | 接口中的 `application_details.wechat` 字段 | `"example"` |
| `application_details.avatar` | string | 否 | maxLength=512 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `application_details.specialties` | array<string> | 否 | 接口中的 `application_details.specialties` 字段 | `["example"]` |
| `application_details.expected_price` | number | 否 | minimum=0.0; maximum=1000000.0 | 接口中的 `application_details.expected_price` 字段 | `1.2` |
| `application_details.success_cases` | array<object> | 否 | 接口中的 `application_details.success_cases` 字段 | `[{"description": "example", "images": ["/storage/uploads/example.jpg"]}]` |
| `application_details.success_cases[].description` | string | 是 | minLength=1; maxLength=1000 | 详细描述 | `"example"` |
| `application_details.success_cases[].images` | array<string> | 否 | 接口中的 `application_details.success_cases[].images` 字段 | `["/storage/uploads/example.jpg"]` |
| `fail_reason` | string | 是 | 接口中的 `fail_reason` 字段 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"example"` |
| `reviewed_at` | string | 是 | 接口中的 `reviewed_at` 字段 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 72. `GET /api/v1/matchmaker/applications/mine` 查询我的红娘申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/applications/mine`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "application_type": "example",
    "status": 1,
    "real_name": "example",
    "phone_masked": "13800138000",
    "intro": "example",
    "cert_images": [
      "/storage/uploads/example.jpg"
    ],
    "application_details": {
      "wechat": "example",
      "avatar": "/storage/uploads/example.jpg",
      "specialties": [
        "example"
      ],
      "expected_price": 1.2,
      "success_cases": [
        {
          "description": "example",
          "images": [
            "/storage/uploads/example.jpg"
          ]
        }
      ]
    },
    "fail_reason": "example",
    "created_at": "example",
    "reviewed_at": "example"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 73. `PATCH /api/v1/admin/matchmaker/applications/{application_id}` 审核红娘申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/matchmaker/applications/{application_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `application_id` | path | integer | 是 | 无额外约束 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "application_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "status": 1,
  "fail_reason": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | 1/2/3 | 是 | 枚举=1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `fail_reason` | string | 否 | maxLength=255 | 接口中的 `fail_reason` 字段 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "application_type": "example",
  "status": 1,
  "real_name": "example",
  "phone_masked": "13800138000",
  "intro": "example",
  "cert_images": [
    "/storage/uploads/example.jpg"
  ],
  "application_details": {
    "wechat": "example",
    "avatar": "/storage/uploads/example.jpg",
    "specialties": [
      "example"
    ],
    "expected_price": 1.2,
    "success_cases": [
      {
        "description": "example",
        "images": [
          "/storage/uploads/example.jpg"
        ]
      }
    ]
  },
  "fail_reason": "example",
  "created_at": "example",
  "reviewed_at": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `application_type` | string | 是 | 接口中的 `application_type` 字段 | `"example"` |
| `status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `real_name` | string | 是 | 接口中的 `real_name` 字段 | `"example"` |
| `phone_masked` | string | 是 | 接口中的 `phone_masked` 字段 | `"13800138000"` |
| `intro` | string | 是 | 接口中的 `intro` 字段 | `"example"` |
| `cert_images` | array<string> | 是 | 接口中的 `cert_images` 字段 | `["/storage/uploads/example.jpg"]` |
| `application_details` | object | 是 | 审核资料；仅返回给申请人本人或管理员，不用于公开红娘资料。 | `{"wechat": "example", "avatar": "/storage/uploads/example.jpg", "specialties": ["example"], "expected_price": 1.2, "success_cases": [{"description": "example", "images": ["/storage/uploads/example.jpg"]}]}` |
| `application_details.wechat` | string | 否 | maxLength=128 | 接口中的 `application_details.wechat` 字段 | `"example"` |
| `application_details.avatar` | string | 否 | maxLength=512 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `application_details.specialties` | array<string> | 否 | 接口中的 `application_details.specialties` 字段 | `["example"]` |
| `application_details.expected_price` | number | 否 | minimum=0.0; maximum=1000000.0 | 接口中的 `application_details.expected_price` 字段 | `1.2` |
| `application_details.success_cases` | array<object> | 否 | 接口中的 `application_details.success_cases` 字段 | `[{"description": "example", "images": ["/storage/uploads/example.jpg"]}]` |
| `application_details.success_cases[].description` | string | 是 | minLength=1; maxLength=1000 | 详细描述 | `"example"` |
| `application_details.success_cases[].images` | array<string> | 否 | 接口中的 `application_details.success_cases[].images` 字段 | `["/storage/uploads/example.jpg"]` |
| `fail_reason` | string | 是 | 接口中的 `fail_reason` 字段 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"example"` |
| `reviewed_at` | string | 是 | 接口中的 `reviewed_at` 字段 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 74. `GET /api/v1/profile/tag-options` 查询固定标签选项

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/profile/tag-options`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "version": "example",
  "categories": [
    {
      "key": "example",
      "label": "example",
      "options": [
        "example"
      ]
    }
  ]
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `version` | string | 是 | 接口中的 `version` 字段 | `"example"` |
| `categories` | array<object> | 是 | 接口中的 `categories` 字段 | `[{"key": "example", "label": "example", "options": ["example"]}]` |
| `categories[].key` | string | 是 | 接口中的 `categories[].key` 字段 | `"example"` |
| `categories[].label` | string | 是 | 接口中的 `categories[].label` 字段 | `"example"` |
| `categories[].options` | array<string> | 是 | 接口中的 `categories[].options` 字段 | `["example"]` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 75. `GET /api/v1/discovery/filter-options` 查询首页筛选选项

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/filter-options`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "genders": [
    {}
  ],
  "marriage_statuses": [
    {}
  ],
  "education_levels": [
    {}
  ],
  "cities": [
    "example"
  ]
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `genders` | array<object> | 是 | 接口中的 `genders` 字段 | `[{}]` |
| `marriage_statuses` | array<object> | 是 | 接口中的 `marriage_statuses` 字段 | `[{}]` |
| `education_levels` | array<object> | 是 | 接口中的 `education_levels` 字段 | `[{}]` |
| `cities` | array<string> | 是 | 接口中的 `cities` 字段 | `["example"]` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 76. `GET /api/v1/discovery/recommendations` 查询推荐名片流

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/recommendations`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `gender` | query | 1/2 | 否 | 无额外约束 | 接口参数 | `1` |
| `age_min` | query | integer | 否 | 无额外约束 | 接口参数 | `1` |
| `age_max` | query | integer | 否 | 无额外约束 | 接口参数 | `1` |
| `province_code` | query | string | 否 | 无额外约束 | 接口参数 | `"example"` |
| `city_code` | query | string | 否 | 无额外约束 | 接口参数 | `"example"` |
| `district_code` | query | string | 否 | 无额外约束 | 接口参数 | `"example"` |
| `marriage_status` | query | 1/2/3 | 否 | 无额外约束 | 接口参数 | `1` |
| `education_min` | query | integer | 否 | 无额外约束 | 接口参数 | `1` |
| `height_min` | query | integer | 否 | 无额外约束 | 接口参数 | `1` |
| `height_max` | query | integer | 否 | 无额外约束 | 接口参数 | `1` |
| `income_min` | query | number | 否 | 无额外约束 | 接口参数 | `1.2` |
| `income_max` | query | number | 否 | 无额外约束 | 接口参数 | `1.2` |
| `pure_free` | query | boolean | 否 | 无额外约束 | 接口参数 | `false` |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=20 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "gender": 1,
  "age_min": 1,
  "age_max": 1,
  "province_code": "example",
  "city_code": "example",
  "district_code": "example",
  "marriage_status": 1,
  "education_min": 1,
  "height_min": 1,
  "height_max": 1,
  "income_min": 1.2,
  "income_max": 1.2,
  "pure_free": false,
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "age": 1,
      "height": 1,
      "education_level": 1,
      "occupation": "example",
      "city_code": "example",
      "income": 1.2,
      "distance_km": 1.2,
      "is_married": 1,
      "online_status": 1,
      "mbti": "example",
      "interest_tags": [
        "example"
      ],
      "certification_tags": [
        "example"
      ],
      "match_score": 1.2,
      "match_reason": "example",
      "is_favorite": true,
      "is_pure_free": true,
      "is_boosted": true,
      "detail_locked": false
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "height": 1, "education_level": 1, "occupation": "example", "city_code": "example", "income": 1.2, "distance_km": 1.2, "is_married": 1, "online_status": 1, "mbti": "example", "interest_tags": ["example"], "certification_tags": ["example"], "match_score": 1.2, "match_reason": "example", "is_favorite": true, "is_pure_free": true, "is_boosted": true, "detail_locked": false}]` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].age` | integer | 是 | 接口中的 `items[].age` 字段 | `1` |
| `items[].height` | integer | 是 | 接口中的 `items[].height` 字段 | `1` |
| `items[].education_level` | integer | 是 | 接口中的 `items[].education_level` 字段 | `1` |
| `items[].occupation` | string | 是 | 接口中的 `items[].occupation` 字段 | `"example"` |
| `items[].city_code` | string | 是 | 接口中的 `items[].city_code` 字段 | `"example"` |
| `items[].income` | number | 是 | 接口中的 `items[].income` 字段 | `1.2` |
| `items[].distance_km` | number | 是 | 与目标的距离，单位公里 | `1.2` |
| `items[].is_married` | integer | 是 | 接口中的 `items[].is_married` 字段 | `1` |
| `items[].online_status` | integer | 是 | 接口中的 `items[].online_status` 字段 | `1` |
| `items[].mbti` | string | 是 | 接口中的 `items[].mbti` 字段 | `"example"` |
| `items[].interest_tags` | array<string> | 是 | 接口中的 `items[].interest_tags` 字段 | `["example"]` |
| `items[].certification_tags` | array<string> | 是 | 接口中的 `items[].certification_tags` 字段 | `["example"]` |
| `items[].match_score` | number | 是 | 接口中的 `items[].match_score` 字段 | `1.2` |
| `items[].match_reason` | string | 是 | 接口中的 `items[].match_reason` 字段 | `"example"` |
| `items[].is_favorite` | boolean | 是 | 接口中的 `items[].is_favorite` 字段 | `true` |
| `items[].is_pure_free` | boolean | 是 | 接口中的 `items[].is_pure_free` 字段 | `true` |
| `items[].is_boosted` | boolean | 是 | 接口中的 `items[].is_boosted` 字段 | `true` |
| `items[].detail_locked` | boolean | 否 | 接口中的 `items[].detail_locked` 字段 | `false` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 77. `GET /api/v1/discovery/plaza` 查询广场名片流

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/plaza`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `gender` | query | 1/2 | 否 | 无额外约束 | 接口参数 | `1` |
| `age_min` | query | integer | 否 | 无额外约束 | 接口参数 | `1` |
| `age_max` | query | integer | 否 | 无额外约束 | 接口参数 | `1` |
| `province_code` | query | string | 否 | 无额外约束 | 接口参数 | `"example"` |
| `city_code` | query | string | 否 | 无额外约束 | 接口参数 | `"example"` |
| `district_code` | query | string | 否 | 无额外约束 | 接口参数 | `"example"` |
| `marriage_status` | query | 1/2/3 | 否 | 无额外约束 | 接口参数 | `1` |
| `education_min` | query | integer | 否 | 无额外约束 | 接口参数 | `1` |
| `height_min` | query | integer | 否 | 无额外约束 | 接口参数 | `1` |
| `height_max` | query | integer | 否 | 无额外约束 | 接口参数 | `1` |
| `income_min` | query | number | 否 | 无额外约束 | 接口参数 | `1.2` |
| `income_max` | query | number | 否 | 无额外约束 | 接口参数 | `1.2` |
| `pure_free` | query | boolean | 否 | 无额外约束 | 接口参数 | `false` |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=20 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "gender": 1,
  "age_min": 1,
  "age_max": 1,
  "province_code": "example",
  "city_code": "example",
  "district_code": "example",
  "marriage_status": 1,
  "education_min": 1,
  "height_min": 1,
  "height_max": 1,
  "income_min": 1.2,
  "income_max": 1.2,
  "pure_free": false,
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "age": 1,
      "height": 1,
      "education_level": 1,
      "occupation": "example",
      "city_code": "example",
      "income": 1.2,
      "distance_km": 1.2,
      "is_married": 1,
      "online_status": 1,
      "mbti": "example",
      "interest_tags": [
        "example"
      ],
      "certification_tags": [
        "example"
      ],
      "match_score": 1.2,
      "match_reason": "example",
      "is_favorite": true,
      "is_pure_free": true,
      "is_boosted": true,
      "detail_locked": false
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "height": 1, "education_level": 1, "occupation": "example", "city_code": "example", "income": 1.2, "distance_km": 1.2, "is_married": 1, "online_status": 1, "mbti": "example", "interest_tags": ["example"], "certification_tags": ["example"], "match_score": 1.2, "match_reason": "example", "is_favorite": true, "is_pure_free": true, "is_boosted": true, "detail_locked": false}]` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].age` | integer | 是 | 接口中的 `items[].age` 字段 | `1` |
| `items[].height` | integer | 是 | 接口中的 `items[].height` 字段 | `1` |
| `items[].education_level` | integer | 是 | 接口中的 `items[].education_level` 字段 | `1` |
| `items[].occupation` | string | 是 | 接口中的 `items[].occupation` 字段 | `"example"` |
| `items[].city_code` | string | 是 | 接口中的 `items[].city_code` 字段 | `"example"` |
| `items[].income` | number | 是 | 接口中的 `items[].income` 字段 | `1.2` |
| `items[].distance_km` | number | 是 | 与目标的距离，单位公里 | `1.2` |
| `items[].is_married` | integer | 是 | 接口中的 `items[].is_married` 字段 | `1` |
| `items[].online_status` | integer | 是 | 接口中的 `items[].online_status` 字段 | `1` |
| `items[].mbti` | string | 是 | 接口中的 `items[].mbti` 字段 | `"example"` |
| `items[].interest_tags` | array<string> | 是 | 接口中的 `items[].interest_tags` 字段 | `["example"]` |
| `items[].certification_tags` | array<string> | 是 | 接口中的 `items[].certification_tags` 字段 | `["example"]` |
| `items[].match_score` | number | 是 | 接口中的 `items[].match_score` 字段 | `1.2` |
| `items[].match_reason` | string | 是 | 接口中的 `items[].match_reason` 字段 | `"example"` |
| `items[].is_favorite` | boolean | 是 | 接口中的 `items[].is_favorite` 字段 | `true` |
| `items[].is_pure_free` | boolean | 是 | 接口中的 `items[].is_pure_free` 字段 | `true` |
| `items[].is_boosted` | boolean | 是 | 接口中的 `items[].is_boosted` 字段 | `true` |
| `items[].detail_locked` | boolean | 否 | 接口中的 `items[].detail_locked` 字段 | `false` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 78. `GET /api/v1/discovery/search` 按昵称或标签搜索用户

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/search`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `nickname` | query | string | 否 | 无额外约束 | 接口参数 | `"example"` |
| `tag` | query | string | 否 | 无额外约束 | 接口参数 | `"example"` |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=20 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "nickname": "example",
  "tag": "example",
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "age": 1,
      "height": 1,
      "education_level": 1,
      "occupation": "example",
      "city_code": "example",
      "income": 1.2,
      "distance_km": 1.2,
      "is_married": 1,
      "online_status": 1,
      "mbti": "example",
      "interest_tags": [
        "example"
      ],
      "certification_tags": [
        "example"
      ],
      "match_score": 1.2,
      "match_reason": "example",
      "is_favorite": true,
      "is_pure_free": true,
      "is_boosted": true,
      "detail_locked": false
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "height": 1, "education_level": 1, "occupation": "example", "city_code": "example", "income": 1.2, "distance_km": 1.2, "is_married": 1, "online_status": 1, "mbti": "example", "interest_tags": ["example"], "certification_tags": ["example"], "match_score": 1.2, "match_reason": "example", "is_favorite": true, "is_pure_free": true, "is_boosted": true, "detail_locked": false}]` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].age` | integer | 是 | 接口中的 `items[].age` 字段 | `1` |
| `items[].height` | integer | 是 | 接口中的 `items[].height` 字段 | `1` |
| `items[].education_level` | integer | 是 | 接口中的 `items[].education_level` 字段 | `1` |
| `items[].occupation` | string | 是 | 接口中的 `items[].occupation` 字段 | `"example"` |
| `items[].city_code` | string | 是 | 接口中的 `items[].city_code` 字段 | `"example"` |
| `items[].income` | number | 是 | 接口中的 `items[].income` 字段 | `1.2` |
| `items[].distance_km` | number | 是 | 与目标的距离，单位公里 | `1.2` |
| `items[].is_married` | integer | 是 | 接口中的 `items[].is_married` 字段 | `1` |
| `items[].online_status` | integer | 是 | 接口中的 `items[].online_status` 字段 | `1` |
| `items[].mbti` | string | 是 | 接口中的 `items[].mbti` 字段 | `"example"` |
| `items[].interest_tags` | array<string> | 是 | 接口中的 `items[].interest_tags` 字段 | `["example"]` |
| `items[].certification_tags` | array<string> | 是 | 接口中的 `items[].certification_tags` 字段 | `["example"]` |
| `items[].match_score` | number | 是 | 接口中的 `items[].match_score` 字段 | `1.2` |
| `items[].match_reason` | string | 是 | 接口中的 `items[].match_reason` 字段 | `"example"` |
| `items[].is_favorite` | boolean | 是 | 接口中的 `items[].is_favorite` 字段 | `true` |
| `items[].is_pure_free` | boolean | 是 | 接口中的 `items[].is_pure_free` 字段 | `true` |
| `items[].is_boosted` | boolean | 是 | 接口中的 `items[].is_boosted` 字段 | `true` |
| `items[].detail_locked` | boolean | 否 | 接口中的 `items[].detail_locked` 字段 | `false` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 79. `GET /api/v1/discovery/filters/saved` 获取已保存筛选条件

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/filters/saved`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "filters": {
    "gender": 1,
    "age_min": 1,
    "age_max": 1,
    "province_code": "example",
    "city_code": "example",
    "district_code": "example",
    "marriage_status": 1,
    "education_min": 1,
    "height_min": 1,
    "height_max": 1,
    "income_min": 1.2,
    "income_max": 1.2,
    "pure_free": false,
    "page": 1,
    "page_size": 20
  }
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `filters` | object | 是 | 接口中的 `filters` 字段 | `{"gender": 1, "age_min": 1, "age_max": 1, "province_code": "example", "city_code": "example", "district_code": "example", "marriage_status": 1, "education_min": 1, "height_min": 1, "height_max": 1, "income_min": 1.2, "income_max": 1.2, "pure_free": false, "page": 1, "page_size": 20}` |
| `filters.gender` | 1/2 | 否 | 枚举=1, 2 | 接口中的 `filters.gender` 字段 | `1` |
| `filters.age_min` | integer | 否 | minimum=18.0; maximum=100.0 | 接口中的 `filters.age_min` 字段 | `1` |
| `filters.age_max` | integer | 否 | minimum=18.0; maximum=100.0 | 接口中的 `filters.age_max` 字段 | `1` |
| `filters.province_code` | string | 否 | maxLength=32 | 接口中的 `filters.province_code` 字段 | `"example"` |
| `filters.city_code` | string | 否 | maxLength=32 | 接口中的 `filters.city_code` 字段 | `"example"` |
| `filters.district_code` | string | 否 | maxLength=32 | 接口中的 `filters.district_code` 字段 | `"example"` |
| `filters.marriage_status` | 1/2/3 | 否 | 枚举=1, 2, 3 | 接口中的 `filters.marriage_status` 字段 | `1` |
| `filters.education_min` | integer | 否 | minimum=1.0; maximum=8.0 | 接口中的 `filters.education_min` 字段 | `1` |
| `filters.height_min` | integer | 否 | minimum=100.0; maximum=250.0 | 接口中的 `filters.height_min` 字段 | `1` |
| `filters.height_max` | integer | 否 | minimum=100.0; maximum=250.0 | 接口中的 `filters.height_max` 字段 | `1` |
| `filters.income_min` | number | 否 | minimum=0.0; maximum=1000000.0 | 接口中的 `filters.income_min` 字段 | `1.2` |
| `filters.income_max` | number | 否 | minimum=0.0; maximum=1000000.0 | 接口中的 `filters.income_max` 字段 | `1.2` |
| `filters.pure_free` | boolean | 否 | 接口中的 `filters.pure_free` 字段 | `false` |
| `filters.page` | integer | 否 | minimum=1.0; maximum=1000.0 | 页码，从 1 开始 | `1` |
| `filters.page_size` | integer | 否 | minimum=1.0; maximum=20.0 | 每页记录数 | `20` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 80. `PUT /api/v1/discovery/filters/saved` 保存筛选条件

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/filters/saved`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "gender": 1,
  "age_min": 1,
  "age_max": 1,
  "province_code": "example",
  "city_code": "example",
  "district_code": "example",
  "marriage_status": 1,
  "education_min": 1,
  "height_min": 1,
  "height_max": 1,
  "income_min": 1.2,
  "income_max": 1.2,
  "pure_free": false,
  "page": 1,
  "page_size": 20
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `gender` | 1/2 | 否 | 枚举=1, 2 | 接口中的 `gender` 字段 | `1` |
| `age_min` | integer | 否 | minimum=18.0; maximum=100.0 | 接口中的 `age_min` 字段 | `1` |
| `age_max` | integer | 否 | minimum=18.0; maximum=100.0 | 接口中的 `age_max` 字段 | `1` |
| `province_code` | string | 否 | maxLength=32 | 接口中的 `province_code` 字段 | `"example"` |
| `city_code` | string | 否 | maxLength=32 | 接口中的 `city_code` 字段 | `"example"` |
| `district_code` | string | 否 | maxLength=32 | 接口中的 `district_code` 字段 | `"example"` |
| `marriage_status` | 1/2/3 | 否 | 枚举=1, 2, 3 | 接口中的 `marriage_status` 字段 | `1` |
| `education_min` | integer | 否 | minimum=1.0; maximum=8.0 | 接口中的 `education_min` 字段 | `1` |
| `height_min` | integer | 否 | minimum=100.0; maximum=250.0 | 接口中的 `height_min` 字段 | `1` |
| `height_max` | integer | 否 | minimum=100.0; maximum=250.0 | 接口中的 `height_max` 字段 | `1` |
| `income_min` | number | 否 | minimum=0.0; maximum=1000000.0 | 接口中的 `income_min` 字段 | `1.2` |
| `income_max` | number | 否 | minimum=0.0; maximum=1000000.0 | 接口中的 `income_max` 字段 | `1.2` |
| `pure_free` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `pure_free` 字段 | `false` |
| `page` | integer | 否 | minimum=1.0; maximum=1000.0 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 否 | minimum=1.0; maximum=20.0 | 每页记录数 | `20` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "filters": {
    "gender": 1,
    "age_min": 1,
    "age_max": 1,
    "province_code": "example",
    "city_code": "example",
    "district_code": "example",
    "marriage_status": 1,
    "education_min": 1,
    "height_min": 1,
    "height_max": 1,
    "income_min": 1.2,
    "income_max": 1.2,
    "pure_free": false,
    "page": 1,
    "page_size": 20
  }
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `filters` | object | 是 | 接口中的 `filters` 字段 | `{"gender": 1, "age_min": 1, "age_max": 1, "province_code": "example", "city_code": "example", "district_code": "example", "marriage_status": 1, "education_min": 1, "height_min": 1, "height_max": 1, "income_min": 1.2, "income_max": 1.2, "pure_free": false, "page": 1, "page_size": 20}` |
| `filters.gender` | 1/2 | 否 | 枚举=1, 2 | 接口中的 `filters.gender` 字段 | `1` |
| `filters.age_min` | integer | 否 | minimum=18.0; maximum=100.0 | 接口中的 `filters.age_min` 字段 | `1` |
| `filters.age_max` | integer | 否 | minimum=18.0; maximum=100.0 | 接口中的 `filters.age_max` 字段 | `1` |
| `filters.province_code` | string | 否 | maxLength=32 | 接口中的 `filters.province_code` 字段 | `"example"` |
| `filters.city_code` | string | 否 | maxLength=32 | 接口中的 `filters.city_code` 字段 | `"example"` |
| `filters.district_code` | string | 否 | maxLength=32 | 接口中的 `filters.district_code` 字段 | `"example"` |
| `filters.marriage_status` | 1/2/3 | 否 | 枚举=1, 2, 3 | 接口中的 `filters.marriage_status` 字段 | `1` |
| `filters.education_min` | integer | 否 | minimum=1.0; maximum=8.0 | 接口中的 `filters.education_min` 字段 | `1` |
| `filters.height_min` | integer | 否 | minimum=100.0; maximum=250.0 | 接口中的 `filters.height_min` 字段 | `1` |
| `filters.height_max` | integer | 否 | minimum=100.0; maximum=250.0 | 接口中的 `filters.height_max` 字段 | `1` |
| `filters.income_min` | number | 否 | minimum=0.0; maximum=1000000.0 | 接口中的 `filters.income_min` 字段 | `1.2` |
| `filters.income_max` | number | 否 | minimum=0.0; maximum=1000000.0 | 接口中的 `filters.income_max` 字段 | `1.2` |
| `filters.pure_free` | boolean | 否 | 接口中的 `filters.pure_free` 字段 | `false` |
| `filters.page` | integer | 否 | minimum=1.0; maximum=1000.0 | 页码，从 1 开始 | `1` |
| `filters.page_size` | integer | 否 | minimum=1.0; maximum=20.0 | 每页记录数 | `20` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 81. `GET /api/v1/discovery/browse-history` 查询浏览记录

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/browse-history`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "target": {
        "user_id": 1,
        "nickname": "example",
        "avatar": "/storage/uploads/example.jpg",
        "age": 1,
        "height": 1,
        "education_level": 1,
        "occupation": "example",
        "city_code": "example",
        "income": 1.2,
        "distance_km": 1.2,
        "is_married": 1,
        "online_status": 1,
        "mbti": "example",
        "interest_tags": [
          "example"
        ],
        "certification_tags": [
          "example"
        ],
        "match_score": 1.2,
        "match_reason": "example",
        "is_favorite": true,
        "is_pure_free": true,
        "is_boosted": true,
        "detail_locked": false
      },
      "viewed_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"target": {"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "height": 1, "education_level": 1, "occupation": "example", "city_code": "example", "income": 1.2, "distance_km": 1.2, "is_married": 1, "online_status": 1, "mbti": "example", "interest_tags": ["example"], "certification_tags": ["example"], "match_score": 1.2, "match_reason": "example", "is_favorite": true, "is_pure_free": true, "is_boosted": true, "detail_locked": false}, "viewed_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].target` | object | 是 | 接口中的 `items[].target` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "height": 1, "education_level": 1, "occupation": "example", "city_code": "example", "income": 1.2, "distance_km": 1.2, "is_married": 1, "online_status": 1, "mbti": "example", "interest_tags": ["example"], "certification_tags": ["example"], "match_score": 1.2, "match_reason": "example", "is_favorite": true, "is_pure_free": true, "is_boosted": true, "detail_locked": false}` |
| `items[].target.user_id` | integer | 是 | 用户 ID | `1` |
| `items[].target.nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].target.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].target.age` | integer | 是 | 接口中的 `items[].target.age` 字段 | `1` |
| `items[].target.height` | integer | 是 | 接口中的 `items[].target.height` 字段 | `1` |
| `items[].target.education_level` | integer | 是 | 接口中的 `items[].target.education_level` 字段 | `1` |
| `items[].target.occupation` | string | 是 | 接口中的 `items[].target.occupation` 字段 | `"example"` |
| `items[].target.city_code` | string | 是 | 接口中的 `items[].target.city_code` 字段 | `"example"` |
| `items[].target.income` | number | 是 | 接口中的 `items[].target.income` 字段 | `1.2` |
| `items[].target.distance_km` | number | 是 | 与目标的距离，单位公里 | `1.2` |
| `items[].target.is_married` | integer | 是 | 接口中的 `items[].target.is_married` 字段 | `1` |
| `items[].target.online_status` | integer | 是 | 接口中的 `items[].target.online_status` 字段 | `1` |
| `items[].target.mbti` | string | 是 | 接口中的 `items[].target.mbti` 字段 | `"example"` |
| `items[].target.interest_tags` | array<string> | 是 | 接口中的 `items[].target.interest_tags` 字段 | `["example"]` |
| `items[].target.certification_tags` | array<string> | 是 | 接口中的 `items[].target.certification_tags` 字段 | `["example"]` |
| `items[].target.match_score` | number | 是 | 接口中的 `items[].target.match_score` 字段 | `1.2` |
| `items[].target.match_reason` | string | 是 | 接口中的 `items[].target.match_reason` 字段 | `"example"` |
| `items[].target.is_favorite` | boolean | 是 | 接口中的 `items[].target.is_favorite` 字段 | `true` |
| `items[].target.is_pure_free` | boolean | 是 | 接口中的 `items[].target.is_pure_free` 字段 | `true` |
| `items[].target.is_boosted` | boolean | 是 | 接口中的 `items[].target.is_boosted` 字段 | `true` |
| `items[].target.detail_locked` | boolean | 否 | 接口中的 `items[].target.detail_locked` 字段 | `false` |
| `items[].viewed_at` | string | 是 | 接口中的 `items[].viewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 82. `GET /api/v1/discovery/visitors` 查询谁看过我

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/visitors`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "can_view_details": true,
  "count": 1,
  "items": [
    {
      "target": {
        "user_id": 1,
        "nickname": "example",
        "avatar": "/storage/uploads/example.jpg",
        "age": 1,
        "height": 1,
        "education_level": 1,
        "occupation": "example",
        "city_code": "example",
        "income": 1.2,
        "distance_km": 1.2,
        "is_married": 1,
        "online_status": 1,
        "mbti": "example",
        "interest_tags": [
          "example"
        ],
        "certification_tags": [
          "example"
        ],
        "match_score": 1.2,
        "match_reason": "example",
        "is_favorite": true,
        "is_pure_free": true,
        "is_boosted": true,
        "detail_locked": false
      },
      "viewed_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `can_view_details` | boolean | 是 | 接口中的 `can_view_details` 字段 | `true` |
| `count` | integer | 是 | 接口中的 `count` 字段 | `1` |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"target": {"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "height": 1, "education_level": 1, "occupation": "example", "city_code": "example", "income": 1.2, "distance_km": 1.2, "is_married": 1, "online_status": 1, "mbti": "example", "interest_tags": ["example"], "certification_tags": ["example"], "match_score": 1.2, "match_reason": "example", "is_favorite": true, "is_pure_free": true, "is_boosted": true, "detail_locked": false}, "viewed_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].target` | object | 是 | 接口中的 `items[].target` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "height": 1, "education_level": 1, "occupation": "example", "city_code": "example", "income": 1.2, "distance_km": 1.2, "is_married": 1, "online_status": 1, "mbti": "example", "interest_tags": ["example"], "certification_tags": ["example"], "match_score": 1.2, "match_reason": "example", "is_favorite": true, "is_pure_free": true, "is_boosted": true, "detail_locked": false}` |
| `items[].target.user_id` | integer | 是 | 用户 ID | `1` |
| `items[].target.nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].target.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].target.age` | integer | 是 | 接口中的 `items[].target.age` 字段 | `1` |
| `items[].target.height` | integer | 是 | 接口中的 `items[].target.height` 字段 | `1` |
| `items[].target.education_level` | integer | 是 | 接口中的 `items[].target.education_level` 字段 | `1` |
| `items[].target.occupation` | string | 是 | 接口中的 `items[].target.occupation` 字段 | `"example"` |
| `items[].target.city_code` | string | 是 | 接口中的 `items[].target.city_code` 字段 | `"example"` |
| `items[].target.income` | number | 是 | 接口中的 `items[].target.income` 字段 | `1.2` |
| `items[].target.distance_km` | number | 是 | 与目标的距离，单位公里 | `1.2` |
| `items[].target.is_married` | integer | 是 | 接口中的 `items[].target.is_married` 字段 | `1` |
| `items[].target.online_status` | integer | 是 | 接口中的 `items[].target.online_status` 字段 | `1` |
| `items[].target.mbti` | string | 是 | 接口中的 `items[].target.mbti` 字段 | `"example"` |
| `items[].target.interest_tags` | array<string> | 是 | 接口中的 `items[].target.interest_tags` 字段 | `["example"]` |
| `items[].target.certification_tags` | array<string> | 是 | 接口中的 `items[].target.certification_tags` 字段 | `["example"]` |
| `items[].target.match_score` | number | 是 | 接口中的 `items[].target.match_score` 字段 | `1.2` |
| `items[].target.match_reason` | string | 是 | 接口中的 `items[].target.match_reason` 字段 | `"example"` |
| `items[].target.is_favorite` | boolean | 是 | 接口中的 `items[].target.is_favorite` 字段 | `true` |
| `items[].target.is_pure_free` | boolean | 是 | 接口中的 `items[].target.is_pure_free` 字段 | `true` |
| `items[].target.is_boosted` | boolean | 是 | 接口中的 `items[].target.is_boosted` 字段 | `true` |
| `items[].target.detail_locked` | boolean | 否 | 接口中的 `items[].target.detail_locked` 字段 | `false` |
| `items[].viewed_at` | string | 是 | 接口中的 `items[].viewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 83. `GET /api/v1/discovery/favorites` 查询我的收藏

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/favorites`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "age": 1,
      "height": 1,
      "education_level": 1,
      "occupation": "example",
      "city_code": "example",
      "income": 1.2,
      "distance_km": 1.2,
      "is_married": 1,
      "online_status": 1,
      "mbti": "example",
      "interest_tags": [
        "example"
      ],
      "certification_tags": [
        "example"
      ],
      "match_score": 1.2,
      "match_reason": "example",
      "is_favorite": true,
      "is_pure_free": true,
      "is_boosted": true,
      "detail_locked": false
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "height": 1, "education_level": 1, "occupation": "example", "city_code": "example", "income": 1.2, "distance_km": 1.2, "is_married": 1, "online_status": 1, "mbti": "example", "interest_tags": ["example"], "certification_tags": ["example"], "match_score": 1.2, "match_reason": "example", "is_favorite": true, "is_pure_free": true, "is_boosted": true, "detail_locked": false}]` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].age` | integer | 是 | 接口中的 `items[].age` 字段 | `1` |
| `items[].height` | integer | 是 | 接口中的 `items[].height` 字段 | `1` |
| `items[].education_level` | integer | 是 | 接口中的 `items[].education_level` 字段 | `1` |
| `items[].occupation` | string | 是 | 接口中的 `items[].occupation` 字段 | `"example"` |
| `items[].city_code` | string | 是 | 接口中的 `items[].city_code` 字段 | `"example"` |
| `items[].income` | number | 是 | 接口中的 `items[].income` 字段 | `1.2` |
| `items[].distance_km` | number | 是 | 与目标的距离，单位公里 | `1.2` |
| `items[].is_married` | integer | 是 | 接口中的 `items[].is_married` 字段 | `1` |
| `items[].online_status` | integer | 是 | 接口中的 `items[].online_status` 字段 | `1` |
| `items[].mbti` | string | 是 | 接口中的 `items[].mbti` 字段 | `"example"` |
| `items[].interest_tags` | array<string> | 是 | 接口中的 `items[].interest_tags` 字段 | `["example"]` |
| `items[].certification_tags` | array<string> | 是 | 接口中的 `items[].certification_tags` 字段 | `["example"]` |
| `items[].match_score` | number | 是 | 接口中的 `items[].match_score` 字段 | `1.2` |
| `items[].match_reason` | string | 是 | 接口中的 `items[].match_reason` 字段 | `"example"` |
| `items[].is_favorite` | boolean | 是 | 接口中的 `items[].is_favorite` 字段 | `true` |
| `items[].is_pure_free` | boolean | 是 | 接口中的 `items[].is_pure_free` 字段 | `true` |
| `items[].is_boosted` | boolean | 是 | 接口中的 `items[].is_boosted` 字段 | `true` |
| `items[].detail_locked` | boolean | 否 | 接口中的 `items[].detail_locked` 字段 | `false` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 84. `GET /api/v1/discovery/favorites/received` 查询收到的收藏

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/favorites/received`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "user": {
        "user_id": 1,
        "nickname": "example",
        "avatar": "/storage/uploads/example.jpg",
        "age": 1,
        "city_code": "example"
      },
      "relation": "example",
      "created_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "user": {"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}, "relation": "example", "created_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].user` | object | 是 | 接口中的 `items[].user` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}` |
| `items[].user.user_id` | integer | 是 | 用户 ID | `1` |
| `items[].user.nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].user.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].user.age` | integer | 是 | 接口中的 `items[].user.age` 字段 | `1` |
| `items[].user.city_code` | string | 是 | 接口中的 `items[].user.city_code` 字段 | `"example"` |
| `items[].relation` | string | 是 | 接口中的 `items[].relation` 字段 | `"example"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 85. `PUT /api/v1/discovery/favorites/{target_id}` 收藏名片

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/favorites/{target_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `target_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "target_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "target_user_id": 1,
  "is_favorite": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `is_favorite` | boolean | 是 | 接口中的 `is_favorite` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 86. `DELETE /api/v1/discovery/favorites/{target_id}` 取消收藏

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/favorites/{target_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `target_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "target_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "target_user_id": 1,
  "is_favorite": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `is_favorite` | boolean | 是 | 接口中的 `is_favorite` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 87. `POST /api/v1/discovery/applications/{target_id}` 申请认识

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/applications/{target_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `target_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "target_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "message": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `message` | string | 否 | maxLength=255 | 接口中的 `message` 字段 | `"example"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "from_user_id": 1,
  "to_user_id": 1,
  "message": "example",
  "status": 0,
  "expire_at": "2026-08-05T10:00:00+08:00",
  "created_at": "2026-08-05T10:00:00+08:00",
  "from_user": {
    "user_id": 1,
    "nickname": "example",
    "avatar": "/storage/uploads/example.jpg",
    "age": 1,
    "city_code": "example"
  },
  "to_user": {
    "user_id": 1,
    "nickname": "example",
    "avatar": "/storage/uploads/example.jpg",
    "age": 1,
    "city_code": "example"
  }
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `from_user_id` | integer | 是 | 接口中的 `from_user_id` 字段 | `1` |
| `to_user_id` | integer | 是 | 接口中的 `to_user_id` 字段 | `1` |
| `message` | string | 是 | 接口中的 `message` 字段 | `"example"` |
| `status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `expire_at` | string | 是 | 订单或资源到期时间 | `"2026-08-05T10:00:00+08:00"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `from_user` | object | 否 | 接口中的 `from_user` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}` |
| `from_user.user_id` | integer | 是 | 用户 ID | `1` |
| `from_user.nickname` | string | 是 | 用户昵称 | `"example"` |
| `from_user.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `from_user.age` | integer | 是 | 接口中的 `from_user.age` 字段 | `1` |
| `from_user.city_code` | string | 是 | 接口中的 `from_user.city_code` 字段 | `"example"` |
| `to_user` | object | 否 | 接口中的 `to_user` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}` |
| `to_user.user_id` | integer | 是 | 用户 ID | `1` |
| `to_user.nickname` | string | 是 | 用户昵称 | `"example"` |
| `to_user.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `to_user.age` | integer | 是 | 接口中的 `to_user.age` 字段 | `1` |
| `to_user.city_code` | string | 是 | 接口中的 `to_user.city_code` 字段 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 88. `GET /api/v1/discovery/applications/incoming` 查询收到的认识申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/applications/incoming`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "from_user_id": 1,
      "to_user_id": 1,
      "message": "example",
      "status": 0,
      "expire_at": "2026-08-05T10:00:00+08:00",
      "created_at": "2026-08-05T10:00:00+08:00",
      "from_user": {
        "user_id": 1,
        "nickname": "example",
        "avatar": "/storage/uploads/example.jpg",
        "age": 1,
        "city_code": "example"
      },
      "to_user": {
        "user_id": 1,
        "nickname": "example",
        "avatar": "/storage/uploads/example.jpg",
        "age": 1,
        "city_code": "example"
      }
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "from_user_id": 1, "to_user_id": 1, "message": "example", "status": 0, "expire_at": "2026-08-05T10:00:00+08:00", "created_at": "2026-08-05T10:00:00+08:00", "from_user": {"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}, "to_user": {"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].from_user_id` | integer | 是 | 接口中的 `items[].from_user_id` 字段 | `1` |
| `items[].to_user_id` | integer | 是 | 接口中的 `items[].to_user_id` 字段 | `1` |
| `items[].message` | string | 是 | 接口中的 `items[].message` 字段 | `"example"` |
| `items[].status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `items[].expire_at` | string | 是 | 订单或资源到期时间 | `"2026-08-05T10:00:00+08:00"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].from_user` | object | 否 | 接口中的 `items[].from_user` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}` |
| `items[].from_user.user_id` | integer | 是 | 用户 ID | `1` |
| `items[].from_user.nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].from_user.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].from_user.age` | integer | 是 | 接口中的 `items[].from_user.age` 字段 | `1` |
| `items[].from_user.city_code` | string | 是 | 接口中的 `items[].from_user.city_code` 字段 | `"example"` |
| `items[].to_user` | object | 否 | 接口中的 `items[].to_user` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}` |
| `items[].to_user.user_id` | integer | 是 | 用户 ID | `1` |
| `items[].to_user.nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].to_user.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].to_user.age` | integer | 是 | 接口中的 `items[].to_user.age` 字段 | `1` |
| `items[].to_user.city_code` | string | 是 | 接口中的 `items[].to_user.city_code` 字段 | `"example"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 89. `GET /api/v1/discovery/applications/outgoing` 查询发出的认识申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/applications/outgoing`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "from_user_id": 1,
      "to_user_id": 1,
      "message": "example",
      "status": 0,
      "expire_at": "2026-08-05T10:00:00+08:00",
      "created_at": "2026-08-05T10:00:00+08:00",
      "from_user": {
        "user_id": 1,
        "nickname": "example",
        "avatar": "/storage/uploads/example.jpg",
        "age": 1,
        "city_code": "example"
      },
      "to_user": {
        "user_id": 1,
        "nickname": "example",
        "avatar": "/storage/uploads/example.jpg",
        "age": 1,
        "city_code": "example"
      }
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "from_user_id": 1, "to_user_id": 1, "message": "example", "status": 0, "expire_at": "2026-08-05T10:00:00+08:00", "created_at": "2026-08-05T10:00:00+08:00", "from_user": {"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}, "to_user": {"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].from_user_id` | integer | 是 | 接口中的 `items[].from_user_id` 字段 | `1` |
| `items[].to_user_id` | integer | 是 | 接口中的 `items[].to_user_id` 字段 | `1` |
| `items[].message` | string | 是 | 接口中的 `items[].message` 字段 | `"example"` |
| `items[].status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `items[].expire_at` | string | 是 | 订单或资源到期时间 | `"2026-08-05T10:00:00+08:00"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].from_user` | object | 否 | 接口中的 `items[].from_user` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}` |
| `items[].from_user.user_id` | integer | 是 | 用户 ID | `1` |
| `items[].from_user.nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].from_user.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].from_user.age` | integer | 是 | 接口中的 `items[].from_user.age` 字段 | `1` |
| `items[].from_user.city_code` | string | 是 | 接口中的 `items[].from_user.city_code` 字段 | `"example"` |
| `items[].to_user` | object | 否 | 接口中的 `items[].to_user` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}` |
| `items[].to_user.user_id` | integer | 是 | 用户 ID | `1` |
| `items[].to_user.nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].to_user.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].to_user.age` | integer | 是 | 接口中的 `items[].to_user.age` 字段 | `1` |
| `items[].to_user.city_code` | string | 是 | 接口中的 `items[].to_user.city_code` 字段 | `"example"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 90. `POST /api/v1/discovery/applications/{application_id}/accept` 同意认识申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/applications/{application_id}/accept`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `application_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "application_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "from_user_id": 1,
  "to_user_id": 1,
  "message": "example",
  "status": 0,
  "expire_at": "2026-08-05T10:00:00+08:00",
  "created_at": "2026-08-05T10:00:00+08:00",
  "from_user": {
    "user_id": 1,
    "nickname": "example",
    "avatar": "/storage/uploads/example.jpg",
    "age": 1,
    "city_code": "example"
  },
  "to_user": {
    "user_id": 1,
    "nickname": "example",
    "avatar": "/storage/uploads/example.jpg",
    "age": 1,
    "city_code": "example"
  }
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `from_user_id` | integer | 是 | 接口中的 `from_user_id` 字段 | `1` |
| `to_user_id` | integer | 是 | 接口中的 `to_user_id` 字段 | `1` |
| `message` | string | 是 | 接口中的 `message` 字段 | `"example"` |
| `status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `expire_at` | string | 是 | 订单或资源到期时间 | `"2026-08-05T10:00:00+08:00"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `from_user` | object | 否 | 接口中的 `from_user` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}` |
| `from_user.user_id` | integer | 是 | 用户 ID | `1` |
| `from_user.nickname` | string | 是 | 用户昵称 | `"example"` |
| `from_user.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `from_user.age` | integer | 是 | 接口中的 `from_user.age` 字段 | `1` |
| `from_user.city_code` | string | 是 | 接口中的 `from_user.city_code` 字段 | `"example"` |
| `to_user` | object | 否 | 接口中的 `to_user` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}` |
| `to_user.user_id` | integer | 是 | 用户 ID | `1` |
| `to_user.nickname` | string | 是 | 用户昵称 | `"example"` |
| `to_user.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `to_user.age` | integer | 是 | 接口中的 `to_user.age` 字段 | `1` |
| `to_user.city_code` | string | 是 | 接口中的 `to_user.city_code` 字段 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 91. `POST /api/v1/discovery/applications/{application_id}/reject` 拒绝认识申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/applications/{application_id}/reject`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `application_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "application_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "reason": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `reason` | string | 否 | maxLength=255 | 操作或审核原因 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "from_user_id": 1,
  "to_user_id": 1,
  "message": "example",
  "status": 0,
  "expire_at": "2026-08-05T10:00:00+08:00",
  "created_at": "2026-08-05T10:00:00+08:00",
  "from_user": {
    "user_id": 1,
    "nickname": "example",
    "avatar": "/storage/uploads/example.jpg",
    "age": 1,
    "city_code": "example"
  },
  "to_user": {
    "user_id": 1,
    "nickname": "example",
    "avatar": "/storage/uploads/example.jpg",
    "age": 1,
    "city_code": "example"
  }
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `from_user_id` | integer | 是 | 接口中的 `from_user_id` 字段 | `1` |
| `to_user_id` | integer | 是 | 接口中的 `to_user_id` 字段 | `1` |
| `message` | string | 是 | 接口中的 `message` 字段 | `"example"` |
| `status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `expire_at` | string | 是 | 订单或资源到期时间 | `"2026-08-05T10:00:00+08:00"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `from_user` | object | 否 | 接口中的 `from_user` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}` |
| `from_user.user_id` | integer | 是 | 用户 ID | `1` |
| `from_user.nickname` | string | 是 | 用户昵称 | `"example"` |
| `from_user.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `from_user.age` | integer | 是 | 接口中的 `from_user.age` 字段 | `1` |
| `from_user.city_code` | string | 是 | 接口中的 `from_user.city_code` 字段 | `"example"` |
| `to_user` | object | 否 | 接口中的 `to_user` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}` |
| `to_user.user_id` | integer | 是 | 用户 ID | `1` |
| `to_user.nickname` | string | 是 | 用户昵称 | `"example"` |
| `to_user.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `to_user.age` | integer | 是 | 接口中的 `to_user.age` 字段 | `1` |
| `to_user.city_code` | string | 是 | 接口中的 `to_user.city_code` 字段 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 92. `POST /api/v1/discovery/superlikes/{target_id}` 爆灯

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/superlikes/{target_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>",
  "Idempotency-Key": "examplex"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `target_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |
| `Idempotency-Key` | header | string | 是 | minLength=8；maxLength=128 | 接口参数 | `"examplex"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "target_id": 1,
  "Idempotency-Key": "examplex"
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`201`。

```json
{
  "target_user_id": 1,
  "remaining_today": 1,
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `remaining_today` | integer | 是 | 接口中的 `remaining_today` 字段 | `1` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 93. `GET /api/v1/discovery/superlikes/sent` 查询我发出的爆灯

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/superlikes/sent`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "user": {
        "user_id": 1,
        "nickname": "example",
        "avatar": "/storage/uploads/example.jpg",
        "age": 1,
        "city_code": "example"
      },
      "direction": "sent",
      "status": 1,
      "created_at": "2026-08-05T10:00:00+08:00",
      "matched": true,
      "order_no": "example"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "user": {"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}, "direction": "sent", "status": 1, "created_at": "2026-08-05T10:00:00+08:00", "matched": true, "order_no": "example"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].user` | object | 是 | 接口中的 `items[].user` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}` |
| `items[].user.user_id` | integer | 是 | 用户 ID | `1` |
| `items[].user.nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].user.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].user.age` | integer | 是 | 接口中的 `items[].user.age` 字段 | `1` |
| `items[].user.city_code` | string | 是 | 接口中的 `items[].user.city_code` 字段 | `"example"` |
| `items[].direction` | sent/received | 是 | 枚举=sent, received | 接口中的 `items[].direction` 字段 | `"sent"` |
| `items[].status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].matched` | boolean | 是 | 接口中的 `items[].matched` 字段 | `true` |
| `items[].order_no` | string | 否 | 业务订单号，用于后续查询或支付 | `"example"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 94. `GET /api/v1/discovery/superlikes/received` 查询收到的爆灯

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/discovery/superlikes/received`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "user": {
        "user_id": 1,
        "nickname": "example",
        "avatar": "/storage/uploads/example.jpg",
        "age": 1,
        "city_code": "example"
      },
      "direction": "sent",
      "status": 1,
      "created_at": "2026-08-05T10:00:00+08:00",
      "matched": true,
      "order_no": "example"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "user": {"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}, "direction": "sent", "status": 1, "created_at": "2026-08-05T10:00:00+08:00", "matched": true, "order_no": "example"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].user` | object | 是 | 接口中的 `items[].user` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "city_code": "example"}` |
| `items[].user.user_id` | integer | 是 | 用户 ID | `1` |
| `items[].user.nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].user.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].user.age` | integer | 是 | 接口中的 `items[].user.age` 字段 | `1` |
| `items[].user.city_code` | string | 是 | 接口中的 `items[].user.city_code` 字段 | `"example"` |
| `items[].direction` | sent/received | 是 | 枚举=sent, received | 接口中的 `items[].direction` 字段 | `"sent"` |
| `items[].status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].matched` | boolean | 是 | 接口中的 `items[].matched` 字段 | `true` |
| `items[].order_no` | string | 否 | 业务订单号，用于后续查询或支付 | `"example"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 95. `GET /api/v1/users/{user_id}/profile` 查看他人主页

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/{user_id}/profile`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `user_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "user_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "user_id": 1,
  "card": {
    "user_id": 1,
    "nickname": "example",
    "avatar": "/storage/uploads/example.jpg",
    "age": 1,
    "height": 1,
    "education_level": 1,
    "occupation": "example",
    "city_code": "example",
    "income": 1.2,
    "distance_km": 1.2,
    "is_married": 1,
    "online_status": 1,
    "mbti": "example",
    "interest_tags": [
      "example"
    ],
    "certification_tags": [
      "example"
    ],
    "match_score": 1.2,
    "match_reason": "example",
    "is_favorite": true,
    "is_pure_free": true,
    "is_boosted": true,
    "detail_locked": false
  },
  "profile": {
    "user_id": 1,
    "nickname": "example",
    "gender": 1,
    "birthday": "2026-08-05",
    "age": 1,
    "is_married": 1,
    "height": 1,
    "weight": 1,
    "occupation": "example",
    "industry": "example",
    "education_level": 1,
    "income": 1.2,
    "hometown_province_code": "example",
    "hometown_city_code": "example",
    "hometown_district_code": "example",
    "residence_province_code": "example",
    "residence_city_code": "example",
    "residence_district_code": "example",
    "self_intro": "example",
    "interest_tags": [
      "example"
    ],
    "personality_tags": [
      "example"
    ],
    "mbti": "INTJ",
    "avatar": "/storage/uploads/example.jpg",
    "background_wall": "example",
    "tag_selections": {},
    "photos": [
      {
        "id": 1,
        "media_type": "avatar",
        "file_url": "/storage/uploads/example.jpg",
        "thumbnail_url": "/storage/uploads/example.jpg",
        "sort_order": 1,
        "is_primary": true,
        "duration_seconds": 1
      }
    ],
    "video": {
      "id": 1,
      "media_type": "avatar",
      "file_url": "/storage/uploads/example.jpg",
      "thumbnail_url": "/storage/uploads/example.jpg",
      "sort_order": 1,
      "is_primary": true,
      "duration_seconds": 1
    },
    "completion_score": 1.2
  },
  "is_vip_viewer": true,
  "browse_quota_remaining": 1,
  "can_apply": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `card` | object | 是 | 接口中的 `card` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1, "height": 1, "education_level": 1, "occupation": "example", "city_code": "example", "income": 1.2, "distance_km": 1.2, "is_married": 1, "online_status": 1, "mbti": "example", "interest_tags": ["example"], "certification_tags": ["example"], "match_score": 1.2, "match_reason": "example", "is_favorite": true, "is_pure_free": true, "is_boosted": true, "detail_locked": false}` |
| `card.user_id` | integer | 是 | 用户 ID | `1` |
| `card.nickname` | string | 是 | 用户昵称 | `"example"` |
| `card.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `card.age` | integer | 是 | 接口中的 `card.age` 字段 | `1` |
| `card.height` | integer | 是 | 接口中的 `card.height` 字段 | `1` |
| `card.education_level` | integer | 是 | 接口中的 `card.education_level` 字段 | `1` |
| `card.occupation` | string | 是 | 接口中的 `card.occupation` 字段 | `"example"` |
| `card.city_code` | string | 是 | 接口中的 `card.city_code` 字段 | `"example"` |
| `card.income` | number | 是 | 接口中的 `card.income` 字段 | `1.2` |
| `card.distance_km` | number | 是 | 与目标的距离，单位公里 | `1.2` |
| `card.is_married` | integer | 是 | 接口中的 `card.is_married` 字段 | `1` |
| `card.online_status` | integer | 是 | 接口中的 `card.online_status` 字段 | `1` |
| `card.mbti` | string | 是 | 接口中的 `card.mbti` 字段 | `"example"` |
| `card.interest_tags` | array<string> | 是 | 接口中的 `card.interest_tags` 字段 | `["example"]` |
| `card.certification_tags` | array<string> | 是 | 接口中的 `card.certification_tags` 字段 | `["example"]` |
| `card.match_score` | number | 是 | 接口中的 `card.match_score` 字段 | `1.2` |
| `card.match_reason` | string | 是 | 接口中的 `card.match_reason` 字段 | `"example"` |
| `card.is_favorite` | boolean | 是 | 接口中的 `card.is_favorite` 字段 | `true` |
| `card.is_pure_free` | boolean | 是 | 接口中的 `card.is_pure_free` 字段 | `true` |
| `card.is_boosted` | boolean | 是 | 接口中的 `card.is_boosted` 字段 | `true` |
| `card.detail_locked` | boolean | 否 | 接口中的 `card.detail_locked` 字段 | `false` |
| `profile` | object | 是 | 接口中的 `profile` 字段 | `{"user_id": 1, "nickname": "example", "gender": 1, "birthday": "2026-08-05", "age": 1, "is_married": 1, "height": 1, "weight": 1, "occupation": "example", "industry": "example", "education_level": 1, "income": 1.2, "hometown_province_code": "example", "hometown_city_code": "example", "hometown_district_code": "example", "residence_province_code": "example", "residence_city_code": "example", "residence_district_code": "example", "self_intro": "example", "interest_tags": ["example"], "personality_tags": ["example"], "mbti": "INTJ", "avatar": "/storage/uploads/example.jpg", "background_wall": "example", "tag_selections": {}, "photos": [{"id": 1, "media_type": "avatar", "file_url": "/storage/uploads/example.jpg", "thumbnail_url": "/storage/uploads/example.jpg", "sort_order": 1, "is_primary": true, "duration_seconds": 1}], "video": {"id": 1, "media_type": "avatar", "file_url": "/storage/uploads/example.jpg", "thumbnail_url": "/storage/uploads/example.jpg", "sort_order": 1, "is_primary": true, "duration_seconds": 1}, "completion_score": 1.2}` |
| `profile.user_id` | integer | 是 | 用户 ID | `1` |
| `profile.nickname` | string | 是 | 用户昵称 | `"example"` |
| `profile.gender` | integer | 是 | 接口中的 `profile.gender` 字段 | `1` |
| `profile.birthday` | string | 是 | 接口中的 `profile.birthday` 字段 | `"2026-08-05"` |
| `profile.age` | integer | 是 | 接口中的 `profile.age` 字段 | `1` |
| `profile.is_married` | integer | 是 | 接口中的 `profile.is_married` 字段 | `1` |
| `profile.height` | integer | 是 | 接口中的 `profile.height` 字段 | `1` |
| `profile.weight` | integer | 是 | 接口中的 `profile.weight` 字段 | `1` |
| `profile.occupation` | string | 是 | 接口中的 `profile.occupation` 字段 | `"example"` |
| `profile.industry` | string | 是 | 接口中的 `profile.industry` 字段 | `"example"` |
| `profile.education_level` | integer | 是 | 接口中的 `profile.education_level` 字段 | `1` |
| `profile.income` | number | 是 | 接口中的 `profile.income` 字段 | `1.2` |
| `profile.hometown_province_code` | string | 是 | 接口中的 `profile.hometown_province_code` 字段 | `"example"` |
| `profile.hometown_city_code` | string | 是 | 接口中的 `profile.hometown_city_code` 字段 | `"example"` |
| `profile.hometown_district_code` | string | 是 | 接口中的 `profile.hometown_district_code` 字段 | `"example"` |
| `profile.residence_province_code` | string | 是 | 接口中的 `profile.residence_province_code` 字段 | `"example"` |
| `profile.residence_city_code` | string | 是 | 接口中的 `profile.residence_city_code` 字段 | `"example"` |
| `profile.residence_district_code` | string | 是 | 接口中的 `profile.residence_district_code` 字段 | `"example"` |
| `profile.self_intro` | string | 是 | 接口中的 `profile.self_intro` 字段 | `"example"` |
| `profile.interest_tags` | array<string> | 是 | 接口中的 `profile.interest_tags` 字段 | `["example"]` |
| `profile.personality_tags` | array<string> | 是 | 接口中的 `profile.personality_tags` 字段 | `["example"]` |
| `profile.mbti` | INTJ/INTP/ENTJ/ENTP/INFJ/INFP/ENFJ/ENFP/ISTJ/ISFJ/ESTJ/ESFJ/ISTP/ISFP/ESTP/ESFP | 是 | 枚举=INTJ, INTP, ENTJ, ENTP, INFJ, INFP, ENFJ, ENFP, ISTJ, ISFJ, ESTJ, ESFJ, ISTP, ISFP, ESTP, ESFP | 接口中的 `profile.mbti` 字段 | `"INTJ"` |
| `profile.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `profile.background_wall` | string | 是 | 接口中的 `profile.background_wall` 字段 | `"example"` |
| `profile.tag_selections` | object | 是 | 接口中的 `profile.tag_selections` 字段 | `{}` |
| `profile.photos` | array<object> | 是 | 接口中的 `profile.photos` 字段 | `[{"id": 1, "media_type": "avatar", "file_url": "/storage/uploads/example.jpg", "thumbnail_url": "/storage/uploads/example.jpg", "sort_order": 1, "is_primary": true, "duration_seconds": 1}]` |
| `profile.photos[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `profile.photos[].media_type` | avatar/background/photo/video | 是 | 枚举=avatar, background, photo, video | 接口中的 `profile.photos[].media_type` 字段 | `"avatar"` |
| `profile.photos[].file_url` | string | 是 | 接口中的 `profile.photos[].file_url` 字段 | `"/storage/uploads/example.jpg"` |
| `profile.photos[].thumbnail_url` | string | 是 | 接口中的 `profile.photos[].thumbnail_url` 字段 | `"/storage/uploads/example.jpg"` |
| `profile.photos[].sort_order` | integer | 是 | 接口中的 `profile.photos[].sort_order` 字段 | `1` |
| `profile.photos[].is_primary` | boolean | 是 | 接口中的 `profile.photos[].is_primary` 字段 | `true` |
| `profile.photos[].duration_seconds` | integer | 是 | 接口中的 `profile.photos[].duration_seconds` 字段 | `1` |
| `profile.video` | object | 是 | 接口中的 `profile.video` 字段 | `{"id": 1, "media_type": "avatar", "file_url": "/storage/uploads/example.jpg", "thumbnail_url": "/storage/uploads/example.jpg", "sort_order": 1, "is_primary": true, "duration_seconds": 1}` |
| `profile.video.id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `profile.video.media_type` | avatar/background/photo/video | 是 | 枚举=avatar, background, photo, video | 接口中的 `profile.video.media_type` 字段 | `"avatar"` |
| `profile.video.file_url` | string | 是 | 接口中的 `profile.video.file_url` 字段 | `"/storage/uploads/example.jpg"` |
| `profile.video.thumbnail_url` | string | 是 | 接口中的 `profile.video.thumbnail_url` 字段 | `"/storage/uploads/example.jpg"` |
| `profile.video.sort_order` | integer | 是 | 接口中的 `profile.video.sort_order` 字段 | `1` |
| `profile.video.is_primary` | boolean | 是 | 接口中的 `profile.video.is_primary` 字段 | `true` |
| `profile.video.duration_seconds` | integer | 是 | 接口中的 `profile.video.duration_seconds` 字段 | `1` |
| `profile.completion_score` | number | 是 | 接口中的 `profile.completion_score` 字段 | `1.2` |
| `is_vip_viewer` | boolean | 是 | 接口中的 `is_vip_viewer` 字段 | `true` |
| `browse_quota_remaining` | integer | 是 | 接口中的 `browse_quota_remaining` 字段 | `1` |
| `can_apply` | boolean | 是 | 接口中的 `can_apply` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 96. `GET /api/v1/users/{user_id}/poster` 生成分享海报

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/{user_id}/poster`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `user_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |
| `template` | query | integer | 否 | minimum=1；maximum=25 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "user_id": 1,
  "template": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 97. `GET /api/v1/matchmakers` 查询服务红娘列表

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmakers`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "application_type": "example",
      "intro": "example",
      "certification_tags": [
        "example"
      ],
      "success_count": 1,
      "rating_score": 1.2,
      "rating_count": 1,
      "is_available": true
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "application_type": "example", "intro": "example", "certification_tags": ["example"], "success_count": 1, "rating_score": 1.2, "rating_count": 1, "is_available": true}]` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].application_type` | string | 是 | 接口中的 `items[].application_type` 字段 | `"example"` |
| `items[].intro` | string | 是 | 接口中的 `items[].intro` 字段 | `"example"` |
| `items[].certification_tags` | array<string> | 是 | 接口中的 `items[].certification_tags` 字段 | `["example"]` |
| `items[].success_count` | integer | 是 | 接口中的 `items[].success_count` 字段 | `1` |
| `items[].rating_score` | number | 是 | 接口中的 `items[].rating_score` 字段 | `1.2` |
| `items[].rating_count` | integer | 是 | 接口中的 `items[].rating_count` 字段 | `1` |
| `items[].is_available` | boolean | 是 | 接口中的 `items[].is_available` 字段 | `true` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 98. `GET /api/v1/matchmakers/ranking` 查询热心红娘排行榜

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmakers/ranking`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "application_type": "example",
      "intro": "example",
      "certification_tags": [
        "example"
      ],
      "success_count": 1,
      "rating_score": 1.2,
      "rating_count": 1,
      "is_available": true
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "application_type": "example", "intro": "example", "certification_tags": ["example"], "success_count": 1, "rating_score": 1.2, "rating_count": 1, "is_available": true}]` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].application_type` | string | 是 | 接口中的 `items[].application_type` 字段 | `"example"` |
| `items[].intro` | string | 是 | 接口中的 `items[].intro` 字段 | `"example"` |
| `items[].certification_tags` | array<string> | 是 | 接口中的 `items[].certification_tags` 字段 | `["example"]` |
| `items[].success_count` | integer | 是 | 接口中的 `items[].success_count` 字段 | `1` |
| `items[].rating_score` | number | 是 | 接口中的 `items[].rating_score` 字段 | `1.2` |
| `items[].rating_count` | integer | 是 | 接口中的 `items[].rating_count` 字段 | `1` |
| `items[].is_available` | boolean | 是 | 接口中的 `items[].is_available` 字段 | `true` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 99. `GET /api/v1/matchmakers/{matchmaker_id}` 查询服务红娘详情

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmakers/{matchmaker_id}`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `matchmaker_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "matchmaker_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "user_id": 1,
  "nickname": "example",
  "avatar": "/storage/uploads/example.jpg",
  "application_type": "example",
  "intro": "example",
  "certification_tags": [
    "example"
  ],
  "success_count": 1,
  "rating_score": 1.2,
  "rating_count": 1,
  "is_available": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `nickname` | string | 是 | 用户昵称 | `"example"` |
| `avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `application_type` | string | 是 | 接口中的 `application_type` 字段 | `"example"` |
| `intro` | string | 是 | 接口中的 `intro` 字段 | `"example"` |
| `certification_tags` | array<string> | 是 | 接口中的 `certification_tags` 字段 | `["example"]` |
| `success_count` | integer | 是 | 接口中的 `success_count` 字段 | `1` |
| `rating_score` | number | 是 | 接口中的 `rating_score` 字段 | `1.2` |
| `rating_count` | integer | 是 | 接口中的 `rating_count` 字段 | `1` |
| `is_available` | boolean | 是 | 接口中的 `is_available` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 100. `GET /api/v1/matchmaker/service-products` 查询在售红娘服务商品

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-products`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "code": "example",
    "name": "example",
    "service_type": 1,
    "price": "11",
    "description": "example",
    "active": true,
    "created_at": "2026-08-05T10:00:00+08:00",
    "updated_at": "2026-08-05T10:00:00+08:00"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 101. `POST /api/v1/matchmaker/service-products` 创建红娘服务商品

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-products`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "code": "example_code",
  "name": "example",
  "service_type": 1,
  "price": 1.2,
  "description": "example",
  "active": true
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `code` | string | 是 | minLength=2; maxLength=32; pattern=^[a-z][a-z0-9_]*$ | 业务编码或地区编码 | `"example_code"` |
| `name` | string | 是 | minLength=1; maxLength=128 | 展示名称 | `"example"` |
| `service_type` | 1/3 | 是 | 枚举=1, 3 | 接口中的 `service_type` 字段 | `1` |
| `price` | number | 是 | maximum=1000000.0 | 商品价格 | `1.2` |
| `description` | string | 是 | minLength=1; maxLength=2000 | 详细描述 | `"example"` |
| `active` | boolean | 否 | 按 Schema 类型校验 | 是否启用 | `true` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "code": "example",
  "name": "example",
  "service_type": 1,
  "price": "11",
  "description": "example",
  "active": true,
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `code` | string | 是 | 业务编码或地区编码 | `"example"` |
| `name` | string | 是 | 展示名称 | `"example"` |
| `service_type` | 1/3 | 是 | 枚举=1, 3 | 接口中的 `service_type` 字段 | `1` |
| `price` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 商品价格 | `"11"` |
| `description` | string | 是 | 详细描述 | `"example"` |
| `active` | boolean | 是 | 是否启用 | `true` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 102. `GET /api/v1/matchmaker/service-products/{product_id}` Service Product Detail

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-products/{product_id}`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `product_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "product_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "code": "example",
  "name": "example",
  "service_type": 1,
  "price": "11",
  "description": "example",
  "active": true,
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `code` | string | 是 | 业务编码或地区编码 | `"example"` |
| `name` | string | 是 | 展示名称 | `"example"` |
| `service_type` | 1/3 | 是 | 枚举=1, 3 | 接口中的 `service_type` 字段 | `1` |
| `price` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 商品价格 | `"11"` |
| `description` | string | 是 | 详细描述 | `"example"` |
| `active` | boolean | 是 | 是否启用 | `true` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 103. `PATCH /api/v1/matchmaker/service-products/{product_id}` 修改或下架红娘服务商品

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-products/{product_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `product_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "product_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "name": "example",
  "service_type": 1,
  "price": 1.2,
  "description": "example",
  "active": true
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `name` | string | 否 | minLength=1; maxLength=128 | 展示名称 | `"example"` |
| `service_type` | 1/3 | 否 | 枚举=1, 3 | 接口中的 `service_type` 字段 | `1` |
| `price` | number | 否 | maximum=1000000.0 | 商品价格 | `1.2` |
| `description` | string | 否 | minLength=1; maxLength=2000 | 详细描述 | `"example"` |
| `active` | boolean | 否 | 按 Schema 类型校验 | 是否启用 | `true` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "code": "example",
  "name": "example",
  "service_type": 1,
  "price": "11",
  "description": "example",
  "active": true,
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `code` | string | 是 | 业务编码或地区编码 | `"example"` |
| `name` | string | 是 | 展示名称 | `"example"` |
| `service_type` | 1/3 | 是 | 枚举=1, 3 | 接口中的 `service_type` 字段 | `1` |
| `price` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 商品价格 | `"11"` |
| `description` | string | 是 | 详细描述 | `"example"` |
| `active` | boolean | 是 | 是否启用 | `true` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 104. `GET /api/v1/matchmaker/service-requests/orders` My Orders

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-requests/orders`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "order_no": "example",
      "user_id": 1,
      "matchmaker_id": 1,
      "product_id": 1,
      "service_type": 1,
      "product_name": "example",
      "amount": "11",
      "status": 0,
      "service_request_id": 1,
      "created_at": "2026-08-05T10:00:00+08:00",
      "pay_time": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "order_no": "example", "user_id": 1, "matchmaker_id": 1, "product_id": 1, "service_type": 1, "product_name": "example", "amount": "11", "status": 0, "service_request_id": 1, "created_at": "2026-08-05T10:00:00+08:00", "pay_time": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].order_no` | string | 是 | 业务订单号，用于后续查询或支付 | `"example"` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `items[].product_id` | integer | 是 | 商品 ID | `1` |
| `items[].service_type` | 1/3 | 是 | 枚举=1, 3 | 接口中的 `items[].service_type` 字段 | `1` |
| `items[].product_name` | string | 是 | 接口中的 `items[].product_name` 字段 | `"example"` |
| `items[].amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 金额，服务端按货币最小单位/定点值处理 | `"11"` |
| `items[].status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `items[].service_request_id` | integer | 是 | 接口中的 `items[].service_request_id` 字段 | `1` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].pay_time` | string | 是 | 接口中的 `items[].pay_time` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 105. `POST /api/v1/matchmaker/service-requests/orders` 创建待支付红娘服务订单

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-requests/orders`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>",
  "Idempotency-Key": "examplex"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `Idempotency-Key` | header | string | 是 | minLength=8；maxLength=128 | 接口参数 | `"examplex"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "Idempotency-Key": "examplex"
}
```

### 请求体 JSON 示例

```json
{
  "product_id": 1,
  "matchmaker_id": 1,
  "requirement": "examplexxx"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `product_id` | integer | 是 | minimum=1.0 | 商品 ID | `1` |
| `matchmaker_id` | integer | 是 | minimum=1.0 | 红娘用户 ID | `1` |
| `requirement` | string | 是 | minLength=10; maxLength=2000 | 接口中的 `requirement` 字段 | `"examplexxx"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "order_no": "example",
  "user_id": 1,
  "matchmaker_id": 1,
  "product_id": 1,
  "service_type": 1,
  "product_name": "example",
  "amount": "11",
  "status": 0,
  "service_request_id": 1,
  "created_at": "2026-08-05T10:00:00+08:00",
  "pay_time": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `order_no` | string | 是 | 业务订单号，用于后续查询或支付 | `"example"` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `product_id` | integer | 是 | 商品 ID | `1` |
| `service_type` | 1/3 | 是 | 枚举=1, 3 | 接口中的 `service_type` 字段 | `1` |
| `product_name` | string | 是 | 接口中的 `product_name` 字段 | `"example"` |
| `amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 金额，服务端按货币最小单位/定点值处理 | `"11"` |
| `status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `service_request_id` | integer | 是 | 接口中的 `service_request_id` 字段 | `1` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `pay_time` | string | 是 | 接口中的 `pay_time` 字段 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 106. `GET /api/v1/matchmaker/service-requests/orders/{order_no}` 查询我的红娘服务订单

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-requests/orders/{order_no}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `order_no` | path | string | 是 | minLength=8；maxLength=64 | 接口参数 | `"examplex"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "order_no": "examplex"
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "order_no": "example",
  "user_id": 1,
  "matchmaker_id": 1,
  "product_id": 1,
  "service_type": 1,
  "product_name": "example",
  "amount": "11",
  "status": 0,
  "service_request_id": 1,
  "created_at": "2026-08-05T10:00:00+08:00",
  "pay_time": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `order_no` | string | 是 | 业务订单号，用于后续查询或支付 | `"example"` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `product_id` | integer | 是 | 商品 ID | `1` |
| `service_type` | 1/3 | 是 | 枚举=1, 3 | 接口中的 `service_type` 字段 | `1` |
| `product_name` | string | 是 | 接口中的 `product_name` 字段 | `"example"` |
| `amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 金额，服务端按货币最小单位/定点值处理 | `"11"` |
| `status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `service_request_id` | integer | 是 | 接口中的 `service_request_id` 字段 | `1` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `pay_time` | string | 是 | 接口中的 `pay_time` 字段 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 107. `POST /api/v1/matchmaker/service-requests` 提交牵线服务申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-requests`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "order_no": "examplex",
  "requirement": "examplexxx"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `order_no` | string | 是 | minLength=8; maxLength=64 | 业务订单号，用于后续查询或支付 | `"examplex"` |
| `requirement` | string | 是 | minLength=10; maxLength=2000 | 接口中的 `requirement` 字段 | `"examplexxx"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "user_id": 1,
  "matchmaker_id": 1,
  "service_type": 1,
  "status": 0,
  "order_id": 1,
  "product_id": 1,
  "requirement": "example",
  "feedback": "example",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00",
  "start_at": "2026-08-05T10:00:00+08:00",
  "end_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `service_type` | 1/3 | 是 | 枚举=1, 3 | 接口中的 `service_type` 字段 | `1` |
| `status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `order_id` | integer | 是 | 订单 ID | `1` |
| `product_id` | integer | 是 | 商品 ID | `1` |
| `requirement` | string | 是 | 接口中的 `requirement` 字段 | `"example"` |
| `feedback` | string | 是 | 处理反馈或结果说明 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `start_at` | string | 是 | 开始时间 | `"2026-08-05T10:00:00+08:00"` |
| `end_at` | string | 是 | 结束时间 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 108. `GET /api/v1/matchmaker/service-requests/mine` 查询我提交的牵线申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-requests/mine`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "user_id": 1,
      "matchmaker_id": 1,
      "service_type": 1,
      "status": 0,
      "order_id": 1,
      "product_id": 1,
      "requirement": "example",
      "feedback": "example",
      "created_at": "2026-08-05T10:00:00+08:00",
      "updated_at": "2026-08-05T10:00:00+08:00",
      "start_at": "2026-08-05T10:00:00+08:00",
      "end_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "user_id": 1, "matchmaker_id": 1, "service_type": 1, "status": 0, "order_id": 1, "product_id": 1, "requirement": "example", "feedback": "example", "created_at": "2026-08-05T10:00:00+08:00", "updated_at": "2026-08-05T10:00:00+08:00", "start_at": "2026-08-05T10:00:00+08:00", "end_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `items[].service_type` | 1/3 | 是 | 枚举=1, 3 | 接口中的 `items[].service_type` 字段 | `1` |
| `items[].status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `items[].order_id` | integer | 是 | 订单 ID | `1` |
| `items[].product_id` | integer | 是 | 商品 ID | `1` |
| `items[].requirement` | string | 是 | 接口中的 `items[].requirement` 字段 | `"example"` |
| `items[].feedback` | string | 是 | 处理反馈或结果说明 | `"example"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].start_at` | string | 是 | 开始时间 | `"2026-08-05T10:00:00+08:00"` |
| `items[].end_at` | string | 是 | 结束时间 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 109. `GET /api/v1/matchmaker/service-requests/assigned` 查询分配给我的牵线申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-requests/assigned`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "user_id": 1,
      "matchmaker_id": 1,
      "service_type": 1,
      "status": 0,
      "order_id": 1,
      "product_id": 1,
      "requirement": "example",
      "feedback": "example",
      "created_at": "2026-08-05T10:00:00+08:00",
      "updated_at": "2026-08-05T10:00:00+08:00",
      "start_at": "2026-08-05T10:00:00+08:00",
      "end_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "user_id": 1, "matchmaker_id": 1, "service_type": 1, "status": 0, "order_id": 1, "product_id": 1, "requirement": "example", "feedback": "example", "created_at": "2026-08-05T10:00:00+08:00", "updated_at": "2026-08-05T10:00:00+08:00", "start_at": "2026-08-05T10:00:00+08:00", "end_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `items[].service_type` | 1/3 | 是 | 枚举=1, 3 | 接口中的 `items[].service_type` 字段 | `1` |
| `items[].status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `items[].order_id` | integer | 是 | 订单 ID | `1` |
| `items[].product_id` | integer | 是 | 商品 ID | `1` |
| `items[].requirement` | string | 是 | 接口中的 `items[].requirement` 字段 | `"example"` |
| `items[].feedback` | string | 是 | 处理反馈或结果说明 | `"example"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].start_at` | string | 是 | 开始时间 | `"2026-08-05T10:00:00+08:00"` |
| `items[].end_at` | string | 是 | 结束时间 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 110. `GET /api/v1/matchmaker/service-requests/{service_id}` Service Request Detail

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-requests/{service_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `service_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "service_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "user_id": 1,
  "matchmaker_id": 1,
  "service_type": 1,
  "status": 0,
  "order_id": 1,
  "product_id": 1,
  "requirement": "example",
  "feedback": "example",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00",
  "start_at": "2026-08-05T10:00:00+08:00",
  "end_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `service_type` | 1/3 | 是 | 枚举=1, 3 | 接口中的 `service_type` 字段 | `1` |
| `status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `order_id` | integer | 是 | 订单 ID | `1` |
| `product_id` | integer | 是 | 商品 ID | `1` |
| `requirement` | string | 是 | 接口中的 `requirement` 字段 | `"example"` |
| `feedback` | string | 是 | 处理反馈或结果说明 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `start_at` | string | 是 | 开始时间 | `"2026-08-05T10:00:00+08:00"` |
| `end_at` | string | 是 | 结束时间 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 111. `PATCH /api/v1/matchmaker/service-requests/{service_id}` 处理牵线服务申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-requests/{service_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `service_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "service_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "status": 1,
  "feedback": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | 1/2/3 | 是 | 枚举=1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `feedback` | string | 否 | maxLength=2000 | 处理反馈或结果说明 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "user_id": 1,
  "matchmaker_id": 1,
  "service_type": 1,
  "status": 0,
  "order_id": 1,
  "product_id": 1,
  "requirement": "example",
  "feedback": "example",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00",
  "start_at": "2026-08-05T10:00:00+08:00",
  "end_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `service_type` | 1/3 | 是 | 枚举=1, 3 | 接口中的 `service_type` 字段 | `1` |
| `status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `order_id` | integer | 是 | 订单 ID | `1` |
| `product_id` | integer | 是 | 商品 ID | `1` |
| `requirement` | string | 是 | 接口中的 `requirement` 字段 | `"example"` |
| `feedback` | string | 是 | 处理反馈或结果说明 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `start_at` | string | 是 | 开始时间 | `"2026-08-05T10:00:00+08:00"` |
| `end_at` | string | 是 | 结束时间 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 112. `GET /api/v1/matchmaker/service-requests/{service_id}/contact` 查看已购买服务的红娘微信

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-requests/{service_id}/contact`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `service_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "service_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "service_id": 1,
  "matchmaker_id": 1,
  "wechat_contact": "example",
  "delivered_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `service_id` | integer | 是 | 红娘服务申请 ID | `1` |
| `matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `wechat_contact` | string | 是 | 接口中的 `wechat_contact` 字段 | `"example"` |
| `delivered_at` | string | 是 | 接口中的 `delivered_at` 字段 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 113. `PATCH /api/v1/matchmaker/service-requests/{service_id}/contact` 提交红娘微信联系方式

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-requests/{service_id}/contact`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `service_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "service_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "wechat_contact": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `wechat_contact` | string | 是 | minLength=1; maxLength=128 | 接口中的 `wechat_contact` 字段 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "service_id": 1,
  "matchmaker_id": 1,
  "wechat_contact": "example",
  "delivered_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `service_id` | integer | 是 | 红娘服务申请 ID | `1` |
| `matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `wechat_contact` | string | 是 | 接口中的 `wechat_contact` 字段 | `"example"` |
| `delivered_at` | string | 是 | 接口中的 `delivered_at` 字段 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 114. `POST /api/v1/matchmaker/service-requests/{service_id}/contact-exchanges` 发起双方联系方式授权

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-requests/{service_id}/contact-exchanges`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `service_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "service_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "target_user_id": 1
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_user_id` | integer | 是 | minimum=1.0 | 目标用户 ID | `1` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "service_id": 1,
  "source_user_id": 1,
  "target_user_id": 1,
  "status": "PENDING",
  "source_consented_at": "2026-08-05T10:00:00+08:00",
  "target_consented_at": "2026-08-05T10:00:00+08:00",
  "delivered_at": "2026-08-05T10:00:00+08:00",
  "hidden_at": "2026-08-05T10:00:00+08:00",
  "hidden_reason": "example",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `service_id` | integer | 是 | 红娘服务申请 ID | `1` |
| `source_user_id` | integer | 是 | 接口中的 `source_user_id` 字段 | `1` |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `status` | PENDING/ONE_SIDE_CONSENT/APPROVED/DELIVERED/REVOKED/HIDDEN | 是 | 枚举=PENDING, ONE_SIDE_CONSENT, APPROVED, DELIVERED, REVOKED, HIDDEN | 当前业务状态，具体枚举见本接口约束 | `"PENDING"` |
| `source_consented_at` | string | 是 | 接口中的 `source_consented_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `target_consented_at` | string | 是 | 接口中的 `target_consented_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `delivered_at` | string | 是 | 接口中的 `delivered_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `hidden_at` | string | 是 | 接口中的 `hidden_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `hidden_reason` | string | 是 | 接口中的 `hidden_reason` 字段 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 115. `GET /api/v1/matchmaker/service-requests/contact-exchanges/{exchange_id}` 查询联系方式授权状态

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-requests/contact-exchanges/{exchange_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `exchange_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "exchange_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "service_id": 1,
  "source_user_id": 1,
  "target_user_id": 1,
  "status": "PENDING",
  "source_consented_at": "2026-08-05T10:00:00+08:00",
  "target_consented_at": "2026-08-05T10:00:00+08:00",
  "delivered_at": "2026-08-05T10:00:00+08:00",
  "hidden_at": "2026-08-05T10:00:00+08:00",
  "hidden_reason": "example",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `service_id` | integer | 是 | 红娘服务申请 ID | `1` |
| `source_user_id` | integer | 是 | 接口中的 `source_user_id` 字段 | `1` |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `status` | PENDING/ONE_SIDE_CONSENT/APPROVED/DELIVERED/REVOKED/HIDDEN | 是 | 枚举=PENDING, ONE_SIDE_CONSENT, APPROVED, DELIVERED, REVOKED, HIDDEN | 当前业务状态，具体枚举见本接口约束 | `"PENDING"` |
| `source_consented_at` | string | 是 | 接口中的 `source_consented_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `target_consented_at` | string | 是 | 接口中的 `target_consented_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `delivered_at` | string | 是 | 接口中的 `delivered_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `hidden_at` | string | 是 | 接口中的 `hidden_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `hidden_reason` | string | 是 | 接口中的 `hidden_reason` 字段 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 116. `PATCH /api/v1/matchmaker/service-requests/contact-exchanges/{exchange_id}` 同意或撤回联系方式授权

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-requests/contact-exchanges/{exchange_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `exchange_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "exchange_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "action": "CONSENT"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `action` | CONSENT/REVOKE | 是 | 枚举=CONSENT, REVOKE | 接口中的 `action` 字段 | `"CONSENT"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "service_id": 1,
  "source_user_id": 1,
  "target_user_id": 1,
  "status": "PENDING",
  "source_consented_at": "2026-08-05T10:00:00+08:00",
  "target_consented_at": "2026-08-05T10:00:00+08:00",
  "delivered_at": "2026-08-05T10:00:00+08:00",
  "hidden_at": "2026-08-05T10:00:00+08:00",
  "hidden_reason": "example",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `service_id` | integer | 是 | 红娘服务申请 ID | `1` |
| `source_user_id` | integer | 是 | 接口中的 `source_user_id` 字段 | `1` |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `status` | PENDING/ONE_SIDE_CONSENT/APPROVED/DELIVERED/REVOKED/HIDDEN | 是 | 枚举=PENDING, ONE_SIDE_CONSENT, APPROVED, DELIVERED, REVOKED, HIDDEN | 当前业务状态，具体枚举见本接口约束 | `"PENDING"` |
| `source_consented_at` | string | 是 | 接口中的 `source_consented_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `target_consented_at` | string | 是 | 接口中的 `target_consented_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `delivered_at` | string | 是 | 接口中的 `delivered_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `hidden_at` | string | 是 | 接口中的 `hidden_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `hidden_reason` | string | 是 | 接口中的 `hidden_reason` 字段 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 117. `GET /api/v1/matchmaker/service-requests/contact-exchanges/{exchange_id}/contacts` 查看已授权双方联系方式

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/service-requests/contact-exchanges/{exchange_id}/contacts`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `exchange_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "exchange_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "exchange_id": 1,
  "source_user_id": 1,
  "target_user_id": 1,
  "source_phone": "13800138000",
  "target_phone": "13800138000",
  "delivered_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `exchange_id` | integer | 是 | 接口中的 `exchange_id` 字段 | `1` |
| `source_user_id` | integer | 是 | 接口中的 `source_user_id` 字段 | `1` |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `source_phone` | string | 是 | 接口中的 `source_phone` 字段 | `"13800138000"` |
| `target_phone` | string | 是 | 接口中的 `target_phone` 字段 | `"13800138000"` |
| `delivered_at` | string | 是 | 接口中的 `delivered_at` 字段 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 118. `POST /api/v1/matchmaker/meetings/requests` 提交约见申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/meetings/requests`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "target_user_id": 1,
  "note": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_user_id` | integer | 是 | minimum=1.0 | 目标用户 ID | `1` |
| `note` | string | 是 | minLength=1; maxLength=2000 | 补充说明 | `"example"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "user_id": 1,
  "target_user_id": 1,
  "service_id": 1,
  "matchmaker_id": 1,
  "organization_id": 1,
  "status": "SUBMITTED",
  "note": "example",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `service_id` | integer | 是 | 红娘服务申请 ID | `1` |
| `matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `organization_id` | integer | 是 | 组织/门店 ID | `1` |
| `status` | SUBMITTED/CONTACTED/ACCEPTED/DECLINED/CLOSED | 是 | 枚举=SUBMITTED, CONTACTED, ACCEPTED, DECLINED, CLOSED | 当前业务状态，具体枚举见本接口约束 | `"SUBMITTED"` |
| `note` | string | 是 | 补充说明 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 119. `POST /api/v1/matchmaker/meetings/requests/from-service` 红娘基于服务单发起约见

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/meetings/requests/from-service`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "service_id": 1,
  "target_user_id": 1,
  "note": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `service_id` | integer | 是 | minimum=1.0 | 红娘服务申请 ID | `1` |
| `target_user_id` | integer | 是 | minimum=1.0 | 目标用户 ID | `1` |
| `note` | string | 是 | minLength=1; maxLength=2000 | 补充说明 | `"example"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "user_id": 1,
  "target_user_id": 1,
  "service_id": 1,
  "matchmaker_id": 1,
  "organization_id": 1,
  "status": "SUBMITTED",
  "note": "example",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `service_id` | integer | 是 | 红娘服务申请 ID | `1` |
| `matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `organization_id` | integer | 是 | 组织/门店 ID | `1` |
| `status` | SUBMITTED/CONTACTED/ACCEPTED/DECLINED/CLOSED | 是 | 枚举=SUBMITTED, CONTACTED, ACCEPTED, DECLINED, CLOSED | 当前业务状态，具体枚举见本接口约束 | `"SUBMITTED"` |
| `note` | string | 是 | 补充说明 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 120. `GET /api/v1/matchmaker/meetings/requests/mine` 查询我的约见申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/meetings/requests/mine`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "user_id": 1,
    "target_user_id": 1,
    "service_id": 1,
    "matchmaker_id": 1,
    "organization_id": 1,
    "status": "SUBMITTED",
    "note": "example",
    "created_at": "2026-08-05T10:00:00+08:00",
    "updated_at": "2026-08-05T10:00:00+08:00"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 121. `PATCH /api/v1/matchmaker/meetings/requests/{request_id}` 处理约见申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/meetings/requests/{request_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `request_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "request_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "status": "CONTACTED",
  "reason": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | CONTACTED/ACCEPTED/DECLINED/CLOSED | 是 | 枚举=CONTACTED, ACCEPTED, DECLINED, CLOSED | 当前业务状态，具体枚举见本接口约束 | `"CONTACTED"` |
| `reason` | string | 否 | maxLength=255 | 操作或审核原因 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "user_id": 1,
  "target_user_id": 1,
  "service_id": 1,
  "matchmaker_id": 1,
  "organization_id": 1,
  "status": "SUBMITTED",
  "note": "example",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `service_id` | integer | 是 | 红娘服务申请 ID | `1` |
| `matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `organization_id` | integer | 是 | 组织/门店 ID | `1` |
| `status` | SUBMITTED/CONTACTED/ACCEPTED/DECLINED/CLOSED | 是 | 枚举=SUBMITTED, CONTACTED, ACCEPTED, DECLINED, CLOSED | 当前业务状态，具体枚举见本接口约束 | `"SUBMITTED"` |
| `note` | string | 是 | 补充说明 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 122. `POST /api/v1/matchmaker/meetings/{meeting_id}/feedback` 提交约见反馈

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/matchmaker/meetings/{meeting_id}/feedback`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `meeting_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "meeting_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "target_rating": 1,
  "matchmaker_rating": 1,
  "continue_intent": 1,
  "private_feedback": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_rating` | integer | 否 | minimum=1.0; maximum=5.0 | 接口中的 `target_rating` 字段 | `1` |
| `matchmaker_rating` | integer | 否 | minimum=1.0; maximum=5.0 | 接口中的 `matchmaker_rating` 字段 | `1` |
| `continue_intent` | 1/2/3 | 否 | 枚举=1, 2, 3 | 接口中的 `continue_intent` 字段 | `1` |
| `private_feedback` | string | 否 | maxLength=2000 | 接口中的 `private_feedback` 字段 | `"example"` |

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 123. `PUT /api/v1/users/{target_id}/like` 喜欢用户

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/{target_id}/like`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `target_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "target_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "target_user_id": 1,
  "relation_type": "like",
  "enabled": true,
  "matched": false
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `relation_type` | like/follow | 是 | 枚举=like, follow | 接口中的 `relation_type` 字段 | `"like"` |
| `enabled` | boolean | 是 | 是否开启该功能 | `true` |
| `matched` | boolean | 否 | 接口中的 `matched` 字段 | `false` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 124. `DELETE /api/v1/users/{target_id}/like` 取消喜欢

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/{target_id}/like`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `target_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "target_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "target_user_id": 1,
  "relation_type": "like",
  "enabled": true,
  "matched": false
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `relation_type` | like/follow | 是 | 枚举=like, follow | 接口中的 `relation_type` 字段 | `"like"` |
| `enabled` | boolean | 是 | 是否开启该功能 | `true` |
| `matched` | boolean | 否 | 接口中的 `matched` 字段 | `false` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 125. `PUT /api/v1/users/{target_id}/follow` 关注用户

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/{target_id}/follow`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `target_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "target_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "target_user_id": 1,
  "relation_type": "like",
  "enabled": true,
  "matched": false
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `relation_type` | like/follow | 是 | 枚举=like, follow | 接口中的 `relation_type` 字段 | `"like"` |
| `enabled` | boolean | 是 | 是否开启该功能 | `true` |
| `matched` | boolean | 否 | 接口中的 `matched` 字段 | `false` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 126. `DELETE /api/v1/users/{target_id}/follow` 取消关注

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/{target_id}/follow`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `target_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "target_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "target_user_id": 1,
  "relation_type": "like",
  "enabled": true,
  "matched": false
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `relation_type` | like/follow | 是 | 枚举=like, follow | 接口中的 `relation_type` 字段 | `"like"` |
| `enabled` | boolean | 是 | 是否开启该功能 | `true` |
| `matched` | boolean | 否 | 接口中的 `matched` 字段 | `false` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 127. `GET /api/v1/relations/likes` 查看我的喜欢

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/relations/likes`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "age": 1
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1}]` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].age` | integer | 是 | 接口中的 `items[].age` 字段 | `1` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 128. `GET /api/v1/relations/liked-by` 查看喜欢我的人

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/relations/liked-by`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "age": 1
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1}]` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].age` | integer | 是 | 接口中的 `items[].age` 字段 | `1` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 129. `GET /api/v1/relations/following` 查看我的关注

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/relations/following`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "age": 1
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1}]` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].age` | integer | 是 | 接口中的 `items[].age` 字段 | `1` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 130. `GET /api/v1/relations/followers` 查看我的粉丝

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/relations/followers`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "age": 1
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1}]` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].age` | integer | 是 | 接口中的 `items[].age` 字段 | `1` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 131. `GET /api/v1/relations/matches` 查看我的匹配

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/relations/matches`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "age": 1
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1}]` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].age` | integer | 是 | 接口中的 `items[].age` 字段 | `1` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 132. `DELETE /api/v1/relations/matches/{target_id}` 取消匹配

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/relations/matches/{target_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `target_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "target_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 133. `GET /api/v1/chat/sessions` 查看聊天会话

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/chat/sessions`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "target": {
        "user_id": 1,
        "nickname": "example",
        "avatar": "/storage/uploads/example.jpg",
        "age": 1
      },
      "last_message": "example",
      "last_message_time": "2026-08-05T10:00:00+08:00",
      "unread_count": 1
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "target": {"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1}, "last_message": "example", "last_message_time": "2026-08-05T10:00:00+08:00", "unread_count": 1}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].target` | object | 是 | 接口中的 `items[].target` 字段 | `{"user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "age": 1}` |
| `items[].target.user_id` | integer | 是 | 用户 ID | `1` |
| `items[].target.nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].target.avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].target.age` | integer | 是 | 接口中的 `items[].target.age` 字段 | `1` |
| `items[].last_message` | string | 是 | 接口中的 `items[].last_message` 字段 | `"example"` |
| `items[].last_message_time` | string | 是 | 接口中的 `items[].last_message_time` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `items[].unread_count` | integer | 是 | 接口中的 `items[].unread_count` 字段 | `1` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 134. `GET /api/v1/chat/sessions/{session_id}/messages` 查看聊天记录

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/chat/sessions/{session_id}/messages`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `session_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "session_id": 1,
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "session_id": 1,
    "from_user_id": 1,
    "to_user_id": 1,
    "type": 1,
    "content": "example",
    "media_url": "/storage/uploads/example.jpg",
    "is_read": true,
    "revoked": true,
    "is_recalled": false,
    "recalled_at": "2026-08-05T10:00:00+08:00",
    "created_at": "2026-08-05T10:00:00+08:00"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 135. `POST /api/v1/chat/sessions/{session_id}/messages` 发送聊天消息

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/chat/sessions/{session_id}/messages`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `session_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "session_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "type": 1,
  "content": "example",
  "media_url": "/storage/uploads/example.jpg"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `type` | 1/2/3/4/5/6 | 否 | 枚举=1, 2, 3, 4, 5, 6 | 接口中的 `type` 字段 | `1` |
| `content` | string | 否 | maxLength=5000 | 文本内容 | `"example"` |
| `media_url` | string | 否 | maxLength=500 | 接口中的 `media_url` 字段 | `"/storage/uploads/example.jpg"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "session_id": 1,
  "from_user_id": 1,
  "to_user_id": 1,
  "type": 1,
  "content": "example",
  "media_url": "/storage/uploads/example.jpg",
  "is_read": true,
  "revoked": true,
  "is_recalled": false,
  "recalled_at": "2026-08-05T10:00:00+08:00",
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `session_id` | integer | 是 | 接口中的 `session_id` 字段 | `1` |
| `from_user_id` | integer | 是 | 接口中的 `from_user_id` 字段 | `1` |
| `to_user_id` | integer | 是 | 接口中的 `to_user_id` 字段 | `1` |
| `type` | integer | 是 | 接口中的 `type` 字段 | `1` |
| `content` | string | 是 | 文本内容 | `"example"` |
| `media_url` | string | 是 | 接口中的 `media_url` 字段 | `"/storage/uploads/example.jpg"` |
| `is_read` | boolean | 是 | 接口中的 `is_read` 字段 | `true` |
| `revoked` | boolean | 是 | 接口中的 `revoked` 字段 | `true` |
| `is_recalled` | boolean | 否 | 接口中的 `is_recalled` 字段 | `false` |
| `recalled_at` | string | 否 | 接口中的 `recalled_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 136. `POST /api/v1/chat/sessions/{session_id}/read` 标记聊天已读

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/chat/sessions/{session_id}/read`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `session_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "session_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 137. `DELETE /api/v1/chat/messages/{message_id}` 撤回聊天消息

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/chat/messages/{message_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `message_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "message_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 138. `POST /api/v1/chat/messages/{message_id}/recall` 撤回聊天消息并返回最新状态

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/chat/messages/{message_id}/recall`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `message_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "message_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "session_id": 1,
  "from_user_id": 1,
  "to_user_id": 1,
  "type": 1,
  "content": "example",
  "media_url": "/storage/uploads/example.jpg",
  "is_read": true,
  "revoked": true,
  "is_recalled": false,
  "recalled_at": "2026-08-05T10:00:00+08:00",
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `session_id` | integer | 是 | 接口中的 `session_id` 字段 | `1` |
| `from_user_id` | integer | 是 | 接口中的 `from_user_id` 字段 | `1` |
| `to_user_id` | integer | 是 | 接口中的 `to_user_id` 字段 | `1` |
| `type` | integer | 是 | 接口中的 `type` 字段 | `1` |
| `content` | string | 是 | 文本内容 | `"example"` |
| `media_url` | string | 是 | 接口中的 `media_url` 字段 | `"/storage/uploads/example.jpg"` |
| `is_read` | boolean | 是 | 接口中的 `is_read` 字段 | `true` |
| `revoked` | boolean | 是 | 接口中的 `revoked` 字段 | `true` |
| `is_recalled` | boolean | 否 | 接口中的 `is_recalled` 字段 | `false` |
| `recalled_at` | string | 否 | 接口中的 `recalled_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 139. `GET /api/v1/notifications` 查看消息通知

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/notifications`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "notification_type": "example",
      "title": "example",
      "content": "example",
      "payload": {},
      "related_user_id": 1,
      "related_id": 1,
      "target_type": "example",
      "target_id": 1,
      "actor_user_id": 1,
      "action": "",
      "is_read": true,
      "created_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "unread_count": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "notification_type": "example", "title": "example", "content": "example", "payload": {}, "related_user_id": 1, "related_id": 1, "target_type": "example", "target_id": 1, "actor_user_id": 1, "action": "", "is_read": true, "created_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].notification_type` | string | 是 | 接口中的 `items[].notification_type` 字段 | `"example"` |
| `items[].title` | string | 是 | 接口中的 `items[].title` 字段 | `"example"` |
| `items[].content` | string | 是 | 文本内容 | `"example"` |
| `items[].payload` | object | 是 | 接口中的 `items[].payload` 字段 | `{}` |
| `items[].related_user_id` | integer | 是 | 接口中的 `items[].related_user_id` 字段 | `1` |
| `items[].related_id` | integer | 是 | 接口中的 `items[].related_id` 字段 | `1` |
| `items[].target_type` | string | 否 | 接口中的 `items[].target_type` 字段 | `"example"` |
| `items[].target_id` | integer | 否 | 接口中的 `items[].target_id` 字段 | `1` |
| `items[].actor_user_id` | integer | 否 | 接口中的 `items[].actor_user_id` 字段 | `1` |
| `items[].action` | string | 否 | 接口中的 `items[].action` 字段 | `""` |
| `items[].is_read` | boolean | 是 | 接口中的 `items[].is_read` 字段 | `true` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `unread_count` | integer | 是 | 接口中的 `unread_count` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 140. `POST /api/v1/notifications/{notification_id}/read` 标记通知已读

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/notifications/{notification_id}/read`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `notification_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "notification_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 141. `POST /api/v1/notifications/read-all` 全部通知已读

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/notifications/read-all`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 142. `GET /api/v1/users/me/privacy` 查看隐私设置

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/privacy`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "user_id": 1,
  "hide_phone": true,
  "hide_school": true,
  "hide_company": true,
  "hide_distance": true,
  "hide_online_status": true,
  "only_auth_can_contact": true,
  "only_vip_can_see_detail": true,
  "who_can_see_me": 1,
  "match_status": 1,
  "anonymous_browse_enabled": true,
  "show_profile": true,
  "show_likes": true,
  "show_posts": true,
  "notify_like": true,
  "notify_comment": true,
  "notify_follow": true,
  "notify_message": true,
  "notify_match": true,
  "notify_apply": true,
  "notify_system": true,
  "notify_activity": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `hide_phone` | boolean | 是 | 接口中的 `hide_phone` 字段 | `true` |
| `hide_school` | boolean | 是 | 接口中的 `hide_school` 字段 | `true` |
| `hide_company` | boolean | 是 | 接口中的 `hide_company` 字段 | `true` |
| `hide_distance` | boolean | 是 | 接口中的 `hide_distance` 字段 | `true` |
| `hide_online_status` | boolean | 是 | 接口中的 `hide_online_status` 字段 | `true` |
| `only_auth_can_contact` | boolean | 是 | 接口中的 `only_auth_can_contact` 字段 | `true` |
| `only_vip_can_see_detail` | boolean | 是 | 接口中的 `only_vip_can_see_detail` 字段 | `true` |
| `who_can_see_me` | integer | 是 | 接口中的 `who_can_see_me` 字段 | `1` |
| `match_status` | integer | 是 | 接口中的 `match_status` 字段 | `1` |
| `anonymous_browse_enabled` | boolean | 是 | 接口中的 `anonymous_browse_enabled` 字段 | `true` |
| `show_profile` | boolean | 是 | 接口中的 `show_profile` 字段 | `true` |
| `show_likes` | boolean | 是 | 接口中的 `show_likes` 字段 | `true` |
| `show_posts` | boolean | 是 | 接口中的 `show_posts` 字段 | `true` |
| `notify_like` | boolean | 是 | 接口中的 `notify_like` 字段 | `true` |
| `notify_comment` | boolean | 是 | 接口中的 `notify_comment` 字段 | `true` |
| `notify_follow` | boolean | 是 | 接口中的 `notify_follow` 字段 | `true` |
| `notify_message` | boolean | 是 | 接口中的 `notify_message` 字段 | `true` |
| `notify_match` | boolean | 是 | 接口中的 `notify_match` 字段 | `true` |
| `notify_apply` | boolean | 是 | 接口中的 `notify_apply` 字段 | `true` |
| `notify_system` | boolean | 是 | 接口中的 `notify_system` 字段 | `true` |
| `notify_activity` | boolean | 是 | 接口中的 `notify_activity` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 143. `PUT /api/v1/users/me/privacy` 更新隐私设置

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/users/me/privacy`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "hide_phone": true,
  "hide_school": true,
  "hide_company": true,
  "hide_distance": true,
  "hide_online_status": true,
  "only_auth_can_contact": true,
  "only_vip_can_see_detail": true,
  "who_can_see_me": 1,
  "match_status": 1,
  "anonymous_browse_enabled": true,
  "show_profile": true,
  "show_likes": true,
  "show_posts": true,
  "notify_like": true,
  "notify_comment": true,
  "notify_follow": true,
  "notify_message": true,
  "notify_match": true,
  "notify_apply": true,
  "notify_system": true,
  "notify_activity": true
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `hide_phone` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `hide_phone` 字段 | `true` |
| `hide_school` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `hide_school` 字段 | `true` |
| `hide_company` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `hide_company` 字段 | `true` |
| `hide_distance` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `hide_distance` 字段 | `true` |
| `hide_online_status` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `hide_online_status` 字段 | `true` |
| `only_auth_can_contact` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `only_auth_can_contact` 字段 | `true` |
| `only_vip_can_see_detail` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `only_vip_can_see_detail` 字段 | `true` |
| `who_can_see_me` | 1/2/3/4 | 否 | 枚举=1, 2, 3, 4 | 接口中的 `who_can_see_me` 字段 | `1` |
| `match_status` | 1/2/3/4/5 | 否 | 枚举=1, 2, 3, 4, 5 | 接口中的 `match_status` 字段 | `1` |
| `anonymous_browse_enabled` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `anonymous_browse_enabled` 字段 | `true` |
| `show_profile` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `show_profile` 字段 | `true` |
| `show_likes` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `show_likes` 字段 | `true` |
| `show_posts` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `show_posts` 字段 | `true` |
| `notify_like` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `notify_like` 字段 | `true` |
| `notify_comment` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `notify_comment` 字段 | `true` |
| `notify_follow` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `notify_follow` 字段 | `true` |
| `notify_message` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `notify_message` 字段 | `true` |
| `notify_match` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `notify_match` 字段 | `true` |
| `notify_apply` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `notify_apply` 字段 | `true` |
| `notify_system` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `notify_system` 字段 | `true` |
| `notify_activity` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `notify_activity` 字段 | `true` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "user_id": 1,
  "hide_phone": true,
  "hide_school": true,
  "hide_company": true,
  "hide_distance": true,
  "hide_online_status": true,
  "only_auth_can_contact": true,
  "only_vip_can_see_detail": true,
  "who_can_see_me": 1,
  "match_status": 1,
  "anonymous_browse_enabled": true,
  "show_profile": true,
  "show_likes": true,
  "show_posts": true,
  "notify_like": true,
  "notify_comment": true,
  "notify_follow": true,
  "notify_message": true,
  "notify_match": true,
  "notify_apply": true,
  "notify_system": true,
  "notify_activity": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `hide_phone` | boolean | 是 | 接口中的 `hide_phone` 字段 | `true` |
| `hide_school` | boolean | 是 | 接口中的 `hide_school` 字段 | `true` |
| `hide_company` | boolean | 是 | 接口中的 `hide_company` 字段 | `true` |
| `hide_distance` | boolean | 是 | 接口中的 `hide_distance` 字段 | `true` |
| `hide_online_status` | boolean | 是 | 接口中的 `hide_online_status` 字段 | `true` |
| `only_auth_can_contact` | boolean | 是 | 接口中的 `only_auth_can_contact` 字段 | `true` |
| `only_vip_can_see_detail` | boolean | 是 | 接口中的 `only_vip_can_see_detail` 字段 | `true` |
| `who_can_see_me` | integer | 是 | 接口中的 `who_can_see_me` 字段 | `1` |
| `match_status` | integer | 是 | 接口中的 `match_status` 字段 | `1` |
| `anonymous_browse_enabled` | boolean | 是 | 接口中的 `anonymous_browse_enabled` 字段 | `true` |
| `show_profile` | boolean | 是 | 接口中的 `show_profile` 字段 | `true` |
| `show_likes` | boolean | 是 | 接口中的 `show_likes` 字段 | `true` |
| `show_posts` | boolean | 是 | 接口中的 `show_posts` 字段 | `true` |
| `notify_like` | boolean | 是 | 接口中的 `notify_like` 字段 | `true` |
| `notify_comment` | boolean | 是 | 接口中的 `notify_comment` 字段 | `true` |
| `notify_follow` | boolean | 是 | 接口中的 `notify_follow` 字段 | `true` |
| `notify_message` | boolean | 是 | 接口中的 `notify_message` 字段 | `true` |
| `notify_match` | boolean | 是 | 接口中的 `notify_match` 字段 | `true` |
| `notify_apply` | boolean | 是 | 接口中的 `notify_apply` 字段 | `true` |
| `notify_system` | boolean | 是 | 接口中的 `notify_system` 字段 | `true` |
| `notify_activity` | boolean | 是 | 接口中的 `notify_activity` 字段 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 144. `GET /api/v1/security/blocks` 查看黑名单

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/security/blocks`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "user_id": 1,
    "nickname": "example",
    "avatar": "/storage/uploads/example.jpg",
    "age": 1
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 145. `PUT /api/v1/security/blocks/{target_id}` 拉黑用户

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/security/blocks/{target_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `target_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "target_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "reason": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `reason` | string | 否 | maxLength=255 | 操作或审核原因 | `"example"` |

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 146. `DELETE /api/v1/security/blocks/{target_id}` 解除拉黑

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/security/blocks/{target_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `target_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "target_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 147. `POST /api/v1/security/reports/{target_id}` 举报用户

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/security/reports/{target_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `target_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "target_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "type": "example",
  "description": "example",
  "images": [
    "/storage/uploads/example.jpg"
  ]
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `type` | string | 是 | minLength=1; maxLength=64 | 接口中的 `type` 字段 | `"example"` |
| `description` | string | 否 | maxLength=1000 | 详细描述 | `"example"` |
| `images` | array<string> | 否 | 按 Schema 类型校验 | 接口中的 `images` 字段 | `["/storage/uploads/example.jpg"]` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "target_user_id": 1,
  "target_type": "user",
  "target_id": 1,
  "type": "example",
  "status": 0,
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `target_type` | user/post/comment/paper_plane | 否 | 枚举=user, post, comment, paper_plane | 接口中的 `target_type` 字段 | `"user"` |
| `target_id` | integer | 否 | 接口中的 `target_id` 字段 | `1` |
| `type` | string | 是 | 接口中的 `type` 字段 | `"example"` |
| `status` | 0/1/2 | 是 | 枚举=0, 1, 2 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 148. `POST /api/v1/community/media/uploads` 上传社区媒体

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/media/uploads`

```json
{
  "Content-Type": "multipart/form-data",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体示例（multipart/form-data）

字段 `file` 使用真实文件上传，其余字段按表单字段提交。

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `file` | string | 是 | 按 Schema 类型校验 | 接口中的 `file` 字段 | `"example"` |
| `purpose` | string | 是 | 按 Schema 类型校验 | 接口中的 `purpose` 字段 | `"example"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "purpose": "post",
  "media_type": "image",
  "url": "/storage/uploads/example.jpg",
  "thumbnail_url": "/storage/uploads/example.jpg",
  "file_size": 1,
  "duration_seconds": 1,
  "status": "ready"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `purpose` | post/paper_plane | 是 | 枚举=post, paper_plane | 接口中的 `purpose` 字段 | `"post"` |
| `media_type` | image/video | 是 | 枚举=image, video | 接口中的 `media_type` 字段 | `"image"` |
| `url` | string | 是 | 接口中的 `url` 字段 | `"/storage/uploads/example.jpg"` |
| `thumbnail_url` | string | 否 | 接口中的 `thumbnail_url` 字段 | `"/storage/uploads/example.jpg"` |
| `file_size` | integer | 否 | 接口中的 `file_size` 字段 | `1` |
| `duration_seconds` | integer | 否 | 接口中的 `duration_seconds` 字段 | `1` |
| `status` | ready/bound/deleted | 是 | 枚举=ready, bound, deleted | 当前业务状态，具体枚举见本接口约束 | `"ready"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 149. `DELETE /api/v1/community/media/{media_id}` 删除未绑定社区媒体

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/media/{media_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `media_id` | path | integer | 是 | 无额外约束 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "media_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 150. `GET /api/v1/community/posts` 查看动态流

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/posts`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `mode` | query | latest/following/city/liked_users/following_and_liked/mine | 否 | 枚举=latest, following, city, liked_users, following_and_liked, mine | 接口参数 | `"latest"` |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |
| `city` | query | string | 否 | 无额外约束 | 接口参数 | `"example"` |
| `city_code` | query | string | 否 | 无额外约束 | 市一级 city_code，只接受 4 或 6 位 ASCII 数字；4 位短码自动补 00 | `"example"` |
| `filter` | query | all/mbti/alumni/hometown/hot/latest | 否 | 无额外约束 | 接口参数 | `"all"` |
| `sort` | query | latest/hot | 否 | 枚举=latest, hot | 接口参数 | `"latest"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "mode": "latest",
  "page": 1,
  "page_size": 20,
  "city": "example",
  "city_code": "example",
  "filter": "all",
  "sort": "latest"
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "content": "example",
      "images": [
        "/storage/uploads/example.jpg"
      ],
      "video": "example",
      "location": "example",
      "visibility": 0,
      "declaration": "",
      "topic_id": 1,
      "topic_name": "example",
      "like_count": 1,
      "comment_count": 1,
      "collect_count": 0,
      "is_liked": true,
      "is_collected": false,
      "is_followed": false,
      "gender": 1,
      "age": 1,
      "mbti": "example",
      "school": "example",
      "hometown": "example",
      "residence": "example",
      "realname_status": 0,
      "moderation_status": "approved",
      "created_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "content": "example", "images": ["/storage/uploads/example.jpg"], "video": "example", "location": "example", "visibility": 0, "declaration": "", "topic_id": 1, "topic_name": "example", "like_count": 1, "comment_count": 1, "collect_count": 0, "is_liked": true, "is_collected": false, "is_followed": false, "gender": 1, "age": 1, "mbti": "example", "school": "example", "hometown": "example", "residence": "example", "realname_status": 0, "moderation_status": "approved", "created_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].content` | string | 是 | 文本内容 | `"example"` |
| `items[].images` | array<string> | 是 | 接口中的 `items[].images` 字段 | `["/storage/uploads/example.jpg"]` |
| `items[].video` | string | 是 | 接口中的 `items[].video` 字段 | `"example"` |
| `items[].location` | string | 是 | 接口中的 `items[].location` 字段 | `"example"` |
| `items[].visibility` | 0/1/2 | 否 | 枚举=0, 1, 2 | 接口中的 `items[].visibility` 字段 | `0` |
| `items[].declaration` | /内容包含虚构演绎/内容包含广告推广/内容可能引起不适 | 否 | 枚举=, 内容包含虚构演绎, 内容包含广告推广, 内容可能引起不适 | 接口中的 `items[].declaration` 字段 | `""` |
| `items[].topic_id` | integer | 否 | 接口中的 `items[].topic_id` 字段 | `1` |
| `items[].topic_name` | string | 否 | 接口中的 `items[].topic_name` 字段 | `"example"` |
| `items[].like_count` | integer | 是 | 接口中的 `items[].like_count` 字段 | `1` |
| `items[].comment_count` | integer | 是 | 接口中的 `items[].comment_count` 字段 | `1` |
| `items[].collect_count` | integer | 否 | 接口中的 `items[].collect_count` 字段 | `0` |
| `items[].is_liked` | boolean | 是 | 接口中的 `items[].is_liked` 字段 | `true` |
| `items[].is_collected` | boolean | 否 | 接口中的 `items[].is_collected` 字段 | `false` |
| `items[].is_followed` | boolean | 否 | 接口中的 `items[].is_followed` 字段 | `false` |
| `items[].gender` | integer | 否 | 接口中的 `items[].gender` 字段 | `1` |
| `items[].age` | integer | 否 | 接口中的 `items[].age` 字段 | `1` |
| `items[].mbti` | string | 否 | 接口中的 `items[].mbti` 字段 | `"example"` |
| `items[].school` | string | 否 | 接口中的 `items[].school` 字段 | `"example"` |
| `items[].hometown` | string | 否 | 接口中的 `items[].hometown` 字段 | `"example"` |
| `items[].residence` | string | 否 | 接口中的 `items[].residence` 字段 | `"example"` |
| `items[].realname_status` | integer | 否 | 接口中的 `items[].realname_status` 字段 | `0` |
| `items[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `items[].moderation_status` 字段 | `"approved"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 151. `POST /api/v1/community/posts` 发布动态

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/posts`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>",
  "Idempotency-Key": "examplex"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `Idempotency-Key` | header | string | 否 | 无额外约束 | 接口参数 | `"examplex"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "Idempotency-Key": "examplex"
}
```

### 请求体 JSON 示例

```json
{
  "content": "",
  "images": [
    "/storage/uploads/example.jpg"
  ],
  "video": "example",
  "image_media_ids": [
    1
  ],
  "video_media_id": 1,
  "location": "example",
  "topic_id": 1,
  "visibility": 0,
  "declaration": ""
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `content` | string | 否 | maxLength=2000 | 文本内容 | `""` |
| `images` | array<string> | 否 | 按 Schema 类型校验 | 接口中的 `images` 字段 | `["/storage/uploads/example.jpg"]` |
| `video` | string | 否 | maxLength=500 | 接口中的 `video` 字段 | `"example"` |
| `image_media_ids` | array<integer> | 否 | 按 Schema 类型校验 | 接口中的 `image_media_ids` 字段 | `[1]` |
| `video_media_id` | integer | 否 | minimum=1.0 | 接口中的 `video_media_id` 字段 | `1` |
| `location` | string | 否 | maxLength=128 | 接口中的 `location` 字段 | `"example"` |
| `topic_id` | integer | 否 | minimum=1.0 | 接口中的 `topic_id` 字段 | `1` |
| `visibility` | 0/1/2 | 否 | 枚举=0, 1, 2 | 接口中的 `visibility` 字段 | `0` |
| `declaration` | /内容包含虚构演绎/内容包含广告推广/内容可能引起不适 | 否 | 枚举=, 内容包含虚构演绎, 内容包含广告推广, 内容可能引起不适 | 接口中的 `declaration` 字段 | `""` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "user_id": 1,
  "nickname": "example",
  "avatar": "/storage/uploads/example.jpg",
  "content": "example",
  "images": [
    "/storage/uploads/example.jpg"
  ],
  "video": "example",
  "location": "example",
  "visibility": 0,
  "declaration": "",
  "topic_id": 1,
  "topic_name": "example",
  "like_count": 1,
  "comment_count": 1,
  "collect_count": 0,
  "is_liked": true,
  "is_collected": false,
  "is_followed": false,
  "gender": 1,
  "age": 1,
  "mbti": "example",
  "school": "example",
  "hometown": "example",
  "residence": "example",
  "realname_status": 0,
  "moderation_status": "approved",
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `nickname` | string | 是 | 用户昵称 | `"example"` |
| `avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `content` | string | 是 | 文本内容 | `"example"` |
| `images` | array<string> | 是 | 接口中的 `images` 字段 | `["/storage/uploads/example.jpg"]` |
| `video` | string | 是 | 接口中的 `video` 字段 | `"example"` |
| `location` | string | 是 | 接口中的 `location` 字段 | `"example"` |
| `visibility` | 0/1/2 | 否 | 枚举=0, 1, 2 | 接口中的 `visibility` 字段 | `0` |
| `declaration` | /内容包含虚构演绎/内容包含广告推广/内容可能引起不适 | 否 | 枚举=, 内容包含虚构演绎, 内容包含广告推广, 内容可能引起不适 | 接口中的 `declaration` 字段 | `""` |
| `topic_id` | integer | 否 | 接口中的 `topic_id` 字段 | `1` |
| `topic_name` | string | 否 | 接口中的 `topic_name` 字段 | `"example"` |
| `like_count` | integer | 是 | 接口中的 `like_count` 字段 | `1` |
| `comment_count` | integer | 是 | 接口中的 `comment_count` 字段 | `1` |
| `collect_count` | integer | 否 | 接口中的 `collect_count` 字段 | `0` |
| `is_liked` | boolean | 是 | 接口中的 `is_liked` 字段 | `true` |
| `is_collected` | boolean | 否 | 接口中的 `is_collected` 字段 | `false` |
| `is_followed` | boolean | 否 | 接口中的 `is_followed` 字段 | `false` |
| `gender` | integer | 否 | 接口中的 `gender` 字段 | `1` |
| `age` | integer | 否 | 接口中的 `age` 字段 | `1` |
| `mbti` | string | 否 | 接口中的 `mbti` 字段 | `"example"` |
| `school` | string | 否 | 接口中的 `school` 字段 | `"example"` |
| `hometown` | string | 否 | 接口中的 `hometown` 字段 | `"example"` |
| `residence` | string | 否 | 接口中的 `residence` 字段 | `"example"` |
| `realname_status` | integer | 否 | 接口中的 `realname_status` 字段 | `0` |
| `moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `moderation_status` 字段 | `"approved"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 152. `GET /api/v1/community/posts/{post_id}` 查看动态详情

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/posts/{post_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `post_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "post_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "user_id": 1,
  "nickname": "example",
  "avatar": "/storage/uploads/example.jpg",
  "content": "example",
  "images": [
    "/storage/uploads/example.jpg"
  ],
  "video": "example",
  "location": "example",
  "visibility": 0,
  "declaration": "",
  "topic_id": 1,
  "topic_name": "example",
  "like_count": 1,
  "comment_count": 1,
  "collect_count": 0,
  "is_liked": true,
  "is_collected": false,
  "is_followed": false,
  "gender": 1,
  "age": 1,
  "mbti": "example",
  "school": "example",
  "hometown": "example",
  "residence": "example",
  "realname_status": 0,
  "moderation_status": "approved",
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `nickname` | string | 是 | 用户昵称 | `"example"` |
| `avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `content` | string | 是 | 文本内容 | `"example"` |
| `images` | array<string> | 是 | 接口中的 `images` 字段 | `["/storage/uploads/example.jpg"]` |
| `video` | string | 是 | 接口中的 `video` 字段 | `"example"` |
| `location` | string | 是 | 接口中的 `location` 字段 | `"example"` |
| `visibility` | 0/1/2 | 否 | 枚举=0, 1, 2 | 接口中的 `visibility` 字段 | `0` |
| `declaration` | /内容包含虚构演绎/内容包含广告推广/内容可能引起不适 | 否 | 枚举=, 内容包含虚构演绎, 内容包含广告推广, 内容可能引起不适 | 接口中的 `declaration` 字段 | `""` |
| `topic_id` | integer | 否 | 接口中的 `topic_id` 字段 | `1` |
| `topic_name` | string | 否 | 接口中的 `topic_name` 字段 | `"example"` |
| `like_count` | integer | 是 | 接口中的 `like_count` 字段 | `1` |
| `comment_count` | integer | 是 | 接口中的 `comment_count` 字段 | `1` |
| `collect_count` | integer | 否 | 接口中的 `collect_count` 字段 | `0` |
| `is_liked` | boolean | 是 | 接口中的 `is_liked` 字段 | `true` |
| `is_collected` | boolean | 否 | 接口中的 `is_collected` 字段 | `false` |
| `is_followed` | boolean | 否 | 接口中的 `is_followed` 字段 | `false` |
| `gender` | integer | 否 | 接口中的 `gender` 字段 | `1` |
| `age` | integer | 否 | 接口中的 `age` 字段 | `1` |
| `mbti` | string | 否 | 接口中的 `mbti` 字段 | `"example"` |
| `school` | string | 否 | 接口中的 `school` 字段 | `"example"` |
| `hometown` | string | 否 | 接口中的 `hometown` 字段 | `"example"` |
| `residence` | string | 否 | 接口中的 `residence` 字段 | `"example"` |
| `realname_status` | integer | 否 | 接口中的 `realname_status` 字段 | `0` |
| `moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `moderation_status` 字段 | `"approved"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 153. `PUT /api/v1/community/posts/{post_id}` 修改动态并重新提交审核

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/posts/{post_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `post_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "post_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "content": "",
  "images": [
    "/storage/uploads/example.jpg"
  ],
  "video": "example",
  "image_media_ids": [
    1
  ],
  "video_media_id": 1,
  "location": "example",
  "topic_id": 1,
  "visibility": 0,
  "declaration": ""
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `content` | string | 否 | maxLength=2000 | 文本内容 | `""` |
| `images` | array<string> | 否 | 按 Schema 类型校验 | 接口中的 `images` 字段 | `["/storage/uploads/example.jpg"]` |
| `video` | string | 否 | maxLength=500 | 接口中的 `video` 字段 | `"example"` |
| `image_media_ids` | array<integer> | 否 | 按 Schema 类型校验 | 接口中的 `image_media_ids` 字段 | `[1]` |
| `video_media_id` | integer | 否 | minimum=1.0 | 接口中的 `video_media_id` 字段 | `1` |
| `location` | string | 否 | maxLength=128 | 接口中的 `location` 字段 | `"example"` |
| `topic_id` | integer | 否 | minimum=1.0 | 接口中的 `topic_id` 字段 | `1` |
| `visibility` | 0/1/2 | 否 | 枚举=0, 1, 2 | 接口中的 `visibility` 字段 | `0` |
| `declaration` | /内容包含虚构演绎/内容包含广告推广/内容可能引起不适 | 否 | 枚举=, 内容包含虚构演绎, 内容包含广告推广, 内容可能引起不适 | 接口中的 `declaration` 字段 | `""` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "user_id": 1,
  "nickname": "example",
  "avatar": "/storage/uploads/example.jpg",
  "content": "example",
  "images": [
    "/storage/uploads/example.jpg"
  ],
  "video": "example",
  "location": "example",
  "visibility": 0,
  "declaration": "",
  "topic_id": 1,
  "topic_name": "example",
  "like_count": 1,
  "comment_count": 1,
  "collect_count": 0,
  "is_liked": true,
  "is_collected": false,
  "is_followed": false,
  "gender": 1,
  "age": 1,
  "mbti": "example",
  "school": "example",
  "hometown": "example",
  "residence": "example",
  "realname_status": 0,
  "moderation_status": "approved",
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `nickname` | string | 是 | 用户昵称 | `"example"` |
| `avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `content` | string | 是 | 文本内容 | `"example"` |
| `images` | array<string> | 是 | 接口中的 `images` 字段 | `["/storage/uploads/example.jpg"]` |
| `video` | string | 是 | 接口中的 `video` 字段 | `"example"` |
| `location` | string | 是 | 接口中的 `location` 字段 | `"example"` |
| `visibility` | 0/1/2 | 否 | 枚举=0, 1, 2 | 接口中的 `visibility` 字段 | `0` |
| `declaration` | /内容包含虚构演绎/内容包含广告推广/内容可能引起不适 | 否 | 枚举=, 内容包含虚构演绎, 内容包含广告推广, 内容可能引起不适 | 接口中的 `declaration` 字段 | `""` |
| `topic_id` | integer | 否 | 接口中的 `topic_id` 字段 | `1` |
| `topic_name` | string | 否 | 接口中的 `topic_name` 字段 | `"example"` |
| `like_count` | integer | 是 | 接口中的 `like_count` 字段 | `1` |
| `comment_count` | integer | 是 | 接口中的 `comment_count` 字段 | `1` |
| `collect_count` | integer | 否 | 接口中的 `collect_count` 字段 | `0` |
| `is_liked` | boolean | 是 | 接口中的 `is_liked` 字段 | `true` |
| `is_collected` | boolean | 否 | 接口中的 `is_collected` 字段 | `false` |
| `is_followed` | boolean | 否 | 接口中的 `is_followed` 字段 | `false` |
| `gender` | integer | 否 | 接口中的 `gender` 字段 | `1` |
| `age` | integer | 否 | 接口中的 `age` 字段 | `1` |
| `mbti` | string | 否 | 接口中的 `mbti` 字段 | `"example"` |
| `school` | string | 否 | 接口中的 `school` 字段 | `"example"` |
| `hometown` | string | 否 | 接口中的 `hometown` 字段 | `"example"` |
| `residence` | string | 否 | 接口中的 `residence` 字段 | `"example"` |
| `realname_status` | integer | 否 | 接口中的 `realname_status` 字段 | `0` |
| `moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `moderation_status` 字段 | `"approved"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 154. `DELETE /api/v1/community/posts/{post_id}` 删除动态

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/posts/{post_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `post_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "post_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 155. `PUT /api/v1/community/posts/{post_id}/like` 点赞动态

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/posts/{post_id}/like`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `post_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "post_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "user_id": 1,
  "nickname": "example",
  "avatar": "/storage/uploads/example.jpg",
  "content": "example",
  "images": [
    "/storage/uploads/example.jpg"
  ],
  "video": "example",
  "location": "example",
  "visibility": 0,
  "declaration": "",
  "topic_id": 1,
  "topic_name": "example",
  "like_count": 1,
  "comment_count": 1,
  "collect_count": 0,
  "is_liked": true,
  "is_collected": false,
  "is_followed": false,
  "gender": 1,
  "age": 1,
  "mbti": "example",
  "school": "example",
  "hometown": "example",
  "residence": "example",
  "realname_status": 0,
  "moderation_status": "approved",
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `nickname` | string | 是 | 用户昵称 | `"example"` |
| `avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `content` | string | 是 | 文本内容 | `"example"` |
| `images` | array<string> | 是 | 接口中的 `images` 字段 | `["/storage/uploads/example.jpg"]` |
| `video` | string | 是 | 接口中的 `video` 字段 | `"example"` |
| `location` | string | 是 | 接口中的 `location` 字段 | `"example"` |
| `visibility` | 0/1/2 | 否 | 枚举=0, 1, 2 | 接口中的 `visibility` 字段 | `0` |
| `declaration` | /内容包含虚构演绎/内容包含广告推广/内容可能引起不适 | 否 | 枚举=, 内容包含虚构演绎, 内容包含广告推广, 内容可能引起不适 | 接口中的 `declaration` 字段 | `""` |
| `topic_id` | integer | 否 | 接口中的 `topic_id` 字段 | `1` |
| `topic_name` | string | 否 | 接口中的 `topic_name` 字段 | `"example"` |
| `like_count` | integer | 是 | 接口中的 `like_count` 字段 | `1` |
| `comment_count` | integer | 是 | 接口中的 `comment_count` 字段 | `1` |
| `collect_count` | integer | 否 | 接口中的 `collect_count` 字段 | `0` |
| `is_liked` | boolean | 是 | 接口中的 `is_liked` 字段 | `true` |
| `is_collected` | boolean | 否 | 接口中的 `is_collected` 字段 | `false` |
| `is_followed` | boolean | 否 | 接口中的 `is_followed` 字段 | `false` |
| `gender` | integer | 否 | 接口中的 `gender` 字段 | `1` |
| `age` | integer | 否 | 接口中的 `age` 字段 | `1` |
| `mbti` | string | 否 | 接口中的 `mbti` 字段 | `"example"` |
| `school` | string | 否 | 接口中的 `school` 字段 | `"example"` |
| `hometown` | string | 否 | 接口中的 `hometown` 字段 | `"example"` |
| `residence` | string | 否 | 接口中的 `residence` 字段 | `"example"` |
| `realname_status` | integer | 否 | 接口中的 `realname_status` 字段 | `0` |
| `moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `moderation_status` 字段 | `"approved"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 156. `DELETE /api/v1/community/posts/{post_id}/like` 取消动态点赞

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/posts/{post_id}/like`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `post_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "post_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "user_id": 1,
  "nickname": "example",
  "avatar": "/storage/uploads/example.jpg",
  "content": "example",
  "images": [
    "/storage/uploads/example.jpg"
  ],
  "video": "example",
  "location": "example",
  "visibility": 0,
  "declaration": "",
  "topic_id": 1,
  "topic_name": "example",
  "like_count": 1,
  "comment_count": 1,
  "collect_count": 0,
  "is_liked": true,
  "is_collected": false,
  "is_followed": false,
  "gender": 1,
  "age": 1,
  "mbti": "example",
  "school": "example",
  "hometown": "example",
  "residence": "example",
  "realname_status": 0,
  "moderation_status": "approved",
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `nickname` | string | 是 | 用户昵称 | `"example"` |
| `avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `content` | string | 是 | 文本内容 | `"example"` |
| `images` | array<string> | 是 | 接口中的 `images` 字段 | `["/storage/uploads/example.jpg"]` |
| `video` | string | 是 | 接口中的 `video` 字段 | `"example"` |
| `location` | string | 是 | 接口中的 `location` 字段 | `"example"` |
| `visibility` | 0/1/2 | 否 | 枚举=0, 1, 2 | 接口中的 `visibility` 字段 | `0` |
| `declaration` | /内容包含虚构演绎/内容包含广告推广/内容可能引起不适 | 否 | 枚举=, 内容包含虚构演绎, 内容包含广告推广, 内容可能引起不适 | 接口中的 `declaration` 字段 | `""` |
| `topic_id` | integer | 否 | 接口中的 `topic_id` 字段 | `1` |
| `topic_name` | string | 否 | 接口中的 `topic_name` 字段 | `"example"` |
| `like_count` | integer | 是 | 接口中的 `like_count` 字段 | `1` |
| `comment_count` | integer | 是 | 接口中的 `comment_count` 字段 | `1` |
| `collect_count` | integer | 否 | 接口中的 `collect_count` 字段 | `0` |
| `is_liked` | boolean | 是 | 接口中的 `is_liked` 字段 | `true` |
| `is_collected` | boolean | 否 | 接口中的 `is_collected` 字段 | `false` |
| `is_followed` | boolean | 否 | 接口中的 `is_followed` 字段 | `false` |
| `gender` | integer | 否 | 接口中的 `gender` 字段 | `1` |
| `age` | integer | 否 | 接口中的 `age` 字段 | `1` |
| `mbti` | string | 否 | 接口中的 `mbti` 字段 | `"example"` |
| `school` | string | 否 | 接口中的 `school` 字段 | `"example"` |
| `hometown` | string | 否 | 接口中的 `hometown` 字段 | `"example"` |
| `residence` | string | 否 | 接口中的 `residence` 字段 | `"example"` |
| `realname_status` | integer | 否 | 接口中的 `realname_status` 字段 | `0` |
| `moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `moderation_status` 字段 | `"approved"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 157. `PUT /api/v1/community/posts/{post_id}/collect` 收藏动态

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/posts/{post_id}/collect`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `post_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "post_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "is_collected": true,
  "collect_count": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `is_collected` | boolean | 是 | 接口中的 `is_collected` 字段 | `true` |
| `collect_count` | integer | 是 | 接口中的 `collect_count` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 158. `DELETE /api/v1/community/posts/{post_id}/collect` 取消收藏动态

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/posts/{post_id}/collect`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `post_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "post_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "is_collected": true,
  "collect_count": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `is_collected` | boolean | 是 | 接口中的 `is_collected` 字段 | `true` |
| `collect_count` | integer | 是 | 接口中的 `collect_count` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 159. `GET /api/v1/community/posts/{post_id}/comments` 查看动态评论

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/posts/{post_id}/comments`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `post_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "post_id": 1,
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "post_id": 1,
    "user_id": 1,
    "nickname": "example",
    "avatar": "/storage/uploads/example.jpg",
    "parent_id": 1,
    "root_id": 1,
    "target_comment_id": 1,
    "target_user_id": 1,
    "reply_to_user": "example",
    "content": "example",
    "like_count": 1,
    "is_liked": false,
    "reply_count": 0,
    "is_deleted": false,
    "can_delete": false,
    "replies": [
      {
        "id": 1,
        "post_id": 1,
        "user_id": 1,
        "nickname": "example",
        "avatar": "/storage/uploads/example.jpg",
        "parent_id": 1,
        "root_id": 1,
        "target_comment_id": 1,
        "target_user_id": 1,
        "reply_to_user": "example",
        "content": "example",
        "like_count": 1,
        "is_liked": false,
        "reply_count": 0,
        "is_deleted": false,
        "can_delete": false,
        "replies": [
          {
            "id": 1,
            "post_id": 1,
            "user_id": 1,
            "nickname": "example",
            "avatar": "/storage/uploads/example.jpg",
            "parent_id": 1,
            "root_id": 1,
            "target_comment_id": 1,
            "target_user_id": 1,
            "reply_to_user": "example",
            "content": "example",
            "like_count": 1,
            "is_liked": false,
            "reply_count": 0,
            "is_deleted": false,
            "can_delete": false,
            "replies": [
              "sample"
            ],
            "created_at": "2026-08-05T10:00:00+08:00",
            "moderation_status": "approved"
          }
        ],
        "created_at": "2026-08-05T10:00:00+08:00",
        "moderation_status": "approved"
      }
    ],
    "created_at": "2026-08-05T10:00:00+08:00",
    "moderation_status": "approved"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 160. `POST /api/v1/community/posts/{post_id}/comments` 发表评论

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/posts/{post_id}/comments`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>",
  "Idempotency-Key": "examplex"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `post_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |
| `Idempotency-Key` | header | string | 否 | 无额外约束 | 接口参数 | `"examplex"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "post_id": 1,
  "Idempotency-Key": "examplex"
}
```

### 请求体 JSON 示例

```json
{
  "content": "example",
  "parent_id": 1
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `content` | string | 是 | minLength=1; maxLength=500 | 文本内容 | `"example"` |
| `parent_id` | integer | 否 | minimum=1.0 | 接口中的 `parent_id` 字段 | `1` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "post_id": 1,
  "user_id": 1,
  "nickname": "example",
  "avatar": "/storage/uploads/example.jpg",
  "parent_id": 1,
  "root_id": 1,
  "target_comment_id": 1,
  "target_user_id": 1,
  "reply_to_user": "example",
  "content": "example",
  "like_count": 1,
  "is_liked": false,
  "reply_count": 0,
  "is_deleted": false,
  "can_delete": false,
  "replies": [
    {
      "id": 1,
      "post_id": 1,
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "parent_id": 1,
      "root_id": 1,
      "target_comment_id": 1,
      "target_user_id": 1,
      "reply_to_user": "example",
      "content": "example",
      "like_count": 1,
      "is_liked": false,
      "reply_count": 0,
      "is_deleted": false,
      "can_delete": false,
      "replies": [
        {
          "id": 1,
          "post_id": 1,
          "user_id": 1,
          "nickname": "example",
          "avatar": "/storage/uploads/example.jpg",
          "parent_id": 1,
          "root_id": 1,
          "target_comment_id": 1,
          "target_user_id": 1,
          "reply_to_user": "example",
          "content": "example",
          "like_count": 1,
          "is_liked": false,
          "reply_count": 0,
          "is_deleted": false,
          "can_delete": false,
          "replies": [
            {
              "id": "sample",
              "post_id": "sample",
              "user_id": "sample",
              "nickname": "sample",
              "avatar": "sample",
              "parent_id": "sample",
              "root_id": "sample",
              "target_comment_id": "sample",
              "target_user_id": "sample",
              "reply_to_user": "sample",
              "content": "sample",
              "like_count": "sample",
              "is_liked": "sample",
              "reply_count": "sample",
              "is_deleted": "sample",
              "can_delete": "sample",
              "replies": "sample",
              "created_at": "sample",
              "moderation_status": "sample"
            }
          ],
          "created_at": "2026-08-05T10:00:00+08:00",
          "moderation_status": "approved"
        }
      ],
      "created_at": "2026-08-05T10:00:00+08:00",
      "moderation_status": "approved"
    }
  ],
  "created_at": "2026-08-05T10:00:00+08:00",
  "moderation_status": "approved"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `post_id` | integer | 是 | 接口中的 `post_id` 字段 | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `nickname` | string | 是 | 用户昵称 | `"example"` |
| `avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `parent_id` | integer | 是 | 接口中的 `parent_id` 字段 | `1` |
| `root_id` | integer | 否 | 接口中的 `root_id` 字段 | `1` |
| `target_comment_id` | integer | 否 | 接口中的 `target_comment_id` 字段 | `1` |
| `target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `reply_to_user` | string | 否 | 接口中的 `reply_to_user` 字段 | `"example"` |
| `content` | string | 是 | 文本内容 | `"example"` |
| `like_count` | integer | 是 | 接口中的 `like_count` 字段 | `1` |
| `is_liked` | boolean | 否 | 接口中的 `is_liked` 字段 | `false` |
| `reply_count` | integer | 否 | 接口中的 `reply_count` 字段 | `0` |
| `is_deleted` | boolean | 否 | 接口中的 `is_deleted` 字段 | `false` |
| `can_delete` | boolean | 否 | 接口中的 `can_delete` 字段 | `false` |
| `replies` | array<object> | 否 | 接口中的 `replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].post_id` | integer | 是 | 接口中的 `replies[].post_id` 字段 | `1` |
| `replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].parent_id` | integer | 是 | 接口中的 `replies[].parent_id` 字段 | `1` |
| `replies[].root_id` | integer | 否 | 接口中的 `replies[].root_id` 字段 | `1` |
| `replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].target_comment_id` 字段 | `1` |
| `replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].reply_to_user` | string | 否 | 接口中的 `replies[].reply_to_user` 字段 | `"example"` |
| `replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].like_count` | integer | 是 | 接口中的 `replies[].like_count` 字段 | `1` |
| `replies[].is_liked` | boolean | 否 | 接口中的 `replies[].is_liked` 字段 | `false` |
| `replies[].reply_count` | integer | 否 | 接口中的 `replies[].reply_count` 字段 | `0` |
| `replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].is_deleted` 字段 | `false` |
| `replies[].can_delete` | boolean | 否 | 接口中的 `replies[].can_delete` 字段 | `false` |
| `replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].replies[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].replies[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].moderation_status` 字段 | `"approved"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `moderation_status` 字段 | `"approved"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 161. `GET /api/v1/community/posts/{post_id}/comments/page` 游标查询一级评论

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/posts/{post_id}/comments/page`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `post_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |
| `cursor` | query | string | 否 | 无额外约束 | 接口参数 | `"example"` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "post_id": 1,
  "cursor": "example",
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "post_id": 1,
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "parent_id": 1,
      "root_id": 1,
      "target_comment_id": 1,
      "target_user_id": 1,
      "reply_to_user": "example",
      "content": "example",
      "like_count": 1,
      "is_liked": false,
      "reply_count": 0,
      "is_deleted": false,
      "can_delete": false,
      "replies": [
        {
          "id": 1,
          "post_id": 1,
          "user_id": 1,
          "nickname": "example",
          "avatar": "/storage/uploads/example.jpg",
          "parent_id": 1,
          "root_id": 1,
          "target_comment_id": 1,
          "target_user_id": 1,
          "reply_to_user": "example",
          "content": "example",
          "like_count": 1,
          "is_liked": false,
          "reply_count": 0,
          "is_deleted": false,
          "can_delete": false,
          "replies": [
            {
              "id": "sample",
              "post_id": "sample",
              "user_id": "sample",
              "nickname": "sample",
              "avatar": "sample",
              "parent_id": "sample",
              "root_id": "sample",
              "target_comment_id": "sample",
              "target_user_id": "sample",
              "reply_to_user": "sample",
              "content": "sample",
              "like_count": "sample",
              "is_liked": "sample",
              "reply_count": "sample",
              "is_deleted": "sample",
              "can_delete": "sample",
              "replies": "sample",
              "created_at": "sample",
              "moderation_status": "sample"
            }
          ],
          "created_at": "2026-08-05T10:00:00+08:00",
          "moderation_status": "approved"
        }
      ],
      "created_at": "2026-08-05T10:00:00+08:00",
      "moderation_status": "approved"
    }
  ],
  "next_cursor": "example",
  "has_more": false
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].post_id` | integer | 是 | 接口中的 `items[].post_id` 字段 | `1` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].parent_id` | integer | 是 | 接口中的 `items[].parent_id` 字段 | `1` |
| `items[].root_id` | integer | 否 | 接口中的 `items[].root_id` 字段 | `1` |
| `items[].target_comment_id` | integer | 否 | 接口中的 `items[].target_comment_id` 字段 | `1` |
| `items[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `items[].reply_to_user` | string | 否 | 接口中的 `items[].reply_to_user` 字段 | `"example"` |
| `items[].content` | string | 是 | 文本内容 | `"example"` |
| `items[].like_count` | integer | 是 | 接口中的 `items[].like_count` 字段 | `1` |
| `items[].is_liked` | boolean | 否 | 接口中的 `items[].is_liked` 字段 | `false` |
| `items[].reply_count` | integer | 否 | 接口中的 `items[].reply_count` 字段 | `0` |
| `items[].is_deleted` | boolean | 否 | 接口中的 `items[].is_deleted` 字段 | `false` |
| `items[].can_delete` | boolean | 否 | 接口中的 `items[].can_delete` 字段 | `false` |
| `items[].replies` | array<object> | 否 | 接口中的 `items[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `items[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].replies[].post_id` | integer | 是 | 接口中的 `items[].replies[].post_id` 字段 | `1` |
| `items[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].replies[].parent_id` | integer | 是 | 接口中的 `items[].replies[].parent_id` 字段 | `1` |
| `items[].replies[].root_id` | integer | 否 | 接口中的 `items[].replies[].root_id` 字段 | `1` |
| `items[].replies[].target_comment_id` | integer | 否 | 接口中的 `items[].replies[].target_comment_id` 字段 | `1` |
| `items[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `items[].replies[].reply_to_user` | string | 否 | 接口中的 `items[].replies[].reply_to_user` 字段 | `"example"` |
| `items[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `items[].replies[].like_count` | integer | 是 | 接口中的 `items[].replies[].like_count` 字段 | `1` |
| `items[].replies[].is_liked` | boolean | 否 | 接口中的 `items[].replies[].is_liked` 字段 | `false` |
| `items[].replies[].reply_count` | integer | 否 | 接口中的 `items[].replies[].reply_count` 字段 | `0` |
| `items[].replies[].is_deleted` | boolean | 否 | 接口中的 `items[].replies[].is_deleted` 字段 | `false` |
| `items[].replies[].can_delete` | boolean | 否 | 接口中的 `items[].replies[].can_delete` 字段 | `false` |
| `items[].replies[].replies` | array<object> | 否 | 接口中的 `items[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `items[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].replies[].replies[].post_id` | integer | 是 | 接口中的 `items[].replies[].replies[].post_id` 字段 | `1` |
| `items[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `items[].replies[].replies[].parent_id` 字段 | `1` |
| `items[].replies[].replies[].root_id` | integer | 否 | 接口中的 `items[].replies[].replies[].root_id` 字段 | `1` |
| `items[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `items[].replies[].replies[].target_comment_id` 字段 | `1` |
| `items[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `items[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `items[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `items[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `items[].replies[].replies[].like_count` | integer | 是 | 接口中的 `items[].replies[].replies[].like_count` 字段 | `1` |
| `items[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `items[].replies[].replies[].is_liked` 字段 | `false` |
| `items[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `items[].replies[].replies[].reply_count` 字段 | `0` |
| `items[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `items[].replies[].replies[].is_deleted` 字段 | `false` |
| `items[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `items[].replies[].replies[].can_delete` 字段 | `false` |
| `items[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `items[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `items[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `items[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `items[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `items[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `items[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `items[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `items[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `items[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `items[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `items[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `items[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `items[].replies[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].replies[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].replies[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].replies[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].replies[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `items[].replies[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `items[].replies[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `items[].replies[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `items[].replies[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `items[].replies[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `items[].replies[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `items[].replies[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `items[].replies[].replies[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].replies[].replies[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].replies[].replies[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `items[].replies[].replies[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `items[].replies[].replies[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `items[].replies[].replies[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `items[].replies[].replies[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `items[].replies[].replies[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `items[].replies[].replies[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `items[].replies[].replies[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].replies[].replies[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `items[].replies[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].replies[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `items[].replies[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `items[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `items[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `items[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `items[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `items[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `items[].replies[].moderation_status` 字段 | `"approved"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `items[].moderation_status` 字段 | `"approved"` |
| `next_cursor` | string | 否 | 接口中的 `next_cursor` 字段 | `"example"` |
| `has_more` | boolean | 否 | 是否还有下一页 | `false` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 162. `GET /api/v1/community/comments/{comment_id}/replies` 游标查询评论回复

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/comments/{comment_id}/replies`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `comment_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |
| `cursor` | query | string | 否 | 无额外约束 | 接口参数 | `"example"` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "comment_id": 1,
  "cursor": "example",
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "post_id": 1,
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "parent_id": 1,
      "root_id": 1,
      "target_comment_id": 1,
      "target_user_id": 1,
      "reply_to_user": "example",
      "content": "example",
      "like_count": 1,
      "is_liked": false,
      "reply_count": 0,
      "is_deleted": false,
      "can_delete": false,
      "replies": [
        {
          "id": 1,
          "post_id": 1,
          "user_id": 1,
          "nickname": "example",
          "avatar": "/storage/uploads/example.jpg",
          "parent_id": 1,
          "root_id": 1,
          "target_comment_id": 1,
          "target_user_id": 1,
          "reply_to_user": "example",
          "content": "example",
          "like_count": 1,
          "is_liked": false,
          "reply_count": 0,
          "is_deleted": false,
          "can_delete": false,
          "replies": [
            {
              "id": "sample",
              "post_id": "sample",
              "user_id": "sample",
              "nickname": "sample",
              "avatar": "sample",
              "parent_id": "sample",
              "root_id": "sample",
              "target_comment_id": "sample",
              "target_user_id": "sample",
              "reply_to_user": "sample",
              "content": "sample",
              "like_count": "sample",
              "is_liked": "sample",
              "reply_count": "sample",
              "is_deleted": "sample",
              "can_delete": "sample",
              "replies": "sample",
              "created_at": "sample",
              "moderation_status": "sample"
            }
          ],
          "created_at": "2026-08-05T10:00:00+08:00",
          "moderation_status": "approved"
        }
      ],
      "created_at": "2026-08-05T10:00:00+08:00",
      "moderation_status": "approved"
    }
  ],
  "next_cursor": "example",
  "has_more": false
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].post_id` | integer | 是 | 接口中的 `items[].post_id` 字段 | `1` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].parent_id` | integer | 是 | 接口中的 `items[].parent_id` 字段 | `1` |
| `items[].root_id` | integer | 否 | 接口中的 `items[].root_id` 字段 | `1` |
| `items[].target_comment_id` | integer | 否 | 接口中的 `items[].target_comment_id` 字段 | `1` |
| `items[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `items[].reply_to_user` | string | 否 | 接口中的 `items[].reply_to_user` 字段 | `"example"` |
| `items[].content` | string | 是 | 文本内容 | `"example"` |
| `items[].like_count` | integer | 是 | 接口中的 `items[].like_count` 字段 | `1` |
| `items[].is_liked` | boolean | 否 | 接口中的 `items[].is_liked` 字段 | `false` |
| `items[].reply_count` | integer | 否 | 接口中的 `items[].reply_count` 字段 | `0` |
| `items[].is_deleted` | boolean | 否 | 接口中的 `items[].is_deleted` 字段 | `false` |
| `items[].can_delete` | boolean | 否 | 接口中的 `items[].can_delete` 字段 | `false` |
| `items[].replies` | array<object> | 否 | 接口中的 `items[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `items[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].replies[].post_id` | integer | 是 | 接口中的 `items[].replies[].post_id` 字段 | `1` |
| `items[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].replies[].parent_id` | integer | 是 | 接口中的 `items[].replies[].parent_id` 字段 | `1` |
| `items[].replies[].root_id` | integer | 否 | 接口中的 `items[].replies[].root_id` 字段 | `1` |
| `items[].replies[].target_comment_id` | integer | 否 | 接口中的 `items[].replies[].target_comment_id` 字段 | `1` |
| `items[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `items[].replies[].reply_to_user` | string | 否 | 接口中的 `items[].replies[].reply_to_user` 字段 | `"example"` |
| `items[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `items[].replies[].like_count` | integer | 是 | 接口中的 `items[].replies[].like_count` 字段 | `1` |
| `items[].replies[].is_liked` | boolean | 否 | 接口中的 `items[].replies[].is_liked` 字段 | `false` |
| `items[].replies[].reply_count` | integer | 否 | 接口中的 `items[].replies[].reply_count` 字段 | `0` |
| `items[].replies[].is_deleted` | boolean | 否 | 接口中的 `items[].replies[].is_deleted` 字段 | `false` |
| `items[].replies[].can_delete` | boolean | 否 | 接口中的 `items[].replies[].can_delete` 字段 | `false` |
| `items[].replies[].replies` | array<object> | 否 | 接口中的 `items[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `items[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].replies[].replies[].post_id` | integer | 是 | 接口中的 `items[].replies[].replies[].post_id` 字段 | `1` |
| `items[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `items[].replies[].replies[].parent_id` 字段 | `1` |
| `items[].replies[].replies[].root_id` | integer | 否 | 接口中的 `items[].replies[].replies[].root_id` 字段 | `1` |
| `items[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `items[].replies[].replies[].target_comment_id` 字段 | `1` |
| `items[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `items[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `items[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `items[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `items[].replies[].replies[].like_count` | integer | 是 | 接口中的 `items[].replies[].replies[].like_count` 字段 | `1` |
| `items[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `items[].replies[].replies[].is_liked` 字段 | `false` |
| `items[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `items[].replies[].replies[].reply_count` 字段 | `0` |
| `items[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `items[].replies[].replies[].is_deleted` 字段 | `false` |
| `items[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `items[].replies[].replies[].can_delete` 字段 | `false` |
| `items[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `items[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `items[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `items[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `items[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `items[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `items[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `items[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `items[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `items[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `items[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `items[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `items[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `items[].replies[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].replies[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].replies[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].replies[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].replies[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `items[].replies[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `items[].replies[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `items[].replies[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `items[].replies[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `items[].replies[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `items[].replies[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `items[].replies[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `items[].replies[].replies[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `items[].replies[].replies[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `items[].replies[].replies[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `items[].replies[].replies[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `items[].replies[].replies[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `items[].replies[].replies[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `items[].replies[].replies[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `items[].replies[].replies[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `items[].replies[].replies[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `items[].replies[].replies[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `items[].replies[].replies[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].replies[].replies[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `items[].replies[].replies[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `items[].replies[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].replies[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `items[].replies[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `items[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `items[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `items[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `items[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `items[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `items[].replies[].moderation_status` 字段 | `"approved"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `items[].moderation_status` 字段 | `"approved"` |
| `next_cursor` | string | 否 | 接口中的 `next_cursor` 字段 | `"example"` |
| `has_more` | boolean | 否 | 是否还有下一页 | `false` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 163. `DELETE /api/v1/community/comments/{comment_id}` 删除评论

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/comments/{comment_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `comment_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "comment_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 164. `PUT /api/v1/community/comments/{comment_id}/like` 点赞评论

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/comments/{comment_id}/like`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `comment_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "comment_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "post_id": 1,
  "user_id": 1,
  "nickname": "example",
  "avatar": "/storage/uploads/example.jpg",
  "parent_id": 1,
  "root_id": 1,
  "target_comment_id": 1,
  "target_user_id": 1,
  "reply_to_user": "example",
  "content": "example",
  "like_count": 1,
  "is_liked": false,
  "reply_count": 0,
  "is_deleted": false,
  "can_delete": false,
  "replies": [
    {
      "id": 1,
      "post_id": 1,
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "parent_id": 1,
      "root_id": 1,
      "target_comment_id": 1,
      "target_user_id": 1,
      "reply_to_user": "example",
      "content": "example",
      "like_count": 1,
      "is_liked": false,
      "reply_count": 0,
      "is_deleted": false,
      "can_delete": false,
      "replies": [
        {
          "id": 1,
          "post_id": 1,
          "user_id": 1,
          "nickname": "example",
          "avatar": "/storage/uploads/example.jpg",
          "parent_id": 1,
          "root_id": 1,
          "target_comment_id": 1,
          "target_user_id": 1,
          "reply_to_user": "example",
          "content": "example",
          "like_count": 1,
          "is_liked": false,
          "reply_count": 0,
          "is_deleted": false,
          "can_delete": false,
          "replies": [
            {
              "id": "sample",
              "post_id": "sample",
              "user_id": "sample",
              "nickname": "sample",
              "avatar": "sample",
              "parent_id": "sample",
              "root_id": "sample",
              "target_comment_id": "sample",
              "target_user_id": "sample",
              "reply_to_user": "sample",
              "content": "sample",
              "like_count": "sample",
              "is_liked": "sample",
              "reply_count": "sample",
              "is_deleted": "sample",
              "can_delete": "sample",
              "replies": "sample",
              "created_at": "sample",
              "moderation_status": "sample"
            }
          ],
          "created_at": "2026-08-05T10:00:00+08:00",
          "moderation_status": "approved"
        }
      ],
      "created_at": "2026-08-05T10:00:00+08:00",
      "moderation_status": "approved"
    }
  ],
  "created_at": "2026-08-05T10:00:00+08:00",
  "moderation_status": "approved"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `post_id` | integer | 是 | 接口中的 `post_id` 字段 | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `nickname` | string | 是 | 用户昵称 | `"example"` |
| `avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `parent_id` | integer | 是 | 接口中的 `parent_id` 字段 | `1` |
| `root_id` | integer | 否 | 接口中的 `root_id` 字段 | `1` |
| `target_comment_id` | integer | 否 | 接口中的 `target_comment_id` 字段 | `1` |
| `target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `reply_to_user` | string | 否 | 接口中的 `reply_to_user` 字段 | `"example"` |
| `content` | string | 是 | 文本内容 | `"example"` |
| `like_count` | integer | 是 | 接口中的 `like_count` 字段 | `1` |
| `is_liked` | boolean | 否 | 接口中的 `is_liked` 字段 | `false` |
| `reply_count` | integer | 否 | 接口中的 `reply_count` 字段 | `0` |
| `is_deleted` | boolean | 否 | 接口中的 `is_deleted` 字段 | `false` |
| `can_delete` | boolean | 否 | 接口中的 `can_delete` 字段 | `false` |
| `replies` | array<object> | 否 | 接口中的 `replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].post_id` | integer | 是 | 接口中的 `replies[].post_id` 字段 | `1` |
| `replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].parent_id` | integer | 是 | 接口中的 `replies[].parent_id` 字段 | `1` |
| `replies[].root_id` | integer | 否 | 接口中的 `replies[].root_id` 字段 | `1` |
| `replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].target_comment_id` 字段 | `1` |
| `replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].reply_to_user` | string | 否 | 接口中的 `replies[].reply_to_user` 字段 | `"example"` |
| `replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].like_count` | integer | 是 | 接口中的 `replies[].like_count` 字段 | `1` |
| `replies[].is_liked` | boolean | 否 | 接口中的 `replies[].is_liked` 字段 | `false` |
| `replies[].reply_count` | integer | 否 | 接口中的 `replies[].reply_count` 字段 | `0` |
| `replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].is_deleted` 字段 | `false` |
| `replies[].can_delete` | boolean | 否 | 接口中的 `replies[].can_delete` 字段 | `false` |
| `replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].replies[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].replies[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].moderation_status` 字段 | `"approved"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `moderation_status` 字段 | `"approved"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 165. `DELETE /api/v1/community/comments/{comment_id}/like` 取消评论点赞

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/comments/{comment_id}/like`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `comment_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "comment_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "post_id": 1,
  "user_id": 1,
  "nickname": "example",
  "avatar": "/storage/uploads/example.jpg",
  "parent_id": 1,
  "root_id": 1,
  "target_comment_id": 1,
  "target_user_id": 1,
  "reply_to_user": "example",
  "content": "example",
  "like_count": 1,
  "is_liked": false,
  "reply_count": 0,
  "is_deleted": false,
  "can_delete": false,
  "replies": [
    {
      "id": 1,
      "post_id": 1,
      "user_id": 1,
      "nickname": "example",
      "avatar": "/storage/uploads/example.jpg",
      "parent_id": 1,
      "root_id": 1,
      "target_comment_id": 1,
      "target_user_id": 1,
      "reply_to_user": "example",
      "content": "example",
      "like_count": 1,
      "is_liked": false,
      "reply_count": 0,
      "is_deleted": false,
      "can_delete": false,
      "replies": [
        {
          "id": 1,
          "post_id": 1,
          "user_id": 1,
          "nickname": "example",
          "avatar": "/storage/uploads/example.jpg",
          "parent_id": 1,
          "root_id": 1,
          "target_comment_id": 1,
          "target_user_id": 1,
          "reply_to_user": "example",
          "content": "example",
          "like_count": 1,
          "is_liked": false,
          "reply_count": 0,
          "is_deleted": false,
          "can_delete": false,
          "replies": [
            {
              "id": "sample",
              "post_id": "sample",
              "user_id": "sample",
              "nickname": "sample",
              "avatar": "sample",
              "parent_id": "sample",
              "root_id": "sample",
              "target_comment_id": "sample",
              "target_user_id": "sample",
              "reply_to_user": "sample",
              "content": "sample",
              "like_count": "sample",
              "is_liked": "sample",
              "reply_count": "sample",
              "is_deleted": "sample",
              "can_delete": "sample",
              "replies": "sample",
              "created_at": "sample",
              "moderation_status": "sample"
            }
          ],
          "created_at": "2026-08-05T10:00:00+08:00",
          "moderation_status": "approved"
        }
      ],
      "created_at": "2026-08-05T10:00:00+08:00",
      "moderation_status": "approved"
    }
  ],
  "created_at": "2026-08-05T10:00:00+08:00",
  "moderation_status": "approved"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `post_id` | integer | 是 | 接口中的 `post_id` 字段 | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `nickname` | string | 是 | 用户昵称 | `"example"` |
| `avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `parent_id` | integer | 是 | 接口中的 `parent_id` 字段 | `1` |
| `root_id` | integer | 否 | 接口中的 `root_id` 字段 | `1` |
| `target_comment_id` | integer | 否 | 接口中的 `target_comment_id` 字段 | `1` |
| `target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `reply_to_user` | string | 否 | 接口中的 `reply_to_user` 字段 | `"example"` |
| `content` | string | 是 | 文本内容 | `"example"` |
| `like_count` | integer | 是 | 接口中的 `like_count` 字段 | `1` |
| `is_liked` | boolean | 否 | 接口中的 `is_liked` 字段 | `false` |
| `reply_count` | integer | 否 | 接口中的 `reply_count` 字段 | `0` |
| `is_deleted` | boolean | 否 | 接口中的 `is_deleted` 字段 | `false` |
| `can_delete` | boolean | 否 | 接口中的 `can_delete` 字段 | `false` |
| `replies` | array<object> | 否 | 接口中的 `replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].post_id` | integer | 是 | 接口中的 `replies[].post_id` 字段 | `1` |
| `replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].parent_id` | integer | 是 | 接口中的 `replies[].parent_id` 字段 | `1` |
| `replies[].root_id` | integer | 否 | 接口中的 `replies[].root_id` 字段 | `1` |
| `replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].target_comment_id` 字段 | `1` |
| `replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].reply_to_user` | string | 否 | 接口中的 `replies[].reply_to_user` 字段 | `"example"` |
| `replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].like_count` | integer | 是 | 接口中的 `replies[].like_count` 字段 | `1` |
| `replies[].is_liked` | boolean | 否 | 接口中的 `replies[].is_liked` 字段 | `false` |
| `replies[].reply_count` | integer | 否 | 接口中的 `replies[].reply_count` 字段 | `0` |
| `replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].is_deleted` 字段 | `false` |
| `replies[].can_delete` | boolean | 否 | 接口中的 `replies[].can_delete` 字段 | `false` |
| `replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].replies[].replies[].replies[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].post_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].post_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].user_id` | integer | 是 | 用户 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].parent_id` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].parent_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].root_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].root_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].target_comment_id` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].target_comment_id` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].target_user_id` | integer | 否 | 目标用户 ID | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].reply_to_user` | string | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].reply_to_user` 字段 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].content` | string | 是 | 文本内容 | `"example"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].like_count` | integer | 是 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].like_count` 字段 | `1` |
| `replies[].replies[].replies[].replies[].replies[].replies[].is_liked` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].is_liked` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].replies[].reply_count` | integer | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].reply_count` 字段 | `0` |
| `replies[].replies[].replies[].replies[].replies[].replies[].is_deleted` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].is_deleted` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].replies[].can_delete` | boolean | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].can_delete` 字段 | `false` |
| `replies[].replies[].replies[].replies[].replies[].replies[].replies` | array<object> | 否 | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].replies` 字段 | `[{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": [{"id": 1, "post_id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "parent_id": 1, "root_id": 1, "target_comment_id": 1, "target_user_id": 1, "reply_to_user": "example", "content": "example", "like_count": 1, "is_liked": false, "reply_count": 0, "is_deleted": false, "can_delete": false, "replies": ["sample"], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}], "created_at": "2026-08-05T10:00:00+08:00", "moderation_status": "approved"}]` |
| `replies[].replies[].replies[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].replies[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].replies[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].replies[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].replies[].moderation_status` 字段 | `"approved"` |
| `replies[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `replies[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `replies[].moderation_status` 字段 | `"approved"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `moderation_status` 字段 | `"approved"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 166. `GET /api/v1/community/topics` 话题列表

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/topics`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `sort` | query | hot/latest | 否 | 枚举=hot, latest | 接口参数 | `"hot"` |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=100 | 接口参数 | `50` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "sort": "hot",
  "page": 1,
  "page_size": 50
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "name": "example",
    "icon": "example",
    "sort": 1,
    "post_count": 1,
    "participant_count": 1,
    "heat": 1,
    "joined": true,
    "created_at": "2026-08-05T10:00:00+08:00",
    "background_url": "/storage/uploads/example.jpg",
    "description": "example",
    "view_count": 0,
    "status": 1
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 167. `GET /api/v1/community/topics/page` 分页话题列表

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/topics/page`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `sort` | query | hot/latest | 否 | 枚举=hot, latest | 接口参数 | `"hot"` |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |
| `exclude_ids` | query | array<integer> | 否 | 无额外约束 | 接口参数 | `[1]` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "sort": "hot",
  "page": 1,
  "page_size": 20,
  "exclude_ids": [
    1
  ]
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "name": "example",
      "icon": "example",
      "sort": 1,
      "post_count": 1,
      "participant_count": 1,
      "heat": 1,
      "joined": true,
      "created_at": "2026-08-05T10:00:00+08:00",
      "background_url": "/storage/uploads/example.jpg",
      "description": "example",
      "view_count": 0,
      "status": 1
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "name": "example", "icon": "example", "sort": 1, "post_count": 1, "participant_count": 1, "heat": 1, "joined": true, "created_at": "2026-08-05T10:00:00+08:00", "background_url": "/storage/uploads/example.jpg", "description": "example", "view_count": 0, "status": 1}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].name` | string | 是 | 展示名称 | `"example"` |
| `items[].icon` | string | 是 | 接口中的 `items[].icon` 字段 | `"example"` |
| `items[].sort` | integer | 是 | 接口中的 `items[].sort` 字段 | `1` |
| `items[].post_count` | integer | 是 | 接口中的 `items[].post_count` 字段 | `1` |
| `items[].participant_count` | integer | 是 | 接口中的 `items[].participant_count` 字段 | `1` |
| `items[].heat` | integer | 是 | 接口中的 `items[].heat` 字段 | `1` |
| `items[].joined` | boolean | 是 | 接口中的 `items[].joined` 字段 | `true` |
| `items[].created_at` | string | 否 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].background_url` | string | 否 | 接口中的 `items[].background_url` 字段 | `"/storage/uploads/example.jpg"` |
| `items[].description` | string | 否 | 详细描述 | `"example"` |
| `items[].view_count` | integer | 否 | minimum=0.0 | 接口中的 `items[].view_count` 字段 | `0` |
| `items[].status` | integer | 否 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 168. `GET /api/v1/community/topics/{topic_id}` 话题详情元信息

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/topics/{topic_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `topic_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "topic_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "name": "example",
  "icon": "example",
  "sort": 1,
  "post_count": 1,
  "participant_count": 1,
  "heat": 1,
  "joined": true,
  "created_at": "2026-08-05T10:00:00+08:00",
  "background_url": "/storage/uploads/example.jpg",
  "description": "example",
  "view_count": 0,
  "status": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `name` | string | 是 | 展示名称 | `"example"` |
| `icon` | string | 是 | 接口中的 `icon` 字段 | `"example"` |
| `sort` | integer | 是 | 接口中的 `sort` 字段 | `1` |
| `post_count` | integer | 是 | 接口中的 `post_count` 字段 | `1` |
| `participant_count` | integer | 是 | 接口中的 `participant_count` 字段 | `1` |
| `heat` | integer | 是 | 接口中的 `heat` 字段 | `1` |
| `joined` | boolean | 是 | 接口中的 `joined` 字段 | `true` |
| `created_at` | string | 否 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `background_url` | string | 否 | 接口中的 `background_url` 字段 | `"/storage/uploads/example.jpg"` |
| `description` | string | 否 | 详细描述 | `"example"` |
| `view_count` | integer | 否 | minimum=0.0 | 接口中的 `view_count` 字段 | `0` |
| `status` | integer | 否 | 当前业务状态，具体枚举见本接口约束 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 169. `GET /api/v1/community/topics/{topic_id}/detail` 话题详情与动态

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/topics/{topic_id}/detail`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `topic_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |
| `sort` | query | hot/latest | 否 | 枚举=hot, latest | 接口参数 | `"hot"` |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "topic_id": 1,
  "sort": "hot",
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "topic": {
    "id": 1,
    "name": "example",
    "icon": "example",
    "sort": 1,
    "post_count": 1,
    "participant_count": 1,
    "heat": 1,
    "joined": true,
    "created_at": "2026-08-05T10:00:00+08:00",
    "background_url": "/storage/uploads/example.jpg",
    "description": "example",
    "view_count": 0,
    "status": 1
  },
  "posts": {
    "items": [
      {
        "id": 1,
        "user_id": 1,
        "nickname": "example",
        "avatar": "/storage/uploads/example.jpg",
        "content": "example",
        "images": [
          "/storage/uploads/example.jpg"
        ],
        "video": "example",
        "location": "example",
        "visibility": 0,
        "declaration": "",
        "topic_id": 1,
        "topic_name": "example",
        "like_count": 1,
        "comment_count": 1,
        "collect_count": 0,
        "is_liked": true,
        "is_collected": false,
        "is_followed": false,
        "gender": 1,
        "age": 1,
        "mbti": "example",
        "school": "example",
        "hometown": "example",
        "residence": "example",
        "realname_status": 0,
        "moderation_status": "approved",
        "created_at": "2026-08-05T10:00:00+08:00"
      }
    ],
    "page": 1,
    "page_size": 1,
    "total": 1
  },
  "sort": "hot"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `topic` | object | 是 | 接口中的 `topic` 字段 | `{"id": 1, "name": "example", "icon": "example", "sort": 1, "post_count": 1, "participant_count": 1, "heat": 1, "joined": true, "created_at": "2026-08-05T10:00:00+08:00", "background_url": "/storage/uploads/example.jpg", "description": "example", "view_count": 0, "status": 1}` |
| `topic.id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `topic.name` | string | 是 | 展示名称 | `"example"` |
| `topic.icon` | string | 是 | 接口中的 `topic.icon` 字段 | `"example"` |
| `topic.sort` | integer | 是 | 接口中的 `topic.sort` 字段 | `1` |
| `topic.post_count` | integer | 是 | 接口中的 `topic.post_count` 字段 | `1` |
| `topic.participant_count` | integer | 是 | 接口中的 `topic.participant_count` 字段 | `1` |
| `topic.heat` | integer | 是 | 接口中的 `topic.heat` 字段 | `1` |
| `topic.joined` | boolean | 是 | 接口中的 `topic.joined` 字段 | `true` |
| `topic.created_at` | string | 否 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `topic.background_url` | string | 否 | 接口中的 `topic.background_url` 字段 | `"/storage/uploads/example.jpg"` |
| `topic.description` | string | 否 | 详细描述 | `"example"` |
| `topic.view_count` | integer | 否 | minimum=0.0 | 接口中的 `topic.view_count` 字段 | `0` |
| `topic.status` | integer | 否 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `posts` | object | 是 | 接口中的 `posts` 字段 | `{"items": [{"id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "content": "example", "images": ["/storage/uploads/example.jpg"], "video": "example", "location": "example", "visibility": 0, "declaration": "", "topic_id": 1, "topic_name": "example", "like_count": 1, "comment_count": 1, "collect_count": 0, "is_liked": true, "is_collected": false, "is_followed": false, "gender": 1, "age": 1, "mbti": "example", "school": "example", "hometown": "example", "residence": "example", "realname_status": 0, "moderation_status": "approved", "created_at": "2026-08-05T10:00:00+08:00"}], "page": 1, "page_size": 1, "total": 1}` |
| `posts.items` | array<object> | 是 | 接口中的 `posts.items` 字段 | `[{"id": 1, "user_id": 1, "nickname": "example", "avatar": "/storage/uploads/example.jpg", "content": "example", "images": ["/storage/uploads/example.jpg"], "video": "example", "location": "example", "visibility": 0, "declaration": "", "topic_id": 1, "topic_name": "example", "like_count": 1, "comment_count": 1, "collect_count": 0, "is_liked": true, "is_collected": false, "is_followed": false, "gender": 1, "age": 1, "mbti": "example", "school": "example", "hometown": "example", "residence": "example", "realname_status": 0, "moderation_status": "approved", "created_at": "2026-08-05T10:00:00+08:00"}]` |
| `posts.items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `posts.items[].user_id` | integer | 是 | 用户 ID | `1` |
| `posts.items[].nickname` | string | 是 | 用户昵称 | `"example"` |
| `posts.items[].avatar` | string | 是 | 头像 URL | `"/storage/uploads/example.jpg"` |
| `posts.items[].content` | string | 是 | 文本内容 | `"example"` |
| `posts.items[].images` | array<string> | 是 | 接口中的 `posts.items[].images` 字段 | `["/storage/uploads/example.jpg"]` |
| `posts.items[].video` | string | 是 | 接口中的 `posts.items[].video` 字段 | `"example"` |
| `posts.items[].location` | string | 是 | 接口中的 `posts.items[].location` 字段 | `"example"` |
| `posts.items[].visibility` | 0/1/2 | 否 | 枚举=0, 1, 2 | 接口中的 `posts.items[].visibility` 字段 | `0` |
| `posts.items[].declaration` | /内容包含虚构演绎/内容包含广告推广/内容可能引起不适 | 否 | 枚举=, 内容包含虚构演绎, 内容包含广告推广, 内容可能引起不适 | 接口中的 `posts.items[].declaration` 字段 | `""` |
| `posts.items[].topic_id` | integer | 否 | 接口中的 `posts.items[].topic_id` 字段 | `1` |
| `posts.items[].topic_name` | string | 否 | 接口中的 `posts.items[].topic_name` 字段 | `"example"` |
| `posts.items[].like_count` | integer | 是 | 接口中的 `posts.items[].like_count` 字段 | `1` |
| `posts.items[].comment_count` | integer | 是 | 接口中的 `posts.items[].comment_count` 字段 | `1` |
| `posts.items[].collect_count` | integer | 否 | 接口中的 `posts.items[].collect_count` 字段 | `0` |
| `posts.items[].is_liked` | boolean | 是 | 接口中的 `posts.items[].is_liked` 字段 | `true` |
| `posts.items[].is_collected` | boolean | 否 | 接口中的 `posts.items[].is_collected` 字段 | `false` |
| `posts.items[].is_followed` | boolean | 否 | 接口中的 `posts.items[].is_followed` 字段 | `false` |
| `posts.items[].gender` | integer | 否 | 接口中的 `posts.items[].gender` 字段 | `1` |
| `posts.items[].age` | integer | 否 | 接口中的 `posts.items[].age` 字段 | `1` |
| `posts.items[].mbti` | string | 否 | 接口中的 `posts.items[].mbti` 字段 | `"example"` |
| `posts.items[].school` | string | 否 | 接口中的 `posts.items[].school` 字段 | `"example"` |
| `posts.items[].hometown` | string | 否 | 接口中的 `posts.items[].hometown` 字段 | `"example"` |
| `posts.items[].residence` | string | 否 | 接口中的 `posts.items[].residence` 字段 | `"example"` |
| `posts.items[].realname_status` | integer | 否 | 接口中的 `posts.items[].realname_status` 字段 | `0` |
| `posts.items[].moderation_status` | approved/pending/rejected/deleted/hidden | 否 | 枚举=approved, pending, rejected, deleted, hidden | 接口中的 `posts.items[].moderation_status` 字段 | `"approved"` |
| `posts.items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `posts.page` | integer | 是 | 页码，从 1 开始 | `1` |
| `posts.page_size` | integer | 是 | 每页记录数 | `1` |
| `posts.total` | integer | 是 | 符合条件的记录总数 | `1` |
| `sort` | hot/latest | 是 | 枚举=hot, latest | 接口中的 `sort` 字段 | `"hot"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 170. `POST /api/v1/community/topics/{topic_id}/join` 参与话题

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/topics/{topic_id}/join`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `topic_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "topic_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "success": true,
  "joined": true,
  "topic_id": 1,
  "participant_count": 0
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `success` | boolean | 否 | 接口中的 `success` 字段 | `true` |
| `joined` | boolean | 否 | 接口中的 `joined` 字段 | `true` |
| `topic_id` | integer | 是 | 接口中的 `topic_id` 字段 | `1` |
| `participant_count` | integer | 否 | 接口中的 `participant_count` 字段 | `0` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 171. `DELETE /api/v1/community/topics/{topic_id}/leave` 取消参与话题

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/topics/{topic_id}/leave`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `topic_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "topic_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "success": true,
  "joined": true,
  "topic_id": 1,
  "participant_count": 0
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `success` | boolean | 否 | 接口中的 `success` 字段 | `true` |
| `joined` | boolean | 否 | 接口中的 `joined` 字段 | `true` |
| `topic_id` | integer | 是 | 接口中的 `topic_id` 字段 | `1` |
| `participant_count` | integer | 否 | 接口中的 `participant_count` 字段 | `0` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 172. `GET /api/v1/community/activities` 线下活动列表

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/activities`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `filter` | query | all/recruiting/mine | 否 | 枚举=all, recruiting, mine | 接口参数 | `"all"` |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "filter": "all",
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "title": "example",
      "cover": "example",
      "type": "example",
      "city": "example",
      "address": "example",
      "start_time": "2026-08-05T10:00:00+08:00",
      "end_time": "2026-08-05T10:00:00+08:00",
      "signup_deadline": "2026-08-05T10:00:00+08:00",
      "max_people": 1,
      "current_people": 1,
      "price": 1.2,
      "status": 1,
      "status_text": "example",
      "description": "example",
      "my_status": 1,
      "my_status_text": "example",
      "created_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "title": "example", "cover": "example", "type": "example", "city": "example", "address": "example", "start_time": "2026-08-05T10:00:00+08:00", "end_time": "2026-08-05T10:00:00+08:00", "signup_deadline": "2026-08-05T10:00:00+08:00", "max_people": 1, "current_people": 1, "price": 1.2, "status": 1, "status_text": "example", "description": "example", "my_status": 1, "my_status_text": "example", "created_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].title` | string | 是 | 接口中的 `items[].title` 字段 | `"example"` |
| `items[].cover` | string | 是 | 接口中的 `items[].cover` 字段 | `"example"` |
| `items[].type` | string | 是 | 接口中的 `items[].type` 字段 | `"example"` |
| `items[].city` | string | 是 | 接口中的 `items[].city` 字段 | `"example"` |
| `items[].address` | string | 是 | 接口中的 `items[].address` 字段 | `"example"` |
| `items[].start_time` | string | 是 | 接口中的 `items[].start_time` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `items[].end_time` | string | 是 | 接口中的 `items[].end_time` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `items[].signup_deadline` | string | 是 | 接口中的 `items[].signup_deadline` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `items[].max_people` | integer | 是 | 接口中的 `items[].max_people` 字段 | `1` |
| `items[].current_people` | integer | 是 | 接口中的 `items[].current_people` 字段 | `1` |
| `items[].price` | number | 是 | 商品价格 | `1.2` |
| `items[].status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `items[].status_text` | string | 是 | 接口中的 `items[].status_text` 字段 | `"example"` |
| `items[].description` | string | 是 | 详细描述 | `"example"` |
| `items[].my_status` | integer | 是 | 接口中的 `items[].my_status` 字段 | `1` |
| `items[].my_status_text` | string | 是 | 接口中的 `items[].my_status_text` 字段 | `"example"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 173. `GET /api/v1/community/activities/mine` 我的活动

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/activities/mine`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `filter` | query | all/pending/joined/ended | 否 | 枚举=all, pending, joined, ended | 接口参数 | `"all"` |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "filter": "all",
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "title": "example",
      "cover": "example",
      "type": "example",
      "city": "example",
      "address": "example",
      "start_time": "2026-08-05T10:00:00+08:00",
      "end_time": "2026-08-05T10:00:00+08:00",
      "signup_deadline": "2026-08-05T10:00:00+08:00",
      "max_people": 1,
      "current_people": 1,
      "price": 1.2,
      "status": 1,
      "status_text": "example",
      "description": "example",
      "my_status": 1,
      "my_status_text": "example",
      "created_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "title": "example", "cover": "example", "type": "example", "city": "example", "address": "example", "start_time": "2026-08-05T10:00:00+08:00", "end_time": "2026-08-05T10:00:00+08:00", "signup_deadline": "2026-08-05T10:00:00+08:00", "max_people": 1, "current_people": 1, "price": 1.2, "status": 1, "status_text": "example", "description": "example", "my_status": 1, "my_status_text": "example", "created_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].title` | string | 是 | 接口中的 `items[].title` 字段 | `"example"` |
| `items[].cover` | string | 是 | 接口中的 `items[].cover` 字段 | `"example"` |
| `items[].type` | string | 是 | 接口中的 `items[].type` 字段 | `"example"` |
| `items[].city` | string | 是 | 接口中的 `items[].city` 字段 | `"example"` |
| `items[].address` | string | 是 | 接口中的 `items[].address` 字段 | `"example"` |
| `items[].start_time` | string | 是 | 接口中的 `items[].start_time` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `items[].end_time` | string | 是 | 接口中的 `items[].end_time` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `items[].signup_deadline` | string | 是 | 接口中的 `items[].signup_deadline` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `items[].max_people` | integer | 是 | 接口中的 `items[].max_people` 字段 | `1` |
| `items[].current_people` | integer | 是 | 接口中的 `items[].current_people` 字段 | `1` |
| `items[].price` | number | 是 | 商品价格 | `1.2` |
| `items[].status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `items[].status_text` | string | 是 | 接口中的 `items[].status_text` 字段 | `"example"` |
| `items[].description` | string | 是 | 详细描述 | `"example"` |
| `items[].my_status` | integer | 是 | 接口中的 `items[].my_status` 字段 | `1` |
| `items[].my_status_text` | string | 是 | 接口中的 `items[].my_status_text` 字段 | `"example"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 174. `GET /api/v1/community/activities/{activity_id}` 活动详情

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/activities/{activity_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `activity_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "activity_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "title": "example",
  "cover": "example",
  "type": "example",
  "city": "example",
  "address": "example",
  "start_time": "2026-08-05T10:00:00+08:00",
  "end_time": "2026-08-05T10:00:00+08:00",
  "signup_deadline": "2026-08-05T10:00:00+08:00",
  "max_people": 1,
  "current_people": 1,
  "price": 1.2,
  "status": 1,
  "status_text": "example",
  "description": "example",
  "my_status": 1,
  "my_status_text": "example",
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `title` | string | 是 | 接口中的 `title` 字段 | `"example"` |
| `cover` | string | 是 | 接口中的 `cover` 字段 | `"example"` |
| `type` | string | 是 | 接口中的 `type` 字段 | `"example"` |
| `city` | string | 是 | 接口中的 `city` 字段 | `"example"` |
| `address` | string | 是 | 接口中的 `address` 字段 | `"example"` |
| `start_time` | string | 是 | 接口中的 `start_time` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `end_time` | string | 是 | 接口中的 `end_time` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `signup_deadline` | string | 是 | 接口中的 `signup_deadline` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `max_people` | integer | 是 | 接口中的 `max_people` 字段 | `1` |
| `current_people` | integer | 是 | 接口中的 `current_people` 字段 | `1` |
| `price` | number | 是 | 商品价格 | `1.2` |
| `status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `status_text` | string | 是 | 接口中的 `status_text` 字段 | `"example"` |
| `description` | string | 是 | 详细描述 | `"example"` |
| `my_status` | integer | 是 | 接口中的 `my_status` 字段 | `1` |
| `my_status_text` | string | 是 | 接口中的 `my_status_text` 字段 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 175. `POST /api/v1/community/activities/{activity_id}/signup` 报名活动

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/activities/{activity_id}/signup`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `activity_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "activity_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "real_name": "example",
  "phone": "13800138000",
  "remark": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `real_name` | string | 否 | maxLength=64 | 接口中的 `real_name` 字段 | `"example"` |
| `phone` | string | 否 | maxLength=20 | 手机号；返回时通常脱敏 | `"13800138000"` |
| `remark` | string | 否 | maxLength=255 | 接口中的 `remark` 字段 | `"example"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "success": true,
  "activity_id": 1,
  "my_status": 1,
  "my_status_text": "example",
  "message": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `success` | boolean | 否 | 接口中的 `success` 字段 | `true` |
| `activity_id` | integer | 是 | 接口中的 `activity_id` 字段 | `1` |
| `my_status` | integer | 是 | 接口中的 `my_status` 字段 | `1` |
| `my_status_text` | string | 是 | 接口中的 `my_status_text` 字段 | `"example"` |
| `message` | string | 是 | 接口中的 `message` 字段 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 176. `GET /api/v1/community/banners` 社区 Banner

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/banners`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `position` | query | string | 否 | maxLength=32 | 接口参数 | `"community"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "position": "community"
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "title": "example",
    "image_url": "/storage/uploads/example.jpg",
    "link_type": "example",
    "link_value": "example",
    "sort": 1,
    "position": "example"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 177. `GET /api/v1/community/quotas` 社区相关日额度

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/quotas`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "apply_daily": {
    "total": 1,
    "used": 1,
    "remain": 1,
    "points_available": true,
    "points_cost": 1
  },
  "paper_plane_daily": {
    "total": 1,
    "used": 1,
    "remain": 1,
    "points_available": true,
    "points_cost": 1
  }
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `apply_daily` | object | 是 | 接口中的 `apply_daily` 字段 | `{"total": 1, "used": 1, "remain": 1, "points_available": true, "points_cost": 1}` |
| `apply_daily.total` | integer | 是 | 符合条件的记录总数 | `1` |
| `apply_daily.used` | integer | 是 | 接口中的 `apply_daily.used` 字段 | `1` |
| `apply_daily.remain` | integer | 是 | 接口中的 `apply_daily.remain` 字段 | `1` |
| `apply_daily.points_available` | boolean | 否 | 接口中的 `apply_daily.points_available` 字段 | `true` |
| `apply_daily.points_cost` | integer | 否 | 接口中的 `apply_daily.points_cost` 字段 | `1` |
| `paper_plane_daily` | object | 是 | 接口中的 `paper_plane_daily` 字段 | `{"total": 1, "used": 1, "remain": 1, "points_available": true, "points_cost": 1}` |
| `paper_plane_daily.total` | integer | 是 | 符合条件的记录总数 | `1` |
| `paper_plane_daily.used` | integer | 是 | 接口中的 `paper_plane_daily.used` 字段 | `1` |
| `paper_plane_daily.remain` | integer | 是 | 接口中的 `paper_plane_daily.remain` 字段 | `1` |
| `paper_plane_daily.points_available` | boolean | 否 | 接口中的 `paper_plane_daily.points_available` 字段 | `true` |
| `paper_plane_daily.points_cost` | integer | 否 | 接口中的 `paper_plane_daily.points_cost` 字段 | `1` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 178. `GET /api/v1/community/city` 当前同城城市

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/city`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "name": "example",
  "code": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `name` | string | 是 | 展示名称 | `"example"` |
| `code` | string | 否 | 业务编码或地区编码 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 179. `PUT /api/v1/community/city` 设置同城城市

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/city`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "name": "example",
  "code": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `name` | string | 是 | minLength=1; maxLength=64 | 展示名称 | `"example"` |
| `code` | string | 否 | maxLength=6; pattern=^(?:[0-9]{4}|[0-9]{6})$ | 业务编码或地区编码 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "name": "example",
  "code": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `name` | string | 是 | 展示名称 | `"example"` |
| `code` | string | 否 | 业务编码或地区编码 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 180. `GET /api/v1/community/report-reasons` 举报原因列表

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/report-reasons`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": "example",
    "label": "example"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 181. `POST /api/v1/community/reports` 举报社区内容

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/reports`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "target_type": "post",
  "target_id": 1,
  "reason_id": "example",
  "description": "example",
  "images": [
    "/storage/uploads/example.jpg"
  ]
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_type` | post/comment/paper_plane | 是 | 枚举=post, comment, paper_plane | 接口中的 `target_type` 字段 | `"post"` |
| `target_id` | integer | 是 | minimum=1.0 | 接口中的 `target_id` 字段 | `1` |
| `reason_id` | string | 是 | minLength=1; maxLength=64 | 接口中的 `reason_id` 字段 | `"example"` |
| `description` | string | 否 | maxLength=1000 | 详细描述 | `"example"` |
| `images` | array<string> | 否 | 按 Schema 类型校验 | 接口中的 `images` 字段 | `["/storage/uploads/example.jpg"]` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "target_type": "post",
  "target_id": 1,
  "target_user_id": 1,
  "type": "example",
  "status": 0,
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `target_type` | post/comment/paper_plane/user | 是 | 枚举=post, comment, paper_plane, user | 接口中的 `target_type` 字段 | `"post"` |
| `target_id` | integer | 是 | 接口中的 `target_id` 字段 | `1` |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `type` | string | 是 | 接口中的 `type` 字段 | `"example"` |
| `status` | 0/1/2 | 是 | 枚举=0, 1, 2 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 182. `GET /api/v1/community/reports/mine` 我的举报与被举报结论

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/reports/mine`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "target_user_id": 1,
      "target_type": "user",
      "target_id": 1,
      "viewer_role": "reporter",
      "type": "example",
      "description": "example",
      "status": 0,
      "result": "example",
      "action": "none",
      "reviewed_at": "2026-08-05T10:00:00+08:00",
      "created_at": "2026-08-05T10:00:00+08:00",
      "updated_at": "2026-08-05T10:00:00+08:00",
      "can_appeal": false
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "target_user_id": 1, "target_type": "user", "target_id": 1, "viewer_role": "reporter", "type": "example", "description": "example", "status": 0, "result": "example", "action": "none", "reviewed_at": "2026-08-05T10:00:00+08:00", "created_at": "2026-08-05T10:00:00+08:00", "updated_at": "2026-08-05T10:00:00+08:00", "can_appeal": false}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `items[].target_type` | user/post/comment/paper_plane | 是 | 枚举=user, post, comment, paper_plane | 接口中的 `items[].target_type` 字段 | `"user"` |
| `items[].target_id` | integer | 是 | 接口中的 `items[].target_id` 字段 | `1` |
| `items[].viewer_role` | reporter/subject | 是 | 枚举=reporter, subject | 接口中的 `items[].viewer_role` 字段 | `"reporter"` |
| `items[].type` | string | 是 | 接口中的 `items[].type` 字段 | `"example"` |
| `items[].description` | string | 是 | 详细描述 | `"example"` |
| `items[].status` | 0/1/2 | 是 | 枚举=0, 1, 2 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `items[].result` | string | 是 | 接口中的 `items[].result` 字段 | `"example"` |
| `items[].action` | none/hide_content/restore_content/restrict_user/dismiss | 否 | 枚举=none, hide_content, restore_content, restrict_user, dismiss | 接口中的 `items[].action` 字段 | `"none"` |
| `items[].reviewed_at` | string | 否 | 接口中的 `items[].reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].updated_at` | string | 否 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].can_appeal` | boolean | 否 | 接口中的 `items[].can_appeal` 字段 | `false` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 183. `POST /api/v1/community/reports/{report_id}/appeals` 提交举报申诉

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/reports/{report_id}/appeals`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `report_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "report_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "reason": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `reason` | string | 是 | minLength=1; maxLength=1000 | 操作或审核原因 | `"example"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "report_id": 1,
  "appellant_user_id": 1,
  "reason": "example",
  "status": 0,
  "result": "example",
  "reviewed_at": "2026-08-05T10:00:00+08:00",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `report_id` | integer | 是 | 接口中的 `report_id` 字段 | `1` |
| `appellant_user_id` | integer | 是 | 接口中的 `appellant_user_id` 字段 | `1` |
| `reason` | string | 是 | 操作或审核原因 | `"example"` |
| `status` | 0/1/2 | 是 | 枚举=0, 1, 2 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `result` | string | 否 | 接口中的 `result` 字段 | `"example"` |
| `reviewed_at` | string | 否 | 接口中的 `reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 否 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 184. `GET /api/v1/community/report-appeals/mine` 我的举报申诉

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/community/report-appeals/mine`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "report_id": 1,
      "appellant_user_id": 1,
      "reason": "example",
      "status": 0,
      "result": "example",
      "reviewed_at": "2026-08-05T10:00:00+08:00",
      "created_at": "2026-08-05T10:00:00+08:00",
      "updated_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "report_id": 1, "appellant_user_id": 1, "reason": "example", "status": 0, "result": "example", "reviewed_at": "2026-08-05T10:00:00+08:00", "created_at": "2026-08-05T10:00:00+08:00", "updated_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].report_id` | integer | 是 | 接口中的 `items[].report_id` 字段 | `1` |
| `items[].appellant_user_id` | integer | 是 | 接口中的 `items[].appellant_user_id` 字段 | `1` |
| `items[].reason` | string | 是 | 操作或审核原因 | `"example"` |
| `items[].status` | 0/1/2 | 是 | 枚举=0, 1, 2 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `items[].result` | string | 否 | 接口中的 `items[].result` 字段 | `"example"` |
| `items[].reviewed_at` | string | 否 | 接口中的 `items[].reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].updated_at` | string | 否 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 185. `GET /api/v1/paper-planes` 捡纸飞机

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/paper-planes`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "content": "example",
    "images": [
      "/storage/uploads/example.jpg"
    ],
    "city": "example",
    "tags": [
      "example"
    ],
    "is_anonymous": true,
    "reply_count": 1,
    "voice_url": "/storage/uploads/example.jpg",
    "voice_duration_sec": 1,
    "created_at": "2026-08-05T10:00:00+08:00"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 186. `POST /api/v1/paper-planes` 发送纸飞机

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/paper-planes`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>",
  "Idempotency-Key": "examplex"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `Idempotency-Key` | header | string | 否 | 无额外约束 | 接口参数 | `"examplex"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "Idempotency-Key": "examplex"
}
```

### 请求体 JSON 示例

```json
{
  "content": "",
  "images": [
    "/storage/uploads/example.jpg"
  ],
  "image_media_ids": [
    1
  ],
  "city": "example",
  "tags": [
    "example"
  ],
  "is_anonymous": true,
  "voice_url": "/storage/uploads/example.jpg",
  "voice_duration_sec": 1
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `content` | string | 否 | maxLength=1000 | 文本内容 | `""` |
| `images` | array<string> | 否 | 按 Schema 类型校验 | 接口中的 `images` 字段 | `["/storage/uploads/example.jpg"]` |
| `image_media_ids` | array<integer> | 否 | 按 Schema 类型校验 | 接口中的 `image_media_ids` 字段 | `[1]` |
| `city` | string | 否 | maxLength=64 | 接口中的 `city` 字段 | `"example"` |
| `tags` | array<string> | 否 | 按 Schema 类型校验 | 接口中的 `tags` 字段 | `["example"]` |
| `is_anonymous` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `is_anonymous` 字段 | `true` |
| `voice_url` | string | 否 | maxLength=500 | 接口中的 `voice_url` 字段 | `"/storage/uploads/example.jpg"` |
| `voice_duration_sec` | integer | 否 | minimum=1.0; maximum=60.0 | 接口中的 `voice_duration_sec` 字段 | `1` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "content": "example",
  "images": [
    "/storage/uploads/example.jpg"
  ],
  "city": "example",
  "tags": [
    "example"
  ],
  "is_anonymous": true,
  "reply_count": 1,
  "voice_url": "/storage/uploads/example.jpg",
  "voice_duration_sec": 1,
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `content` | string | 是 | 文本内容 | `"example"` |
| `images` | array<string> | 是 | 接口中的 `images` 字段 | `["/storage/uploads/example.jpg"]` |
| `city` | string | 是 | 接口中的 `city` 字段 | `"example"` |
| `tags` | array<string> | 是 | 接口中的 `tags` 字段 | `["example"]` |
| `is_anonymous` | boolean | 是 | 接口中的 `is_anonymous` 字段 | `true` |
| `reply_count` | integer | 是 | 接口中的 `reply_count` 字段 | `1` |
| `voice_url` | string | 否 | 接口中的 `voice_url` 字段 | `"/storage/uploads/example.jpg"` |
| `voice_duration_sec` | integer | 否 | 接口中的 `voice_duration_sec` 字段 | `1` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 187. `GET /api/v1/paper-planes/mine` 查看我的纸飞机

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/paper-planes/mine`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "content": "example",
    "images": [
      "/storage/uploads/example.jpg"
    ],
    "city": "example",
    "tags": [
      "example"
    ],
    "is_anonymous": true,
    "reply_count": 1,
    "voice_url": "/storage/uploads/example.jpg",
    "voice_duration_sec": 1,
    "created_at": "2026-08-05T10:00:00+08:00"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 188. `POST /api/v1/paper-planes/{plane_id}/replies` 回复纸飞机

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/paper-planes/{plane_id}/replies`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>",
  "Idempotency-Key": "examplex"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `plane_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |
| `Idempotency-Key` | header | string | 否 | 无额外约束 | 接口参数 | `"examplex"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "plane_id": 1,
  "Idempotency-Key": "examplex"
}
```

### 请求体 JSON 示例

```json
{
  "content": "example",
  "is_anonymous": true
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `content` | string | 是 | minLength=1; maxLength=1000 | 文本内容 | `"example"` |
| `is_anonymous` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `is_anonymous` 字段 | `true` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "plane_id": 1,
  "user_id": 1,
  "content": "example",
  "is_anonymous": true,
  "conversation_id": 1,
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `plane_id` | integer | 是 | 接口中的 `plane_id` 字段 | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `content` | string | 是 | 文本内容 | `"example"` |
| `is_anonymous` | boolean | 是 | 接口中的 `is_anonymous` 字段 | `true` |
| `conversation_id` | integer | 否 | 接口中的 `conversation_id` 字段 | `1` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 189. `GET /api/v1/paper-plane-conversations` 纸飞机匿名会话列表

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/paper-plane-conversations`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "plane_id": 1,
    "owner_id": 1,
    "replier_id": 1,
    "status": 1,
    "last_message": "example",
    "last_message_at": "2026-08-05T10:00:00+08:00",
    "unread_count": 0,
    "plane_content": "example",
    "peer_label": "匿名用户",
    "created_at": "2026-08-05T10:00:00+08:00"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 190. `GET /api/v1/paper-plane-conversations/{conversation_id}/messages` 纸飞机匿名会话消息

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/paper-plane-conversations/{conversation_id}/messages`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `conversation_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=100 | 接口参数 | `50` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "conversation_id": 1,
  "page": 1,
  "page_size": 50
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "conversation_id": 1,
    "from_user_id": 1,
    "mine": true,
    "content": "example",
    "type": 1,
    "media_url": "/storage/uploads/example.jpg",
    "voice_duration_sec": 1,
    "created_at": "2026-08-05T10:00:00+08:00"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 191. `POST /api/v1/paper-plane-conversations/{conversation_id}/messages` 发送纸飞机匿名会话消息

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/paper-plane-conversations/{conversation_id}/messages`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>",
  "Idempotency-Key": "examplex"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `conversation_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |
| `Idempotency-Key` | header | string | 否 | 无额外约束 | 接口参数 | `"examplex"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "conversation_id": 1,
  "Idempotency-Key": "examplex"
}
```

### 请求体 JSON 示例

```json
{
  "content": "",
  "type": 1,
  "media_url": "/storage/uploads/example.jpg",
  "voice_duration_sec": 1
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `content` | string | 否 | maxLength=1000 | 文本内容 | `""` |
| `type` | 1/3 | 否 | 枚举=1, 3 | 接口中的 `type` 字段 | `1` |
| `media_url` | string | 否 | maxLength=500 | 接口中的 `media_url` 字段 | `"/storage/uploads/example.jpg"` |
| `voice_duration_sec` | integer | 否 | minimum=1.0; maximum=60.0 | 接口中的 `voice_duration_sec` 字段 | `1` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "conversation_id": 1,
  "from_user_id": 1,
  "mine": true,
  "content": "example",
  "type": 1,
  "media_url": "/storage/uploads/example.jpg",
  "voice_duration_sec": 1,
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `conversation_id` | integer | 是 | 接口中的 `conversation_id` 字段 | `1` |
| `from_user_id` | integer | 是 | 接口中的 `from_user_id` 字段 | `1` |
| `mine` | boolean | 是 | 接口中的 `mine` 字段 | `true` |
| `content` | string | 是 | 文本内容 | `"example"` |
| `type` | integer | 是 | 接口中的 `type` 字段 | `1` |
| `media_url` | string | 否 | 接口中的 `media_url` 字段 | `"/storage/uploads/example.jpg"` |
| `voice_duration_sec` | integer | 否 | 接口中的 `voice_duration_sec` 字段 | `1` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 192. `POST /api/v1/paper-plane-conversations/{conversation_id}/read` 标记纸飞机会话已读

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/paper-plane-conversations/{conversation_id}/read`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `conversation_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "conversation_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "plane_id": 1,
  "owner_id": 1,
  "replier_id": 1,
  "status": 1,
  "last_message": "example",
  "last_message_at": "2026-08-05T10:00:00+08:00",
  "unread_count": 0,
  "plane_content": "example",
  "peer_label": "匿名用户",
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `plane_id` | integer | 是 | 接口中的 `plane_id` 字段 | `1` |
| `owner_id` | integer | 是 | 接口中的 `owner_id` 字段 | `1` |
| `replier_id` | integer | 是 | 接口中的 `replier_id` 字段 | `1` |
| `status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `last_message` | string | 否 | 接口中的 `last_message` 字段 | `"example"` |
| `last_message_at` | string | 否 | 接口中的 `last_message_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `unread_count` | integer | 否 | 接口中的 `unread_count` 字段 | `0` |
| `plane_content` | string | 否 | 接口中的 `plane_content` 字段 | `"example"` |
| `peer_label` | string | 否 | 接口中的 `peer_label` 字段 | `"匿名用户"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 193. `POST /api/v1/paper-plane-conversations/{conversation_id}/end` 结束纸飞机匿名会话

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/paper-plane-conversations/{conversation_id}/end`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `conversation_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "conversation_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "plane_id": 1,
  "owner_id": 1,
  "replier_id": 1,
  "status": 1,
  "last_message": "example",
  "last_message_at": "2026-08-05T10:00:00+08:00",
  "unread_count": 0,
  "plane_content": "example",
  "peer_label": "匿名用户",
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `plane_id` | integer | 是 | 接口中的 `plane_id` 字段 | `1` |
| `owner_id` | integer | 是 | 接口中的 `owner_id` 字段 | `1` |
| `replier_id` | integer | 是 | 接口中的 `replier_id` 字段 | `1` |
| `status` | integer | 是 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `last_message` | string | 否 | 接口中的 `last_message` 字段 | `"example"` |
| `last_message_at` | string | 否 | 接口中的 `last_message_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `unread_count` | integer | 否 | 接口中的 `unread_count` 字段 | `0` |
| `plane_content` | string | 否 | 接口中的 `plane_content` 字段 | `"example"` |
| `peer_label` | string | 否 | 接口中的 `peer_label` 字段 | `"匿名用户"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 194. `POST /api/v1/media/uploads` 上传媒体文件

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/media/uploads`

```json
{
  "Content-Type": "multipart/form-data",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体示例（multipart/form-data）

字段 `file` 使用真实文件上传，其余字段按表单字段提交。

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `file` | string | 是 | 按 Schema 类型校验 | 接口中的 `file` 字段 | `"example"` |
| `purpose` | string | 否 | 按 Schema 类型校验 | 接口中的 `purpose` 字段 | `"paper_plane_voice"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "url": "/storage/uploads/example.jpg",
  "content_type": "example",
  "size": 1,
  "purpose": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `url` | string | 是 | 接口中的 `url` 字段 | `"/storage/uploads/example.jpg"` |
| `content_type` | string | 是 | 接口中的 `content_type` 字段 | `"example"` |
| `size` | integer | 是 | 接口中的 `size` 字段 | `1` |
| `purpose` | string | 是 | 接口中的 `purpose` 字段 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 195. `GET /api/v1/admin/users/{user_id}/restrictions` 查询用户限制记录

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/users/{user_id}/restrictions`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `user_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=100 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "user_id": 1,
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "user_id": 1,
      "restriction_type": "TOTAL_BAN",
      "reason_code": "example",
      "reason": "example",
      "starts_at": "2026-08-05T10:00:00+08:00",
      "ends_at": "2026-08-05T10:00:00+08:00",
      "status": 1,
      "ended_at": "2026-08-05T10:00:00+08:00",
      "created_by": 1,
      "note": "example",
      "created_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "user_id": 1, "restriction_type": "TOTAL_BAN", "reason_code": "example", "reason": "example", "starts_at": "2026-08-05T10:00:00+08:00", "ends_at": "2026-08-05T10:00:00+08:00", "status": 1, "ended_at": "2026-08-05T10:00:00+08:00", "created_by": 1, "note": "example", "created_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].restriction_type` | TOTAL_BAN/POST_RESTRICTED/COMMENT_RESTRICTED/MESSAGE_RESTRICTED/APPLICATION_RESTRICTED | 是 | 枚举=TOTAL_BAN, POST_RESTRICTED, COMMENT_RESTRICTED, MESSAGE_RESTRICTED, APPLICATION_RESTRICTED | 接口中的 `items[].restriction_type` 字段 | `"TOTAL_BAN"` |
| `items[].reason_code` | string | 是 | 接口中的 `items[].reason_code` 字段 | `"example"` |
| `items[].reason` | string | 是 | 操作或审核原因 | `"example"` |
| `items[].starts_at` | string | 是 | 接口中的 `items[].starts_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `items[].ends_at` | string | 是 | 接口中的 `items[].ends_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `items[].status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `items[].ended_at` | string | 是 | 接口中的 `items[].ended_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `items[].created_by` | integer | 是 | 接口中的 `items[].created_by` 字段 | `1` |
| `items[].note` | string | 是 | 补充说明 | `"example"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 196. `PATCH /api/v1/admin/users/{user_id}/restrictions` 设置用户限制或总封禁

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/users/{user_id}/restrictions`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `user_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "user_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "restriction_type": "TOTAL_BAN",
  "reason_code": "example",
  "reason": "example",
  "starts_at": "2026-08-05T10:00:00+08:00",
  "ends_at": "2026-08-05T10:00:00+08:00",
  "note": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `restriction_type` | TOTAL_BAN/POST_RESTRICTED/COMMENT_RESTRICTED/MESSAGE_RESTRICTED/APPLICATION_RESTRICTED | 是 | 枚举=TOTAL_BAN, POST_RESTRICTED, COMMENT_RESTRICTED, MESSAGE_RESTRICTED, APPLICATION_RESTRICTED | 接口中的 `restriction_type` 字段 | `"TOTAL_BAN"` |
| `reason_code` | string | 是 | minLength=1; maxLength=64 | 接口中的 `reason_code` 字段 | `"example"` |
| `reason` | string | 是 | minLength=1; maxLength=255 | 操作或审核原因 | `"example"` |
| `starts_at` | string | 否 | 按 Schema 类型校验 | 接口中的 `starts_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `ends_at` | string | 否 | 按 Schema 类型校验 | 接口中的 `ends_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `note` | string | 否 | maxLength=1000 | 补充说明 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "user_id": 1,
  "restriction_type": "TOTAL_BAN",
  "reason_code": "example",
  "reason": "example",
  "starts_at": "2026-08-05T10:00:00+08:00",
  "ends_at": "2026-08-05T10:00:00+08:00",
  "status": 1,
  "ended_at": "2026-08-05T10:00:00+08:00",
  "created_by": 1,
  "note": "example",
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `restriction_type` | TOTAL_BAN/POST_RESTRICTED/COMMENT_RESTRICTED/MESSAGE_RESTRICTED/APPLICATION_RESTRICTED | 是 | 枚举=TOTAL_BAN, POST_RESTRICTED, COMMENT_RESTRICTED, MESSAGE_RESTRICTED, APPLICATION_RESTRICTED | 接口中的 `restriction_type` 字段 | `"TOTAL_BAN"` |
| `reason_code` | string | 是 | 接口中的 `reason_code` 字段 | `"example"` |
| `reason` | string | 是 | 操作或审核原因 | `"example"` |
| `starts_at` | string | 是 | 接口中的 `starts_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `ends_at` | string | 是 | 接口中的 `ends_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `ended_at` | string | 是 | 接口中的 `ended_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `created_by` | integer | 是 | 接口中的 `created_by` 字段 | `1` |
| `note` | string | 是 | 补充说明 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 197. `DELETE /api/v1/admin/users/{user_id}/restrictions/{restriction_id}` 解除用户限制

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/users/{user_id}/restrictions/{restriction_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `user_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |
| `restriction_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "user_id": 1,
  "restriction_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 198. `GET /api/v1/admin/community/moderation-items` 查看社区待审核内容

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/community/moderation-items`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=100 | 接口参数 | `20` |
| `status` | query | pending/approved/rejected/replaced/deleted/hidden | 否 | 枚举=pending, approved, rejected, replaced, deleted, hidden | 接口参数 | `"pending"` |
| `target_type` | query | post/comment/paper_plane/paper_plane_reply/paper_plane_message/media | 否 | 无额外约束 | 接口参数 | `"post"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20,
  "status": "pending",
  "target_type": "post"
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "target_type": "post",
      "target_id": 1,
      "user_id": 1,
      "status": "pending",
      "risk_level": 1,
      "provider": "local",
      "matched_words": [
        "example"
      ],
      "raw_content": "example",
      "display_content": "example",
      "reason": "example",
      "created_at": "2026-08-05T10:00:00+08:00",
      "expires_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "target_type": "post", "target_id": 1, "user_id": 1, "status": "pending", "risk_level": 1, "provider": "local", "matched_words": ["example"], "raw_content": "example", "display_content": "example", "reason": "example", "created_at": "2026-08-05T10:00:00+08:00", "expires_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].target_type` | post/comment/paper_plane/paper_plane_reply/paper_plane_message/media | 是 | 枚举=post, comment, paper_plane, paper_plane_reply, paper_plane_message, media | 接口中的 `items[].target_type` 字段 | `"post"` |
| `items[].target_id` | integer | 是 | 接口中的 `items[].target_id` 字段 | `1` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].status` | pending/approved/rejected/replaced/deleted/hidden | 是 | 枚举=pending, approved, rejected, replaced, deleted, hidden | 当前业务状态，具体枚举见本接口约束 | `"pending"` |
| `items[].risk_level` | integer | 是 | 接口中的 `items[].risk_level` 字段 | `1` |
| `items[].provider` | string | 否 | 接口中的 `items[].provider` 字段 | `"local"` |
| `items[].matched_words` | array<string> | 是 | 接口中的 `items[].matched_words` 字段 | `["example"]` |
| `items[].raw_content` | string | 是 | 接口中的 `items[].raw_content` 字段 | `"example"` |
| `items[].display_content` | string | 是 | 接口中的 `items[].display_content` 字段 | `"example"` |
| `items[].reason` | string | 是 | 操作或审核原因 | `"example"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].expires_at` | string | 是 | 到期时间 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 199. `PATCH /api/v1/admin/community/moderation-items/{task_id}/review` 审核社区内容

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/community/moderation-items/{task_id}/review`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `task_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "task_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "action": "approve",
  "reason": "example",
  "display_content": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `action` | approve/reject/replace/delete/hide | 是 | 枚举=approve, reject, replace, delete, hide | 接口中的 `action` 字段 | `"approve"` |
| `reason` | string | 是 | minLength=1; maxLength=255 | 操作或审核原因 | `"example"` |
| `display_content` | string | 否 | maxLength=2000 | 接口中的 `display_content` 字段 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "target_type": "example",
  "target_id": 1,
  "status": "example",
  "reason": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `target_type` | string | 是 | 接口中的 `target_type` 字段 | `"example"` |
| `target_id` | integer | 是 | 接口中的 `target_id` 字段 | `1` |
| `status` | string | 是 | 当前业务状态，具体枚举见本接口约束 | `"example"` |
| `reason` | string | 是 | 操作或审核原因 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 200. `POST /api/v1/admin/users/grant` 授予管理员及权限

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/users/grant`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "user_id": 1,
  "permissions": [
    "example"
  ]
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | minimum=1.0 | 用户 ID | `1` |
| `permissions` | array<string> | 否 | 按 Schema 类型校验 | 接口中的 `permissions` 字段 | `["example"]` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "user_id": 1,
  "role_code": "example",
  "permissions": [
    "example"
  ]
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `role_code` | string | 是 | 接口中的 `role_code` 字段 | `"example"` |
| `permissions` | array<string> | 是 | 接口中的 `permissions` 字段 | `["example"]` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 201. `PATCH /api/v1/admin/media/{media_id}/review` 审核用户媒体

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/media/{media_id}/review`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `media_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "media_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "status": 1,
  "reason": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | 1/2/3 | 是 | 枚举=1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `reason` | string | 否 | maxLength=255 | 操作或审核原因 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "media_id": 1,
  "user_id": 1,
  "status": 1,
  "reason": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `media_id` | integer | 是 | 接口中的 `media_id` 字段 | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `status` | 1/2/3 | 是 | 枚举=1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `reason` | string | 是 | 操作或审核原因 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 202. `GET /api/v1/admin/reports` 举报列表

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/reports`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=100 | 接口参数 | `20` |
| `status` | query | 0/1/2 | 否 | 无额外约束 | 接口参数 | `0` |
| `target_type` | query | user/post/comment/paper_plane | 否 | 无额外约束 | 接口参数 | `"user"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20,
  "status": 0,
  "target_type": "user"
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "reporter_user_id": 1,
      "target_user_id": 1,
      "target_type": "user",
      "target_id": 1,
      "type": "example",
      "description": "example",
      "status": 0,
      "result": "example",
      "action": "none",
      "reviewed_by": 1,
      "reviewed_at": "2026-08-05T10:00:00+08:00",
      "created_at": "2026-08-05T10:00:00+08:00",
      "updated_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "reporter_user_id": 1, "target_user_id": 1, "target_type": "user", "target_id": 1, "type": "example", "description": "example", "status": 0, "result": "example", "action": "none", "reviewed_by": 1, "reviewed_at": "2026-08-05T10:00:00+08:00", "created_at": "2026-08-05T10:00:00+08:00", "updated_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].reporter_user_id` | integer | 是 | 接口中的 `items[].reporter_user_id` 字段 | `1` |
| `items[].target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `items[].target_type` | user/post/comment/paper_plane | 是 | 枚举=user, post, comment, paper_plane | 接口中的 `items[].target_type` 字段 | `"user"` |
| `items[].target_id` | integer | 是 | 接口中的 `items[].target_id` 字段 | `1` |
| `items[].type` | string | 是 | 接口中的 `items[].type` 字段 | `"example"` |
| `items[].description` | string | 是 | 详细描述 | `"example"` |
| `items[].status` | 0/1/2 | 是 | 枚举=0, 1, 2 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `items[].result` | string | 是 | 接口中的 `items[].result` 字段 | `"example"` |
| `items[].action` | none/hide_content/restore_content/restrict_user/dismiss | 否 | 枚举=none, hide_content, restore_content, restrict_user, dismiss | 接口中的 `items[].action` 字段 | `"none"` |
| `items[].reviewed_by` | integer | 否 | 接口中的 `items[].reviewed_by` 字段 | `1` |
| `items[].reviewed_at` | string | 否 | 接口中的 `items[].reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].updated_at` | string | 否 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 203. `GET /api/v1/admin/reports/{report_id}` 举报详情

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/reports/{report_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `report_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "report_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "reporter_user_id": 1,
  "target_user_id": 1,
  "target_type": "user",
  "target_id": 1,
  "type": "example",
  "description": "example",
  "status": 0,
  "result": "example",
  "action": "none",
  "reviewed_by": 1,
  "reviewed_at": "2026-08-05T10:00:00+08:00",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `reporter_user_id` | integer | 是 | 接口中的 `reporter_user_id` 字段 | `1` |
| `target_user_id` | integer | 是 | 目标用户 ID | `1` |
| `target_type` | user/post/comment/paper_plane | 是 | 枚举=user, post, comment, paper_plane | 接口中的 `target_type` 字段 | `"user"` |
| `target_id` | integer | 是 | 接口中的 `target_id` 字段 | `1` |
| `type` | string | 是 | 接口中的 `type` 字段 | `"example"` |
| `description` | string | 是 | 详细描述 | `"example"` |
| `status` | 0/1/2 | 是 | 枚举=0, 1, 2 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `result` | string | 是 | 接口中的 `result` 字段 | `"example"` |
| `action` | none/hide_content/restore_content/restrict_user/dismiss | 否 | 枚举=none, hide_content, restore_content, restrict_user, dismiss | 接口中的 `action` 字段 | `"none"` |
| `reviewed_by` | integer | 否 | 接口中的 `reviewed_by` 字段 | `1` |
| `reviewed_at` | string | 否 | 接口中的 `reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 否 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 204. `PATCH /api/v1/admin/reports/{report_id}/review` 处理用户举报

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/reports/{report_id}/review`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `report_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "report_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "status": 1,
  "result": "example",
  "action": "none",
  "restriction_type": "TOTAL_BAN",
  "restriction_reason_code": "example",
  "restriction_ends_at": "2026-08-05T10:00:00+08:00"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `result` | string | 是 | minLength=1; maxLength=255 | 接口中的 `result` 字段 | `"example"` |
| `action` | none/hide_content/restore_content/restrict_user/dismiss | 否 | 枚举=none, hide_content, restore_content, restrict_user, dismiss | 接口中的 `action` 字段 | `"none"` |
| `restriction_type` | TOTAL_BAN/POST_RESTRICTED/COMMENT_RESTRICTED/MESSAGE_RESTRICTED/APPLICATION_RESTRICTED | 否 | 枚举=TOTAL_BAN, POST_RESTRICTED, COMMENT_RESTRICTED, MESSAGE_RESTRICTED, APPLICATION_RESTRICTED | 接口中的 `restriction_type` 字段 | `"TOTAL_BAN"` |
| `restriction_reason_code` | string | 否 | minLength=1; maxLength=64 | 接口中的 `restriction_reason_code` 字段 | `"example"` |
| `restriction_ends_at` | string | 否 | 按 Schema 类型校验 | 接口中的 `restriction_ends_at` 字段 | `"2026-08-05T10:00:00+08:00"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "report_id": 1,
  "status": 1,
  "result": "example",
  "action": "none",
  "content_moderated": false,
  "restriction_created": false
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `report_id` | integer | 是 | 接口中的 `report_id` 字段 | `1` |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `result` | string | 是 | 接口中的 `result` 字段 | `"example"` |
| `action` | none/hide_content/restore_content/restrict_user/dismiss | 否 | 枚举=none, hide_content, restore_content, restrict_user, dismiss | 接口中的 `action` 字段 | `"none"` |
| `content_moderated` | boolean | 否 | 接口中的 `content_moderated` 字段 | `false` |
| `restriction_created` | boolean | 否 | 接口中的 `restriction_created` 字段 | `false` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 205. `GET /api/v1/admin/report-appeals` 举报申诉列表

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/report-appeals`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `page` | query | integer | 否 | minimum=1 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=100 | 接口参数 | `20` |
| `status` | query | 0/1/2 | 否 | 无额外约束 | 接口参数 | `0` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "page": 1,
  "page_size": 20,
  "status": 0
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "report_id": 1,
      "appellant_user_id": 1,
      "reason": "example",
      "status": 0,
      "result": "example",
      "reviewed_by": 1,
      "reviewed_at": "2026-08-05T10:00:00+08:00",
      "created_at": "2026-08-05T10:00:00+08:00",
      "updated_at": "2026-08-05T10:00:00+08:00",
      "target_type": "user",
      "target_id": 1,
      "original_reviewer_id": 1
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "report_id": 1, "appellant_user_id": 1, "reason": "example", "status": 0, "result": "example", "reviewed_by": 1, "reviewed_at": "2026-08-05T10:00:00+08:00", "created_at": "2026-08-05T10:00:00+08:00", "updated_at": "2026-08-05T10:00:00+08:00", "target_type": "user", "target_id": 1, "original_reviewer_id": 1}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].report_id` | integer | 是 | 接口中的 `items[].report_id` 字段 | `1` |
| `items[].appellant_user_id` | integer | 是 | 接口中的 `items[].appellant_user_id` 字段 | `1` |
| `items[].reason` | string | 是 | 操作或审核原因 | `"example"` |
| `items[].status` | 0/1/2 | 是 | 枚举=0, 1, 2 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `items[].result` | string | 否 | 接口中的 `items[].result` 字段 | `"example"` |
| `items[].reviewed_by` | integer | 否 | 接口中的 `items[].reviewed_by` 字段 | `1` |
| `items[].reviewed_at` | string | 否 | 接口中的 `items[].reviewed_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].updated_at` | string | 否 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].target_type` | user/post/comment/paper_plane | 是 | 枚举=user, post, comment, paper_plane | 接口中的 `items[].target_type` 字段 | `"user"` |
| `items[].target_id` | integer | 是 | 接口中的 `items[].target_id` 字段 | `1` |
| `items[].original_reviewer_id` | integer | 否 | 接口中的 `items[].original_reviewer_id` 字段 | `1` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 206. `PATCH /api/v1/admin/report-appeals/{appeal_id}/review` 复审举报申诉

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/report-appeals/{appeal_id}/review`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `appeal_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "appeal_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "status": 1,
  "result": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `result` | string | 是 | minLength=1; maxLength=255 | 接口中的 `result` 字段 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "appeal_id": 1,
  "report_id": 1,
  "status": 1,
  "result": "example",
  "content_restored": false
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `appeal_id` | integer | 是 | 接口中的 `appeal_id` 字段 | `1` |
| `report_id` | integer | 是 | 接口中的 `report_id` 字段 | `1` |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `result` | string | 是 | 接口中的 `result` 字段 | `"example"` |
| `content_restored` | boolean | 否 | 接口中的 `content_restored` 字段 | `false` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 207. `PATCH /api/v1/admin/community/posts/{post_id}/moderation` 下架或恢复动态

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/community/posts/{post_id}/moderation`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `post_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "post_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "status": 1,
  "reason": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `reason` | string | 否 | maxLength=255 | 操作或审核原因 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "target_type": "post",
  "target_id": 1,
  "status": 1,
  "reason": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_type` | post/comment/paper_plane | 是 | 枚举=post, comment, paper_plane | 接口中的 `target_type` 字段 | `"post"` |
| `target_id` | integer | 是 | 接口中的 `target_id` 字段 | `1` |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `reason` | string | 否 | 操作或审核原因 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 208. `PATCH /api/v1/admin/community/comments/{comment_id}/moderation` 下架或恢复评论

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/community/comments/{comment_id}/moderation`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `comment_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "comment_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "status": 1,
  "reason": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `reason` | string | 否 | maxLength=255 | 操作或审核原因 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "target_type": "post",
  "target_id": 1,
  "status": 1,
  "reason": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_type` | post/comment/paper_plane | 是 | 枚举=post, comment, paper_plane | 接口中的 `target_type` 字段 | `"post"` |
| `target_id` | integer | 是 | 接口中的 `target_id` 字段 | `1` |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `reason` | string | 否 | 操作或审核原因 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 209. `PATCH /api/v1/admin/community/paper-planes/{plane_id}/moderation` 下架或恢复纸飞机

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/community/paper-planes/{plane_id}/moderation`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `plane_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "plane_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "status": 1,
  "reason": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `reason` | string | 否 | maxLength=255 | 操作或审核原因 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "target_type": "post",
  "target_id": 1,
  "status": 1,
  "reason": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `target_type` | post/comment/paper_plane | 是 | 枚举=post, comment, paper_plane | 接口中的 `target_type` 字段 | `"post"` |
| `target_id` | integer | 是 | 接口中的 `target_id` 字段 | `1` |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `reason` | string | 否 | 操作或审核原因 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 210. `PATCH /api/v1/admin/users/{user_id}/certifications/{kind}/review` 审核用户资质认证

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/users/{user_id}/certifications/{kind}/review`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `user_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |
| `kind` | path | string | 是 | pattern=^(education|house|marriage)$ | 接口参数 | `"example"` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "user_id": 1,
  "kind": "example"
}
```

### 请求体 JSON 示例

```json
{
  "status": 2,
  "reason": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | 2/3 | 是 | 枚举=2, 3 | 当前业务状态，具体枚举见本接口约束 | `2` |
| `reason` | string | 否 | maxLength=255 | 操作或审核原因 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "user_id": 1,
  "kind": "education",
  "status": 2,
  "reason": "example"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `kind` | education/house/marriage | 是 | 枚举=education, house, marriage | 接口中的 `kind` 字段 | `"education"` |
| `status` | 2/3 | 是 | 枚举=2, 3 | 当前业务状态，具体枚举见本接口约束 | `2` |
| `reason` | string | 是 | 操作或审核原因 | `"example"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 211. `GET /api/v1/admin/matchmaker/service-requests` 管理员查询牵线服务申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/matchmaker/service-requests`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `status` | query | integer | 否 | 无额外约束 | 接口参数 | `1` |
| `page` | query | integer | 否 | minimum=1；maximum=1000 | 接口参数 | `1` |
| `page_size` | query | integer | 否 | minimum=1；maximum=50 | 接口参数 | `20` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "status": 1,
  "page": 1,
  "page_size": 20
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "items": [
    {
      "id": 1,
      "user_id": 1,
      "matchmaker_id": 1,
      "service_type": 1,
      "status": 0,
      "order_id": 1,
      "product_id": 1,
      "requirement": "example",
      "feedback": "example",
      "created_at": "2026-08-05T10:00:00+08:00",
      "updated_at": "2026-08-05T10:00:00+08:00",
      "start_at": "2026-08-05T10:00:00+08:00",
      "end_at": "2026-08-05T10:00:00+08:00"
    }
  ],
  "page": 1,
  "page_size": 1,
  "total": 1,
  "has_more": true
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `items` | array<object> | 是 | 接口中的 `items` 字段 | `[{"id": 1, "user_id": 1, "matchmaker_id": 1, "service_type": 1, "status": 0, "order_id": 1, "product_id": 1, "requirement": "example", "feedback": "example", "created_at": "2026-08-05T10:00:00+08:00", "updated_at": "2026-08-05T10:00:00+08:00", "start_at": "2026-08-05T10:00:00+08:00", "end_at": "2026-08-05T10:00:00+08:00"}]` |
| `items[].id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `items[].user_id` | integer | 是 | 用户 ID | `1` |
| `items[].matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `items[].service_type` | 1/3 | 是 | 枚举=1, 3 | 接口中的 `items[].service_type` 字段 | `1` |
| `items[].status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `items[].order_id` | integer | 是 | 订单 ID | `1` |
| `items[].product_id` | integer | 是 | 商品 ID | `1` |
| `items[].requirement` | string | 是 | 接口中的 `items[].requirement` 字段 | `"example"` |
| `items[].feedback` | string | 是 | 处理反馈或结果说明 | `"example"` |
| `items[].created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `items[].start_at` | string | 是 | 开始时间 | `"2026-08-05T10:00:00+08:00"` |
| `items[].end_at` | string | 是 | 结束时间 | `"2026-08-05T10:00:00+08:00"` |
| `page` | integer | 是 | 页码，从 1 开始 | `1` |
| `page_size` | integer | 是 | 每页记录数 | `1` |
| `total` | integer | 是 | 符合条件的记录总数 | `1` |
| `has_more` | boolean | 是 | 是否还有下一页 | `true` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 212. `PATCH /api/v1/admin/matchmaker/service-requests/{service_id}` 管理员分配或处理牵线服务申请

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/matchmaker/service-requests/{service_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `service_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "service_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "matchmaker_id": 1,
  "status": 0,
  "feedback": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `matchmaker_id` | integer | 否 | minimum=1.0 | 红娘用户 ID | `1` |
| `status` | 0/1/2/3 | 否 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `feedback` | string | 否 | maxLength=2000 | 处理反馈或结果说明 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "user_id": 1,
  "matchmaker_id": 1,
  "service_type": 1,
  "status": 0,
  "order_id": 1,
  "product_id": 1,
  "requirement": "example",
  "feedback": "example",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00",
  "start_at": "2026-08-05T10:00:00+08:00",
  "end_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `service_type` | 1/3 | 是 | 枚举=1, 3 | 接口中的 `service_type` 字段 | `1` |
| `status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `order_id` | integer | 是 | 订单 ID | `1` |
| `product_id` | integer | 是 | 商品 ID | `1` |
| `requirement` | string | 是 | 接口中的 `requirement` 字段 | `"example"` |
| `feedback` | string | 是 | 处理反馈或结果说明 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `start_at` | string | 是 | 开始时间 | `"2026-08-05T10:00:00+08:00"` |
| `end_at` | string | 是 | 结束时间 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 213. `POST /api/v1/admin/matchmaker/meetings/requests/{request_id}/schedule` 安排约会

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/matchmaker/meetings/requests/{request_id}/schedule`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `request_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "request_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "organizer_id": 1,
  "organization_id": 1,
  "scheduled_at": "2026-08-05T10:00:00+08:00",
  "location": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `organizer_id` | integer | 是 | minimum=1.0 | 接口中的 `organizer_id` 字段 | `1` |
| `organization_id` | integer | 否 | minimum=1.0 | 组织/门店 ID | `1` |
| `scheduled_at` | string | 是 | 按 Schema 类型校验 | 接口中的 `scheduled_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `location` | string | 是 | minLength=1; maxLength=255 | 接口中的 `location` 字段 | `"example"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "request_id": 1,
  "organizer_id": 1,
  "organization_id": 1,
  "scheduled_at": "2026-08-05T10:00:00+08:00",
  "location": "example",
  "status": "SCHEDULED",
  "cancel_reason": "example",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `request_id` | integer | 是 | 接口中的 `request_id` 字段 | `1` |
| `organizer_id` | integer | 是 | 接口中的 `organizer_id` 字段 | `1` |
| `organization_id` | integer | 是 | 组织/门店 ID | `1` |
| `scheduled_at` | string | 是 | 接口中的 `scheduled_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `location` | string | 是 | 接口中的 `location` 字段 | `"example"` |
| `status` | SCHEDULED/REMINDED/CHECKED_IN/COMPLETED/CANCELLED/NO_SHOW | 是 | 枚举=SCHEDULED, REMINDED, CHECKED_IN, COMPLETED, CANCELLED, NO_SHOW | 当前业务状态，具体枚举见本接口约束 | `"SCHEDULED"` |
| `cancel_reason` | string | 是 | 接口中的 `cancel_reason` 字段 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 214. `GET /api/v1/admin/finance/commission-rules` 查询分成规则

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/finance/commission-rules`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "beneficiary_type": "example",
    "name": "example",
    "mode": "example",
    "fixed_amount": "11",
    "rate_percent": "11",
    "priority": 1,
    "version": 1,
    "status": 1,
    "created_at": "2026-08-05T10:00:00+08:00"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 215. `POST /api/v1/admin/finance/commission-rules` 创建分成规则

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/finance/commission-rules`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "beneficiary_type": "service_matchmaker",
  "name": "example",
  "mode": "fixed",
  "fixed_amount": 1.2,
  "rate_percent": 1.2,
  "priority": 0
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `beneficiary_type` | service_matchmaker/store/promoter/partner | 是 | 枚举=service_matchmaker, store, promoter, partner | 接口中的 `beneficiary_type` 字段 | `"service_matchmaker"` |
| `name` | string | 是 | minLength=1; maxLength=128 | 展示名称 | `"example"` |
| `mode` | fixed/rate | 是 | 枚举=fixed, rate | 接口中的 `mode` 字段 | `"fixed"` |
| `fixed_amount` | number | 否 | minimum=0.0 | 接口中的 `fixed_amount` 字段 | `1.2` |
| `rate_percent` | number | 否 | minimum=0.0; maximum=100.0 | 接口中的 `rate_percent` 字段 | `1.2` |
| `priority` | integer | 否 | minimum=0.0; maximum=100000.0 | 接口中的 `priority` 字段 | `0` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "beneficiary_type": "example",
  "name": "example",
  "mode": "example",
  "fixed_amount": "11",
  "rate_percent": "11",
  "priority": 1,
  "version": 1,
  "status": 1,
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `beneficiary_type` | string | 是 | 接口中的 `beneficiary_type` 字段 | `"example"` |
| `name` | string | 是 | 展示名称 | `"example"` |
| `mode` | string | 是 | 接口中的 `mode` 字段 | `"example"` |
| `fixed_amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 接口中的 `fixed_amount` 字段 | `"11"` |
| `rate_percent` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 接口中的 `rate_percent` 字段 | `"11"` |
| `priority` | integer | 是 | 接口中的 `priority` 字段 | `1` |
| `version` | integer | 是 | 接口中的 `version` 字段 | `1` |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 216. `POST /api/v1/admin/finance/product-commission-rules/{product_id}` 配置商品分成对象

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/finance/product-commission-rules/{product_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `product_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "product_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "beneficiary_type": "service_matchmaker",
  "mode": "fixed",
  "fixed_amount": 1.2,
  "rate_percent": 1.2
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `beneficiary_type` | service_matchmaker/store/promoter/partner | 是 | 枚举=service_matchmaker, store, promoter, partner | 接口中的 `beneficiary_type` 字段 | `"service_matchmaker"` |
| `mode` | fixed/rate | 是 | 枚举=fixed, rate | 接口中的 `mode` 字段 | `"fixed"` |
| `fixed_amount` | number | 否 | minimum=0.0 | 接口中的 `fixed_amount` 字段 | `1.2` |
| `rate_percent` | number | 否 | minimum=0.0; maximum=100.0 | 接口中的 `rate_percent` 字段 | `1.2` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "product_id": 1,
  "beneficiary_type": "example",
  "mode": "example",
  "fixed_amount": "11",
  "rate_percent": "11",
  "version": 1,
  "status": 1,
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `product_id` | integer | 是 | 商品 ID | `1` |
| `beneficiary_type` | string | 是 | 接口中的 `beneficiary_type` 字段 | `"example"` |
| `mode` | string | 是 | 接口中的 `mode` 字段 | `"example"` |
| `fixed_amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 接口中的 `fixed_amount` 字段 | `"11"` |
| `rate_percent` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 接口中的 `rate_percent` 字段 | `"11"` |
| `version` | integer | 是 | 接口中的 `version` 字段 | `1` |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 217. `GET /api/v1/admin/finance/report` 查询分成汇总报表

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/finance/report`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "beneficiary_type": "example",
    "beneficiary_id": 1,
    "order_count": 1,
    "total_amount": "11",
    "pending_amount": "11",
    "available_amount": "11"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 218. `POST /api/v1/admin/finance/orders/{order_id}/settle` 结算已支付订单分成

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/finance/orders/{order_id}/settle`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `order_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "order_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "order_id": 1,
    "beneficiary_type": "example",
    "beneficiary_id": 1,
    "base_amount": "11",
    "amount": "11",
    "status": "example",
    "created_at": "2026-08-05T10:00:00+08:00"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 219. `POST /api/v1/admin/finance/orders/{order_id}/refund` 退款并冲正分成

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/finance/orders/{order_id}/refund`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `order_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "order_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "reason": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `reason` | string | 是 | minLength=1; maxLength=255 | 操作或审核原因 | `"example"` |

### 成功返回示例

成功状态码：`204`。

无返回体。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 220. `POST /api/v1/admin/finance/commission-entries/{entry_id}/release` 释放待结算分成

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/finance/commission-entries/{entry_id}/release`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `entry_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "entry_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "order_id": 1,
  "beneficiary_type": "example",
  "beneficiary_id": 1,
  "base_amount": "11",
  "amount": "11",
  "status": "example",
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `order_id` | integer | 是 | 订单 ID | `1` |
| `beneficiary_type` | string | 是 | 接口中的 `beneficiary_type` 字段 | `"example"` |
| `beneficiary_id` | integer | 是 | 接口中的 `beneficiary_id` 字段 | `1` |
| `base_amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 接口中的 `base_amount` 字段 | `"11"` |
| `amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 金额，服务端按货币最小单位/定点值处理 | `"11"` |
| `status` | string | 是 | 当前业务状态，具体枚举见本接口约束 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 221. `PATCH /api/v1/admin/finance/withdrawals/{withdrawal_id}` 审核提现

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/admin/finance/withdrawals/{withdrawal_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `withdrawal_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "withdrawal_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "status": "APPROVED",
  "failure_reason": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `status` | APPROVED/REJECTED/PROCESSING/SUCCEEDED/FAILED | 是 | 枚举=APPROVED, REJECTED, PROCESSING, SUCCEEDED, FAILED | 当前业务状态，具体枚举见本接口约束 | `"APPROVED"` |
| `failure_reason` | string | 否 | maxLength=255 | 接口中的 `failure_reason` 字段 | `"example"` |

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "account_type": "example",
  "account_id": 1,
  "amount": "11",
  "status": "example",
  "payee_masked": "example",
  "failure_reason": "example",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `account_type` | string | 是 | 接口中的 `account_type` 字段 | `"example"` |
| `account_id` | integer | 是 | 接口中的 `account_id` 字段 | `1` |
| `amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 金额，服务端按货币最小单位/定点值处理 | `"11"` |
| `status` | string | 是 | 当前业务状态，具体枚举见本接口约束 | `"example"` |
| `payee_masked` | string | 是 | 接口中的 `payee_masked` 字段 | `"example"` |
| `failure_reason` | string | 是 | 接口中的 `failure_reason` 字段 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 222. `GET /api/v1/organizations/stores` 查询门店

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/organizations/stores`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "code": "example",
    "name": "example",
    "display_name": "example",
    "region_code": "example",
    "status": 1,
    "auto_redirect": true,
    "created_at": "2026-08-05T10:00:00+08:00"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 223. `POST /api/v1/organizations/stores` 创建门店

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/organizations/stores`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "code": "example",
  "name": "example",
  "display_name": "example",
  "region_code": "example",
  "auto_redirect": false
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `code` | string | 是 | minLength=2; maxLength=64; pattern=^[A-Za-z0-9_-]+$ | 业务编码或地区编码 | `"example"` |
| `name` | string | 是 | minLength=1; maxLength=128 | 展示名称 | `"example"` |
| `display_name` | string | 否 | maxLength=128 | 接口中的 `display_name` 字段 | `"example"` |
| `region_code` | string | 否 | maxLength=64 | 接口中的 `region_code` 字段 | `"example"` |
| `auto_redirect` | boolean | 否 | 按 Schema 类型校验 | 接口中的 `auto_redirect` 字段 | `false` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "code": "example",
  "name": "example",
  "display_name": "example",
  "region_code": "example",
  "status": 1,
  "auto_redirect": true,
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `code` | string | 是 | 业务编码或地区编码 | `"example"` |
| `name` | string | 是 | 展示名称 | `"example"` |
| `display_name` | string | 是 | 接口中的 `display_name` 字段 | `"example"` |
| `region_code` | string | 是 | 接口中的 `region_code` 字段 | `"example"` |
| `status` | 1/2/3 | 是 | 枚举=1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `auto_redirect` | boolean | 是 | 接口中的 `auto_redirect` 字段 | `true` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 224. `GET /api/v1/organizations/stores/{store_id}` 查询门店详情

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/organizations/stores/{store_id}`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `store_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "store_id": 1
}
```

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "id": 1,
  "code": "example",
  "name": "example",
  "display_name": "example",
  "region_code": "example",
  "status": 1,
  "auto_redirect": true,
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `code` | string | 是 | 业务编码或地区编码 | `"example"` |
| `name` | string | 是 | 展示名称 | `"example"` |
| `display_name` | string | 是 | 接口中的 `display_name` 字段 | `"example"` |
| `region_code` | string | 是 | 接口中的 `region_code` 字段 | `"example"` |
| `status` | 1/2/3 | 是 | 枚举=1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `auto_redirect` | boolean | 是 | 接口中的 `auto_redirect` 字段 | `true` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 225. `POST /api/v1/organizations/stores/{store_id}/members` 添加门店成员

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/organizations/stores/{store_id}/members`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

| 参数 | 位置 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- | --- |
| `store_id` | path | integer | 是 | minimum=1 | 接口参数 | `1` |

### Path/Query/Header 参数 JSON 示例

```json
{
  "store_id": 1
}
```

### 请求体 JSON 示例

```json
{
  "user_id": 1,
  "role_code": "store_manager"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | minimum=1.0 | 用户 ID | `1` |
| `role_code` | store_manager/store_matchmaker | 是 | 枚举=store_manager, store_matchmaker | 接口中的 `role_code` 字段 | `"store_manager"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "organization_id": 1,
  "user_id": 1,
  "role_code": "example",
  "status": 1,
  "started_at": "2026-08-05T10:00:00+08:00",
  "ended_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `organization_id` | integer | 是 | 组织/门店 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `role_code` | string | 是 | 接口中的 `role_code` 字段 | `"example"` |
| `status` | 1/2/3 | 是 | 枚举=1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `started_at` | string | 是 | 接口中的 `started_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `ended_at` | string | 是 | 接口中的 `ended_at` 字段 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 226. `POST /api/v1/organizations/assignments` 分派会员资源

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/organizations/assignments`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "user_id": 1,
  "organization_id": 1,
  "matchmaker_id": 1,
  "source": "manual"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | integer | 是 | minimum=1.0 | 用户 ID | `1` |
| `organization_id` | integer | 否 | minimum=1.0 | 组织/门店 ID | `1` |
| `matchmaker_id` | integer | 否 | minimum=1.0 | 红娘用户 ID | `1` |
| `source` | manual/promotion/default/self_created | 否 | 枚举=manual, promotion, default, self_created | 接口中的 `source` 字段 | `"manual"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "user_id": 1,
  "organization_id": 1,
  "matchmaker_id": 1,
  "source": "example",
  "status": 1,
  "effective_at": "2026-08-05T10:00:00+08:00",
  "ended_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `organization_id` | integer | 是 | 组织/门店 ID | `1` |
| `matchmaker_id` | integer | 是 | 红娘用户 ID | `1` |
| `source` | string | 是 | 接口中的 `source` 字段 | `"example"` |
| `status` | 1/2 | 是 | 枚举=1, 2 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `effective_at` | string | 是 | 接口中的 `effective_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `ended_at` | string | 是 | 接口中的 `ended_at` 字段 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 227. `POST /api/v1/promotions/touches` 创建推广触点

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/promotions/touches`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "code": "example",
  "promoter_id": 1,
  "partner_team_id": 1
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `code` | string | 是 | minLength=6; maxLength=128; pattern=^[A-Za-z0-9_-]+$ | 业务编码或地区编码 | `"example"` |
| `promoter_id` | integer | 是 | minimum=1.0 | 接口中的 `promoter_id` 字段 | `1` |
| `partner_team_id` | integer | 否 | minimum=1.0 | 接口中的 `partner_team_id` 字段 | `1` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "code": "example",
  "promoter_id": 1,
  "partner_team_id": 1,
  "registered_user_id": 1,
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `code` | string | 是 | 业务编码或地区编码 | `"example"` |
| `promoter_id` | integer | 是 | 接口中的 `promoter_id` 字段 | `1` |
| `partner_team_id` | integer | 是 | 接口中的 `partner_team_id` 字段 | `1` |
| `registered_user_id` | integer | 是 | 接口中的 `registered_user_id` 字段 | `1` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 228. `POST /api/v1/promotions/attributions` 确认会员推广归属

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/promotions/attributions`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "code": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `code` | string | 是 | minLength=6; maxLength=128 | 业务编码或地区编码 | `"example"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "user_id": 1,
  "promoter_id": 1,
  "touch_id": 1,
  "status": 1,
  "effective_at": "2026-08-05T10:00:00+08:00",
  "ended_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `promoter_id` | integer | 是 | 接口中的 `promoter_id` 字段 | `1` |
| `touch_id` | integer | 是 | 接口中的 `touch_id` 字段 | `1` |
| `status` | 1/2/3 | 是 | 枚举=1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `effective_at` | string | 是 | 接口中的 `effective_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `ended_at` | string | 是 | 接口中的 `ended_at` 字段 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 229. `POST /api/v1/partners/teams` 创建合伙团队

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/partners/teams`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "owner_user_id": 1,
  "name": "example",
  "open_mode": "manual"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `owner_user_id` | integer | 是 | minimum=1.0 | 接口中的 `owner_user_id` 字段 | `1` |
| `name` | string | 是 | minLength=1; maxLength=128 | 展示名称 | `"example"` |
| `open_mode` | manual/paid | 否 | 枚举=manual, paid | 接口中的 `open_mode` 字段 | `"manual"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "owner_user_id": 1,
  "name": "example",
  "status": 1,
  "open_mode": "example",
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `owner_user_id` | integer | 是 | 接口中的 `owner_user_id` 字段 | `1` |
| `name` | string | 是 | 展示名称 | `"example"` |
| `status` | 1/2/3 | 是 | 枚举=1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `open_mode` | string | 是 | 接口中的 `open_mode` 字段 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 230. `POST /api/v1/partners/memberships` 加入合伙团队

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/partners/memberships`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "team_id": 1
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `team_id` | integer | 是 | minimum=1.0 | 接口中的 `team_id` 字段 | `1` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "team_id": 1,
  "promoter_id": 1,
  "status": 1,
  "joined_at": "2026-08-05T10:00:00+08:00",
  "left_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `team_id` | integer | 是 | 接口中的 `team_id` 字段 | `1` |
| `promoter_id` | integer | 是 | 接口中的 `promoter_id` 字段 | `1` |
| `status` | 1/2/3 | 是 | 枚举=1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `1` |
| `joined_at` | string | 是 | 接口中的 `joined_at` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `left_at` | string | 是 | 接口中的 `left_at` 字段 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 231. `POST /api/v1/finance/orders` 创建待支付订单

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/finance/orders`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "product_type": 1,
  "product_name": "example",
  "amount": 1.2
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `product_type` | integer | 是 | minimum=1.0; maximum=32.0 | 接口中的 `product_type` 字段 | `1` |
| `product_name` | string | 是 | minLength=1; maxLength=128 | 接口中的 `product_name` 字段 | `"example"` |
| `amount` | number | 是 | maximum=1000000.0 | 金额，服务端按货币最小单位/定点值处理 | `1.2` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "order_no": "example",
  "user_id": 1,
  "product_type": 1,
  "product_name": "example",
  "amount": "11",
  "status": 0,
  "pay_time": "2026-08-05T10:00:00+08:00",
  "created_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `order_no` | string | 是 | 业务订单号，用于后续查询或支付 | `"example"` |
| `user_id` | integer | 是 | 用户 ID | `1` |
| `product_type` | integer | 是 | 接口中的 `product_type` 字段 | `1` |
| `product_name` | string | 是 | 接口中的 `product_name` 字段 | `"example"` |
| `amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 金额，服务端按货币最小单位/定点值处理 | `"11"` |
| `status` | 0/1/2/3 | 是 | 枚举=0, 1, 2, 3 | 当前业务状态，具体枚举见本接口约束 | `0` |
| `pay_time` | string | 是 | 接口中的 `pay_time` 字段 | `"2026-08-05T10:00:00+08:00"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 232. `GET /api/v1/finance/balance` 查询我的余额

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/finance/balance`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{
  "account_type": "example",
  "account_id": 1,
  "pending_amount": "11",
  "available_amount": "11"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `account_type` | string | 是 | 接口中的 `account_type` 字段 | `"example"` |
| `account_id` | integer | 是 | 接口中的 `account_id` 字段 | `1` |
| `pending_amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 接口中的 `pending_amount` 字段 | `"11"` |
| `available_amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 接口中的 `available_amount` 字段 | `"11"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 233. `GET /api/v1/finance/commission-entries` 查询我的分成明细

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/finance/commission-entries`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
[
  {
    "id": 1,
    "order_id": 1,
    "beneficiary_type": "example",
    "beneficiary_id": 1,
    "base_amount": "11",
    "amount": "11",
    "status": "example",
    "created_at": "2026-08-05T10:00:00+08:00"
  }
]
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

无数据时返回：`[]`。

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 234. `POST /api/v1/finance/withdrawals` 申请提现

**基本信息**：需要登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/api/v1/finance/withdrawals`

```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <access-token>"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体 JSON 示例

```json
{
  "amount": 1.2,
  "payee_masked": "example"
}
```

### 请求体字段

| 字段 | 类型 | 必填 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `amount` | number | 是 | maximum=1000000.0 | 金额，服务端按货币最小单位/定点值处理 | `1.2` |
| `payee_masked` | string | 是 | minLength=1; maxLength=128 | 接口中的 `payee_masked` 字段 | `"example"` |

### 成功返回示例

成功状态码：`201`。

```json
{
  "id": 1,
  "account_type": "example",
  "account_id": 1,
  "amount": "11",
  "status": "example",
  "payee_masked": "example",
  "failure_reason": "example",
  "created_at": "2026-08-05T10:00:00+08:00",
  "updated_at": "2026-08-05T10:00:00+08:00"
}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `id` | integer | 是 | 当前记录的唯一 ID | `1` |
| `account_type` | string | 是 | 接口中的 `account_type` 字段 | `"example"` |
| `account_id` | integer | 是 | 接口中的 `account_id` 字段 | `1` |
| `amount` | string | 是 | pattern=^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$ | 金额，服务端按货币最小单位/定点值处理 | `"11"` |
| `status` | string | 是 | 当前业务状态，具体枚举见本接口约束 | `"example"` |
| `payee_masked` | string | 是 | 接口中的 `payee_masked` 字段 | `"example"` |
| `failure_reason` | string | 是 | 接口中的 `failure_reason` 字段 | `"example"` |
| `created_at` | string | 是 | 创建时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |
| `updated_at` | string | 是 | 最后更新时间，ISO 8601 格式 | `"2026-08-05T10:00:00+08:00"` |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

## 235. `GET /` Root

**基本信息**：无需登录；成功状态码以 OpenAPI 为准。

### 请求地址和 Headers

请求地址：`http://127.0.0.1:8000/`

```json
{
  "Content-Type": "application/json"
}
```

### 请求参数

无 Path、Query、Header 参数。

### 请求体

无请求体。

### 成功返回示例

成功状态码：`200`。

```json
{}
```

### 返回字段

| 字段 | 类型 | 必返 | 约束 | 含义 | 示例 |
| --- | --- | --- | --- | --- | --- |

### 错误返回示例

```json
{"detail":"参数校验失败"}
```

---

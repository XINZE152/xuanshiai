# 2026-09-17

## 前端按钮无响应排查（部分修复，未端到端验收）
- 前端 src/lib/admin-api.ts 的 adminApi 与 downloadAdminFile 改用已有 /api/backend/* 同源代理，保持接口路径、Token 和参数不变；媒体 URL 解析未改。
- src/app/api/backend/[...path]/route.ts 修复 DELETE 请求体被丢弃。不改 UI，不提交/发布。
- TypeScript 检查通过（代理 DELETE 改动前）。独立行为测试因命令转义损坏未执行成功，不计通过。
- 单次远端 POST /api/v1/admin/matchmaker/auth/login 空 JSON 返回 422，明确缺 username/password，证明路径存在，不证明真实登录成功。
- 当前本地 127.0.0.1:3000 连接拒绝；浏览器点击验收未完成。此前 localhost CORS 通过而 127.0.0.1 被拒绝，不能将所有按钮故障都归因于 CORS。
- 当前 system-feedback 仍为 mockData 空数组，提交/查看无事件；reg-user-cancel 已调用后台账号注销列表/审核接口，不能再按会员注销重建。
- 纠正早前检查口径：页面存在 API 调用不等于每个按钮已完成接线；需具体页面与按钮逐项验收。
- 两处修改完成后再次运行 TypeScript 检查，退出码 0；登录页已有 catch/setError 与可见错误区域。

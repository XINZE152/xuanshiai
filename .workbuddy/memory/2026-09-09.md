# 2026-09-09

## UI配置项清单逐项核对（配合管理端 16 页接线）
- admin_config.py 再增 16 个配置域：econtract_config（含4条默认键值）/econtract_records/econtract_templates/econtract_seals、outbound_seats/outbound_call_records、sms_broadcasts/sms_send_records、wechat_mp（含 platform_templates 清单）/wechat_mp_fans/wechat_mp_menu（top_menus+sub_menus+published_at）/wechat_mp_replies（follow/keyword/message 三组）/wechat_mp_templates/wechat_mp_broadcasts、wechat_mini（realname_enabled）、admin_groups。
- 新增 `GET /admin/matchmaker/audit-logs`（schemas+services+routes matchmaker_admin_account）：business_audit_log 按 action 前缀/操作人/时间/关键词分页，join matchmaker_admin_account 取操作人名。支撑管理端系统日志页各 Tab（登录日志走既有 /accounts/login-logs）。
- runtime_config.py 新增 mini_realname_enabled；auth.submit_realname 开头检查（关闭→403"实名认证功能暂未开放"，默认 True 行为不变）。
- 后台账号无物理删除路由（仅 status 停用 1/2/3）；无分组表（分组存 admin_groups 配置域，创建账号时组权限注入账号 permissions）。
- 验证：py_compile 全过；pytest 301 passed / 4 failed（既有：ai_avatar FK 断言、community 幂等表2例、MatchmakerStaffCreate 无 password 属性，均与改动无关）。

## 后端全量体检（应用户要求"统统检查一遍"）
- 全量编译 158 个 .py 全过；全量导入 159 模块（含 app.main）0 失败；全量单测 301 passed / 13 skipped / 4 failed。
- 4 个失败经 git stash 验证为**既有问题**（stash 后照样失败）：test_ai_avatar（断言 database_setup_marriage.py 内 FK 文本片段）、test_community_features 幂等表 2 例（同样解析建表脚本）、test_matchmaker_staff_admin（MatchmakerStaffCreate 无 password 属性，schema 与用例不匹配）。均与本轮改动无关。
- 人工复读全部 diff（auth/discovery/community/finance/admin_config/runtime_config）逻辑无误；清理一处死导入（finance.py 的 refund_manual_review——后端无用户端自动退款路径，refund_order 本身就是管理员手动操作，无可挂接分支，按"不造假消费"删除导入），财务单测 3 passed。
- 已向用户明示两个语义点：①权限页 line.beLinked 前端默认 "no"，管理员保存权限页后"未实名不允许被牵线"真正生效（旧后端无此检查，属 UI 语义落地而非错误）；②finance 配置 withdrawal.enabled 前端默认 false，保存财务系统配置后提现被拦需后台开启。
- 临时脚本 _import_check.py 已删除，工作区干净（8 个修改文件 + runtime_config.py 新文件）。

## 配置消费改造：把 platform/sys/finance 配置接进运行时（P0 接线）
- 新增 `services/runtime_config.py` 运行期消费层（load_config/runtime_bool/match_line_permissions/withdrawal_policy 等，缺失回退默认不抛错）。
- 已接通（管理员改动即生效，默认=旧行为）：①注册开关(platform_operation+sys_access 双域)在 auth get_or_create_user_by_phone/login_wechat 新号创建处；②浏览开关在 discovery get_discovery_page/search_discovery/view_profile；③权限页卡片4 line.realname(未实名禁牵线)、beLinked(向未实名对象发起拦截,保存分组后生效)、times(>0 统一每日上限，community 配额展示同步)；④other.multiAccount 一证多号在 submit_realname；⑤finance.withdrawal enabled/min_amount 在 request_withdrawal（⚠️ finance 默认 withdrawal.enabled=False，部署后提现被拦需后台开启）。
- 明确语义：permissions 前端存 browse/view/state/line/other 嵌套键，后端旧扁平配额键(8处)仍在 discovery/community 被读但页面无控件可改；profile_tags 字典、config_membership_package 价格表为真实来源不走 admin_config。
- 单测：auth/discovery/community/profile 4 文件 67 passed（2 failed 为解析 database_setup_marriage.py 文本的既有用例）。
- 审计文档 `docs/配置消费改造-接线与逐项审计-2026-09-09.md`（平台14域+sys10域+finance 逐项状态、接法建议、P0=用户端公开配置下发接口）。

## 财务管理后端补充（配合管理端财务 5 页接线）
- `schemas/finance.py` 新增 `FinanceDailyRow`；`services/finance.py` 新增 `admin_revenue_daily_report`（payment_order 按 DATE(pay_time) 聚合，status=1 收入/status=3 退款笔数与金额，支持 start_date/end_date）；`routes/finance.py` 新增 `GET /admin/finance/daily-report`。
- `admin_list_orders/admin_list_withdrawals/admin_list_ledger` 补充 Query：orders 原仅 status/user_id/order_no，现支持 start_time/end_time(created_at 范围)；withdrawals 新增 account_id 与时间范围；ledger 新增时间范围。
- 前端 finance-config 读写的 `finance` 配置域仍无运行时消费（支付走 settings.wechat_payment_mode）；审计建议 P0：提现 enabled/min_amount 在 request_withdrawal 校验、refund.manual_review 在 refund_order 消费。
- 一期财务真实表：payment_order/account_ledger/withdrawal_request/commission_rule/commission_entry/product_commission_config；用户端可 POST /finance/orders、/finance/withdrawals。

## 系统管理 sys_* 配置域新增（管理端纯配置页接线）
- 配合管理端系统管理纯配置页新增 10 个配置域：sys_site/sys_access/sys_storage/sys_payment/sys_watermark/sys_posters/sys_region/sys_ads/sys_outbound/sys_sms（services/admin_config.py）。仅为管理端占位存储，无运行时消费；后端真实能力仍是 settings(mock)：wechat_payment_mode/sms_provider=disabled|mock。
- 注意 config_banner 是社区模块专用（community_admin），与页面"广告管理"(sys_ads)无关；省市区在 regions 路由/表；后台账号在 matchmaker_admin_account。

## 平台配置后端执行逐项审计（重要）
- 用户担心"后端代码是否真的按配置执行"。核查结论：**后端运行时消费配置的唯一入口是 `get_runtime_value`，全后端仅 discovery.py(6处) + community.py(2处) 调用，全部读 `platform_permissions` 配额类扁平键**（free_browse_daily_limit/high_match_browse_bonus/free|vip_apply_daily_limit/free|vip_superlike_daily_limit/paper_plane_daily_limit）。
- 前端 7 个平台配置页面保存的是嵌套分组键（如 permissions 的 browse/view/state/line/other、nav sections、layout home/member/team/pc），与后端读取键**不对应** → 页面改动实际不生效。
- 用户端资料下拉字典来自写死常量 `app/core/profile_tags.py`（TAG_CATEGORIES 20组）；VIP 套餐/价格来自 `config_membership_package` 表；均不走 admin_config。无用户端公共接口下发 platform_* 配置（配置读取均在 /admin/configs/* 需权限）。
- 已产出审计报告 `docs/平台配置-后端执行逐项审计报告-2026-09-09.md`，含逐页面/逐配置域消费点、汇总表、根因与 P0-P3 修复建议。

## admin_config 平台配置域扩展（配合管理端平台配置 7 页接线）
- `app/services/admin_config.py` DEFAULT_CONFIGS 扩展既有 platform_*（platform_basic 文案/籍贯文本、platform_operation mode、platform_navigation sections/icon_rows、platform_layout home/member/team/pc、platform_permissions browse/view/state/line/other、platform_content、platform_base_data、platform_pay），并新增 6 个配置域：platform_register_guide、platform_register_fields、platform_private_fields、platform_filter_config、platform_member_states、platform_custom_pages。
- ensure_defaults 采用「合并缺失键」策略，不覆盖存量值；老 DB 行读取时自动补齐新键。配置接口复用 /admin/configs/{namespace} GET/PATCH，无需新增路由。
- 前端管理端（E:\HTML\xuanshiai-admin）新增 src/lib/platform-config.ts，所有平台配置页读写此机制并附带 /admin/common/upload 图片上传。

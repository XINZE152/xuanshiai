# 2026-09-11 工作日志

## 管理后台全量摸底（前端 E:\HTML\xuanshiai-admin + 后端 E:\houduan\xuanshiai）

### 关键发现（必须记住）
1. **前端管理后台是「UI 壳」**：`src/app/(admin)/` 下共 **121 个 page.tsx**，其中只有 **37 个真的接了后端**，其余 **84 个是纯静态硬编码假数据**（典型如 `love-matchmaker-list/page.tsx` 直接 `const rows: Row[] = [{ name: "芸希老师" ... }]`）。
2. 已接线的 37 个分两类：
   - **13 个走业务接口**（`@/lib/admin-endpoints.ts` 或 `@/lib/admin-api.ts`）：home、reg-user-all、reg-user-log、love-customer-list、love-customer-statistics、customer-follow-up、love-customer-abandon、love-customer-abandon-log、love-user-list、love-user-vip、system-setting-admin-log、system-setting-admin-user、system-setting-admin-user-add
   - **24 个走通用配置域**（`@/lib/platform-config.ts` → `useConfigDomain(ns)` → `/admin/configs/{namespace}`）：platform-* 7 个 + system-setting-basic/adconfig + outbound-call-platform + out-call-list/record + sms-signature/notices/record + system-setting-admin-group + wechat-* 6 个 + miniprogram-config + e-contract-yinzhang
3. **`admin-endpoints.ts` 里已有封装但页面从未调用**：`apportionConfigs` / `commissionEntryList` / `promoterStaffList` / `commissionLevels` 等（grep 全仓库只命中定义处）。即「总店红娘、推广红娘」等页面的后端**接口早已就绪**，缺的只是前端接线。→ 我此前记忆中的"已完成"前端改造，在当前 E:\HTML 目录里并不存在。
4. **配置域页面多为"只存不消费"**：见 `docs/系统管理-后端执行逐项审计报告-2026-09-09.md`、`平台配置-...`、`财务管理-...`。全后端读 `admin_config_snapshot` 的运行时入口只有 `app/services/admin_config.py:232 get_runtime_value`，调用点仅 2 个文件 8 处，全在 `platform_permissions` 的**扁平配额键**；而前端保存的是**嵌套分组**，键名无交集 → 页面改了不生效。
5. **无表的功能域**（需新建表或复用通用内容域）：互选活动、商家联盟、短视频、喜讯、搭子社群、电子合同、公众号/小程序配置、短信模板/发送日志（仅 `admin_sms_statistics`）。
6. **现成的通用内容域机制**：`admin_content_item` 表（`domain` + `title/subtitle/image_url/amount/status/sort/extra_json`，`app/db/business_schema.py:756`）+ 通用 CRUD `/api/v1/admin/content/{domain}`（`app/api/routes/admin_content.py`，权限 `platform.config.read/write`）。喜讯/锦旗/搭子社群/商家/短视频等内容型页面**优先复用此机制，不要新建表**。

### 后端约定（复述，避免踩坑）
- 前缀 `/api/v1`；成功**不包 `data`**；错误走 `HTTPException` → `{"detail": "..."}`，**无业务错误码**；分页固定 `items/page/page_size/total/has_more`。
- 两套登录态并存：`get_current_admin`（C 端 user_role=admin）与 `get_current_matchmaker_admin`（`matchmaker_admin_account`，前端后台主用后者）。
- `app/api/dependencies.py:_matchmaker_admin_permission(request)` 按 path 子串映射权限点；新增路由要同步加映射。
- 建表脚本 `database_setup_marriage.py`，幂等：`_ensure_table_columns` 补列、`INSERT IGNORE` 种子；`init_all_tables` 内 `tables.update(BUSINESS_TABLES)`。

### 用户拍板（2026-09-11）
1. ⚫废弃页 **也要补全前后端**（覆盖 PROJECT_RULES 规则 6）。
2. 首批模块 = **M1 总店红娘**。
3. 24 个「只存不消费」的配置域页面 **算已完成联调，本期不动**。
4. 每批 **必带 docs/api 完整契约文档**（按 PROJECT_RULES 2.1.1）。

### M1 总店红娘 —— 已完成（2026-09-11）
**数据库变更**：`matchmaker_profile` 补 4 列（`slogan varchar(64)` / `sort int NOT NULL DEFAULT 0` / `contact_editable tinyint NOT NULL DEFAULT 1` / `lock_at datetime`）+ 索引 `idx_matchmaker_profile_sort(sort, deleted_at)`；落点 `app/db/business_schema.py` L707-711 与 `database_setup_marriage.py::_ensure_matchmaker_staff_defaults` 的 `_ensure_table_columns`。

**后端改动**：
- `app/schemas/matchmaker_staff_admin.py`：`MatchmakerStaffItem` / `Create` / `Update` 加 4 字段；`MatchmakerWorkReport` 加 5 指标（`new_member_count` / `lead_follow_up_count` / `meeting_request_count` / `meeting_arranged_count` / `offline_income: Decimal`）。
- `app/services/matchmaker_staff_admin.py`：SELECT 补 4 字段（旧行 NULL 回退默认）、create/update 写入、`work_report` 补 5 条统计 SQL。
- 测试 `tests/test_matchmaker_staff_admin_ext.py`（7 用例）；`pytest tests/test_matchmaker_staff_admin_ext.py tests/test_promoter_staff_admin.py -q` → **19 passed**（自己复核过）。

**5 个新指标的真实数据源**（关键，别再猜）：
- `new_member_count` → `resource_assignment(matchmaker_id, status=1, effective_at)` 区间计数（项目**没有** `user_profile` 表，会员归属只在 `resource_assignment`）
- `lead_follow_up_count` → `customer_lead_follow_up f JOIN customer_lead c ON c.id=f.lead_id`（表里**无** matchmaker_id，必须经 `customer_lead` 取归属）
- `meeting_request_count` → `meeting_request.matchmaker_id`
- `meeting_arranged_count` → `meeting_record r JOIN meeting_request q ON q.id=r.request_id`（`meeting_record` **无** matchmaker_id）
- `offline_income` → `payment_order(product_type='offline_vip', status=1, matchmaker_id, pay_time)` 聚合（**不是**无数据源）

**前端改动**（`E:\HTML\xuanshiai-admin`）：
- `src/lib/admin-endpoints.ts`：新增 ~18 个 M1 方法 + 相关类型（约 L676 起）。关键修正：`updateMatchmakerStaff` 用 **PUT**（文件内 `update()` helper 是 PATCH，不能直接用）；`apportionConfigs` 返回类型从假的 `ApportionConfigPayload[]` 改为 `ApportionConfig[]`；`toggleApportionConfig` 请求体从错误的 `enabled` 改为 **`is_enabled`**。
- 5 个页面：`love-matchmaker-list`(13 处调用)、`love-matchmaker-apportion2`(4)、`love-matchmaker-distribution`(3)、`love-matchmaker-distribution-details`(1) 已接真实接口；`love-matchmaker-apportion` 是**跳转壳**（`router.replace("/love-matchmaker-apportion2")`），已加注释说明，不写重复实现。
- `node ./node_modules/typescript/bin/tsc --noEmit` → 通过（注意：`node_modules/.bin/tsc` 在本机 shell 下报 `Cannot find module 'E:\typescript\bin\tsc'`，必须走 `node ./node_modules/typescript/bin/tsc`）。

**文档**：新建 `docs/api/matchmaker-staff.md`（19 接口完整契约）；补全 `docs/api/matchmaker-apportion-config.md`、`matchmaker-commission-levels.md`、`matchmaker-commission-entries.md`（加自检清单 + 变更记录）。

**M1 已知 UI↔后端差异（后续模块可能同类问题，记住）**：
- 分派配置 UI 的 `by-partner`（按合伙红娘分派）后端枚举**不支持** → 前端已禁止提交并提示。
- 红娘编辑抽屉的「微信二维码」后端**无字段** → 仅前端预览不提交。
- 「定时锁定」UI 只有开关，后端是 `lock_at datetime` → 前端补了 `datetime-local` 输入。
- 分成配置弹窗的「平台额外奖励支付方式」后端无字段 → 保留 UI 仅展示。

### 环境踩坑（重要）
- 本机 **Bash 工具不可用**（缺 `dirname`/`tail`），`PowerShell` 工具 stdout 不回显 → 验证命令要 **重定向到文件再用 Read 读**。
- 跑后端测试必须用 **`E:\houduan\xuanshiai\.venv\Scripts\python.exe`**；`C:\Users\21612\.workbuddy\binaries\python\versions\3.13.12\python.exe` **没装项目依赖**（无 pydantic），不能用来跑测试。

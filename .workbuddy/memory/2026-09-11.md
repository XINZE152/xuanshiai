# 2026-09-11 工作日志

## 管理后台全量摸底（前端 E:\HTML\xuanshiai-admin + 后端 E:\houduan\xuanshiai）

### 关键发现（必须记住）
1. **前端管理后台是「UI 壳」**：`src/app/(admin)/` 下共 **121 个 page.tsx**，其中只有 **37 个真的接了后端**，其余 **84 个是纯静态硬编码假数据**（典型如 `love-matchmaker-list/page.tsx` 直接 `const rows: Row[] = [{ name: "芸希老师" ... }]`）。
2. 已接线的 37 个分两类：
   - **13 个走业务接口**（`@/lib/admin-endpoints.ts` 或 `@/lib/admin-api.ts`）：home、reg-user-all、reg-user-log、love-customer-list、love-customer-statistics、customer-follow-up、love-customer-abandon、love-customer-abandon-log、love-user-list、love-user-vip、system-setting-admin-log、system-setting-admin-user、system-setting-admin-user-add
   - **24 个走通用配置域**（`@/lib/platform-config.ts` → `useConfigDomain(ns)` → `/admin/configs/{namespace}`）：platform-* 7 个 + system-setting-basic/adconfig + outbound-call-platform + out-call-list/record + sms-signature/notices/record + system-setting-admin-group + wechat-* 6 个 + miniprogram-config + e-contract-yinzhang
3. **`admin-endpoints.ts` 里已有封装但页面从未调用**：`apportionConfigs` / `commissionEntryList` / `promoterStaffList` / `commissionLevels` 等（grep 全仓库只命中定义处）。即「总店红娘、推广红娘」等页面的后端**接口早已就绪**，缺的只是前端接线。→ 我此前记忆中的"已完成"前端改造，在当前 E:\HTML 目录里并不存在。
4. **配置域页面多为"只存不消费"**：见 `docs/系统管理-后端执行逐项审计报告-2026-09-09.md`、`平台配置-...`、`财务管理-...`。全后端读 `admin_config_snapshot` 的运行时入口只有 `app/services/admin_config.py:232 get_runtime_value`，调用点仅 2 个文件 8 处，全在 `platform_permissions` 的**扁平配额键**；而前端保存的是**嵌套分组**，键名无交集 → 页面改了不生效。
5. **无表的功能域**（需新建表或复用通用内容域）：互选活动、商家联盟、短视频、喜讯、搭子社群、电子合同、公众号/小程序配置、短信模板/发送日志（仅 `admin_sms_statistics`）。
6. **现成的通用内容域机制**：`admin_content_item` 表（`domain` + `title/subtitle/image_url/amount/status/sort/extra_json`，`app/db/business_schema.py:756`）+ 通用 CRUD `/api/v1/admin/content/{domain}`（`app/api/routes/admin_content.py`，权限 `platform.config.read/write`）。喜讯/锦旗/搭子社群/商家/短视频等内容型页面**优先复用此机制，不要新建表**。

### 后端约定（复述，避免踩坑）
- 前缀 `/api/v1`；成功**不包 `data`**；错误走 `HTTPException` → `{"detail": "..."}`，**无业务错误码**；分页固定 `items/page/page_size/total/has_more`。
- 两套登录态并存：`get_current_admin`（C 端 user_role=admin）与 `get_current_matchmaker_admin`（`matchmaker_admin_account`，前端后台主用后者）。
- `app/api/dependencies.py:_matchmaker_admin_permission(request)` 按 path 子串映射权限点；新增路由要同步加映射。
- 建表脚本 `database_setup_marriage.py`，幂等：`_ensure_table_columns` 补列、`INSERT IGNORE` 种子；`init_all_tables` 内 `tables.update(BUSINESS_TABLES)`。

### 用户拍板（2026-09-11）
1. ⚫废弃页 **也要补全前后端**（覆盖 PROJECT_RULES 规则 6）。
2. 首批模块 = **M1 总店红娘**。
3. 24 个「只存不消费」的配置域页面 **算已完成联调，本期不动**。
4. 每批 **必带 docs/api 完整契约文档**（按 PROJECT_RULES 2.1.1）。

### M1 总店红娘 —— 已完成（2026-09-11）
**数据库变更**：`matchmaker_profile` 补 4 列（`slogan varchar(64)` / `sort int NOT NULL DEFAULT 0` / `contact_editable tinyint NOT NULL DEFAULT 1` / `lock_at datetime`）+ 索引 `idx_matchmaker_profile_sort(sort, deleted_at)`；落点 `app/db/business_schema.py` L707-711 与 `database_setup_marriage.py::_ensure_matchmaker_staff_defaults` 的 `_ensure_table_columns`。

**后端改动**：
- `app/schemas/matchmaker_staff_admin.py`：`MatchmakerStaffItem` / `Create` / `Update` 加 4 字段；`MatchmakerWorkReport` 加 5 指标（`new_member_count` / `lead_follow_up_count` / `meeting_request_count` / `meeting_arranged_count` / `offline_income: Decimal`）。
- `app/services/matchmaker_staff_admin.py`：SELECT 补 4 字段（旧行 NULL 回退默认）、create/update 写入、`work_report` 补 5 条统计 SQL。
- 测试 `tests/test_matchmaker_staff_admin_ext.py`（7 用例）；`pytest tests/test_matchmaker_staff_admin_ext.py tests/test_promoter_staff_admin.py -q` → **19 passed**（自己复核过）。

**5 个新指标的真实数据源**（关键，别再猜）：
- `new_member_count` → `resource_assignment(matchmaker_id, status=1, effective_at)` 区间计数（项目**没有** `user_profile` 表，会员归属只在 `resource_assignment`）
- `lead_follow_up_count` → `customer_lead_follow_up f JOIN customer_lead c ON c.id=f.lead_id`（表里**无** matchmaker_id，必须经 `customer_lead` 取归属）
- `meeting_request_count` → `meeting_request.matchmaker_id`
- `meeting_arranged_count` → `meeting_record r JOIN meeting_request q ON q.id=r.request_id`（`meeting_record` **无** matchmaker_id）
- `offline_income` → `payment_order(product_type='offline_vip', status=1, matchmaker_id, pay_time)` 聚合（**不是**无数据源）

**前端改动**（`E:\HTML\xuanshiai-admin`）：
- `src/lib/admin-endpoints.ts`：新增 ~18 个 M1 方法 + 相关类型（约 L676 起）。关键修正：`updateMatchmakerStaff` 用 **PUT**（文件内 `update()` helper 是 PATCH，不能直接用）；`apportionConfigs` 返回类型从假的 `ApportionConfigPayload[]` 改为 `ApportionConfig[]`；`toggleApportionConfig` 请求体从错误的 `enabled` 改为 **`is_enabled`**。
- 5 个页面：`love-matchmaker-list`(13 处调用)、`love-matchmaker-apportion2`(4)、`love-matchmaker-distribution`(3)、`love-matchmaker-distribution-details`(1) 已接真实接口；`love-matchmaker-apportion` 是**跳转壳**（`router.replace("/love-matchmaker-apportion2")`），已加注释说明，不写重复实现。
- `node ./node_modules/typescript/bin/tsc --noEmit` → 通过（注意：`node_modules/.bin/tsc` 在本机 shell 下报 `Cannot find module 'E:\typescript\bin\tsc'`，必须走 `node ./node_modules/typescript/bin/tsc`）。

**文档**：新建 `docs/api/matchmaker-staff.md`（19 接口完整契约）；补全 `docs/api/matchmaker-apportion-config.md`、`matchmaker-commission-levels.md`、`matchmaker-commission-entries.md`（加自检清单 + 变更记录）。

**M1 已知 UI↔后端差异（后续模块可能同类问题，记住）**：
- 分派配置 UI 的 `by-partner`（按合伙红娘分派）后端枚举**不支持** → 前端已禁止提交并提示。
- 红娘编辑抽屉的「微信二维码」后端**无字段** → 仅前端预览不提交。
- 「定时锁定」UI 只有开关，后端是 `lock_at datetime` → 前端补了 `datetime-local` 输入。
- 分成配置弹窗的「平台额外奖励支付方式」后端无字段 → 保留 UI 仅展示。

### M1 收尾补丁 —— 用户拍板「后端补枚举、后端补字段」（2026-09-11）
三处缺口全部由**后端补齐**（前端同步跟上），已落盘并验证：
1. **`by_partner` 枚举**：`app/schemas/matchmaker_admin.py:119 ApportionStrategy` 加 `"by_partner"`；`app/services/apportion_config_admin.py` 删掉 `customer_lead` 对 `by_region/by_promoter` 的 400 拦截（改为保留 by_partner 支持）。
2. **`matchmaker_profile.wechat_qr`**：`varchar(500)`，落点 `app/db/business_schema.py:712` + `database_setup_marriage.py:599` 幂等补列。
3. **`commission_level.platform_extra_pay_mode`**：`varchar(16) NOT NULL DEFAULT 'manual'`（manual 人工转账 / balance 转入余额），落点 `app/db/business_schema.py:668`；`commission_level_admin.py` 的 SELECT 与 payload 补齐（旧行 NULL 回退 `manual`）。
前端同步：`admin-endpoints.ts` 加 `by_partner`、`wechat_qr`、`platform_extra_pay_mode`；`love-matchmaker-apportion2` 的 `STRATEGY_MAP["by-partner"]` 改为 `"by_partner"` 并删掉拦截；`love-matchmaker-list` 提交/回填 `wechat_qr`；`love-matchmaker-distribution` 提交/回填 `platform_extra_pay_mode`。
> 仍有「客源线索 Tab 下 by_region/by_promoter/by_partner 不适用」的前端 disabled 逻辑（后端对 customer_lead 这几种策略语义不成立）。

### M2 推广红娘 —— 已完成（2026-09-11）
**后端（8 个新端点 + 1 个列表改造 + 1 个 bug 修复）**
- `app/api/routes/promoter_staff_admin.py`：新增 `GET /statistics`、`GET /teams`、`GET /commission-entries`、`GET /commission-entries/options`、`PUT /{user_id}/team`、`POST /{user_id}/poster`、`POST /{user_id}/platform-token`、`DELETE /{user_id}`；`GET ""` 加 4 个 query（`commission_level_id`/`team_id`/`visible`/`sort`）。**静态路径必须声明在 `/{user_id}` 之前**（已照 `/user-candidates` 的位置放）。
- `app/schemas/promoter_staff_admin.py`：`PromoterStaffItem` 扩到 25 字段（新增 `account`/`matchmaker_type`/`matchmaker_type_label`/`commission_level_id`/`commission_level_name`/`team_id`/`team_name`/`member_month`/`lead_total`/`lead_month`/`order_amount`/`visible`）；`PromoterStaffDetail` **去掉**重复声明的 `matchmaker_type`/`commission_level_id`（否则与 Item 冲突）；新增 `PromoterStatistics`/`PromoterTeamItem`/`PromoterTeamUpdate`/`PromoterCommissionEntryItem|Page|Options`/`PromoterPosterResponse`/`PromoterPlatformTokenResponse`/`PromoterDeleteResponse`；**修 bug**：`PromoterStaffCreate` 补 `phone` 字段（原 service 访问 `body.phone` 必 500）。
- `app/services/promoter_staff_admin.py`：抽出 `_FROM_BODY`（list 与 count 共用）；`_SELECT_BODY` 用相关子查询取聚合；`_money()` 统一 Decimal→字符串；`_item`/`_detail` 同步（`_detail` 不能再传 `matchmaker_type`/`commission_level_id`，会与 `**item.model_dump()` 撞）；新增 8 个函数。
- **8 个统计/聚合字段的真实口径**：`member_count`/`member_month` ← `promotion_attribution(status=1, effective_at)`；`lead_total`/`lead_month` ← `customer_lead.created_by`；`order_amount` ← `commission_entry(beneficiary_type='promoter', beneficiary_id, status<>'REVERSED')` 的 SUM(amount)；团队 ← `partner_membership(status=1) JOIN partner_team`；级别名 ← `promoter_level_config.level_name`。`statistics` 全部限定**在职 status=1** 的推广红娘名下。
- **数据库**：`user_matchmaker_apply` 加 `visible tinyint NOT NULL DEFAULT 1`，落点 `database_setup_marriage.py:377`（幂等补列 map）与 `:1641`（CREATE TABLE）。
- **测试**：新建 `tests/test_promoter_staff_admin_ext.py`（16 用例）；M1+M2 合并回归 `pytest tests/test_promoter_staff_admin_ext.py tests/test_promoter_staff_admin.py tests/test_promoter_level_admin.py tests/test_matchmaker_staff_admin_ext.py tests/test_commission_level_admin.py tests/test_apportion_config_admin.py tests/test_commission_entry_admin.py -q` → **62 passed**（本人复核）。
- **落点提醒**：`partner_membership` 有 `UNIQUE KEY uk_partner_membership_active(promoter_id, status)`，所以「变更团队」必须先置旧 status=1 记录为 2，再用 `INSERT ... ON DUPLICATE KEY UPDATE` 幂等写入，否则历史 status=2 行会撞键。

**前端**：3 页全部接线完成（`poplove-matchmaker-list` 8 处调用 + `poplove-matchmaker-distribution` 3 处 + `poplove-matchmaker-distribution-details` 2 处）；`admin-endpoints.ts` 补齐 20+ 个 promoter 方法与类型。`tsc --noEmit` → 通过。
> `poplove-matchmaker-list` 有个坑：`import type { PROMOTER_LEVELS }` 引用的是**值**不是类型，必须用普通 import（已修）。

**文档**：`docs/api/matchmaker-promoter-staff.md` 扩到 14 个端点（新增 3.7~3.14 + 变更记录）；`docs/api/matchmaker-promoter-level-config.md` 补自检清单 + 变更记录。

### M2 已知 UI↔后端差异
- 「导出EXCEL」「图文解说推广红娘」无后端接口 → 保留 UI 不报错。
- 分成明细「按关键字搜」后端无 keyword 参数 → 前端改为仅当前页本地过滤。
- 分成明细「录入一笔分成」后端无写接口 → 点击 toast「该功能后端暂未开放，请联系开发」。
- `poster` / `platform-token` 为占位实现（`/storage/posters/promoter-{id}.png` 无实体文件；`jump_url` 复用总店 `/matchmaker/workbench`）。

### 环境踩坑（重要）
- 本机 **Bash 工具不可用**（缺 `dirname`/`tail`/`grep`/`head`），`PowerShell` 工具 stdout 不回显 → 验证命令要 **重定向到文件再用 Read 读**。搜索/读取一律用 Grep/Glob/Read 工具。
- 跑后端测试必须用 **`E:\houduan\xuanshiai\.venv\Scripts\python.exe`**；`C:\Users\21612\.workbuddy\binaries\python\versions\3.13.12\python.exe` **没装项目依赖**（无 pydantic），不能用来跑测试。
- 前端类型检查必须用 **`node .\node_modules\typescript\bin\tsc --noEmit`**（`node_modules\.bin\tsc` 在本机报 `Cannot find module 'E:\typescript\bin\tsc'`）。
- 子代理（Agent 工具）偶发 **502 网络断连 + 频繁触限**（写多了就 429），且断连/触限前可能已经改了文件 → 恢复后**必须先核验落盘状态**再继续，不能盲信「未执行」。当 Agent 频繁触限时，**自己接着干**比再起 Agent 更稳。
- 并行 Edit 同一文件容易互相覆盖（M3-4 数据报表踩过一次），涉及同一文件的多个改动**顺序执行**或**整合到一处再 Edit**。

### M3 会员CRM 7 页 —— 已完成（2026-09-11）
口径「以前端 UI 为准，UI 有而后端无的由后端补」。数据库 **7 张新表 + 3 列补全 + 1 真实存在的 `user_partner_preference` 加 `preferred_occupation` 列**。

**数据库变更（已落盘，详见 `docs/红娘后台接口.md` 附录）**：
1. 新表 `user_commitment_sign` (`id, user_id, title, content text, file_url, sign_times int, status 0/1/2, remark, reviewed_by, reviewed_at`)
2. 新表 `user_marriage_check` (`user_id, check_method, declared_status, result married/no_record/divorced, cost decimal(10,2), checked_at`)
3. 新表 `config_auth_type` (`name uk, icon_url, description, require_realname tinyint, sort int, status`)
4. 新表 `user_auth_extra` (`user_id, auth_type_id FK, file_url, status 0/1/2, reviewed_*`)
5. 新表 `user_gift_record` (`gift_id, sender_user_id, receiver_user_id, quantity int, unit_price decimal(12,2), total_cost string, message varchar(200), scene varchar(64), consumed tinyint, created_at`)
6. 新表 `offline_vip` (`member_code uk, user_id, package_name, contract_amount string, total_meetings int, used_meetings int default 0, sales_matchmaker_user_id, store_id, status enum, signed_at, period_end_at, remark`) + `offline_vip_meet_log` (`vip_id FK, meetings_delta int default 1, reason varchar(255), changed_by, created_at`)
7. `user_auth` 补列 `face_method / face_vendor / face_score decimal(5,2) / id_card_issued varchar(64)`
8. `user_boost` 补列 `pay_status / pay_method / status 1生效/2过期/3撤销`
9. `user_report` 补列 `submit_ip / evidence_url`
10. `user_partner_preference` 补列 `preferred_occupation varchar(64)`
均为 `database_setup_marriage.py` 内 CREATE TABLE + `_ensure_table_columns` 幂等补列。

**7 个页面接线**（后端全部新文件 + 前端统一调 `adminEndpoints`）：

| 页面 | 后端路由 | 后端能力 | 前端 |
|---|---|---|---|
| love-user-auth | new `/admin/members/auth/*` (10+ 端点) | 4 schema/service/route + 6 类认证待审 + 审核动作 + 认证类型 CRUD + 3 个配置抽屉走 `member_auth` namespace | 6 Tab 全部接通 + 3 个 drawer 用 `platform-config` 的 `memberAuthConfigUpdate` |
| content-verify | new `/admin/members/media/*` (7 端点) | 5 schema/service/route + 个人介绍 4 种字符过滤 + 头像/照片/视频 卡片审核 + 历史头像 | intro 4 个 select 接字符过滤；avatar/photo/video 卡片网格接审核/替换 |
| love-user-behavior | new `/admin/members/behavior-events/*` | 5 schema/service/route + 5 类行为（browse/favorite/superlike/gift/report）+ superlike/gift/report 支持删除 | 5 Tab 接 5 类（superlike/gift/report 可删除；browse/favorite 只读） |
| love-user-statistics | 扩展 `app/api/routes/admin_home.py:148 `/member-statistics` | **几乎完全复用 admin_home.member_statistics** + 加 7 类扩展（member_realname/male_female_growth/intention_catalog/preference_occupation 等）；favourite 是 admin_home 自带 capability，不动 | 7 Tab 接 7 类扩展；返回结构与前端类型 `MemberStatisticsReport` 逐字对齐 |
| love-user-follow-up + import | 扩展 `/admin/members/follow-ups/*` (3 端点) | 5 schema/service/route + 12 个 query 字段（按 number、intention、stat、meet、date、type 与红娘 search） + Excel 模板下载 + 多 Sheet 文件解析（依赖 `uv add openpyxl`） | 全览页 12 条件 + 表格 + 5 种跟进方式展示；导入页 4 Step 流程 + 错误/跳过/成功行展示 |
| love-user-vip-underline | new `/admin/members/offline-vips/*` (10 端点) | 4 schema/service/route + 含 `offline_vip_meet_log` 联动修改 `used_meetings` + 5 张统计卡（新增/维保中/约见到期/到期未续约/流失/续约）+ `options` 下拉（销售红娘+门店+套餐名） + 4 状态流转 | 5 统计卡 + 5 状态 Tab + 表格列（合同金额字符串保留 2 位小数）+ 编辑/详情/续约/退订 4 drawer |

**每页测试**（新增 6 个 ext 测试，均 Pydantic + OpenAPI 断言）：
- `test_member_auth_admin_ext.py` (13 cases) — 6 类认证 schema + 10 端点注册 + 权限
- `test_member_media_admin_ext.py` (10 cases) — 媒体 schema + 7 端点
- `test_member_behavior_admin_ext.py` (6 cases) — 5 类 schema + 2 端点 + 入参校验
- `test_member_statistics_ext.py` (7 cases) — 返回结构 + 标签名 + 路由注册
- `test_member_follow_up_admin_ext.py` (12 cases) — Excel 模板读写 + 入参正则 + 路由注册
- `test_offline_vip_admin_ext.py` (15 cases) — schema + 状态流转 + 静态路径在动态路径前
**M1+M2+M3 全量回归** `pytest ...` → **135 passed**（本人复核）。

**文档（6 份）**：
- `docs/api/member-auth.md` · `member-media.md` · `member-behavior.md` · `member-statistics.md` · `member-follow-up.md` · `offline-vip.md`
每份按 PROJECT_RULES 2.1.1 模板，含请求参数表/请求体+非法示例/返回参数表（嵌套展开）/空数据示例/使用方法与业务规则/错误表/文档完成自检清单。

**M3 已知 UI↔后端 差异 / 妥协（前端妥善处理）**：
- 数据报表：前端「择偶职业前10」后端 `user_partner_preference` 无列 → 已补 `preferred_occupation`；「签约服务」字段 → 后端 `payment_order.product_type='offline_vip'` 聚合；「服务数据 - 约见数据」后端 `meeting_request/record` 聚合。
- 跟进导入：前端是带 Step 流程的 4 步新 UI，**直接按 UI 写代码不简化**；后端用 `openpyxl` 解析（uv 已加 dep）。
- 跟进全览统计：前端「转化客源」走 `customer_lead`，但 `member_follow_up.member_id=NULL` 时前端的「已认领」为零 → 已在 UI 文案区分。
- 线下VIP「服务数据 - 约见数据」下拉「销售红娘」：复用 `matchmaker_staff_dict` 接口（前 5 为有效匹配）。
- 线下VIP「门店」选项：取 `organization.status=1`（DISTINCT `name`）→ UI dropdown value 用 store id 与 label 用 store name。
- 全局：所有金额字段用 **string** 序列化（Decimal），前端禁止 Number 运算。

### M3 后端 6 个新文件
- `app/schemas/{member_auth_admin,member_media_admin,member_behavior_admin,member_follow_up_admin,offline_vip_admin}.py`
- `app/services/{member_auth_admin,member_media_admin,member_behavior_admin,member_follow_up_admin,offline_vip_admin}.py`
- `app/api/routes/{member_auth_admin,member_media_admin,member_behavior_admin,member_follow_up_admin,offline_vip_admin}.py`
- `tests/test_{member_auth,member_media,member_behavior,member_follow_up,offline_vip}_admin_ext.py` + `test_member_statistics_ext.py`
- `docs/api/{member-auth,member-media,member-behavior,member-statistics,member-follow-up,offline-vip}.md`
- `app/api/dependencies.py:_matchmaker_admin_permission` 加 `/offline-vips` → `finance.read/finance.write` 映射（与 M2 promoter-levels 同模式）
- `app/api/router.py` 注册全部 6 个新路由

### 旁挂备注
- **openpyxl** 已用 `uv add` 加入项目依赖（`pyproject.toml` + `uv.lock` 同步更新），旧库 venv 自动安装。后续「批量资料卡」「积分兑换 Excel」也可能复用。
- **临时探针脚本**（`_tmp_list_tables.py`/`_tmp_decimal_probe.py`）已清理，scripts 目录下只剩原有用脚本（4 个）。
- **本机工具状态**：PowerShell 工具偶发参数解析错误（"Cannot read properties of undefined (reading 'split')"），可用 Grep/Glob/Read 工具作主要验证方式；命令结果务必重定向到 `C:\Users\21612\AppData\Local\Temp\*.txt` 再用 Read 工具读。

---

## M4（客源线索 + 会员服务）已完成 — 2026-09-12

**后端真实补齐**（之前 5 轮对话因网络中断反复断连）：
- `app/api/routes/meeting.py:120-135` 新增 3 端点：
  - `GET /admin/matchmaker/meetings/options` → 下拉字典
  - `DELETE /admin/matchmaker/meetings/requests/{request_id}` → 404/409 状态机保护
  - `DELETE /admin/matchmaker/meetings/{meeting_id}` → COMPLETED 保护 + 审计日志
- `app/services/meeting.py:286-329` 补 3 个 service 函数（`admin_options` / `admin_delete_request` / `admin_delete_meeting`）
- 既有 C 端 meeting.admin_router 7 端点 + 新增 3 = **共 11 端点**

**M4 后端整体盘点（4 模块共 28 端点）**：
- 客源线索 `app/api/routes/customer_leads_admin.py`：13 端点（已有）
- 约见/约会 `app/api/routes/meeting.py:admin_router`：11 端点（7+3+1 schedule）
- 红娘牵线 `app/api/routes/matchmaker_crm_admin.py:match_records`：1 端点 GET（已有，POST 不开放后台录入）
- 服务申请 `app/api/routes/matchmaker.py:admin_router:service-requests`：2 端点（已有）
- **前端接线状态**：12 页里 `love-customer-list` 已真实，其余 11 个 `apiCalls=0`（静态假数据），需要下一轮继续补

**测试**：`tests/test_meeting_admin_ext.py` 8 用例全过；全量回归 `pytest tests/` → **1379 passed**（与我修改相关的所有测试零失败；68 failed 全是 moxiang/ai-release/community-demo/memory-docs 等无关脚本）。
- ⚠️ pytest 默认会 collect 失败：`test_ai_migration_manifest.py` / `test_build_ai_evidence.py` / `test_moxiang_prompt_loop.py`（这 3 个 import `scripts.xxx` 模块缺失是已有问题，与 M4 无关）。回归脚本必须 `--ignore=tests/test_{ai_migration_manifest,build_ai_evidence,moxiang_prompt_loop}.py`。

**契约文档**：`docs/api/m4-customer-lead-and-service.md`（v1.1，含 28 端点全表 + 错误码表 + 自检清单 + 变更记录）。

**前端 M4 接线**：下一轮要做。当前 11 个静态页面：
- 客源线索：love-customer-statistics / customer-follow-up / love-customer-batch-import / love-customer-abandon / love-customer-abandon-log / love-customer-config（6 个）
- 会员服务：vip-line-record / love-interview / love-appointment / love-promotion / vip-popularize-record（5 个）
- 其中 `vip-popularize-record` 与 `poplove-matchmaker-distribution-details` 重复，按 M1 清单口径「⚫废弃→补全」建议作为跳转壳

